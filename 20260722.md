# 📊 プロダクトマネジメント事例解説: 2026年07月22日号

おはようございます！シニア・プロダクトマネージャーの「学習コーチ」です。

今日は、GitHub Trending 上で今週爆発的に伸びている **OpenCut**（オープンソース版 CapCut）を主軸に、要件定義・DB設計・アーキテクチャを PM 視点で解剖します。「AI ブームの裏で、既存 SaaS がどう脅かされ、個人開発者にどんな余白が生まれているか」を掴んでいきましょう。

> ⚠️ 補足: 本日、指定ソースのうち Product Hunt（`https://www.producthunt.com/`）と Hacker News（`https://news.ycombinator.com/`）はエージェント経由の egress ポリシーにより HTTP 403 でアクセス不可でした。事例分析の一次ソースは、実データが取れた **GitHub Trending（daily / weekly）** に置き換えています。数値は取得時点の実測値のみを載せ、憶測は除外しました。

---

## 本日のハイライト

- **OpenCut が週間+11,676 スター、通算 76,924 スター**（言語: TypeScript）。CapCut は TikTok（ByteDance）系プロダクトで、米欧を中心にプライバシー・データ主権への懸念が慢性的に持ち上がっているカテゴリ。そこに「オープンソース + セルフホスト可」というポジショニングで刺さっているのが可視化されています。
  - ソース: https://github.com/OpenCut-app/OpenCut
  - Trending 集計: https://github.com/trending?since=weekly
- **Nutlope/hallmark が週間+9,173 スター、通算 14,879 スター**（言語: CSS）。「Anti-AI-slop design skill for Claude Code, Cursor, and Codex」というコンセプトで、"AI 生成物のクオリティが低い" という**ユーザーの痛み**を、コーディング支援 IDE の "スキル" 拡張で解こうという商品設計。プロダクトの粒度を「アプリ」から「スキル」に落とす動きが今週の裏テーマです。
  - ソース: https://github.com/Nutlope/hallmark
- **every-app/open-seo が通算 6,521 スター、今日+850**（言語: TypeScript）。Semrush / Ahrefs という月額 $100 超クラスの SEO SaaS に対する OSS 代替。MCP（Model Context Protocol）と Google Search Console 連携を売りにしており、"エンタープライズ SaaS を LLM 時代にアンバンドルする" 流れの好例。
  - ソース: https://github.com/every-app/open-seo
- **bojieli/ai-agent-book が本日+4,434 スター**、**tirth8205/code-review-graph が本日+1,921 スター**。エージェント設計の「知識」自体が急速にコモディティ化しており、Web サービスとしての "AI エージェント" よりも **書籍 / スキル / MCP サーバー** としての流通が加速しています。
  - ソース: https://github.com/trending?since=daily

---

## 注目リポジトリ / 事例

### ① OpenCut-app/OpenCut — 本日の主役ケーススタディ

- 概要: ブラウザ / セルフホストで動く動画エディタ。CapCut の "無料だが規約変更・プライバシー・BAN リスク" に不満を持つクリエイター向け。
- 実データ: 通算 76,924 スター / 週間+11,676 / フォーク 7,707 / 未クローズ Issue 355（2026-07-22 JST 時点、リポジトリ metadata より）
- ソース: https://github.com/OpenCut-app/OpenCut

**PM 視点の要件定義（何を "MVP" にするか）**

CapCut の全機能を最初から追うと沈みます。市場に刺さる MVP を切るなら:

1. インポート（mp4 / mov / 画像 / 音声）
2. タイムライン編集（分割・トリム・並べ替え）
3. テキスト / 字幕オーバーレイ（TikTok 系動画で最も使われる機能）
4. エクスポート（mp4 / 縦横比プリセット）
5. 「エンジンはローカル、プロジェクトメタデータはクラウド同期」というハイブリッド

→ 差別化は **「動画本体はクラウドに上げなくても済む」** という**プライバシー約束**。これが CapCut に対する明確な "why us"。

**Supabase 前提のテーブル設計サンプル**

```sql
-- ユーザー（auth.users を参照）
create table profiles (
  id uuid primary key references auth.users(id) on delete cascade,
  display_name text not null,
  plan text not null default 'free', -- 'free' | 'pro' | 'team'
  created_at timestamptz not null default now()
);

-- プロジェクト（1 本の動画 = 1 プロジェクト）
create table projects (
  id uuid primary key default gen_random_uuid(),
  owner_id uuid not null references profiles(id) on delete cascade,
  title text not null,
  aspect_ratio text not null default '9:16', -- '9:16' | '16:9' | '1:1'
  duration_ms int not null default 0,
  updated_at timestamptz not null default now()
);

-- メディア資産（動画のクラウド同期は "任意"）
create table media_assets (
  id uuid primary key default gen_random_uuid(),
  project_id uuid not null references projects(id) on delete cascade,
  kind text not null, -- 'video' | 'audio' | 'image'
  storage_path text, -- null 可: ローカル参照のみのケース
  duration_ms int,
  created_at timestamptz not null default now()
);

-- タイムライン上のクリップ
create table timeline_clips (
  id uuid primary key default gen_random_uuid(),
  project_id uuid not null references projects(id) on delete cascade,
  track int not null,
  start_ms int not null,
  end_ms int not null,
  asset_id uuid references media_assets(id) on delete set null,
  text_content text -- テキストクリップの場合
);

-- コラボレーター（team プランで解放）
create table project_members (
  project_id uuid references projects(id) on delete cascade,
  user_id uuid references profiles(id) on delete cascade,
  role text not null, -- 'owner' | 'editor' | 'viewer'
  primary key (project_id, user_id)
);
```

**RLS（Row Level Security）の考え方**

```sql
-- projects: 所有者、または project_members にいる人だけ読める
alter table projects enable row level security;

create policy "read own or member projects" on projects
for select using (
  owner_id = auth.uid()
  or exists (
    select 1 from project_members m
    where m.project_id = projects.id and m.user_id = auth.uid()
  )
);

-- 書き込みは owner / editor のみ
create policy "write as owner or editor" on projects
for update using (
  owner_id = auth.uid()
  or exists (
    select 1 from project_members m
    where m.project_id = projects.id
      and m.user_id = auth.uid()
      and m.role in ('owner','editor')
  )
);
```

PM としての勘所は「**RLS を "後付け" しない**」こと。projects と media_assets は最初からユーザー境界を持たせ、SELECT/UPDATE/DELETE 全ポリシーをテーブル作成と同時に書きます。個人開発の SaaS で 9 割の事故は "テーブルは作ったが RLS を書き忘れて全世界公開" です。

**アーキテクチャ選択**

- フロント: Next.js 15 + TypeScript + TailwindCSS（さとるさんのスタックそのまま）
- 動画処理: **WebCodecs API + ffmpeg.wasm** をブラウザ側で実行 → サーバーコストを潰す
- 永続化: Supabase（Auth + Postgres + Storage）
- デプロイ: Vercel（フロント）+ Supabase（バックエンド）

「サーバーで動画変換しない」設計にすると、無料枠でもユーザー 1,000 人まで回ります。ここが個人開発で "動画系 SaaS" が現実的になった最大の変化。

**収益化モデル**

| プラン | 価格帯（想定） | 差分 |
|---|---|---|
| Free | $0 | 動画は端末内、プロジェクト 3 本まで |
| Pro | $9 / 月 | プロジェクト無制限 + クラウド同期 |
| Team | $19 / seat / 月 | 共同編集 + コメント + 承認フロー |

CapCut / Adobe Premiere Rush が「ツール単価」勝負なのに対し、OSS 系は "**個人は無料、チーム機能で課金**" が定石。

---

### ② Nutlope/hallmark — "スキル" という新しい流通単位

- 概要: Claude Code / Cursor / Codex 用の "デザインスキル"。AI が吐きがちなダサい UI（"AI slop"）を回避するためのガイドラインとテンプレートをスキル形式で提供。
- 実データ: 通算 14,879 スター / 週間+9,173 / フォーク 741（2026-07-22 JST 時点）
- ソース: https://github.com/Nutlope/hallmark

**PM 視点**: プロダクトの単位が「Web アプリ」ではなく「**IDE に読ませる MDX + アセット集**」になっている点が革新。配布コスト＝ゼロ、ユーザーのオンボーディング＝リポジトリを clone するだけ。**"アプリを作らずに DAU を持てる"** 新しい配布チャネルです。

---

### ③ every-app/open-seo — エンタープライズ SaaS のアンバンドリング

- 概要: Semrush / Ahrefs の OSS 代替。MCP と Google Search Console を組み合わせて、AI エージェントから自然言語で SEO 調査ができる。
- 実データ: 通算 6,521 スター / 本日+850 / フォーク 709（2026-07-22 JST 時点）
- ソース: https://github.com/every-app/open-seo

**PM 視点**: 高価な B2B SaaS が「AI がインターフェースになる」ことでコモディティ化する流れの典型例。**"UI ではなく AI から呼ばれる前提の SaaS"** をどう設計するか、が来年以降の PM 必須スキルになります。

---

## 実践ポイント（さとるさんへ）

さとるさんの現状（Next.js / TypeScript / TailwindCSS / Supabase / Vercel、非エンジニア業務からステップアップ中）を踏まえた具体的アクション:

1. **今週 30 分だけ OpenCut の repo を眺める**: `apps/web` 配下の Next.js ディレクトリ構成と、Supabase を使っている場合は `supabase/migrations` の SQL を読むだけで、"実運用の Next.js × Supabase" の空気が掴めます。コードを書く必要はなく、まず **構造の型**を目に焼き付けてください。
2. **自分の LP コーディング業務を "プロダクト" として捉え直す**: OpenCut の Free / Pro / Team テーブルを見て、「自分は今、Free 相当のことを納品しているのか？ Pro 相当は何ができれば売れるのか？」と自問してみる。単価交渉の材料になります。
3. **Supabase の "テーブルより先に RLS" 練習**: 手元で `projects` + `project_members` の 2 テーブルだけ作り、上記の RLS ポリシーを丸コピして SELECT が意図通り絞られるか試す。個人開発の 9 割の事故がここで潰せます。
4. **Product Hunt / HN が読めなかった日は "Trending" が代替になる**という運用感覚を持っておく。情報源は 1 本に頼らない。

---

## 今日の思考トリガー

**「あなたが今週触っている LP や個人開発の機能を "Free / Pro / Team" の 3 段に切り分けるとしたら、どの機能が Free の "撒き餌" で、どの機能が Pro の "課金トリガー" になりますか？ 逆に、Team プランでしか成立しない機能は何ですか？」**

この 3 段設計は、"作ってから売る" ではなく "**売れる形で作る**" ためのフレームです。CapCut がなぜ Team 機能に本気なのか、その裏返しで OpenCut がどこで勝負しているのかを、自分の手元のプロジェクトに置き換えて言語化してみてください。

---

_ソース: https://github.com/trending?since=daily , https://github.com/trending?since=weekly , https://github.com/OpenCut-app/OpenCut , https://github.com/Nutlope/hallmark , https://github.com/every-app/open-seo （いずれも 2026年07月22日 JST 時点の実測値）_

_取得不可: https://www.producthunt.com/ , https://news.ycombinator.com/ （本日はエージェント経由で HTTP 403、egress ポリシー denial）_
