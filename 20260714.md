# 🎨 トレンドUI実装チュートリアル: 2026年07月14日号

おはようございます！シニアUI/UXデザイナー兼フロントエンドエンジニアの「学習コーチ」です。

今日は火曜日。GitHubの週間トレンドを眺めていると、**shadcn/ui** が新リリース（`@shadcn/react@0.2.1`、2026/07/08）を出していました。個人開発の武器として、そして「デザインをコードで語る力」を鍛えるための最高の教材なので、今日はここを深掘りしましょう。

---

## 本日のハイライト

- **shadcn/ui が `@shadcn/react@0.2.1` をリリース（2026/07/08）**
  React + TypeScript + TailwindCSS で作られた「コピー&ペースト型」コンポーネントライブラリ。npm でインストールして「ブラックボックスとして呼ぶ」のではなく、ソースコードをプロジェクトに取り込んで自分の資産にする、という設計思想が最大の特徴です。
  ソース: https://github.com/shadcn-ui/ui （★119k）

- **vercel/next.js が v16.2.10 をリリース（2026/07/01）**
  Next.js は今週も週間トレンドに登場（★141k, +910/week）。App Router 世代のプロジェクトを作る際は、shadcn/ui との組み合わせがほぼデファクトになっています。
  ソース: https://github.com/vercel/next.js

- **GitHub Weekly JavaScript Trending：AI関連の勢いが依然強い**
  上位は `asgeirtj/system_prompts_leaks`（★57k, +7.1k/週）、`addyosmani/agent-skills`（★77k, +8k/週）など、AIエージェント関連が席巻。フロントエンド純度が高いのは Next.js のみ、という状況。
  ソース: https://github.com/trending/javascript?since=weekly

---

## 注目リポジトリ / 事例

1. **shadcn-ui/ui**（★119k）
   Radix UI（アクセシビリティ）+ TailwindCSS（スタイル）+ CVA（バリアント管理）の組み合わせを、そのままコピペできる形で提供。
   URL: https://github.com/shadcn-ui/ui

2. **vercel/next.js**（★141k）
   App Router + Server Components が完全に主流化。shadcn/ui はそのまま Server Component 前提で書かれています。
   URL: https://github.com/vercel/next.js

3. **mrdoob/three.js**（★113k）
   本日のデイリートレンドにも登場。3D演出をランディングページに取り入れる案件が増えており、LPコーディングを本業にしているさとるさんの提案の幅を広げるカードになります。
   URL: https://github.com/mrdoob/three.js

---

## 実践ポイント（さとるさんへ）

さとるさんの現スタック（React / Next.js / TypeScript / TailwindCSS）に、shadcn/ui は完璧にハマります。今日は「トレンドUI = **Bento Grid（弁当箱グリッド）レイアウト**」を、shadcn/ui 風の書き方で組み立てるチュートリアルを用意しました。

### なぜ Bento Grid が今トレンドなのか（UX/CVR視点）

- **視線誘導が強い**：大きなセルに主役KPI、小さなセルにサブ情報。ユーザーの目線がZ字ではなく「大→小」の階層で流れます。
- **情報密度と余白のバランス**：LPやダッシュボードで「たくさん見せたい」×「詰め込み感を出したくない」を両立できます。
- **CVR観点**：CTAを大セルに、社会的証明（レビュー数など）を小セルに置くことで、意思決定に必要な情報を1スクロールで完結させられます。

### 実装コード（Next.js App Router + TypeScript + TailwindCSS）

`app/dashboard/page.tsx` を作る想定です。

```tsx
// app/dashboard/page.tsx
import type { ReactNode } from "react";

// 1. 「1枚のセル」を表す型を定義する
//    ジェネリクスや複雑な型は避け、まずは "props の設計図" として型を書く感覚でOK。
type BentoItem = {
  title: string;
  value: string;         // "1,240" のような表示用文字列
  hint?: string;         // 補足コピー（オプショナル）
  span?: "sm" | "md" | "lg"; // セルの大きさ。Union型で「取りうる値」を制限する
};

// 2. データはページ内に閉じ込めておく（後で Supabase から取ってくる想定）
const items: BentoItem[] = [
  { title: "今月の売上", value: "¥1,240,000", hint: "前月比 +18%", span: "lg" },
  { title: "新規ユーザー", value: "312",       hint: "うちLP経由 74%", span: "md" },
  { title: "平均滞在時間", value: "2:41",      span: "sm" },
  { title: "CVR",         value: "3.8%",      hint: "先週 3.1%",  span: "sm" },
];

// 3. span の値から Tailwind のクラスを引く小さなヘルパー
//    「文字列を動的に組み立てる」のではなく、事前にマップとして持つのが Tailwind の鉄則。
//    （JITコンパイラは静的な文字列しかスキャンできないため）
const spanClass: Record<NonNullable<BentoItem["span"]>, string> = {
  sm: "col-span-1 row-span-1",
  md: "col-span-2 row-span-1",
  lg: "col-span-2 row-span-2",
};

// 4. セルコンポーネント。children ではなく value を受け取るシンプル設計
function BentoCard({ item }: { item: BentoItem }) {
  return (
    <div
      className={[
        "rounded-2xl border border-neutral-200 bg-white p-6",
        "shadow-sm transition hover:shadow-md",
        "dark:border-neutral-800 dark:bg-neutral-900",
        spanClass[item.span ?? "sm"],
      ].join(" ")}
    >
      <p className="text-sm text-neutral-500">{item.title}</p>
      <p className="mt-2 text-3xl font-semibold tracking-tight">{item.value}</p>
      {item.hint && (
        <p className="mt-1 text-xs text-emerald-600">{item.hint}</p>
      )}
    </div>
  );
}

// 5. ページ本体。grid-cols-4 の4列に、span で大小をつけていく
export default function DashboardPage(): ReactNode {
  return (
    <main className="mx-auto max-w-5xl p-8">
      <h1 className="mb-6 text-2xl font-bold">今週のサマリー</h1>
      <section
        className="grid grid-cols-4 auto-rows-[10rem] gap-4"
        aria-label="週次KPIサマリー"
      >
        {items.map((item, i) => (
          <BentoCard key={i} item={item} />
        ))}
      </section>
    </main>
  );
}
```

### ここで押さえたい TypeScript / React の型のしくみ

- `type BentoItem = { ... }` の `span?: "sm" | "md" | "lg"`
  → `?` で「あってもなくてもいい」プロパティを表現し、`|`（**ユニオン型**）で「取りうる値」を3つに制限しています。これで `span: "xl"` などのタイポをコンパイル時に検出できます。
- `Record<NonNullable<BentoItem["span"]>, string>`
  → `Record<K, V>` は「K をキーに、V を値に持つオブジェクト」の型。`NonNullable<...>` で `undefined` を除外し、「`sm`・`md`・`lg` の3キーを漏れなく埋めよ」というルールをコンパイラに教えられます。ヘルパーマップの網羅性チェックに便利です。
- `spanClass[item.span ?? "sm"]`
  → `??`（**Null合体演算子**）は「左が `null` / `undefined` のときだけ右を使う」演算子。`||` と違い、`0` や `""` を「値あり」として扱ってくれます。
- `className={[...].join(" ")}`
  → 条件付きクラス名を配列で組んで最後に空白 join する、shadcn/ui でも使われる素朴なパターン。慣れてきたら `clsx` や `cva` を導入すると、より宣言的に書けます。

### 動作確認の手順（3ステップ）

1. `npx create-next-app@latest my-bento --typescript --tailwind --app`
2. 上のコードを `app/dashboard/page.tsx` として保存
3. `npm run dev` → `http://localhost:3000/dashboard` を開く

保存すると即座に4×2のBentoレイアウトが表示されます。まずはこれを「動く土台」として、`items` をSupabaseから取ってくる形に発展させれば、そのままミニダッシュボードSaaSのMVPになります。

---

## 今日の思考トリガー

**「今つくっているLP（もしくは個人開発の1画面）を『Bento Grid』で組み直すとしたら、"最大セル" に置くべき情報は何か？その情報がユーザーの意思決定に効く理由を、1文で説明できますか？」**

大セルに置くべきは、**ユーザーがコンバージョンする前に必ず一度は目を留める情報**です。KPIダッシュボードなら「今月の売上」、SaaSのLPなら「主要ベネフィット」、ポートフォリオなら「代表作」。この視点を持てるかどうかが、「見た目を作れる人」と「成果を作れる人」の分岐点になります。

---

_ソース: https://github.com/shadcn-ui/ui , https://github.com/vercel/next.js , https://github.com/trending/javascript?since=weekly , https://github.com/trending/typescript?since=weekly （2026年07月14日時点）_
