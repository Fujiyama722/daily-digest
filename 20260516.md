# 🛠️ 週末ハンズオン実践: 2026年05月16日号

おはようございます！フロントエンドメンターの「学習コーチ」です。

土曜の朝、いかがお過ごしですか。今週の GitHub Trending（週間）では、地味ながら効く道具 **pnpm** が再び上位に顔を出していました。今日はこれを起点に、Next.js + TypeScript + Tailwind の小さな部品を 30 分で組み上げ、TypeScript の「守りの型」を体感してみましょう。

---

## 本日のハイライト

GitHub Trending（週間）の TypeScript / 総合ランキングから、今週の動きをいくつか:

- **pnpm/pnpm（35,068★ / 今週 +164）** — Node.js のパッケージマネージャ。npm と互換性を保ちつつ、依存をグローバル保管庫＋ハードリンクで管理し、ディスクとインストール時間を削減。
  ソース: https://github.com/pnpm/pnpm
- **bytedance/UI-TARS-desktop（34,073★ / 今週 +4,184）** — マルチモーダル AI エージェントの OSS スタック。GUI を操作するエージェントの参照実装。
  ソース: https://github.com/bytedance/UI-TARS-desktop
- **thesysdev/openui（5,648★ / 今週 +1,097）** — 「生成 UI（Generative UI）」の標準を目指すフレームワーク。React 部品ライブラリ + LLM への構造化出力を統合する設計。
  ソース: https://github.com/thesysdev/openui
- **playcanvas/supersplat（8,052★ / 今週 +2,866）** — 3D ガウシアン・スプラットを編集 / 最適化 / 公開できる完全ブラウザ製ツール。TypeScript 92.6%。
  ソース: https://github.com/playcanvas/supersplat

※本日は Hacker News Algolia 検索（hn.algolia.com）が 403 で取得できなかったため、GitHub Trending（週間）を主ソースにしています。
ソース: https://github.com/trending?since=weekly , https://github.com/trending/typescript?since=weekly

---

## 注目リポジトリ / 事例

1. **pnpm/pnpm** — npm / yarn と同じ操作感のまま、依存パッケージを `~/.local/share/pnpm/store` に集約してプロジェクト側にはハードリンクを貼る方式。複数の Next.js プロジェクトを抱えるほどディスクと時間が浮く。最新は 11.1.2（2026-05-14）。
   https://github.com/pnpm/pnpm
2. **thesysdev/openui** — LLM が UI を返す前提で設計された軽量マークアップ言語 + React ランタイム。「AI が画面を生成する時代」のフロントの形を覗くのに良い教材。
   https://github.com/thesysdev/openui
3. **playcanvas/supersplat** — 3D ガウシアン・スプラットの編集・公開を全部ブラウザで完結させた事例。Three.js / WebGPU 系を将来扱うなら、UI 設計のお手本としても見ておく価値あり。
   https://github.com/playcanvas/supersplat

---

## 実践ポイント（さとるさんへ）

非エンジニア → シニアフロントエンドへの最短ルートは「**毎週、自分のローカル環境を 1 つ磨く**」こと。今日は pnpm を入れて、Next.js プロジェクトに**型で守られた小さな部品**を 1 つ作ります。所要時間 30 分。

### Step 1: pnpm をインストール

```bash
# Node.js 18+ が入っていればこれで OK
npm install -g pnpm

# 確認
pnpm -v
```

### Step 2: Next.js + TypeScript + Tailwind を pnpm で作る

```bash
pnpm create next-app@latest weekend-pnpm \
  --typescript --tailwind --eslint \
  --app --src-dir --import-alias "@/*"

cd weekend-pnpm
pnpm dev
```

`pnpm create ...` は `npx create-...` の pnpm 版です。違いは、依存パッケージが**端末内で 1 か所に集約**され、プロジェクトには参照（ハードリンク）が貼られるだけ、という点。`node_modules` の重複ダウンロードがなくなります。

### Step 3: 型で守られた `Notice` 部品を書く

`src/components/Notice.tsx`:

```tsx
// Discriminated Union（判別可能なユニオン型）
// type プロパティで「どの形のオブジェクトか」を区別する
type NoticeProps =
  | { type: "info"; message: string }
  | { type: "warn"; message: string; action: { label: string; href: string } }
  | { type: "error"; message: string };

export function Notice(props: NoticeProps) {
  // Record<K, V>: K のすべての値をキーに持つ V のオブジェクト型
  // → info / warn / error のどれかを書き忘れると TS が赤線で教えてくれる
  const styles: Record<NoticeProps["type"], string> = {
    info: "bg-blue-50 text-blue-900 border-blue-200",
    warn: "bg-yellow-50 text-yellow-900 border-yellow-200",
    error: "bg-red-50 text-red-900 border-red-200",
  };

  return (
    <div className={`rounded-md border p-4 ${styles[props.type]}`}>
      <p className="text-sm">{props.message}</p>

      {/* 型ガード: この if の中だけ props.action の存在が保証される */}
      {props.type === "warn" && (
        <a
          href={props.action.href}
          className="mt-2 inline-block text-sm underline"
        >
          {props.action.label}
        </a>
      )}
    </div>
  );
}
```

`src/app/page.tsx`:

```tsx
import { Notice } from "@/components/Notice";

export default function Home() {
  return (
    <main className="mx-auto max-w-xl space-y-3 p-8">
      <Notice type="info" message="今日もダイジェストを読みました。えらい。" />
      <Notice
        type="warn"
        message="pnpm のグローバル設定が未調整です。"
        action={{ label: "設定する", href: "/settings" }}
      />
      <Notice type="error" message="API キーが見つかりません。" />
    </main>
  );
}
```

### ロジックと型の解説（JS / TS 初心者向け）

- **Discriminated Union**: `type: "info" | "warn" | "error"` という共通プロパティで「どの形か」を区別するパターン。JavaScript なら `if (props.type === "warn") { /* action があるはず */ }` と**人間が暗黙に約束していた**ものを、TypeScript は**型で約束**してくれる。`type` が `"info"` のときに `props.action` を触ろうとすると、Antigravity の TS サーバーが赤線で止めてくれます。
- **型ガード（type guard）**: `props.type === "warn"` という分岐文そのものが、その if ブロック内では「props は warn 形」と TS に伝える役割を持ちます。条件分岐 = 型の絞り込み、と覚えてしまってよいです。
- **`Record<K, V>`**: `{ info: string; warn: string; error: string }` を `Record<NoticeProps["type"], string>` と書ける、と思えば十分。`info` / `warn` / `error` を増やしたとき、styles オブジェクト側の書き忘れがコンパイル時に検出されます。
- **なぜ嬉しい？** — Webサイト運用や LP の現場で「想定外の type が来てバナーが崩れる」事故は、運用が長引くほど増える類のバグです。Discriminated Union + Record は、それを**プログラムが世に出る前に**潰してくれます。

### Step 4: 動作確認 → コミット

```bash
pnpm dev    # http://localhost:3000 で 3 種類のバナーが並べば成功
git init && git add . && git commit -m "feat: pnpm + Notice component"
```

---

## 今日の思考トリガー

**「自分が普段書いている JavaScript の中で、『if 文で分岐したあとは当然こうなっているはず』と暗黙に信じている性質はどれだろう？ その暗黙の前提を Discriminated Union で書き直したら、半年後の自分は何分の作業時間とデバッグから解放される？」**

_ソース: https://github.com/trending?since=weekly , https://github.com/trending/typescript?since=weekly , https://github.com/pnpm/pnpm , https://github.com/thesysdev/openui , https://github.com/playcanvas/supersplat , https://github.com/bytedance/UI-TARS-desktop (2026年05月16日時点)_
