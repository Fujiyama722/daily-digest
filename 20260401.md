# 🧭 プロダクトマネジメント事例解説: 2026年04月01日号

おはようございます！シニア・プロダクトマネージャーの「学習コーチ」です。

今週GitHubで大きな注目を集めているプロダクトを題材に、要件定義からDB設計・アーキテクチャまで深掘りしていきます。個人開発でMVPを目指すさとるさんに、実践的な視点をお届けします。

---

## 本日のハイライト

### 1. Twenty — Salesforce対抗のオープンソースCRM が週間2,600+スター獲得

**Twenty**（twentyhq/twenty）は、Salesforceに代わるモダンなCRMをコミュニティ主導で構築するプロジェクトです。累計43,000+スターを獲得し、今週だけで2,663スターを積み上げました。

注目すべきは**ビジネスモデルの設計**です。オープンソースでコア機能を無料提供し、エンタープライズ向けの高度な機能（SSO、監査ログ、専用サポートなど）を有料化する「オープンコア」モデルを採用しています。個人開発者が参考にすべき、コミュニティの力で開発コストを下げながら収益化する戦略です。

ソース: https://github.com/twentyhq/twenty

### 2. pascalorg/editor — 3Dアーキテクチャエディタが週間5,600+スター

ブラウザ上で3D建築プロジェクトを作成・共有できるTypeScript製エディタです。累計8,767スターで、わずか1週間で5,612スターを獲得しました。

PM視点で注目すべきは「**ニッチ × Web技術**」の組み合わせです。建築・インテリア分野の専門ツールをWebアプリとして提供することで、インストール不要・共有が簡単という強力なUXを実現しています。

ソース: https://github.com/pascalorg/editor

### 3. supermemoryai/supermemory — AI時代の「記憶API」

高速でスケーラブルなメモリエンジンとAPIを提供するプロジェクトです（累計20,762スター、週間+2,617）。AIアプリケーションに「長期記憶」を持たせるインフラ的プロダクトで、今後のAIアプリ開発の基盤になり得る存在です。

ソース: https://github.com/supermemoryai/supermemory

---

## 注目リポジトリ / 事例

| リポジトリ | 概要 | 週間スター |
|---|---|---|
| [twentyhq/twenty](https://github.com/twentyhq/twenty) | オープンソースCRM。Salesforce代替をコミュニティで構築 | +2,663 |
| [pascalorg/editor](https://github.com/pascalorg/editor) | ブラウザで3D建築プロジェクトを作成・共有 | +5,612 |
| [supermemoryai/supermemory](https://github.com/supermemoryai/supermemory) | AI向け高速メモリエンジン＆API | +2,617 |

---

## 深掘り: Twentyに学ぶプロダクト設計とアーキテクチャ

### なぜTwentyを取り上げるのか？

Twentyは「巨大な既存市場（CRM）に、オープンソースで挑む」という個人開発者にも応用可能な戦略を取っています。さとるさんがSupabase + Next.jsで個人開発する際に直接参考になるアーキテクチャ思想が詰まっています。

### 要件定義のポイント

Twentyが解決している課題を整理すると:

- **ペイン**: Salesforceは高額・複雑・カスタマイズが困難
- **ターゲット**: 中小企業・スタートアップのセールスチーム
- **コアバリュー**: シンプル・カスタマイズ可能・データを自分で所有できる

個人開発のMVPで意識すべきフレームワーク:

```
1. 誰の（ターゲット）
2. どんな痛み（ペイン）を
3. どう解決するか（ソリューション）
4. なぜ既存のもので十分でないか（差別化）
```

### Supabase前提のテーブル設計例（CRM風）

さとるさんがSupabaseで同様のアプリを作る場合のテーブル設計を考えてみましょう:

```sql
-- ユーザー（認証はSupabase Authを利用）
-- auth.users テーブルが自動で作られる

-- ワークスペース（チーム単位の管理）
create table workspaces (
  id uuid primary key default gen_random_uuid(),
  name text not null,
  created_at timestamptz default now()
);

-- メンバー管理（多対多）
create table workspace_members (
  workspace_id uuid references workspaces(id) on delete cascade,
  user_id uuid references auth.users(id) on delete cascade,
  role text check (role in ('owner', 'admin', 'member')) default 'member',
  primary key (workspace_id, user_id)
);

-- コンタクト（顧客情報）
create table contacts (
  id uuid primary key default gen_random_uuid(),
  workspace_id uuid references workspaces(id) on delete cascade,
  name text not null,
  email text,
  company text,
  status text check (status in ('lead', 'prospect', 'customer', 'churned')) default 'lead',
  created_at timestamptz default now(),
  updated_at timestamptz default now()
);

-- 商談（ディール管理）
create table deals (
  id uuid primary key default gen_random_uuid(),
  workspace_id uuid references workspaces(id) on delete cascade,
  contact_id uuid references contacts(id) on delete set null,
  title text not null,
  amount integer default 0,  -- 金額（円）
  stage text check (stage in ('initial', 'negotiation', 'proposal', 'closed_won', 'closed_lost')) default 'initial',
  created_at timestamptz default now()
);
```

### RLS（Row Level Security）の考え方

Supabaseで最も重要なセキュリティの仕組みがRLSです。「どのユーザーがどのデータにアクセスできるか」をデータベースレベルで制御します:

```sql
-- RLSを有効化
alter table contacts enable row level security;

-- ポリシー: 自分のワークスペースのコンタクトのみ閲覧可能
create policy "Users can view contacts in their workspace"
  on contacts for select
  using (
    workspace_id in (
      select workspace_id from workspace_members
      where user_id = auth.uid()
    )
  );

-- ポリシー: 自分のワークスペースにのみコンタクトを追加可能
create policy "Users can insert contacts in their workspace"
  on contacts for insert
  with check (
    workspace_id in (
      select workspace_id from workspace_members
      where user_id = auth.uid()
    )
  );
```

**RLSのポイント**:
- `auth.uid()` でログイン中のユーザーIDを取得できる
- `using` は読み取り時のフィルタ、`with check` は書き込み時のバリデーション
- ワークスペースメンバーかどうかをサブクエリで確認する設計がマルチテナントの基本

### アーキテクチャ: MVP構成

```
┌─────────────┐     ┌──────────────┐     ┌──────────────┐
│   Next.js   │────▶│   Supabase   │────▶│  PostgreSQL  │
│  (Vercel)   │     │  (Auth/API)  │     │  (RLS適用)   │
└─────────────┘     └──────────────┘     └──────────────┘
     │                     │
     │ TailwindCSS         │ Realtime
     │ React               │ (リアルタイム更新)
     │ TypeScript           │
```

MVPで「作りすぎない」ことが最も重要です。Twentyも最初はシンプルなコンタクト管理から始まり、徐々に機能を追加しています。

---

## 実践ポイント（さとるさんへ）

1. **「オープンコア」モデルを研究する**: Twentyのように、コア機能を無料で提供しつつ有料機能で収益化する戦略は、個人開発でも有効です。まずは無料で使えるツールを作り、ユーザーがついてから有料機能を考えましょう。

2. **Supabaseのテーブル設計を手を動かして試す**: 上記のSQL文をSupabaseのSQL Editorにコピペして実行してみてください。テーブルが作られ、RLSが有効になる感覚を体で覚えることが大切です。

3. **ニッチ市場を狙う**: pascalorg/editorが「3D建築」というニッチに絞ったように、さとるさんの業務経験（Web制作・LP制作）で感じている「ここが不便」を解決するツールが最高のMVPの種になります。

4. **AIエージェントトレンドに注目**: 今週のGitHubトレンドでは、Claude Code関連のツール（oh-my-claudecode、everything-claude-code、learn-claude-code等）が上位を独占しています。AIエージェント向けのツールやプラグインは、今まさにブルーオーシャンです。

---

## 今日の思考トリガー

**「あなたが普段のWeb制作業務で "これ、毎回やるの面倒だな" と感じている作業は何ですか？それをSupabase + Next.jsで解決するツールにしたら、同じ悩みを持つ他のWeb制作者にも売れるかもしれません。今日1つだけ "面倒な作業" を書き出してみてください。」**

---

_ソース: [GitHub Trending](https://github.com/trending?since=weekly), [GitHub Trending TypeScript](https://github.com/trending/typescript?since=weekly), [GitHub Trending JavaScript](https://github.com/trending/javascript?since=weekly) (2026年04月01日時点)_

_※ 本日はHacker News・Product Huntのデータ取得が制限されたため、GitHub Trendingのデータを中心に構成しています。_
