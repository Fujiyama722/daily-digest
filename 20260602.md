# 🎨 トレンドUI実装チュートリアル: 2026年06月02日号

おはようございます！シニアUI/UXデザイナー兼フロントエンドエンジニアの「学習コーチ」です。

今日はGitHub Trendingで急上昇している `manaflow-ai/cmux`（週+1,653★ / 累計20.7k★）にヒントを得て、**Arc BrowserやLinearでも採用されている「Vertical Tabs（縦タブ）UI」**をReact + TypeScript + TailwindCSSで実装するチュートリアルをお届けします。

---

## 本日のハイライト

### 1. AI Coding Agent向けターミナル「cmux」がJSトレンド4位に
- macOS向けターミナル「Ghostty」をベースに、**縦タブ + 通知**でAIコーディングエージェントの並列実行を快適にするツール。
- 「複数のエージェントを同時に走らせる時代」に対応した縦タブUIは、ブラウザ（Arc）・エディタ（VS Code）・タスク管理（Linear）でも採用が加速。
- ソース: https://github.com/manaflow-ai/cmux

### 2. AIエージェント開発の「ハーネス」概念が首位独走
- `affaan-m/ECC` が週+10,473★で総合上位に。Claude Code/Cursor/Codex向けの63エージェント・249スキルを束ねる"オペレータシステム"。
- 非エンジニアでも「AIに何をさせるか」を構造化する発想は、UI設計のコンポーネント分割と地続き。
- ソース: https://github.com/affaan-m/ECC

### 3. 個人開発者向けの教材リポジトリも健在
- `microsoft/Web-Dev-For-Beginners`（24レッスン・12週間）が今週も+100★ 。基礎固め中のさとるさんにとって絶好の足場。
- ソース: https://github.com/microsoft/Web-Dev-For-Beginners

---

## 注目リポジトリ / 事例

| # | リポジトリ | 概要 | 週間★ | 累計★ |
|---|---|---|---|---|
| 1 | [manaflow-ai/cmux](https://github.com/manaflow-ai/cmux) | Ghostty製の縦タブターミナル。AIエージェント並列実行UXの参考になる | +1,653 | 20.7k |
| 2 | [microsoft/Web-Dev-For-Beginners](https://github.com/microsoft/Web-Dev-For-Beginners) | 12週でWeb開発の基礎が学べる公式教材 | +100 | 95.8k |
| 3 | [Axorax/awesome-free-apps](https://github.com/Axorax/awesome-free-apps) | フリーアプリ集。カテゴリ別カード一覧UIの設計参考に | +2,345 | 6.4k |

ソース: https://github.com/trending/javascript?since=weekly （2026年06月02日時点）

---

## 実践ポイント（さとるさんへ）

### 今日のテーマ: **Vertical Tabs UI** をTailwindCSSで実装

横幅が増えるほど横タブは可読性が落ちますが、**縦タブは「いくつ開いても等幅・等間隔」で視認性が崩れません**。LPや管理画面のサイドナビ設計にもそのまま転用できます。

### ① コンポーネント設計（型から始める）

```tsx
// types.ts
// 「タブ1件」が持つべき情報を型で定義する。idは識別、labelは表示、
// iconはオプション（?を付けると未指定でもOK）になる。
export type Tab = {
  id: string;
  label: string;
  icon?: React.ReactNode;
};
```

> **TypeScript解説**: `?` を付けたプロパティは「あってもなくてもいい」を意味します。`icon: React.ReactNode | undefined` の短縮形と考えてください。これで「アイコンなしのタブ」もエラーなく作れます。

### ② 縦タブ本体（React + TailwindCSS）

```tsx
'use client';
import { useState } from 'react';
import type { Tab } from './types';

type Props = {
  tabs: Tab[];           // タブ配列
  defaultId?: string;    // 初期選択タブ（省略可）
};

export function VerticalTabs({ tabs, defaultId }: Props) {
  // useStateは「変化する値」を保持するフック。
  // 第1引数が現在値、第2引数(setActiveId)が更新関数。
  const [activeId, setActiveId] = useState<string>(
    defaultId ?? tabs[0]?.id ?? ''
  );

  const active = tabs.find((t) => t.id === activeId);

  return (
    <div className="flex h-[480px] w-full rounded-2xl border border-zinc-200 bg-white shadow-sm">
      {/* 左カラム: 縦タブ一覧 */}
      <nav
        aria-label="Sections"
        className="flex w-56 shrink-0 flex-col gap-1 border-r border-zinc-200 p-3"
      >
        {tabs.map((tab) => {
          const isActive = tab.id === activeId;
          return (
            <button
              key={tab.id}
              type="button"
              onClick={() => setActiveId(tab.id)}
              aria-current={isActive ? 'page' : undefined}
              className={[
                'group flex items-center gap-2 rounded-xl px-3 py-2 text-left text-sm transition',
                isActive
                  ? 'bg-zinc-900 text-white shadow-sm'
                  : 'text-zinc-700 hover:bg-zinc-100',
              ].join(' ')}
            >
              {tab.icon && (
                <span className="shrink-0 opacity-80">{tab.icon}</span>
              )}
              <span className="truncate">{tab.label}</span>
            </button>
          );
        })}
      </nav>

      {/* 右カラム: 選択中タブのコンテンツ表示エリア */}
      <section className="flex-1 overflow-auto p-6">
        <h2 className="mb-2 text-lg font-semibold text-zinc-900">
          {active?.label}
        </h2>
        <p className="text-sm text-zinc-600">
          ここに「{active?.label}」用のコンテンツを差し込みます。
        </p>
      </section>
    </div>
  );
}
```

### ③ 使い方（page.tsx などから呼び出す）

```tsx
import { VerticalTabs } from './VerticalTabs';

export default function Page() {
  return (
    <main className="mx-auto max-w-4xl p-8">
      <VerticalTabs
        tabs={[
          { id: 'overview', label: '概要' },
          { id: 'pricing', label: '料金' },
          { id: 'faq', label: 'よくある質問' },
        ]}
      />
    </main>
  );
}
```

### ④ UX/CVR視点のポイント

1. **タップターゲット**: `py-2 px-3` で縦44px前後を確保 → モバイルでも誤タップを防ぐ。
2. **状態の明確化**: アクティブを「黒背景+白文字」で強コントラスト。ホバーは `hover:bg-zinc-100` で控えめに。離脱率低下に直結。
3. **アクセシビリティ**: `aria-current="page"` でスクリーンリーダーに現在位置を伝える。LPでは検索流入の数%がここを聞いている、と考えると無視できない。
4. **`truncate` で崩れ防止**: 長いラベルが来ても1行省略 → 縦タブは横並びと違いレイアウト崩壊しません。

### ⑤ 次の一手（拡張ヒント）

- `useState` を `useReducer` に置き換えると、「タブ閉じる」「並び替え」など複雑な操作に強くなる。
- URLハッシュ（`?tab=pricing`）と同期させると、ユーザーが「タブを開いた状態のURLを共有」できる → CVRに効くシェアラビリティ向上。

---

## 今日の思考トリガー

**「あなたが今関わっているLPやWebサイトで、もし『縦タブ』を導入したら、どのセクションを集約できますか？ そしてそれは離脱率・回遊率にどんな影響を与えそうですか？」**

横並びナビを縦タブにするだけで、「全項目が一覧でき」「現在地が常に見え」「追加コンテンツの拡張余地が残る」という3つの利点が同時に得られます。次の業務委託案件で提案できないか、5分だけ考えてみてください。

---

_ソース:_
- _https://github.com/trending/javascript?since=weekly (2026年06月02日時点)_
- _https://github.com/manaflow-ai/cmux (2026年06月02日時点)_
- _https://github.com/microsoft/Web-Dev-For-Beginners (2026年06月02日時点)_

_※本日はHacker News（hn.algolia.com / news.ycombinator.com）が取得できなかったため、GitHub Trendingのデータを中心に構成しました。_
