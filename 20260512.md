## 🎨 トレンドUI実装チュートリアル: 2026年5月12日号

おはようございます！シニアUI/UXデザイナー兼フロントエンドエンジニアの「学習コーチ」です。

今週のGitHubトレンドはAIコーディングツール系が席巻していますが、その流れの中で改めて注目されているのが「**AIチャット風メッセージUI / ストリーミング表示**」です。今日はこれをReact + Next.js + TypeScript + TailwindCSSで一緒に組んでみます。

---

## 本日のハイライト

- **decolua/9router**（週+3,318⭐ / 累計8,208⭐）: Claude Code / Codex / Cursor / Antigravity などを40+のLLMプロバイダーに接続するルーター。複数AIを切り替えるUI設計の参考になります。
  - ソース: https://github.com/decolua/9router
- **datawhalechina/easy-vibe**（週+1,519⭐ / 累計9,831⭐）: 初心者向け「vibe coding」教材。AIに指示を出して動くものを作る思考プロセスがまさに今のフロント界隈のスタンダードに。
  - ソース: https://github.com/datawhalechina/easy-vibe
- **browserbase/skills**（週+1,403⭐ / 累計3,141⭐）: Claude Codeにブラウザ自動化スキルを与える公式skill集。LP制作の検証フローを自動化できる可能性大。
  - ソース: https://github.com/browserbase/skills

※ Hacker News Algolia API は本日 HTTP 403 で取得できませんでした。GitHub Trending の実データのみで構成しています。
- ソース: https://github.com/trending/javascript?since=weekly

---

## 注目リポジトリ / 事例

1. **fspecii/ace-step-ui**（週+1,216⭐ / 累計3,668⭐）
   - 概要: ローカル動作のオープンソース音楽生成UI。React的なコンポーネント分割の参考に。波形ビジュアライズの実装が見どころ。
   - ソース: https://github.com/fspecii/ace-step-ui

2. **Haleclipse/CodexDesktop-Rebuild**（週+376⭐ / 累計1,681⭐）
   - 概要: Codex Desktop のクロスプラットフォーム再実装。AIチャット風レイアウトの構造（サイドバー＋メッセージリスト＋入力欄）を学べる。
   - ソース: https://github.com/Haleclipse/CodexDesktop-Rebuild

3. **browserbase/skills**
   - 概要: Claude Codeから直接ブラウザ操作する公式skill。LPコーディング後の動作確認・E2Eを自動化したい人にハマる。
   - ソース: https://github.com/browserbase/skills
   - 注意: Browserbase本体のクラウドブラウザ実行には**有料プラン**が必要です（無料枠あり）。

---

## 今週のトレンドUI: 「AIチャット風ストリーミング表示」

ChatGPT/Claudeの返信のように、**文字が1文字ずつ流れて表示される演出**は今やAI系プロダクトの標準UIです。CVR/UX観点でのメリットは:

- **体感速度の向上**: 全文生成を待たずに読み始められる → 離脱率を下げる
- **「考えている感」の演出**: 待ち時間を価値に変える
- **視線誘導**: 自然と最新メッセージにフォーカスが集まる

### React + TypeScript + TailwindCSS での実装例

`components/StreamingMessage.tsx` として作成します:

```tsx
"use client";

import { useEffect, useState } from "react";

type StreamingMessageProps = {
  /** 表示する全文 */
  text: string;
  /** 1文字あたりの遅延ms（デフォルト30ms） */
  speed?: number;
  /** 完了時のコールバック */
  onDone?: () => void;
};

export function StreamingMessage({
  text,
  speed = 30,
  onDone,
}: StreamingMessageProps) {
  const [displayed, setDisplayed] = useState("");

  useEffect(() => {
    setDisplayed(""); // text が変わったらリセット
    let i = 0;
    const timer = setInterval(() => {
      i += 1;
      setDisplayed(text.slice(0, i));
      if (i >= text.length) {
        clearInterval(timer);
        onDone?.();
      }
    }, speed);

    return () => clearInterval(timer); // クリーンアップ重要
  }, [text, speed, onDone]);

  const isDone = displayed.length === text.length;

  return (
    <p className="whitespace-pre-wrap text-slate-800 leading-relaxed">
      {displayed}
      {!isDone && (
        <span className="ml-0.5 inline-block w-1.5 h-4 align-middle bg-slate-500 animate-pulse" />
      )}
    </p>
  );
}
```

そして使う側 (`app/chat/page.tsx` など):

```tsx
"use client";

import { useState } from "react";
import { StreamingMessage } from "@/components/StreamingMessage";

type Message = {
  id: string;
  role: "user" | "assistant";
  content: string;
};

export default function ChatPage() {
  const [messages, setMessages] = useState<Message[]>([
    {
      id: "1",
      role: "assistant",
      content:
        "こんにちは、さとるさん。今日はストリーミングUIを一緒に作りましょう。",
    },
  ]);

  return (
    <main className="mx-auto max-w-2xl p-6 space-y-4">
      {messages.map((m) => (
        <div
          key={m.id}
          className={
            m.role === "assistant"
              ? "rounded-2xl bg-slate-50 border border-slate-200 px-4 py-3"
              : "rounded-2xl bg-blue-600 text-white px-4 py-3 ml-auto max-w-[80%]"
          }
        >
          {m.role === "assistant" ? (
            <StreamingMessage text={m.content} />
          ) : (
            <p>{m.content}</p>
          )}
        </div>
      ))}
    </main>
  );
}
```

### JS/TSの「ロジック」と「型」をやさしく解説

1. **`type StreamingMessageProps = { ... }`**
   - これは「プロパティの設計図」です。`text: string`は必須、`speed?: number`の`?`は「省略可」を意味します。型をつけておくと、呼び出し側で`text`を渡し忘れたときにエディタが赤線で教えてくれます。

2. **`useState<Message[]>([...])`**
   - 山括弧 `<Message[]>` は「この箱には Message型の配列が入りますよ」という宣言。最初は`role`に`"admin"`を入れようとしてもTypeScriptが止めてくれます（`"user" | "assistant"`しか許されていないため）。

3. **`useEffect` のクリーンアップ**
   - `return () => clearInterval(timer)` がないと、コンポーネントが消えたあとも`setInterval`が動き続け、メモリリークの原因になります。**「副作用は後片付けまでセット」** と覚えると安全です。

4. **`text.slice(0, i)`**
   - 文字列の先頭から`i`文字を切り出すだけのシンプルな処理。`i`を1ずつ増やしながら再描画することで、タイピング演出が完成します。

5. **`onDone?.()` の `?.`**
   - 「もし`onDone`が渡されていれば呼び出す」という安全演算子。`if (onDone) onDone()`の短縮形と覚えればOK。

---

## 実践ポイント（さとるさんへ）

- **業務LPへの応用**: ヒーローセクションのキャッチコピーを`StreamingMessage`で表示するだけで、AI時代らしいモダンな印象になります。`speed={50}`くらいに遅めにすると、視線誘導効果が高まりCTAクリック率が上がりやすい（ABテストでよく効くパターン）。
- **個人開発への応用**: Supabaseの`messages`テーブル（id / user_id / role / content / created_at）にデータを保存し、Vercel上のNext.js Route Handlerで取得 → クライアントで`StreamingMessage`に流す、で「AIチャットアプリのMVP」が1日で組めます。
- **FLOCSS的視点**: 上記コンポーネントはBEMで書くなら`.c-chat-message`（Component）と`.c-chat-message__cursor`にあたります。Tailwindでもこの命名感覚を持っておくと、後でCSS Modulesに移行するときに楽です。
- **JS基礎固めとして**: `setInterval` / `clearInterval` / 配列の`slice`は今日の3点セット。明日の自分のために、空のNext.jsプロジェクトを作って`StreamingMessage`だけでも写経してみてください。

---

## 今日の思考トリガー

**「あなたが今関わっているLPやサイトに、もし1秒の『待ち時間』を意図的に作るとしたら、その間にユーザーに何を見せれば離脱されずに価値を感じてもらえるだろうか？」**

_ソース: https://github.com/trending/javascript?since=weekly (2026年5月12日時点) ／ Hacker News Algolia API は本日 HTTP 403 で取得不可_
