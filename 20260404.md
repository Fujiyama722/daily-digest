# 🛠️ 週末ハンズオン実践 — Reactで学ぶモダンデスクトップアプリの設計パターン: 2026年04月04日号

おはようございます！フロントエンドメンターの「学習コーチ」です。

週末がやってきました！今週はGitHubトレンドで注目を集めているプロジェクトから、さとるさんが実際に手を動かして学べるテーマをピックアップしました。今日のテーマは **「Electron + React + TypeScriptで作るデスクトップアプリの設計パターン」** です。トレンド1位の`openscreen`を題材に、React/TypeScriptの実践的なコード構造を学びましょう。

---

## 本日のハイライト

### 今週のGitHubトレンド注目ポイント

1. **obra/superpowers** ⭐ +17,089/週 — AIエージェント向けスキルフレームワーク。開発方法論としても注目されており、Claude Codeとの相性が良いツール群です。
   - [GitHub](https://github.com/obra/superpowers)

2. **luongnv89/claude-howto** ⭐ +15,450/週 — Claude Codeのビジュアル解説ガイド。基礎から高度なエージェント活用まで網羅。さとるさんのClaude Code活用スキルアップに直結します。
   - [GitHub](https://github.com/luongnv89/claude-howto)

3. **microsoft/VibeVoice** ⭐ +11,071/週 — Microsoftが公開したオープンソースの音声AI。フロントエンドとの統合事例としても興味深いプロジェクトです。
   - [GitHub](https://github.com/microsoft/VibeVoice)

4. **Yeachan-Heo/oh-my-claudecode** ⭐ +9,761/週 — Claude Codeのマルチエージェントオーケストレーション。TypeScript製で、19種類の専門エージェントを組み合わせた開発パイプライン自動化ツールです。
   - [GitHub](https://github.com/Yeachan-Heo/oh-my-claudecode)

5. **siddharthvaddem/openscreen** ⭐ +5,954/週 — 無料でプロ品質のデモ動画を作成できるオープンソースツール。**Electron + React + TypeScript + Vite** という、今日のハンズオンの題材です。
   - [GitHub](https://github.com/siddharthvaddem/openscreen)

---

## 注目リポジトリ / 事例

### 1. openscreen — React + TypeScriptデスクトップアプリの実践教材

- **概要**: Screen Studioの無料代替。画面録画・ズームエフェクト・アノテーション・動画トリミングなど充実の機能
- **技術スタック**: Electron / React / TypeScript / Vite / PixiJS
- **注目点**: UIコンポーネントの設計、状態管理、描画ライブラリとの連携パターンが学べる
- [GitHub](https://github.com/siddharthvaddem/openscreen)

### 2. oh-my-claudecode — TypeScript製エージェントオーケストレーション

- **概要**: Claude Codeにマルチエージェント機能を追加。plan → prd → exec → verify → fixの自動パイプライン
- **技術スタック**: Node.js / TypeScript
- **注目点**: TypeScriptの型システムを活用した大規模なプラグインアーキテクチャの設計が参考になる
- [GitHub](https://github.com/Yeachan-Heo/oh-my-claudecode)

### 3. claude-howto — Claude Code学習の決定版ガイド

- **概要**: Claude Codeの使い方をビジュアル＋実例ベースで解説。初心者〜上級者まで対応
- **注目点**: さとるさんが日々使っているClaude Codeの「知らなかった機能」を発見できる
- [GitHub](https://github.com/luongnv89/claude-howto)

---

## 週末ハンズオン: React + TypeScriptの設計パターンを学ぼう

今日は`openscreen`のようなデスクトップアプリで使われている **React + TypeScript** の設計パターンを、さとるさんのNext.js/TailwindCSSスタックで再現してみましょう。

### テーマ: カスタムフックで「録画状態管理」を型安全に実装する

デスクトップアプリのような複雑な状態（録画中/停止/一時停止）を扱うとき、Reactの **カスタムフック** と TypeScriptの **ユニオン型** が威力を発揮します。

#### ステップ1: 状態の型を定義する

```typescript
// types/recorder.ts

// ユニオン型（Union Type）で「録画の状態」を厳密に定義
// ポイント: string ではなく、決まった値だけを許可する → バグ防止
type RecordingStatus = "idle" | "recording" | "paused";

// 録画データの型定義
// ポイント: 各プロパティに型をつけることで、誤った値の代入をコンパイル時に防げる
interface RecordingState {
  status: RecordingStatus; // 現在の状態
  duration: number; // 録画時間（秒）
  startedAt: Date | null; // 録画開始時刻（未開始ならnull）
}
```

**初心者向け解説:**

- `type RecordingStatus = "idle" | "recording" | "paused"` は **リテラル型のユニオン** です。`string`と違い、この3つの値以外を代入しようとするとエラーになります。
- `Date | null` は「Date型 または null」を意味します。録画前は開始時刻がないので`null`を許可しています。

#### ステップ2: カスタムフックを作る

```typescript
// hooks/useRecorder.ts
import { useState, useCallback, useRef, useEffect } from "react";

// カスタムフック = ロジックを再利用可能な関数に切り出すReactのパターン
// ポイント: "use" で始める命名規則がReactの約束
export function useRecorder() {
  // useState にジェネリクス型 <RecordingStatus> を指定
  // → status には "idle" | "recording" | "paused" 以外入らない
  const [status, setStatus] = useState<RecordingStatus>("idle");
  const [duration, setDuration] = useState<number>(0);

  // useRef でタイマーIDを保持（再レンダリングしても値が消えない）
  // NodeJS.Timeout | null 型を明示
  const timerRef = useRef<NodeJS.Timeout | null>(null);

  // useCallback でメモ化 → 不要な再生成を防ぐ
  const start = useCallback(() => {
    setStatus("recording");
    setDuration(0);
    // 1秒ごとにdurationを+1する
    timerRef.current = setInterval(() => {
      setDuration((prev) => prev + 1);
    }, 1000);
  }, []);

  const pause = useCallback(() => {
    setStatus("paused");
    // タイマー停止（nullチェック付き）
    if (timerRef.current) {
      clearInterval(timerRef.current);
    }
  }, []);

  const stop = useCallback(() => {
    setStatus("idle");
    if (timerRef.current) {
      clearInterval(timerRef.current);
    }
    setDuration(0);
  }, []);

  // クリーンアップ: コンポーネントがアンマウントされたらタイマーを解除
  useEffect(() => {
    return () => {
      if (timerRef.current) {
        clearInterval(timerRef.current);
      }
    };
  }, []);

  // as const で返り値の型を厳密に推論させる
  return { status, duration, start, pause, stop } as const;
}
```

**初心者向け解説:**

- `useCallback` は関数をメモ化（キャッシュ）するフックです。依存配列`[]`が空なので、コンポーネントの初回レンダリング時に1回だけ関数が作られます。
- `useRef` はレンダリングをまたいで値を保持するフックです。`useState`と違い、値が変わっても再レンダリングが起きません。タイマーIDの保持に最適です。
- `as const` をつけると、TypeScriptが返り値の型をより厳密に推論してくれます。

#### ステップ3: UIコンポーネントで使う

```tsx
// components/RecorderPanel.tsx
"use client"; // Next.js App Routerではクライアントコンポーネントに必要

import { useRecorder } from "@/hooks/useRecorder";

export function RecorderPanel() {
  // カスタムフックからstate と 操作関数を取得
  const { status, duration, start, pause, stop } = useRecorder();

  // 秒数を "MM:SS" 形式にフォーマットする関数
  const formatTime = (seconds: number): string => {
    const m = Math.floor(seconds / 60)
      .toString()
      .padStart(2, "0");
    const s = (seconds % 60).toString().padStart(2, "0");
    return `${m}:${s}`;
  };

  return (
    <div className="flex flex-col items-center gap-6 p-8 rounded-2xl bg-gray-900 text-white max-w-sm mx-auto">
      {/* 状態表示バッジ */}
      <div
        className={`px-3 py-1 rounded-full text-sm font-medium ${
          status === "recording"
            ? "bg-red-500"
            : status === "paused"
              ? "bg-yellow-500 text-black"
              : "bg-gray-600"
        }`}
      >
        {status === "recording"
          ? "● 録画中"
          : status === "paused"
            ? "⏸ 一時停止"
            : "⏹ 停止中"}
      </div>

      {/* タイマー表示 */}
      <p className="text-5xl font-mono tabular-nums">{formatTime(duration)}</p>

      {/* 操作ボタン — status に応じて表示を切り替え */}
      <div className="flex gap-3">
        {status === "idle" && (
          <button
            onClick={start}
            className="px-6 py-2 bg-red-600 hover:bg-red-700 rounded-lg transition-colors"
          >
            録画開始
          </button>
        )}
        {status === "recording" && (
          <>
            <button
              onClick={pause}
              className="px-6 py-2 bg-yellow-500 hover:bg-yellow-600 text-black rounded-lg transition-colors"
            >
              一時停止
            </button>
            <button
              onClick={stop}
              className="px-6 py-2 bg-gray-600 hover:bg-gray-700 rounded-lg transition-colors"
            >
              停止
            </button>
          </>
        )}
        {status === "paused" && (
          <>
            <button
              onClick={start}
              className="px-6 py-2 bg-red-600 hover:bg-red-700 rounded-lg transition-colors"
            >
              再開
            </button>
            <button
              onClick={stop}
              className="px-6 py-2 bg-gray-600 hover:bg-gray-700 rounded-lg transition-colors"
            >
              停止
            </button>
          </>
        )}
      </div>
    </div>
  );
}
```

**初心者向け解説:**

- `"use client"` はNext.js App Routerで、ブラウザ側で動くコンポーネントであることを宣言するディレクティブです。`useState`や`useEffect`を使うコンポーネントには必須です。
- 三項演算子 `status === "recording" ? ... : ...` で状態に応じたスタイル切り替えをしています。TypeScriptのユニオン型のおかげで、`status`の値を間違えるとエディタが警告してくれます。
- `tabular-nums` はTailwindCSSのクラスで、数字の幅を均一にします。タイマー表示がガタつかなくなる便利なクラスです。

### 発展課題（余裕があれば）

1. **録画履歴をSupabaseに保存**: `duration`と`startedAt`をSupabaseのテーブルに`INSERT`してみましょう
2. **TailwindCSSのアニメーション追加**: 録画中に赤いドットを点滅させる`animate-pulse`を試してみましょう
3. **openscreenのソースコードを読む**: 実際のプロジェクトで`PixiJS`とReactがどう連携しているか観察してみましょう

---

## 実践ポイント（さとるさんへ）

1. **カスタムフックを作る習慣をつけよう**: 今回の`useRecorder`のように、UIロジックをフックに切り出す練習は、シニアエンジニアへの最短ルートです。LP制作で使うフォームバリデーションやモーダル制御も同じパターンで整理できます。

2. **TypeScriptのユニオン型を武器にしよう**: `"idle" | "recording" | "paused"` のようなリテラル型は、バグを未然に防ぐ強力な仕組みです。業務のHTML/CSSコーディングでも、コンポーネントの`variant`（例: `"primary" | "secondary" | "danger"`）に使えます。

3. **claude-howtoリポジトリを週末に読もう**: さとるさんが毎日使っているClaude Codeの「もっと上手な使い方」が見つかるはずです。特にエージェントモードの活用法は個人開発の効率を大きく変えます。

4. **今日のコードをそのまま個人開発に組み込もう**: Next.jsプロジェクトに`hooks/`ディレクトリを作り、`useRecorder.ts`を配置するだけで動きます。小さなカスタムフックを積み重ねることが、プロジェクトの品質向上につながります。

---

## 今日の思考トリガー

**「あなたが今作っている（または作りたい）個人開発プロダクトで、3つ以上の状態を持つ機能はありますか？ それをユニオン型 + カスタムフックで設計するとしたら、どんな型定義になるでしょうか？」**

_ソース: [GitHub Trending](https://github.com/trending?since=weekly), [openscreen](https://github.com/siddharthvaddem/openscreen), [oh-my-claudecode](https://github.com/Yeachan-Heo/oh-my-claudecode), [claude-howto](https://github.com/luongnv89/claude-howto), [React TypeScript Guide (Medium)](https://medium.com/@robinviktorsson/complete-guide-to-setting-up-react-with-typescript-and-vite-2025-468f6556aaf2) (2026年04月04日時点)_
