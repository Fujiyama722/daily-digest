# 🎨 トレンドUI実装チュートリアル: 2026年5月26日号

おはようございます！シニアUI/UXデザイナー兼フロントエンドエンジニアの「学習コーチ」です。

今日のテーマは **コンテナクエリ（Container Queries）** です。「メディアクエリは知ってるけど…」というさとるさんに、2026年のレスポンシブ設計のスタンダードになりつつあるこの技術を、UX視点 + 実装コードでじっくり解説します。LP制作にそのまま効く内容なので、ぜひ手を動かしてみてください。

---

## 本日のハイライト

### 1. レスポンシブ設計の主役が「画面幅」から「要素の幅」へ

長年、私たちは `@media (min-width: 768px)` のように **ブラウザの画面幅** を基準にレイアウトを切り替えてきました。しかし2026年現在、Tailwind CSS v4 でコア機能になった **コンテナクエリ（`@container`）** により、「**親要素の幅**を基準に子要素を変える」設計が主流になっています。

- ソース: [Tailwind CSS v4.0 公式ブログ](https://tailwindcss.com/blog/tailwindcss-v4)（2026年5月26日時点）
- ソース: [Tailwind CSS 公式ドキュメント — Responsive design](https://tailwindcss.com/docs/responsive-design)（2026年5月26日時点）

なぜ重要か？ 例えば「カード」コンポーネントを、**サイドバーの中**でも**メインエリアの中**でも使い回したいとき。従来のメディアクエリだと「画面が広い＝カードも広い」と決めつけていましたが、サイドバーの中ではカードは狭いままです。コンテナクエリなら「**カードを置いた場所の幅**」で見た目を切り替えられるため、コンポーネントが真の意味で再利用可能になります。

### 2. Tailwind v4 ではプラグイン不要でコンテナクエリが使える

v3 時代は `@tailwindcss/container-queries` プラグインの追加が必要でしたが、**v4 からはコア機能**として標準搭載されました。`@container` クラスと `@sm:` `@md:` `@lg:` といったバリアントだけで完結します。

- ソース: [Tailwind CSS v4.0 公式ブログ](https://tailwindcss.com/blog/tailwindcss-v4)（2026年5月26日時点）

### 3. GitHub Trending（JavaScript・週間）の傾向

今週の JavaScript トレンドは「学習コンテンツ」と「実プロダクトの Web クライアント」が目立ちました（数値は GitHub Trending 掲載の実データ）。

- `datawhalechina/easy-vibe` — 初心者向けのモダンコーディング学習コース（14,684★ / 今週 +2,406）
- `Stremio/stremio-web` — Stremio のメディア Web クライアント（11,850★ / 今週 +1,036）
- `jellyfin/jellyfin-web` — FOSS メディアサーバーの公式 Web クライアント（3,597★ / 今週 +29）
- ソース: [GitHub Trending JavaScript（週間）](https://github.com/trending/javascript?since=weekly)（2026年5月26日時点）

> ※ 本日は Hacker News（hn.algolia.com / firebaseio.com）が実行環境のネットワーク制限により取得できませんでした。HN発のフロントエンド記事は次回以降に持ち越します。

---

## 注目リポジトリ / 事例

UI実装の学習に役立つ「**実プロダクトのコードを読む**」教材として、今週トレンド入りした以下3つを挙げます。すべて GitHub Trending 掲載の実データです。

1. **`datawhalechina/easy-vibe`**（14,684★ / 今週 +2,406）
   初心者がステップ・バイ・ステップでモダン開発を学ぶ「vibe coding」コース。※コンテンツは主に中国語です。
   - URL: https://github.com/datawhalechina/easy-vibe

2. **`jellyfin/jellyfin-web`**（3,597★ / 今週 +29）
   実運用されているメディアサーバーの Web クライアント。コンポーネント分割やUI状態管理を「動いている本物のコード」で学べます。
   - URL: https://github.com/jellyfin/jellyfin-web

3. **`Stremio/stremio-web`**（11,850★ / 今週 +1,036）
   React 製のメディア Web クライアント。レイアウトやレスポンシブ対応の実例として読み応えがあります。
   - URL: https://github.com/Stremio/stremio-web

_いずれも無料（OSS）です。学習目的でクローンして `README` のセットアップ手順を読むだけでも勉強になります。_

---

## 実践ポイント（さとるさんへ）

さとるさんの普段の仕事（LP・Webページのコーディング、レスポンシブ対応）に**そのまま効く**コンテナクエリを、手順を追って実装してみましょう。

### Step 1: まず「メディアクエリ版」を見て課題を理解する

よくある「商品カードのグリッド」です。Tailwind の `sm:` `lg:` は**画面幅**で切り替わります。

```tsx
// ProductGrid.tsx（従来のメディアクエリ版）
export function ProductGrid() {
  return (
    <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
      {/* 画面が広ければ4列。でも「狭いサイドバーの中」に置いても4列にしようとして崩れる */}
    </div>
  );
}
```

課題：このグリッドを **狭いサイドバー** と **広いメインエリア** の両方で使い回すと、画面幅だけで判断するため、狭い場所でも無理やり多列にして崩れてしまいます。

### Step 2: コンテナクエリ版に書き換える

親に `@container`、子のグリッドに `@sm:` `@lg:` を付けるだけ。今度は「**この親要素の幅**」で列数が決まります。

```tsx
// ProductGrid.tsx（コンテナクエリ版）
export function ProductGrid() {
  return (
    // 👇 この要素が「測る基準（コンテナ）」になる
    <div className="@container">
      {/* 👇 @sm/@lg は "親(@container)の幅" を見て切り替わる */}
      <div className="grid grid-cols-1 @sm:grid-cols-2 @lg:grid-cols-4 gap-4">
        {/* カードたち */}
      </div>
    </div>
  );
}
```

これで、同じ `<ProductGrid />` を狭いサイドバーに置けば自動的に1列、広いメインに置けば4列に。**コンポーネントが置かれた場所に賢く適応**します。

- 構文の出典: [Tailwind CSS v4.0 公式ブログ](https://tailwindcss.com/blog/tailwindcss-v4)（2026年5月26日時点）

### Step 3: TypeScript で「型」も意識する（初心者向け解説）

カードに渡すデータの「形」を `type` で定義しておくと、props の渡し間違いを**エディタ（Antigravity）が書いている最中に**指摘してくれます。これが TypeScript の最大の恩恵です。

```tsx
// 「商品1件はこういう形のデータですよ」という設計図 = 型
type Product = {
  id: string;
  name: string;
  price: number;       // 数値型。"1000円" のような文字列を入れると即エラーになる
  imageUrl: string;
  isSoldOut?: boolean; // 末尾の "?" は「あってもなくてもOK（任意）」の意味
};

// Props（このコンポーネントが受け取る値）の型
type ProductCardProps = {
  product: Product;
};

export function ProductCard({ product }: ProductCardProps) {
  return (
    <article className="@container rounded-xl border p-4">
      {/* @max-md は「コンテナが md 未満のとき」= 狭いとき縦並びに */}
      <div className="flex flex-col @md:flex-row gap-3">
        <img
          src={product.imageUrl}
          alt={product.name}
          className="w-full @md:w-32 rounded-lg object-cover"
        />
        <div>
          <h3 className="font-bold">{product.name}</h3>
          <p className="text-gray-600">{product.price.toLocaleString()}円</p>
          {/* isSoldOut が true のときだけ "&&" の右側が表示される（条件付きレンダリング） */}
          {product.isSoldOut && (
            <span className="text-red-500 text-sm">売り切れ</span>
          )}
        </div>
      </div>
    </article>
  );
}
```

**ポイント解説（JS/TSの仕組み）:**
- `?`（オプショナル）… 型の末尾に付けると「その項目は無くてもよい」。`isSoldOut` を渡さなくてもエラーになりません。
- `&&` による条件表示 … `条件 && <JSX/>` は「左が `true` のときだけ右を表示」。React で「あるときだけ出す」定番パターンです。
- `.toLocaleString()` … 数値 `1000` を `1,000` のように3桁区切りに整形する標準メソッド。価格表示で頻出します。
- `@max-md:flex-row` ではなく `@md:flex-row` … 「コンテナが md 以上になったら横並び」。**モバイルファースト**（まず狭い前提で書き、広がったら上書き）の考え方です。

### UX / CVR の視点

LP やカード一覧での**離脱を防ぐ**鍵は「どの幅でも崩れず、視線が迷わない」こと。コンテナクエリを使うと、再利用するセクションごとに個別調整する必要が減り、**表示崩れによる不信感（＝離脱・CVR低下）**を構造的に防げます。「画面幅」ではなく「置き場所の幅」で考える癖をつけると、保守性も一段上がります。

---

## 今日の思考トリガー

**「あなたが今あるLPで使っている『同じ見た目のパーツ』はどれですか？ それを `@container` で包んで、サイドバーにもメインにも置ける"場所を選ばない部品"にできないか、1つ設計してみましょう。」**

_ソース: [Tailwind CSS v4.0 公式ブログ](https://tailwindcss.com/blog/tailwindcss-v4) / [Tailwind CSS 公式ドキュメント Responsive design](https://tailwindcss.com/docs/responsive-design) / [GitHub Trending JavaScript（週間）](https://github.com/trending/javascript?since=weekly) (2026年5月26日時点)_
