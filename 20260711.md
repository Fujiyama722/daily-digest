# 🛠️ 週末ハンズオン実践: 2026年07月11日号

おはようございます！フロントエンドメンターの「学習コーチ」です。今日は土曜日、週末ハンズオンの日。今週GitHubで話題になった「Agent Ready」なOSSデザインシステムを題材に、Next.js × TypeScript × TailwindCSSで手を動かしてみましょう。

---

## 本日のハイライト

- **Meta発、Agent Readyな新OSSデザインシステム「Astryx」が急上昇** — 今週GitHub Trendingで週間+4,087スター、累計7,705スター獲得。React + StyleX製で150以上のアクセシブルなコンポーネントを提供します（[facebook/astryx](https://github.com/facebook/astryx)）。「Agent Ready」を掲げているのが特徴で、AIエージェントによる操作・拡張を前提とした設計思想が個人開発時代のスタンダードになりそうな予感です。
- **AlibabaがJavaScript製の「in-page GUI エージェント」page-agentを公開** — 週間+4,459スター、累計25,796スター。スクリーンショットに頼らず、DOM上で直接自然言語操作できるのが革新的（[alibaba/page-agent](https://github.com/alibaba/page-agent)）。TypeScript 82.6%製、npm/Chrome拡張/MCPサーバーとして使えます。
- **プライバシーファーストなAI議事録アプリ「Meetily」が今週の首位** — 週間+8,885スター、累計22,619スター（[Zackriya-Solutions/meetily](https://github.com/Zackriya-Solutions/meetily))。Rust + Tauri製で、Whisper/Parakeetをローカル実行することでクラウドAPIに音声を送らず動作します。個人開発でプライバシーを打ち出す事例として学ぶ価値大。
- ※ Hacker News Algolia検索APIは本日403 Forbiddenで取得できませんでした。

## 注目リポジトリ / 事例

### 1. [facebook/astryx](https://github.com/facebook/astryx) — Agent Ready React Design System
- **言語**: TypeScript (74.7%)
- **累計スター**: 7,705（週間+4,087）
- **概要**: Meta社内で開発された、React + StyleXベースのOSSデザインシステム。150+のアクセシブルなコンポーネント、テーマ機能、CLIを同梱。現時点でbeta段階。
- **さとる的注目ポイント**: 「AIエージェントと人間の開発者が並走して使う」ことを設計思想に置いている点。CLIでコンポーネントを追加していくshadcn/uiに近い体験で、個人開発でも導入しやすい。

### 2. [alibaba/page-agent](https://github.com/alibaba/page-agent) — In-page GUI Agent for the Web
- **言語**: TypeScript (82.6%)
- **累計スター**: 25,796（週間+4,459）
- **概要**: ヘッドレスブラウザやスクリーンショットに頼らず、ページ内JavaScriptだけで自然言語→DOM操作を実現するエージェント基盤。MCPサーバーとしても利用可能。
- **さとる的注目ポイント**: Webサイト運用の業務（フォーム自動入力、動作確認、コンテンツ更新チェック等）の自動化に応用できる余地あり。まずはChrome拡張版から触ってみるとイメージがつかめる。

### 3. [Zackriya-Solutions/meetily](https://github.com/Zackriya-Solutions/meetily) — Local-first AI Meeting Assistant
- **言語**: Rust (46.2%) + TypeScript (29.7%)
- **累計スター**: 22,619（週間+8,885）
- **概要**: 音声認識・要約すべてをローカルで完結させるAI議事録アプリ。有料の"PRO"ティアあり（クラウド精度・エンタープライズ機能）。MITライセンス。
- **さとる的注目ポイント**: 個人開発の収益化モデルとして「OSS + PROプラン」が今も有効な事例。README・ランディングの見せ方が学びになる。

---

## 実践ポイント（さとるさんへ）

### 今週末のハンズオン：Astryxの「Agent Ready」を体験する

さとるさんのスタック（Next.js + TypeScript + TailwindCSS）に、話題のAstryxを部分的に組み込んでみましょう。まだbeta段階のライブラリなので、いきなり全部を置き換えず、1コンポーネントだけ試すのがコツです。

```bash
# 新しいNext.js（App Router）プロジェクトを立ち上げ
npx create-next-app@latest astryx-lab --typescript --tailwind --app
cd astryx-lab

# Astryxを追加（※betaにつきAPIが変わる可能性あり、公式READMEを必ず参照）
npm install @astryx/react @astryx/tokens
```

そのうえで、`app/page.tsx` に「Astryxのボタン」と「TailwindCSSのカード」を同居させてみます。

```tsx
// app/page.tsx
import { Button } from '@astryx/react';

// props の型を明示。onClickは「引数なし・戻り値なし」の関数
type CTACardProps = {
  title: string;
  description: string;
  onCta: () => void;
};

export default function Home() {
  const handleClick = () => {
    console.log('Astryx button clicked');
  };

  return (
    <main className="min-h-screen flex items-center justify-center bg-slate-50">
      <CTACard
        title="週末ハンズオン"
        description="AstryxのButtonとTailwindのレイアウトを同居させる練習"
        onCta={handleClick}
      />
    </main>
  );
}

function CTACard({ title, description, onCta }: CTACardProps) {
  return (
    <section className="w-full max-w-md rounded-2xl bg-white p-8 shadow-md">
      <h1 className="mb-2 text-2xl font-bold text-slate-900">{title}</h1>
      <p className="mb-6 text-slate-600">{description}</p>
      <Button onClick={onCta}>Astryxで作られたボタン</Button>
    </section>
  );
}
```

### コードのポイント解説（TypeScript初心者向け）

- **`type CTACardProps = { ... }`**: コンポーネントが受け取るpropsの「形」を1か所で定義することで、呼び出し側で `title` を書き忘れたりtypoしたら**エディタが即座に赤線を出してくれる**。これがTypeScriptの一番のご褒美。
- **`onCta: () => void`**: 「引数なし・戻り値なしの関数」を受け取るという意味。ボタンのクリックハンドラのように「実行してほしいだけ」の場面で頻出します。慣れると英文みたいに読めるようになる。
- **`function CTACard({ title, description, onCta }: CTACardProps)`**: 分割代入 + 型注釈のセット。「オブジェクトを受け取って、その中から3つのプロパティを取り出す」ことを1行で表現しています。
- **Astryx（デザインシステム）とTailwindCSS（ユーティリティ）の同居**: `Button`のような「意味を持ったパーツ」はデザインシステムに任せ、「余白・レイアウト」はTailwindで書く、という役割分担ができるとコードが一気にすっきりします。LPコーディングで培ったCSSの感覚が、そのままレイアウト設計に活きます。

### Claude Code (CLI)への渡し方

Antigravityから Claude Code CLI に依頼するときは、こう投げるとブレません。

```
このコンポーネントの Button を、Astryx の
variant="primary" と size="lg" に差し替えて。
型エラーが出たら、Astryx の README に沿った props を
使う方針で修正して。
```

「差し替える対象」「制約」「エラー時の方針」の3点セットを渡すと、Claude Codeが暴走せずに1コミット分の変更に収めてくれます。

## 今日の思考トリガー

**「AIエージェントが自分のコードを『読み書き』する時代に、自分だけが読める設計・命名・コメントの価値はどう変わるだろう？ 逆に、エージェントに優しいコードを書くことは、半年後の自分にも優しいのだろうか？」**

Astryxが掲げる "Agent Ready" は単なるバズワードではなく、**「AIが理解しやすいコード = 人間にとっても読みやすいコード」** という普遍原則の再確認だと僕は捉えています。FLOCSSで培った命名の一貫性は、そのままエージェント時代の武器になります。

_ソース: [GitHub Trending Weekly](https://github.com/trending?since=weekly), [facebook/astryx](https://github.com/facebook/astryx), [alibaba/page-agent](https://github.com/alibaba/page-agent), [Zackriya-Solutions/meetily](https://github.com/Zackriya-Solutions/meetily) (2026年07月11日時点)_
