# 🛠️ 週末ハンズオン実践: 2026年4月25日号

おはようございます！フロントエンドメンターの「学習コーチ」です。
今週末は、WebサイトやLPに"動き"をつける **anime.js** を実践的に学んでいきましょう。
LPコーディングをしているさとるさんに、そのまま使えるコード付きでお届けします。

---

## 本日のハイライト

### 今週のJavaScriptトレンド: anime.js が再注目

今週の GitHub JavaScript トレンドで `juliangarnier/anime`（累計 ⭐ 67,358、今週 +209）がランクイン。
anime.js は「JavaScript Animation Engine」として知られる軽量アニメーションライブラリです。
CSS プロパティ・SVG・DOM 属性・JavaScript オブジェクトを統一した API でアニメートできます。

**なぜ今 anime.js が注目されるのか？**

- Webサイト・LPの「スクロールで要素がフワっと表示」「数字がカウントアップ」「ボタンがバウンス」といった演出はCVR向上に直結する
- CSS アニメーションだけでは難しい「タイミング制御」「連続アニメーション」を JavaScript で柔軟に扱える
- ファイルサイズが軽量で、Next.js/React との相性も良い
- TypeScript の型定義（`@types/animejs`）が整備されており、初心者でも型エラーに助けられながら使える

ソース: https://github.com/trending/javascript?since=weekly (2026年4月25日時点)

> ※ Hacker News および HN Algolia は本日取得できませんでした（403エラー）。

---

## 注目リポジトリ

### 1. juliangarnier/anime
JavaScript アニメーションエンジン。CSS プロパティ・SVG・DOM 属性・JS オブジェクトをアニメートできる軽量ライブラリ。LP演出・UI トランジションに最適。
累計 ⭐ 67,358 | 今週 +209
https://github.com/juliangarnier/anime

### 2. mrdoob/three.js
WebGL ベースの 3D JavaScript ライブラリ。ブラウザ上で 3D グラフィックスを描画できる定番ライブラリ。ポートフォリオ・製品ページの差別化に使われる。まずは anime.js を習得してから挑戦すると良い。
累計 ⭐ 112,164 | 今週 +207
https://github.com/mrdoob/three.js

### 3. HeyPuter/puter
「The Internet Computer」を謳う、セルフホスト可能なクラウドプラットフォーム。ブラウザ上でOS風のUIを実現するOSSプロジェクト。UI実装の参考になるコードが多い。
累計 ⭐ 40,638 | 今週 +301
https://github.com/HeyPuter/puter

---

## 実践ポイント（さとるさんへ）

### 週末ハンズオン: anime.js × React/TypeScript/TailwindCSS でLPアニメーションを作る

#### Step 1: インストール

```bash
npm install animejs
npm install --save-dev @types/animejs
```

---

#### Step 2: anime.js の基本 API を理解する

「**何を**」「**どんな動きで**」「**どれくらいかけて**」の3つを指定するだけです。

```typescript
import anime from 'animejs';

anime({
  targets: '.my-element',  // アニメーションするHTML要素（CSSセレクタ or DOM参照）
  translateY: -40,          // Y方向に -40px 移動
  opacity: [0, 1],          // 透明度 0 → 1（フェードイン）
  duration: 800,            // 800ミリ秒かけて変化
  easing: 'easeOutExpo',   // イージング（動きの緩急を指定）
});
```

**TypeScriptの型チェックが助けてくれる例:**
`duration` に `'800'`（文字列）を渡すと TypeScript がエラーを出します。
型定義のおかげで「数値を渡すべき場所」がコードを書きながらわかります。

---

#### Step 3: React コンポーネントに組み込む

スクロールしたら要素がフェードインしながら下から上へスライドする `FadeInSection` コンポーネントを作ります。

```tsx
// components/FadeInSection.tsx
import { useEffect, useRef } from 'react';
import anime from 'animejs';

type Props = {
  children: React.ReactNode;
  delay?: number; // 遅延時間（ミリ秒）。省略すると 0
};

export default function FadeInSection({ children, delay = 0 }: Props) {
  // useRef: この div 要素への「直接参照」を保持する
  // React は仮想 DOM で動くため、anime.js のような直接 DOM 操作をするときに useRef が必要
  const ref = useRef<HTMLDivElement>(null);

  useEffect(() => {
    // useEffect: コンポーネントが実際の DOM に描画された後に実行される
    if (!ref.current) return;

    anime({
      targets: ref.current,
      translateY: [30, 0],  // 30px 下から 0px（元の位置）へスライド
      opacity: [0, 1],       // 透明 → 不透明にフェードイン
      duration: 700,
      delay,
      easing: 'easeOutCubic',
    });
  }, []); // [] = マウント（画面表示）時に1回だけ実行

  return (
    // TailwindCSS で初期状態を opacity-0 に（JS無効環境へのフォールバック）
    <div ref={ref} className="opacity-0">
      {children}
    </div>
  );
}
```

**このコンポーネントの使い方:**

```tsx
// app/page.tsx（Next.js App Router）
import FadeInSection from '@/components/FadeInSection';

export default function LandingPage() {
  return (
    <main className="flex flex-col items-center gap-8 py-20">
      <FadeInSection>
        <h1 className="text-4xl font-bold text-gray-900">サービスタイトル</h1>
      </FadeInSection>

      {/* delay を少しずつずらすと、要素が順番に現れて自然な演出になる */}
      <FadeInSection delay={200}>
        <p className="text-gray-600 text-lg">キャッチコピーが入ります</p>
      </FadeInSection>

      <FadeInSection delay={400}>
        <button className="bg-blue-500 hover:bg-blue-600 text-white px-8 py-4 rounded-xl text-lg transition-colors">
          今すぐ始める
        </button>
      </FadeInSection>
    </main>
  );
}
```

---

#### Step 4: 数値カウントアップ（LP の実績数字演出）

「導入実績 1,200件」「満足度 98%」のような数字をアニメーションさせる定番演出です。

```tsx
// components/CountUp.tsx
import { useEffect, useRef } from 'react';
import anime from 'animejs';

type Props = {
  end: number;        // カウントアップする最終値
  suffix?: string;    // 数字の後ろにつける文字（例: "件", "%"）
};

export default function CountUp({ end, suffix = '' }: Props) {
  const ref = useRef<HTMLSpanElement>(null);

  useEffect(() => {
    if (!ref.current) return;

    // アニメーションさせるオブジェクト（DOM ではなく JS オブジェクトを anime.js に渡す）
    const counter = { value: 0 };

    anime({
      targets: counter,
      value: end,       // 0 → end まで変化させる
      round: 1,         // 小数点以下を切り捨て（整数表示）
      duration: 1500,
      easing: 'easeOutExpo',
      update() {
        // アニメーション中、毎フレーム呼ばれるコールバック
        if (ref.current) {
          ref.current.textContent = counter.value.toLocaleString() + suffix;
        }
      },
    });
  }, [end, suffix]);

  return <span ref={ref}>0{suffix}</span>;
}
```

**使い方:**

```tsx
// LP の実績セクション
<div className="flex gap-16 justify-center py-12">
  <div className="text-center">
    <p className="text-5xl font-bold text-blue-600">
      <CountUp end={1200} suffix="件" />
    </p>
    <p className="text-gray-500 mt-2">累計導入実績</p>
  </div>
  <div className="text-center">
    <p className="text-5xl font-bold text-blue-600">
      <CountUp end={98} suffix="%" />
    </p>
    <p className="text-gray-500 mt-2">顧客満足度</p>
  </div>
</div>
```

---

#### 補足: `useRef` と `useState` の使い分け

| | `useState` | `useRef` |
|---|---|---|
| 値が変わると | 画面が再レンダリングされる | 再レンダリングされない |
| 主な用途 | 表示に影響する状態管理 | DOM への直接アクセス、アニメーション |
| anime.js との相性 | 不向き（毎回リセットされる） | 最適（DOM を直接渡せる） |

anime.js のようなライブラリは「React の外側」で DOM を直接操作します。`useRef` はその橋渡し役です。

---

## 今日の思考トリガー

**「今作りたい（または改善したい）LP・Webサイトに、どんな"動き"があるとユーザーが思わず目を留めると思いますか？　その演出は CVR にどうつながりますか？」**

_ソース: https://github.com/trending/javascript?since=weekly (2026年4月25日時点)_
