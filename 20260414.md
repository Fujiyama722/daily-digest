# 🎨 トレンドUI実装チュートリアル: 2026年04月14日号

おはようございます！シニアUI/UXデザイナー兼フロントエンドエンジニアの「学習コーチ」です。

今日取り上げるのは、今週 GitHub Trending にランクインしている **GSAP（GreenSock Animation Platform）のスクロールアニメーション** です。GSAP は週間 +149 スターを獲得し、累計 24,379 スター。2024年に全プラグインが無料化されたことで、これまで有料だった ScrollTrigger・SplitText・MorphSVG なども自由に使えるようになりました。さとるさんの業務（LP コーディング・新規ページ作成）で **ファーストビューの離脱率を下げ、CVR を押し上げる「スクロール駆動アニメーション」** を、React + Next.js + TypeScript + TailwindCSS で実装する方法を解説します。

---

## 本日のハイライト

### 1. GSAP が全プラグイン無料化後もトレンド継続

GSAP は jQuery の 20 倍高速とも言われるアニメーションライブラリで、1,200 万以上のサイトで採用されています。ScrollTrigger プラグインを使うと「スクロール位置に連動したアニメーション」を宣言的に書けます。LP のセクションごとにフェードイン・スライドインを付けるだけで、ユーザーの滞在時間とエンゲージメントが大幅に向上します。

ソース: https://github.com/greensock/GSAP

### 2. Material UI が v6 安定版に向け着実に進化

**mui/material-ui**（⭐ 98,141 / 今週 +165）は React コンポーネントライブラリの最大手。Google の Material Design を忠実に実装しており、ダッシュボードや管理画面を高速に構築したい場面で定番です。GSAP との組み合わせで「Material UI の堅牢な UI + GSAP の滑らかなアニメーション」という構成が増えています。

ソース: https://github.com/mui/material-ui

### 3. Svelte が週間 +182 スターで堅調

**sveltejs/svelte**（⭐ 86,293）はコンパイラ型フレームワークとして独自路線を走り続けています。Svelte のトランジション API は組み込みで非常に強力ですが、React エコシステムでは GSAP がその役割を担います。「フレームワークに依存しない」GSAP の設計思想は、将来スタックを変えても知識が無駄にならない点が魅力です。

ソース: https://github.com/sveltejs/svelte

### 4. AI コーディングツール周辺も引き続き活況

TypeScript トレンドでは **multica-ai/multica**（今週 +6,846）、**openscreen**（今週 +6,776）、**claude-mem**（今週 +3,534）が爆発的に伸びています。GSAP のようなアニメーション実装こそ、Claude Code に「ScrollTrigger でこのセクションをフェードインさせて」と指示すれば一発で生成できる分野です。

ソース: https://github.com/trending/typescript?since=weekly

---

## 注目リポジトリ / 事例

1. **greensock/GSAP**（⭐ 24,379 / 今週 +149）— CSS, SVG, Canvas, React, Vue, WebGL、あらゆるものをアニメーションできるフレームワーク非依存ライブラリ。ScrollTrigger, SplitText 等の全プラグインが無料に。
   ソース: https://github.com/greensock/GSAP

2. **siddharthvaddem/openscreen**（⭐ 29,336 / 今週 +6,776）— OSSのスクリーンレコーディングツール。React + TypeScript + Electron で構築。アニメーション付き LP のデモ動画を無料で作れるため、GSAP で作った LP の訴求にも使える。
   ソース: https://github.com/siddharthvaddem/openscreen

3. **Anil-matcha/Open-Generative-AI**（⭐ 4,672 / 今週 +1,801）— 20以上のモデルを使える無料 AI 画像生成プラットフォーム。LP のヒーロー画像やアイキャッチを AI で生成し、GSAP でアニメーションさせる組み合わせが面白い。
   ソース: https://github.com/Anil-matcha/Open-Generative-AI

---

## 実践ポイント（さとるさんへ）

### なぜスクロールアニメーションは CVR に効くのか（UX 視点）

- **「発見の快感」を演出する**: コンテンツがスクロールに応じて現れることで、ユーザーは「次に何があるか」を知りたくなり、自然とページを読み進めます。静的な LP と比べて平均スクロール深度が 20〜40% 向上するケースも。
- **情報の認知負荷を下げる**: すべてを一度に見せるのではなく、スクロールに合わせて段階的に表示することで、ユーザーの脳が情報を処理しやすくなります。
- **ブランドの品質感を伝える**: Apple, Stripe, Linear などの一流プロダクトが採用するスクロールアニメーションは、「このサービスはしっかり作り込まれている」というシグナルになります。

### React + Next.js + TypeScript + TailwindCSS + GSAP での実装

まずインストール:

```bash
npm install gsap
```

#### ステップ 1: useGSAP カスタムフック

GSAP v3.12+ には React 公式の `@gsap/react` パッケージがあり、`useGSAP` フックが提供されています。これを使うとクリーンアップ（アンマウント時のアニメーション破棄）を自動でやってくれます。

```bash
npm install @gsap/react
```

#### ステップ 2: スクロールフェードインコンポーネント

```tsx
// app/components/ScrollReveal.tsx
"use client"; // Next.js App Router ではクライアントコンポーネントに明示が必要

import { useRef } from "react";
import gsap from "gsap";
import { ScrollTrigger } from "gsap/ScrollTrigger";
import { useGSAP } from "@gsap/react";

// 👇 GSAP のプラグインは「登録」が必要。
//    これを忘れると ScrollTrigger が動かない（初心者がハマるポイント）
gsap.registerPlugin(ScrollTrigger);

// ---------- 型定義 ----------
// 👇 children: React が描画できるもの全般（文字列、JSX、配列など）
//    direction: アニメーションの出現方向。ユニオン型で 4 方向に制限
type ScrollRevealProps = {
  children: React.ReactNode;
  direction?: "up" | "down" | "left" | "right";
  delay?: number;       // 秒単位。0.2 = 200ms
  className?: string;   // 外側から TailwindCSS クラスを渡せるようにする
};

// 👇 direction から「初期位置のオフセット」を計算するヘルパー
//    Record<K, V> は「キーが K、値が V」のオブジェクト型
//    { x: number; y: number } が各方向の移動量を表す
const offsetMap: Record<
  NonNullable<ScrollRevealProps["direction"]>,
  { x: number; y: number }
> = {
  up:    { x: 0,   y: 60 },   // 下から上に 60px 移動して現れる
  down:  { x: 0,   y: -60 },
  left:  { x: 60,  y: 0 },    // 右から左に 60px 移動して現れる
  right: { x: -60, y: 0 },
};

export function ScrollReveal({
  children,
  direction = "up",
  delay = 0,
  className = "",
}: ScrollRevealProps) {
  // 👇 useRef: DOM 要素への参照を保持する React フック
  //    <HTMLDivElement> はこの ref が div 要素を指すことを TypeScript に伝える
  const containerRef = useRef<HTMLDivElement>(null);

  // 👇 useGSAP: GSAP 公式の React フック
  //    第 2 引数の { scope } で「この ref の中だけ」にアニメーション対象を限定
  //    コンポーネントがアンマウントされると自動でアニメーションを破棄してくれる
  useGSAP(
    () => {
      const offset = offsetMap[direction];

      gsap.from(containerRef.current, {
        x: offset.x,
        y: offset.y,
        opacity: 0,           // 透明 → 不透明
        duration: 0.8,         // 0.8 秒かけてアニメーション
        delay,
        ease: "power2.out",    // イージング: 最初は速く、最後はゆっくり
        scrollTrigger: {
          trigger: containerRef.current,  // この要素がビューポートに入ったら発火
          start: "top 85%",               // 要素の上端が画面の 85% 位置に来たら開始
          toggleActions: "play none none none",
          // ↑ onEnter / onLeave / onEnterBack / onLeaveBack の順
          //   "play none none none" = 最初に入った時だけ再生、逆スクロールでは何もしない
        },
      });
    },
    { scope: containerRef }
  );

  return (
    <div ref={containerRef} className={className}>
      {children}
    </div>
  );
}
```

#### ステップ 3: LP のセクションで使う

```tsx
// app/page.tsx
import { ScrollReveal } from "./components/ScrollReveal";

// 👇 as const を付けると配列の中身がリテラル型に固定される
//    features[0].title の型が string ではなく "爆速パフォーマンス" になる
const features = [
  {
    title: "爆速パフォーマンス",
    description: "Next.js App Router + Edge Runtime で世界中どこからでも 100ms 以下のレスポンス。",
    icon: "⚡",
  },
  {
    title: "堅牢なセキュリティ",
    description: "Supabase RLS でユーザーごとのデータアクセスを DB レベルで制御。",
    icon: "🔒",
  },
  {
    title: "AI でスマートに",
    description: "Claude API 連携でユーザー体験を自動最適化。",
    icon: "🤖",
  },
] as const;

export default function Home() {
  return (
    <main>
      {/* ヒーローセクション */}
      <section className="flex min-h-screen items-center justify-center bg-gradient-to-b from-slate-900 to-slate-800 px-4">
        <ScrollReveal direction="up">
          <h1 className="text-center text-5xl font-bold tracking-tight text-white md:text-7xl">
            あなたのプロダクトを
            <br />
            <span className="bg-gradient-to-r from-blue-400 to-emerald-400 bg-clip-text text-transparent">
              次のレベルへ
            </span>
          </h1>
        </ScrollReveal>
      </section>

      {/* 特徴セクション */}
      <section className="mx-auto max-w-5xl px-4 py-24">
        <ScrollReveal direction="up">
          <h2 className="mb-16 text-center text-3xl font-bold tracking-tight">
            選ばれる 3 つの理由
          </h2>
        </ScrollReveal>

        <div className="grid gap-8 md:grid-cols-3">
          {features.map((feature, index) => (
            <ScrollReveal
              key={feature.title}
              direction="up"
              delay={index * 0.15}
              className="rounded-2xl border border-neutral-200 bg-white p-8 shadow-sm"
            >
              {/* 👇 delay={index * 0.15} で各カードが 0.15 秒ずつずれて出現する
                     この「ずらし」がプロっぽさの秘訣。CSS だけでは制御が難しい部分 */}
              <div className="mb-4 text-4xl">{feature.icon}</div>
              <h3 className="mb-2 text-xl font-semibold">{feature.title}</h3>
              <p className="leading-relaxed text-neutral-600">
                {feature.description}
              </p>
            </ScrollReveal>
          ))}
        </div>
      </section>
    </main>
  );
}
```

### JS/TS 初心者向け解説

- **`"use client"` ディレクティブ**: Next.js App Router ではデフォルトで Server Component（サーバー側で HTML を生成）です。GSAP はブラウザの DOM を操作するため、`"use client"` を書いて Client Component にする必要があります。書き忘れると `window is not defined` エラーになります。
- **`useRef<HTMLDivElement>(null)`**: `useRef` は「再レンダリングしても値が保持される箱」です。`<HTMLDivElement>` というジェネリクスを付けると、`containerRef.current` が `HTMLDivElement | null` 型になり、`.getBoundingClientRect()` などの DOM メソッドが型安全に使えます。
- **`gsap.from()` と `gsap.to()` の違い**: `from()` は「指定した状態 → 現在の状態」へアニメーションします。つまり `opacity: 0` なら「透明から → 不透明に」。`to()` は逆で「現在の状態 → 指定した状態」。LP では「現れる」演出が多いので `from()` を使うケースが多いです。
- **`toggleActions` の 4 つの値**: `"play none none none"` は「初回だけ再生」。`"play reverse play reverse"` にすると、スクロールで戻った時にアニメーションが逆再生されます。LP では初回のみ（`play none none none`）が CVR 的に最適です。逆再生は「ユーザーが何度も行き来する」ダッシュボードやドキュメント向き。
- **`NonNullable<T>`**: `direction?: "up" | "down" | "left" | "right"` はオプショナルなので型に `undefined` が含まれます。`offsetMap` のキーに `undefined` は使えないため、`NonNullable` で除外しています。

### FLOCSS 的に見ると？

`ScrollReveal` は Foundation レイヤー（`.f-scroll-reveal`）相当の「振る舞い」コンポーネントです。見た目の責務を持たず、`className` を通じて外側からスタイルを注入する設計にしているので、LP でもダッシュボードでもポートフォリオでも再利用できます。Tailwind と FLOCSS のレイヤー思考は共存できるということを、この設計が示しています。

---

## 今日の思考トリガー

**「あなたが次に作る LP で、ユーザーがスクロールした瞬間に『おっ』と感じるアニメーションを 1 つだけ追加するとしたら、どのセクションに・どんな動きを付けますか？ そしてそのアニメーションは、ユーザーの『次のアクション（申し込み・購入・登録）』にどう繋がりますか？」**

_ソース: [GitHub Trending JavaScript Weekly](https://github.com/trending/javascript?since=weekly), [GitHub Trending TypeScript Weekly](https://github.com/trending/typescript?since=weekly), [GSAP - GreenSock Animation Platform](https://github.com/greensock/GSAP) (2026年04月14日時点)_
_※ Hacker News Algolia API (hn.algolia.com) はネットワーク制限によりアクセス不可のため、本日は GitHub Trending データを中心に構成しています。_
