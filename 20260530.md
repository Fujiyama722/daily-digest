#  週末ハンズオン実践: 2026年05月30日号

おはようございます！フロントエンドメンターの「学習コーチ」です。

今週のGitHubトレンドを眺めていて、はっきりとしたうねりが見えました。**「Skill / Plugin / Knowledge Graph」をMarkdownで配布する文化が一気にコモディティ化した週**です。Claude Code、Codex、Cursor、Gemini CLI、OpenCode……どのAIエージェントも、最終的に「Markdownで書かれた知識単位（=Skill）」を読み込んで動くようになりつつあります。

今日はこの流れに乗って、さとるさんの個人開発スタック（React / Next.js / TypeScript / TailwindCSS）で **「Skill Picker（スキル選択UI）」をゼロから作るハンズオン** をお届けします。型・状態管理・コンポーネント分割の基礎が全部入った、週末2〜3時間でちょうど終わるサイズです。

---

## 本日のハイライト

### 1.  GitHub週間1位は「Understand-Anything」— コードを知識グラフに変えるツール

- リポジトリ: [Lum1104/Understand-Anything](https://github.com/Lum1104/Understand-Anything)
- スター数: 44,342（今週 +26,212）/ 言語: TypeScript
- 概要: 任意のコードベースをインタラクティブな知識グラフに変換し、Claude Code / Codex / Cursor / Copilot / Gemini CLI などから検索・質問できるようにするツール。
- メンターの一言: 「人間が読むためのドキュメント」ではなく「AIエージェントが読むためのドキュメント」をどう構造化するか、という問いが今週の主役でした。

### 2. 2位グループに並ぶ「Code Knowledge Graph」系

- リポジトリ: [colbymchenry/codegraph](https://github.com/colbymchenry/codegraph)
- スター数: 33,104（今週 +19,128）/ 言語: TypeScript
- 概要: 事前にインデックス化されたコード知識グラフ。Claude Code / Codex / Cursor / OpenCode / Antigravity（さとるさんが使っているIDE！）など、主要なエージェントに対応し、トークン消費とツール呼び出しを減らす、100%ローカル動作。
- メンターの一言: Antigravity対応が明記されている数少ない人気リポジトリです。**自分の使ってるIDEがトレンドリポジトリのREADMEに名前で出てくる**のは、エコシステムが成熟してきた合図。

### 3. Anthropic公式の「Claude Plugins Directory」

- リポジトリ: [anthropics/claude-plugins-official](https://github.com/anthropics/claude-plugins-official)
- スター数: 28,591（今週 +6,953）/ 言語: Python
- 概要: Anthropicが管理する、高品質なClaude Codeプラグインの公式ディレクトリ。
- メンターの一言: 「プラグイン = Markdownで書かれた小さなスキル」というモデルが事実上の標準になりつつあります。フロントエンドの世界で言えば、npmパッケージのような流通基盤が生まれる前夜です。

---

## 注目リポジトリ / 事例

| # | リポジトリ | 概要 | スター数（週間増分） |
|---|-----------|------|---------------------|
| 1 | [Lum1104/Understand-Anything](https://github.com/Lum1104/Understand-Anything) | コードベース→インタラクティブ知識グラフ | 44,342（+26,212） |
| 2 | [colbymchenry/codegraph](https://github.com/colbymchenry/codegraph) | AIエージェント向け事前インデックス済みコードグラフ | 33,104（+19,128） |
| 3 | [presenton/presenton](https://github.com/presenton/presenton) | OSSのAIプレゼン生成器（Gamma代替） | 7,467（+1,740） |

参考: [anthropics/claude-plugins-official](https://github.com/anthropics/claude-plugins-official) （28,591 / +6,953）、[rohitg00/ai-engineering-from-scratch](https://github.com/rohitg00/ai-engineering-from-scratch)（24,554 / +13,159）。

---

## 今日のハンズオン: Markdownで配布する「Skill Picker」を作る

### ねらい

- **AIエージェントのSkill / Pluginという発想を、自分の手で再現**する
- 個人開発のNext.jsプロジェクトに転用できる「カードグリッドUI＋詳細モーダル」の型を身につける
- TypeScriptの `type` / `interface` / Discriminated Union（判別可能なユニオン型）を、ロジックの中で“役立つ場面”として体験する

### 完成イメージ

`/skills` という1ページを作ります。
- 左: スキル一覧（Tailwindでカードグリッド）
- カードクリック: 詳細パネルがスライドイン
- データ: ローカルの `skills.ts` で配列定義（後でMarkdownファイル化も可能）

### Step 0: プロジェクト準備

```bash
npx create-next-app@latest skill-picker --typescript --tailwind --app --eslint
cd skill-picker
```

App Router構成。さとるさんが普段触っているスタックなので追加ライブラリは不要です。

### Step 1: 型を先に決める（“型から書く”の練習）

`src/types/skill.ts` を作成します。

```ts
// カテゴリは決め打ちで4種類。これで typo を型チェックで弾けます。
export type SkillCategory = "frontend" | "backend" | "ai" | "ops";

// すべてのスキルに共通する基本情報
export interface SkillBase {
  id: string;
  title: string;
  summary: string;          // カードに出す1行要約
  category: SkillCategory;
  estimatedMinutes: number; // 学習目安時間
}

// 「外部リンクで学ぶスキル」
export interface SkillExternal extends SkillBase {
  kind: "external";
  url: string;
}

// 「インラインのMarkdownを表示するスキル」
export interface SkillInline extends SkillBase {
  kind: "inline";
  body: string; // Markdown文字列（今回はプレーンテキスト扱いでOK）
}

// 2種類のスキルを1つの型に合体（Discriminated Union）
export type Skill = SkillExternal | SkillInline;
```

**ここがミソ:** `kind: "external" | "inline"` という“判別キー”を持たせると、後で `if (skill.kind === "external") { skill.url }` のように **その分岐の中だけ `url` が存在することをTypeScriptが理解** してくれます。さとるさんが今後個人開発でDBから返ってくるデータを扱うときに、最も使う型パターンの一つです。

### Step 2: ダミーデータ

`src/data/skills.ts`:

```ts
import type { Skill } from "@/types/skill";

export const skills: Skill[] = [
  {
    id: "tw-grid",
    kind: "inline",
    title: "Tailwindのgridを1分でマスター",
    summary: "grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 で完結する考え方",
    category: "frontend",
    estimatedMinutes: 15,
    body: "## やること\n\n- モバイル1列、タブレット2列、PC3列\n- ブレークポイントは sm / lg のみで十分\n",
  },
  {
    id: "ts-union",
    kind: "inline",
    title: "Discriminated Unionで分岐を型安全にする",
    summary: "kindプロパティで安全に分岐する書き方",
    category: "frontend",
    estimatedMinutes: 20,
    body: "## ポイント\n\n- 共通プロパティ＋判別キーを揃える\n- switch文の網羅性チェックはnever型で実現\n",
  },
  {
    id: "supabase-rls",
    kind: "external",
    title: "Supabase RLSをはじめて触る",
    summary: "auth.uid()ベースの最小ポリシーを書いてみる",
    category: "backend",
    estimatedMinutes: 30,
    url: "https://supabase.com/docs/guides/database/postgres/row-level-security",
  },
];
```

### Step 3: カードコンポーネント

`src/components/SkillCard.tsx`:

```tsx
import type { Skill } from "@/types/skill";

type Props = {
  skill: Skill;
  onSelect: (skill: Skill) => void;
};

const categoryStyle: Record<Skill["category"], string> = {
  frontend: "bg-sky-100 text-sky-700",
  backend: "bg-emerald-100 text-emerald-700",
  ai: "bg-violet-100 text-violet-700",
  ops: "bg-amber-100 text-amber-700",
};

export function SkillCard({ skill, onSelect }: Props) {
  return (
    <button
      type="button"
      onClick={() => onSelect(skill)}
      className="text-left rounded-2xl border border-slate-200 bg-white p-5 shadow-sm transition hover:shadow-md hover:-translate-y-0.5"
    >
      <div className="flex items-center justify-between">
        <span
          className={`rounded-full px-2 py-0.5 text-xs font-medium ${categoryStyle[skill.category]}`}
        >
          {skill.category}
        </span>
        <span className="text-xs text-slate-500">
          約 {skill.estimatedMinutes} 分
        </span>
      </div>
      <h3 className="mt-3 text-lg font-semibold text-slate-900">
        {skill.title}
      </h3>
      <p className="mt-1 text-sm text-slate-600">{skill.summary}</p>
    </button>
  );
}
```

**ここがミソ:** `Record<Skill["category"], string>` という書き方。
- `Skill["category"]` は **「Skill型のcategoryプロパティの型」を取り出す** 書き方
- `Record<K, V>` は **「K型のキーすべてに対してV型の値が必要」** という意味
- → カテゴリを後から増やしたとき、ここでスタイル定義し忘れるとTypeScriptが赤線で教えてくれます。これが「型を活かす」の具体的な体験です。

### Step 4: 詳細パネル + ページ全体

`src/app/skills/page.tsx`:

```tsx
"use client";

import { useState } from "react";
import { skills } from "@/data/skills";
import type { Skill } from "@/types/skill";
import { SkillCard } from "@/components/SkillCard";

export default function SkillsPage() {
  const [selected, setSelected] = useState<Skill | null>(null);

  return (
    <main className="min-h-screen bg-slate-50 px-6 py-10">
      <h1 className="text-2xl font-bold text-slate-900">Skill Picker</h1>
      <p className="mt-1 text-sm text-slate-600">
        週末に1個ずつつぶす、自分用スキルカタログ。
      </p>

      <section className="mt-8 grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3">
        {skills.map((skill) => (
          <SkillCard key={skill.id} skill={skill} onSelect={setSelected} />
        ))}
      </section>

      {selected && (
        <aside
          className="fixed inset-y-0 right-0 w-full max-w-md overflow-y-auto bg-white p-6 shadow-2xl"
          aria-label="スキル詳細"
        >
          <button
            type="button"
            onClick={() => setSelected(null)}
            className="text-sm text-slate-500 hover:underline"
          >
            閉じる
          </button>
          <h2 className="mt-3 text-xl font-semibold">{selected.title}</h2>

          {/* ここで kind による分岐 */}
          {selected.kind === "external" ? (
            <a
              href={selected.url}
              target="_blank"
              rel="noreferrer"
              className="mt-4 inline-block rounded-lg bg-slate-900 px-4 py-2 text-sm text-white"
            >
              ドキュメントを開く →
            </a>
          ) : (
            <pre className="mt-4 whitespace-pre-wrap rounded-lg bg-slate-100 p-4 text-sm text-slate-800">
              {selected.body}
            </pre>
          )}
        </aside>
      )}
    </main>
  );
}
```

**ここがミソ（最重要）:** 詳細パネル内の三項演算子。
- `selected.kind === "external"` の枝の中では、TypeScriptが **`selected.url`を参照できると保証**
- もう一方の枝では **`selected.body`が参照可能**
- もし `kind` を持たない素朴な型だった場合、両方の枝で `url?` と `body?` を毎回 `if` ガードしなければなりません

この差が、Discriminated Unionを覚える価値の正体です。

### Step 5: 起動して確認

```bash
npm run dev
# http://localhost:3000/skills を開く
```

3枚のカード → クリックで右からパネル → 「閉じる」で消える。これだけ動けば今日のゴール達成です。

---

## 発展課題（余裕があれば日曜に）

1. **`skills.ts` を `/skills/*.md` ファイルから読み込む** — Next.jsの `fs.readdir` を `app/skills/page.tsx` のサーバーコンポーネント側で使えば、AI Skillの“配布フォーマット”そのものになります。
2. **カテゴリでフィルタ** — `useState<SkillCategory | "all">("all")` を1つ足すだけ。
3. **Supabaseに乗せる** — `skills` テーブル＋ `kind` カラム＋ RLSで `auth.uid() = owner_id` の最小ポリシー。これは水曜のPM視点とつながります。

---

## 実践ポイント（さとるさんへ）

- **「Markdownで配布される小さな知識単位」がこの週のキーワード**。LP制作の現場でも、各セクション仕様を `.md` で持ち回すクライアントが今後増えます。Skill Picker のUIは、その納品ビューアにそのまま流用できます。
- **Discriminated Union は“TypeScriptを使う理由”そのもの**。今日のハンズオンで `kind` 分岐の気持ちよさを一度体験しておけば、Supabaseのレスポンスや、フォームの状態管理（`{ status: "idle" } | { status: "loading" } | { status: "error", message: string }` など）で必ず効きます。
- **Antigravity が trending リポジトリで明示対応されている**事実は、さとるさんの選んだIDEが「シニアエンジニアが本気で支援対象に選ぶ環境」になりつつある証拠です。学習リソースが今後一気に増えます。

---

## 今日の思考トリガー

**「自分が今週学んだことのうち1つだけを Markdown 1枚に圧縮して “Skill” として配布するとしたら、タイトル・1行要約・本文の3要素にどう書きますか？ — それを毎週続けたら、半年後の自分の市場価値はどう変わると思いますか？」**

_ソース: [GitHub Trending (weekly)](https://github.com/trending?since=weekly) (2026年05月30日時点)_
