# 🛠️ 週末ハンズオン実践: 2026年07月18日号

おはようございます！フロントエンドメンターの「学習コーチ」です。

週末は、平日の忙しさで飛ばしがちな「なぜそう書くか」に立ち止まる絶好のタイミング。今日は今週のトレンドから、さとるさんの日々の LP コーディング〜個人開発の両方に効く 1 本を選んで、手を動かしながら React/TypeScript/Tailwind の型と設計を体で覚えていきます。

---

## 本日のハイライト

- **Nutlope/hallmark — 週間 6,573⭐（今週）／累計 11.9k⭐** : Together AI（Nutlope）による「AI っぽく見えないデザイン」を強制する Claude Code / Cursor / Codex 用スキル。57 の "slop test" ゲートと自己批評でテンプレっぽさを弾く。Build / Audit / Redesign / Study の 4 操作。( https://github.com/Nutlope/hallmark )
- **OpenCut-app/OpenCut — 週間 11,731⭐／累計 74.7k⭐** : OSS の動画エディタ。TypeScript 97.2% で書き直し中、Rust コアで Web/デスクトップ/モバイルを統一。プラグインファースト設計で、MCP サーバー統合まで見据えている点が今週の目玉。( https://github.com/OpenCut-app/OpenCut )
- **google-labs-code/stitch-skills — 週間 1,090⭐** : Google Labs の Stitch MCP 用 Agent Skills ライブラリ。hallmark と並んで「スキル as コードの資産」という同じ潮流にあるので、両方見ておくと "スキル" というレイヤーの重要性が実感できます。( https://github.com/google-labs-code/stitch-skills )

## 注目リポジトリ / 事例

1. **Nutlope/hallmark** — https://github.com/Nutlope/hallmark
   Claude Code に `npx skills add nutlope/hallmark` で導入できるデザインスキル。20 テーマ・57 スロップテスト・マクロ構造の自動選定つき。ライセンス MIT。
2. **OpenCut-app/OpenCut** — https://github.com/OpenCut-app/OpenCut
   TS で書かれた本格 OSS 動画エディタ。Editor API と Plugin システムの構造が、Next.js/TS で「拡張可能なアプリ」を書くときの参考実装として読める。
3. **wonderwhy-er/DesktopCommanderMCP — 週間 1,991⭐** — https://github.com/wonderwhy-er/DesktopCommanderMCP
   Claude にターミナル / ファイルシステム / 差分編集を渡す MCP サーバー。Claude Code CLI ユーザーが「自分の環境を MCP で強化する」入り口として最短。

## 実践ポイント（さとるさんへ）

今週は **hallmark を触りつつ、AI っぽくない小さなカード UI を Next.js + TS + Tailwind で組んでみる** ハンズオンをやりましょう。

### ① まず 5 分で hallmark を入れて "Audit" させる

日々の LP コーディング案件で書いた HTML/CSS を、いったん hallmark の Audit にかけると「テンプレ臭さ」がスコアで返ってきます。学習として一番効くのは、自分の書いたコードのどこがスロップ扱いされたかを見ることです。

```bash
# プロジェクト直下で
npx skills add nutlope/hallmark
# 以降 Claude Code で
# > /hallmark:audit ./index.html
```

### ② React で "AI っぽくない" FeatureCard を書く

hallmark のスロップ判定でよく引っかかるのは「中央揃え・グラデ紫青・アイコン + 見出し + 一行説明の均一な 3 カラム」。それを崩す小さなコンポーネントを、型から書きます。

```tsx
// components/FeatureCard.tsx
import { type ReactNode } from "react";

// 1) props の "形" を先に型で決める。ここが設計。
type FeatureCardProps = {
  eyebrow: string;              // 上に置く小さいラベル（"Case 01" など）
  title: string;                // 主見出し
  body: string;                 // 説明本文
  metric?: { label: string; value: string }; // 任意: 数値強調
  action: { href: string; label: string };   // 必ずアクションを持たせる
  children?: ReactNode;         // 図やコードなど自由要素
};

// 2) props を型で受けるので、呼び出し側で "書き忘れ" が赤線になる
export function FeatureCard({
  eyebrow, title, body, metric, action, children,
}: FeatureCardProps) {
  return (
    <article
      // 左寄せ・非対称グリッド・単色。中央揃え & グラデを封じる。
      className="
        grid grid-cols-12 gap-6
        border-t border-neutral-900/10
        py-10
        text-neutral-900
      "
    >
      <header className="col-span-12 md:col-span-4">
        <p className="text-xs tracking-widest uppercase text-neutral-500">
          {eyebrow}
        </p>
        <h3 className="mt-2 text-2xl font-semibold leading-tight">
          {title}
        </h3>
      </header>

      <div className="col-span-12 md:col-span-6 md:col-start-6">
        <p className="text-base leading-relaxed text-neutral-700">{body}</p>

        {metric && (
          <p className="mt-6 flex items-baseline gap-2">
            <span className="text-4xl font-semibold tabular-nums">
              {metric.value}
            </span>
            <span className="text-sm text-neutral-500">{metric.label}</span>
          </p>
        )}

        {children}

        <a
          href={action.href}
          className="mt-8 inline-flex items-center gap-1 border-b border-neutral-900 pb-0.5 text-sm font-medium hover:opacity-70"
        >
          {action.label} →
        </a>
      </div>
    </article>
  );
}
```

使う側:

```tsx
<FeatureCard
  eyebrow="Case 01"
  title="LP の直帰率を CVR に読み替える"
  body="直帰率だけでは判断できない。到達スクロール深度と CTA 押下率を合わせて..."
  metric={{ value: "31%", label: "CVR 改善 (自社事例)" }}
  action={{ href: "/cases/01", label: "詳細を読む" }}
/>
```

### ③ 型が教えてくれること（初心者向け解説）

- `type FeatureCardProps = { ... }` : props の "形" を先に決める。あとから
  `<FeatureCard title="..." />` と書き忘れると、IDE 上で赤線が出る。これが型の一番のご利益。
- `metric?: { ... }` の `?` は「省略可」の意味。省略した瞬間に `metric` は `undefined` になるので、コード側で `{metric && (...)}` と書いて存在チェックしないと壊れる。TS はここを **強制** してくれる。
- `action: { href: string; label: string }` はネストしたオブジェクト型。JSX 側でも `action.href` が string であることが保証されるので、`href={action.href}` に `undefined` が入ってビルドが通ってしまう事故が起きない。
- `children?: ReactNode` は「中に何でも入れられる箱」を型で表現する定番。React が公式で用意している型なので、`import { type ReactNode } from "react"` で持ってくるだけで OK。

### ④ なぜこの CSS で「AI っぽくない」のか

- **中央寄せをやめて 12 カラムグリッドで非対称に**（`md:col-span-6 md:col-start-6`）— ヒーロー・カード・フッターが全部センターの LP は今年のスロップ最頻出パターン。
- **グラデ紫青を封印して単色 + 数値タブラー**（`text-neutral-900` / `tabular-nums`）— 数値は等幅にすると信頼感が出る。
- **境界線は影ではなく 1px の hairline**（`border-t border-neutral-900/10`）— 影 + 角丸 + グラデの三点セットが最も "AI 生成っぽい" と判定されがち。

## 今日の思考トリガー

**「あなたが直近で書いた LP や個人開発の 1 画面を、hallmark の Audit にかけたら、どこが `slop` と判定されると思いますか？ そしてその判定を、`FeatureCard` のような "型 + レイアウト設計" で自分側から先回りして潰せるとしたら、どの 3 パターンを自分の再利用コンポーネントに昇格させますか？」**

_ソース: https://github.com/trending?since=weekly / https://github.com/Nutlope/hallmark / https://github.com/OpenCut-app/OpenCut / https://github.com/wonderwhy-er/DesktopCommanderMCP (2026年07月18日時点)_
