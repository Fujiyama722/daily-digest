# 🧭 プロダクトマネジメント事例解説: 2026年06月24日号

おはようございます！シニア・プロダクトマネージャーの「学習コーチ」です。

水曜日は、実際に動いている本物のプロダクトを1つ取り上げ、**「自分が個人開発でこれを作るなら、どう要件定義・DB設計・収益化するか」** を、さとるさんが扱えるSupabase / Next.js / TypeScript の土俵で具体的に考えていきます。

---

## 本日のハイライト

今日は、本日のGitHub Trending（全体）で上位に入っている、地に足のついた "プロダクト" 系リポジトリを軸に、プロダクトマネジメントの視点から読み解いていきます。

### 1. Voicebox — ローカルファーストのAIボイススタジオ

- **リポジトリ**: [jamiepine/voicebox](https://github.com/jamiepine/voicebox)（TypeScript / MIT / ★33,057・+1,042 today）
- **公式サイト**: https://voicebox.sh
- **要約**: ElevenLabs（音声出力）と WisprFlow（音声入力）の "両側" を1つに統合し、**全部ローカルで動かす** OSSデスクトップアプリ。数秒の音声からのボイスクローン、23言語・7TTSエンジンでの音声生成、グローバルホットキーでの口述入力、MCP対応エージェントへの「声の付与」までを1本でカバーする。
- **PM視点のキモ**: 巨大SaaS2社（ElevenLabs / WisprFlow）の "間の余白" を狙い、「**サブスク不要・自分のデータ・自分のマシン**」という明確な対立軸を立てている。コモディティ化が進む音声AI領域における、ポジショニング設計のお手本。

### 2. deer-flow — 「ロングホライズン」を売りにしたエージェントハーネス

- **リポジトリ**: [bytedance/deer-flow](https://github.com/bytedance/deer-flow)（Python / MIT / ★73,843・+741 today）
- **公式サイト**: https://deerflow.tech
- **要約**: ByteDance公式の「数分〜数時間かかるタスクを回し切る」ためのスーパーエージェント基盤。サンドボックス、メモリ、ツール、スキル、サブエージェント、メッセージゲートウェイを束ねている。
- **PM視点のキモ**: 「**所要時間 = 価値**」というアングルでLLMエージェント市場を切り直している。"早い・賢い" の競争から "長く粘れる" 競争へ。プロダクト言語の作り方として参考になる。

### 3. OpenMontage — 「エージェント型動画制作」という新カテゴリ提案

- **リポジトリ**: [calesthio/OpenMontage](https://github.com/calesthio/OpenMontage)（Python / AGPL-3.0 / ★15,316・+3,590 today）
- **要約**: 12パイプライン・52ツール・500以上のスキルを束ね、「コーディングエージェントを動画制作スタジオに変える」ことを謳うOSS。
- **PM視点のキモ**: ライセンスを **AGPL-3.0** に振っているのが象徴的。「OSSで配るが、SaaS化したい競合には改変公開を強制する」=「将来的に自分たちが商用版を出す布石」という、典型的なオープンコア戦略の入口。

> ※ 本来のソースとして指定されていた **Hacker News（news.ycombinator.com）と Product Hunt（producthunt.com）は、本日この実行環境からはネットワークポリシーにより取得できませんでした。** GitHub Trending のみを一次ソースとして使用しています。

---

## 注目リポジトリ / 事例

| # | リポジトリ | 一言で | リンク |
|---|---|---|---|
| 1 | jamiepine/voicebox | 個人ユーザー向けローカル音声I/Oスタジオ。**B2C・ローカルファースト**の好例 | https://github.com/jamiepine/voicebox |
| 2 | bytedance/deer-flow | 長時間タスク特化のエージェントハーネス。**B2D（開発者向け基盤）** の好例 | https://github.com/bytedance/deer-flow |
| 3 | calesthio/OpenMontage | 「エージェント型動画制作」という新カテゴリの提案。**カテゴリ作り**の好例 | https://github.com/calesthio/OpenMontage |

---

## 実践ポイント（さとるさんへ）

今日は **Voicebox を題材に、自分でSaaS版を作るならどう要件定義するか** を、PMの定石どおり「ユーザー → 機能 → データモデル → RLS → MVP → 収益化」の順で並べてみます。FLOCSSでDOMを構造化する感覚と同じで、**プロダクトもまず "層" に分けて考える** のがポイントです。

### Step 1. 要件定義 — まず「誰の・どの30秒」を奪うか1行で書く

Voiceboxの本質は、ローカルアプリでもSaaSでも変わらず以下に集約できます。

> **「自分の声で、考えたことを、好きなアプリに、サブスクなしで打ち込めるようにする」**

PMの最初の仕事は、ここを **1文で書き切れるまで削る** こと。さとるさんの個人開発でも、機能リストを書く前に必ずこれをやってください。1文に収まらない＝まだ "プロダクト" になっていません。

### Step 2. DB設計 — Supabase前提の最小テーブル

仮に「Voiceboxのクラウド共有版」を作るなら、最小スキーマはこのくらいで始められます。**最初は3〜4テーブル以上には絶対増やさない**のが、MVPを死なせないコツです。

```sql
-- 1. ユーザー（auth.users を参照、Supabase標準）
create table public.profiles (
  id uuid primary key references auth.users (id) on delete cascade,
  display_name text not null,
  created_at timestamptz not null default now()
);

-- 2. 声のプロファイル（クローンソース1件 = 1行）
create table public.voice_profiles (
  id uuid primary key default gen_random_uuid(),
  owner_id uuid not null references public.profiles (id) on delete cascade,
  name text not null,
  source_audio_path text not null, -- Storageのオブジェクトキー
  language text not null default 'ja',
  is_public boolean not null default false,
  created_at timestamptz not null default now()
);

-- 3. 生成履歴（"何を喋らせたか" のログ）
create table public.generations (
  id uuid primary key default gen_random_uuid(),
  owner_id uuid not null references public.profiles (id) on delete cascade,
  voice_profile_id uuid not null references public.voice_profiles (id) on delete cascade,
  input_text text not null,
  output_audio_path text,
  status text not null check (status in ('queued','running','done','error')),
  created_at timestamptz not null default now()
);
```

### Step 3. RLS設計 — 「他人の声で勝手に喋らせない」を技術で担保

音声クローン系は **倫理リスク（なりすまし）** が最大の地雷です。RLSを後付けで考えると必ず事故ります。最初からこのレベルで縛ります。

```sql
alter table public.voice_profiles enable row level security;
alter table public.generations    enable row level security;

-- 自分の声プロファイル: 本人だけがCRUD可
create policy "own voice profiles - rw"
on public.voice_profiles
for all
using  (owner_id = auth.uid())
with check (owner_id = auth.uid());

-- 公開フラグが立っている声: 誰でもSELECTだけ可
create policy "public voice profiles - read"
on public.voice_profiles
for select
using (is_public = true);

-- 生成履歴: 本人だけ
create policy "own generations - rw"
on public.generations
for all
using  (owner_id = auth.uid())
with check (owner_id = auth.uid());
```

ポイントは「**`with check` も必ず書く**」こと。`using` だけだと「読めない他人の行は更新できない」は守れても、「他人を `owner_id` にして INSERT する」攻撃を弾けません。RLSは "SELECT用フィルタ" ではなく "全操作のゲート" と捉えてください。

### Step 4. MVPスコープ — "削る" のがPMの本業

このプロダクトでさとるさんが1人で作るなら、初週リリース版は思い切ってここまで削ります。

- ✅ サインアップ / ログイン（Supabase Auth）
- ✅ 声プロファイルを **1人1個まで** 作成（複数はv2へ）
- ✅ テキスト → 音声生成（言語は日本語のみ）
- ✅ 生成履歴を一覧表示
- ❌ 多言語、❌ 共有・公開、❌ チーム機能、❌ MCP連携 → 全部v2以降

「やらないことリスト」を **要件定義書の最上段に書く** だけで、開発スピードは体感3倍変わります。

### Step 5. 収益化 — OSSとSaaSの「2階建て」を最初から設計する

Voiceboxは現状は完全OSS（MIT）ですが、もし収益化するなら最も筋がいいのは「**ローカル版は永遠に無料、クラウド同期・チーム共有・モデルホスティングだけ有料**」のオープンコア型。OpenMontageが選んだ AGPL-3.0 もこの戦略の入口です。

さとるさんの個人開発では、最初から以下を意識しておくと無駄が減ります。

- **Free**: 自分1人で使う / 月N回まで生成
- **Pro（月$8〜$15）**: 生成回数アンロック、共有リンク、API
- **Team（後回し）**: 組織内共有、監査ログ

価格は機能ではなく **「節約できる時間」or「代替コスト」** から逆算します。ElevenLabsの月$22を見ているユーザーには、$8でも十分に響きます。

---

## 今日の思考トリガー

**「自分が今 "なんとなく作りたい" と思っているプロダクトを、Voiceboxにならって "1文" で書いてみてください。書けないとしたら、足りないのは技術力ではなく "誰のどの30秒を奪うか" の解像度です。さとるさんなら、その1文をどう書きますか？」**

_ソース: https://github.com/trending , https://github.com/jamiepine/voicebox , https://github.com/bytedance/deer-flow , https://github.com/calesthio/OpenMontage （2026年6月24日 JST 取得）_
_補足: 本日は news.ycombinator.com および producthunt.com への接続が当実行環境のネットワークポリシーにより403で拒否されたため、GitHub Trending のみを一次ソースとして使用しました。_
