# 🎨 トレンドUI実装チュートリアル: 2026年07月07日号

おはようございます！シニアUI/UXデザイナー兼フロントエンドエンジニアの「学習コーチ」です。

今日はGitHubで急伸中の「shadcn/ui × アニメーション付きカード」の潮流を切り口に、Vercel・Linear・Cursorのランディングでよく見る **Spotlight Hover Card（マウス追従グラデーション）** を、React + Next.js + TypeScript + TailwindCSS で1コンポーネントに落とし込みます。

---

## 本日のハイライト

- **shadcn/ui が11.8万スター到達、Vue系にも波及**
  「コピペで自分のコードとして所有できる」設計思想の React コンポーネント配布プラットフォーム。今週も更新が続き、Laravel/Vue（vue-vben-admin）にまで拡張が広がっています。デザイントークンを「ライブラリ」ではなく「ソースコード」として取り込む発想は、非エンジニア→フロントエンドエンジニアへの階段としても学ぶ価値が高いです。
  ソース: https://github.com/shadcn-ui/ui （2026年07月07日時点 ⭐118,257）

- **Magic UI が「デザインエンジニア向け」アニメーションUIとして定着**
  Framer Motion + Tailwind + shadcn の組み合わせで、コピペ可能な派手系コンポーネント（Spotlight・Marquee・Meteors 等）を提供。LP やポートフォリオで見栄えを瞬時に底上げできる王道パターンです。
  ソース: https://github.com/magicuidesign/magicui （2026年07月07日時点 ⭐21,452）

- **Radix Primitives、依然としてアクセシビリティの土台**
  shadcn/ui の中身は Radix の未スタイル部品。「見た目は自作、アクセシビリティは Radix に委ねる」構造は今週も王道です。キーボード操作・フォーカス管理・ARIA を自作しない選択が正解、という業界的合意が続いています。
  ソース: https://github.com/radix-ui/primitives （2026年07月07日時点 ⭐19,029）

## 注目リポジトリ / 事例

- **shadcn-ui/ui** — React コンポーネント配布プラットフォーム（Next.js / Vite / Laravel / TanStack 対応）。⭐118,257 / https://github.com/shadcn-ui/ui
- **magicuidesign/magicui** — Design Engineer 向けのアニメーション UI ライブラリ。⭐21,452 / https://github.com/magicuidesign/magicui
- **mantinedev/mantine** — フル機能の React コンポーネントライブラリ（Hooks 単体でも使える）。⭐31,394 / https://github.com/mantinedev/mantine

いずれも OSS。今日紹介する Spotlight Card 相当のコンポーネントは magicui にも収録されていますが、仕組みを理解する意味で、下のチュートリアルは「素の React で1から書く」構成にしています。

## 実践ポイント（さとるさんへ）

### 今日作るもの：Spotlight Hover Card

マウスカーソルの位置を追従してカード内にラジアルグラデーションが浮かぶ、あの「未来っぽいカード」です。仕組みはたった3つ：

1. `onMouseMove` でカーソルの座標を取得
2. カード内相対座標を **CSS 変数** (`--x`, `--y`) に流し込む
3. Tailwind の任意値クラス `bg-[radial-gradient(...)]` でその変数を読む

DOM再レンダリング0回で光が動くので、React にも優しい実装です。

### コード

`app/components/spotlight-card.tsx` として保存してください。Next.js App Router 前提、`"use client"` が必須です（`onMouseMove` はブラウザでしか動かないため）。

```tsx
"use client";

import { useRef, type MouseEvent, type ReactNode } from "react";

type SpotlightCardProps = {
  children: ReactNode;
  className?: string;
};

export function SpotlightCard({ children, className = "" }: SpotlightCardProps) {
  const cardRef = useRef<HTMLDivElement>(null);

  const handleMouseMove = (e: MouseEvent<HTMLDivElement>) => {
    const card = cardRef.current;
    if (!card) return;
    const rect = card.getBoundingClientRect();
    const x = e.clientX - rect.left;
    const y = e.clientY - rect.top;
    card.style.setProperty("--x", `${x}px`);
    card.style.setProperty("--y", `${y}px`);
  };

  return (
    <div
      ref={cardRef}
      onMouseMove={handleMouseMove}
      className={`group relative overflow-hidden rounded-2xl border border-white/10 bg-neutral-900 p-8 ${className}`}
    >
      <div
        className="pointer-events-none absolute inset-0 opacity-0 transition-opacity duration-300 group-hover:opacity-100"
        style={{
          background:
            "radial-gradient(400px circle at var(--x) var(--y), rgba(56,189,248,0.15), transparent 40%)",
        }}
      />
      <div className="relative z-10 text-neutral-100">{children}</div>
    </div>
  );
}
```

呼び出し例：

```tsx
<SpotlightCard>
  <h3 className="text-xl font-semibold">Ship faster</h3>
  <p className="mt-2 text-neutral-400">
    デザイナー発想の細かい表現を、Reactに落とすと差がつきます。
  </p>
</SpotlightCard>
```

### JS/TSのポイント解説（初心者向け）

- **`type SpotlightCardProps`**
  `type` は「オブジェクトの形」に名前をつける機能。`children: ReactNode` は「子要素として何を渡してもいい（文字列でもJSXでも）」の意。`className?` の `?` は「省略可」。TSはここで「渡し忘れ・型違い」をコンパイル時に叩いてくれます。

- **`useRef<HTMLDivElement>(null)`**
  DOM要素への「参照」を保持する React Hook。`<HTMLDivElement>` は「この ref に入るのは div の DOM ノード」というTSへの申告。`ref.current` は最初 `null` なので、下の `if (!card) return;` は TS を黙らせるだけでなく、実行時の安全弁でもあります。

- **`e.clientX - rect.left`**
  マウス座標は「ビューポート基準」で来ます。カード内の相対座標に直すには、カードの左端を引く。この5行が本チュートリアルの心臓部です。

- **CSS変数を経由する理由**
  `useState` で座標を持って再レンダリングする実装だと、`onMouseMove` の高頻度発火で React が悲鳴を上げます。CSS変数に流し込むと、DOM再構築ゼロで見た目だけ更新できる = 60fps が出やすい。**「Reactに任せる場所」と「CSSに任せる場所」を分ける** 感覚は、シニアフロントエンドエンジニアが常に意識している設計判断です。

- **Tailwind の `group` / `group-hover:`**
  親に `group`、子に `group-hover:opacity-100` と書くと、「親にホバーしたとき子のクラスが効く」が実現します。BEM の Modifier に近い発想ですが、状態はCSSだけで表現される点が FLOCSS 経験者に馴染みやすいはずです。

### 業務への転用アイデア

- LP のヒーロー下「特徴3カード」に適用 → 一気に近代的な印象。
- ダッシュボードのメトリックカードに適用 → 高級SaaS感が出て、単価交渉の材料にもなります。
- 案件で「派手すぎ」の懸念があるなら、`opacity` と `radial-gradient` の広がり半径だけを落とす。ロジックは変えない。

## 今日の思考トリガー

**「あなたの直近の案件で、"React で state を作らずに CSS 変数に逃がしたほうがいい" 相互作用はどれですか？　ホバー・スクロール連動・ドラッグ位置——1つ選び、`useState` を CSS 変数に置き換える差分だけを、明日Claude Codeに相談してみてください。」**

_ソース: https://github.com/shadcn-ui/ui , https://github.com/magicuidesign/magicui , https://github.com/radix-ui/primitives , https://github.com/mantinedev/mantine (2026年07月07日時点)_
