# 🛠️ 週末ハンズオン実践 — フロントエンド最前線を手を動かして学ぶ: 2026年04月05日号

おはようございます！フロントエンドメンターの「学習コーチ」です。

今週のGitHubトレンドは **AIエージェントツール** と **開発者体験（DX）向上ツール** が席巻しています。今日はその中から、さとるさんのLP制作・個人開発に直結する「デモ動画・スクリーンショット生成ツール」 **OpenScreen** をピックアップし、React/Next.js/TailwindCSSでプロダクトのショーケースUIを実装するハンズオンをお届けします。

---

## 本日のハイライト

### 1. OpenScreen — 無料でプロ品質のデモ動画を作成

TypeScript製のオープンソースツールで、Webアプリのデモ動画をウォーターマークなし・サブスク不要で生成できます。今週だけで **+8,513スター** の急上昇。LPやポートフォリオに載せるデモ素材の作成に最適です。

🔗 [siddharthvaddem/openscreen](https://github.com/siddharthvaddem/openscreen) — ⭐ 19,607

### 2. oh-my-claudecode — Claude Codeのマルチエージェント連携

Claude Codeに「チーム」の概念を持ち込み、複数エージェントを協調動作させるツール。今週 **+9,465スター** と爆発的な人気。さとるさんが日常使っているClaude Codeの可能性を大きく広げます。

🔗 [Yeachan-Heo/oh-my-claudecode](https://github.com/Yeachan-Heo/oh-my-claudecode) — ⭐ 23,825

### 3. learn-claude-code — Claude Codeをゼロから理解する教材

Bashベースでnano版のClaude Codeエージェントを一から構築する学習リポジトリ。AIエージェントの仕組みを「中身から」理解できます。今週 **+7,241スター**。

🔗 [shareAI-lab/learn-claude-code](https://github.com/shareAI-lab/learn-claude-code) — ⭐ 48,218

### 4. repomix — リポジトリをAIに丸ごと渡す

コードベース全体を1つのAIフレンドリーなファイルにパッキング。Claude Codeへのコンテキスト提供を効率化できます。

🔗 [yamadashy/repomix](https://github.com/yamadashy/repomix) — ⭐ 23,032

### 5. simstudio — AIエージェントをビジュアルに構築

ノーコード/ローコードでAIエージェントのワークフローを設計・デプロイ。個人開発でAI機能を組み込む際のプロトタイピングに有用です。

🔗 [simstudioai/sim](https://github.com/simstudioai/sim) — ⭐ 27,547

---

## 注目リポジトリ

| リポジトリ | 概要 | 今週のスター増加 |
|---|---|---|
| [openscreen](https://github.com/siddharthvaddem/openscreen) | 無料・OSS のデモ動画作成ツール（TypeScript） | +8,513 |
| [oh-my-claudecode](https://github.com/Yeachan-Heo/oh-my-claudecode) | Claude Code マルチエージェント連携 | +9,465 |
| [TaxHacker](https://github.com/vas3k/TaxHacker) | セルフホスト型AI会計アプリ（TypeScript） | +1,861 |

---

## 🎯 週末ハンズオン: プロダクトショーケースUIを作ろう

OpenScreenのようなデモツールで作った動画を「映える形」で見せるために、**プロダクトショーケースコンポーネント**をReact + TailwindCSSで実装しましょう。LPや個人開発サイトにそのまま使えます。

### 完成イメージ

ブラウザのモックアップ風フレーム内にデモ動画/スクリーンショットを表示し、ホバーでCTAが浮かび上がるカード型UI。

### コード実装

```tsx
// components/ProductShowcase.tsx

// ---- 型定義 ----
// TypeScriptの「type」はデータの形を事前に決める設計図です
// これにより、間違ったデータを渡すとエディタが即座に警告してくれます
type ShowcaseItem = {
  title: string;        // プロダクト名
  description: string;  // 一行説明
  mediaUrl: string;     // デモ動画 or スクリーンショットのURL
  mediaType: "video" | "image"; // ユニオン型: "video" か "image" のどちらかだけ許可
  ctaUrl: string;       // CTAボタンのリンク先
  ctaLabel: string;     // CTAボタンのテキスト
};

// ---- Props型 ----
// コンポーネントが外から受け取るデータの型を定義
type ProductShowcaseProps = {
  items: ShowcaseItem[]; // ShowcaseItemの配列
};

export function ProductShowcase({ items }: ProductShowcaseProps) {
  // items を分割代入（destructuring）でPropsから取り出す
  // { items } は items = props.items のショートハンド

  return (
    <section className="mx-auto max-w-6xl px-4 py-16">
      {/* セクションタイトル */}
      <h2 className="mb-12 text-center text-3xl font-bold tracking-tight">
        プロダクトデモ
      </h2>

      {/* グリッドレイアウト: モバイル1列 → md(768px)で2列 → lg(1024px)で3列 */}
      <div className="grid gap-8 md:grid-cols-2 lg:grid-cols-3">
        {items.map((item) => (
          // key: Reactがリスト内の各要素を識別するための一意な値
          // titleをkeyに使用（実運用ではidを推奨）
          <article
            key={item.title}
            className="group relative overflow-hidden rounded-2xl border border-gray-200 bg-white shadow-sm transition-shadow hover:shadow-xl"
          >
            {/* ブラウザモックアップ風ヘッダー */}
            <div className="flex items-center gap-1.5 border-b border-gray-100 bg-gray-50 px-4 py-2">
              {/* 信号機ドット（macOS風） */}
              <span className="h-2.5 w-2.5 rounded-full bg-red-400" />
              <span className="h-2.5 w-2.5 rounded-full bg-yellow-400" />
              <span className="h-2.5 w-2.5 rounded-full bg-green-400" />
              <span className="ml-2 text-xs text-gray-400">demo</span>
            </div>

            {/* メディア表示エリア */}
            <div className="relative aspect-video bg-gray-900">
              {/* 条件分岐: mediaTypeが"video"ならvideoタグ、それ以外はimgタグ */}
              {item.mediaType === "video" ? (
                <video
                  src={item.mediaUrl}
                  autoPlay
                  loop
                  muted
                  playsInline
                  className="h-full w-full object-cover"
                />
              ) : (
                <img
                  src={item.mediaUrl}
                  alt={item.title}
                  className="h-full w-full object-cover"
                />
              )}

              {/* ホバーオーバーレイ + CTAボタン */}
              {/* group-hover: 親要素(article)にホバーした時に発動 */}
              {/* translate-y-4 → group-hover:translate-y-0 でスライドアップアニメーション */}
              <div className="absolute inset-0 flex items-center justify-center bg-black/50 opacity-0 transition-opacity group-hover:opacity-100">
                <a
                  href={item.ctaUrl}
                  target="_blank"
                  rel="noopener noreferrer"
                  className="translate-y-4 rounded-full bg-white px-6 py-2.5 text-sm font-semibold text-gray-900 shadow-lg transition-transform group-hover:translate-y-0"
                >
                  {item.ctaLabel}
                </a>
              </div>
            </div>

            {/* テキストエリア */}
            <div className="p-5">
              <h3 className="text-lg font-semibold text-gray-900">
                {item.title}
              </h3>
              <p className="mt-1 text-sm leading-relaxed text-gray-600">
                {item.description}
              </p>
            </div>
          </article>
        ))}
      </div>
    </section>
  );
}
```

### 使い方（ページで呼び出す例）

```tsx
// app/page.tsx（Next.js App Router）
import { ProductShowcase } from "@/components/ProductShowcase";

// サンプルデータ: ShowcaseItem[] 型の配列
const demoItems = [
  {
    title: "AI チャットボット",
    description: "Supabase + Next.js で作ったリアルタイムチャット",
    mediaUrl: "/demos/chatbot.mp4",
    mediaType: "video" as const, // as const で リテラル型 "video" に固定
    ctaUrl: "https://example.com/chatbot",
    ctaLabel: "デモを見る →",
  },
  {
    title: "ポートフォリオサイト",
    description: "TailwindCSS のアニメーションを活用したLP",
    mediaUrl: "/demos/portfolio.png",
    mediaType: "image" as const,
    ctaUrl: "https://example.com/portfolio",
    ctaLabel: "サイトを見る →",
  },
];

export default function Home() {
  return <ProductShowcase items={demoItems} />;
}
```

### 学習ポイント解説

| 概念 | コード例 | 解説 |
|---|---|---|
| **ユニオン型** | `"video" \| "image"` | 許可する値を限定する。typoをコンパイル時に防げる |
| **as const** | `"video" as const` | 文字列をstring型ではなくリテラル型`"video"`に固定する |
| **条件付きレンダリング** | `{condition ? <A/> : <B/>}` | JSX内の三項演算子でUIを出し分け |
| **group-hover** | `group` + `group-hover:` | Tailwindの親子連動ホバー。親にhoverすると子のスタイルが変わる |
| **分割代入** | `{ items }` | オブジェクトから特定のプロパティだけを変数として取り出す |

---

## 実践ポイント（さとるさんへ）

1. **今日やること**: 上のコードを `npx create-next-app` で作ったプロジェクトにコピペして動かしてみてください。`/public/demos/` フォルダに適当な画像を置けばすぐに表示されます。

2. **LP業務への応用**: このショーケースコンポーネントは、クライアントへの納品物のデモ紹介セクションにそのまま転用できます。FLOCSS的に考えると「c-showcase」のようなComponentレイヤーに配置する設計です。

3. **OpenScreenを試す**: [openscreen](https://github.com/siddharthvaddem/openscreen) をローカルで起動して、自分の個人開発プロジェクトのデモ動画を撮影 → 上のコンポーネントに差し込む、という一連の流れを試してみましょう。

4. **Claude Code活用**: 「このコンポーネントにフェードインアニメーションを追加して」のようにClaude Codeに依頼すると、`framer-motion` や CSS animation を使った拡張案をすぐに提案してもらえます。

---

## 今日の思考トリガー

**「自分の個人開発プロダクトを "30秒のデモ動画" で説明するとしたら、どの画面のどの操作を見せますか？ その動画をLPのファーストビューに置いたら、CVRはどう変わると思いますか？」**

_ソース: [GitHub Trending](https://github.com/trending?since=weekly), [GitHub Trending TypeScript](https://github.com/trending/typescript?since=weekly), [GitHub Trending JavaScript](https://github.com/trending/javascript?since=weekly) (2026年04月05日時点)_
