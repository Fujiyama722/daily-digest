# 📊 プロダクトマネジメント事例解説: 2026年4月15日号

おはようございます！シニア・プロダクトマネージャーの「学習コーチ」です。

今日はGitHub Trendingの上位に食い込んでいる、**AIコーディングコストの可視化SaAS候補**「codeburn」を題材に、個人開発者が最短で収益化までたどり着くための**要件定義 → DB設計 → アーキテクチャ**をまるごと解剖します。Supabase前提のテーブル設計とRLS（Row Level Security）の考え方も、さとるさんのスタックに寄せて解説します。

---

## 本日のハイライト

### 1. 🔥 codeburn — 「AIトークンの出費、見える化します」（823★ / TypeScript）

Claude Code や OpenAI Codex の**トークン消費とコストをターミナル上でリアルタイム可視化**するTUIダッシュボード。個人・チーム問わず「気づいたら月$200溶けていた」という痛みを狙い撃ちする、きわめてPM的にシャープなプロダクトです。

- ソース: https://github.com/AgentSeal/codeburn
- 提供形態: OSS（MITライセンス）／ビジネス化の余地が大きい
- 現在のマネタイズ: なし（＝参入のチャンス）

### 2. 🧠 hermes-hudui — AIエージェントの「意識」をWeb UIで監視（877★ / TypeScript）

持続メモリを持つAIエージェント「Hermes」の状態を可視化するWebダッシュボード。**長期記憶を持つAIエージェントの運用監視**という、まだ誰も正解を出していないカテゴリ。

- ソース: https://github.com/joeynyc/hermes-hudui

### 3. 🔍 anything-analyzer — ブラウザ×CDP×AIでプロトコル解析を自動化（631★ / TypeScript）

Chrome DevTools Protocolでキャプチャ→AIが解析→「API仕様書」「連携コード」を自動生成。**逆解析×AI**という組み合わせで開発者の手間を90%削る、技術ニッチの好例。

- ソース: https://github.com/Mouseww/anything-analyzer

参考ソース（GitHub Trending 今週分）: https://github.com/trending/typescript?since=weekly

---

## 注目リポジトリ / 事例

| プロダクト | カテゴリ | 学べるPM観点 |
|---|---|---|
| **codeburn** (AgentSeal) | Dev Tools / FinOps | 痛み駆動のニッチ攻略・SaaS化 |
| **hermes-hudui** (joeynyc) | AI Ops / Observability | 新カテゴリの「最初の定規」を握る戦略 |
| **anything-analyzer** (Mouseww) | Reverse Engineering × AI | 既存業務の80%自動化＝高単価 |

---

## 本日のケーススタディ深掘り: codeburnを個人開発で「収益化」まで持っていくなら

### ① 要件定義（Who / Problem / Why now）

- **Who（誰の）**: Claude Code・Codex・Cursor等を日常的に使う個人開発者〜少人数チーム（5〜30名）
- **Problem（どんな痛み）**:
  - 「月末に請求書を見るまで本当のコストが分からない」
  - 「どのプロジェクト／どのエージェントがコストを食っているか切り分けられない」
  - 「チームで使うと誰が使いすぎているか分からず、注意しづらい」
- **Why now（なぜ今）**: AIコーディングが「一部の人の道具」から「全員の日常インフラ」に移行中。FinOps（コスト最適化）需要は必ず後追いで来ます。クラウドでAWS Cost Explorerが生まれたのと同じ構造です。

### ② MVPスコープ（捨てる勇気）

最初のバージョンで**絶対に入れるもの**だけを残します。

| 含める | 含めない（v1では捨てる） |
|---|---|
| APIキー1本登録 → 使用量取り込み | チーム招待・権限管理 |
| 日次・週次・月次のコスト推移グラフ | 予算アラートのSlack通知 |
| プロジェクト別の内訳 | AIによる最適化提案 |
| 月額$9の単一プラン | Enterprise SSO |

💡 **PMのコツ**: 「**あったら嬉しい**」は全部v2以降。MVPでは「これが無いと使い物にならない」だけを実装。Supabase + Next.js なら、ここまで1〜2週間で出せます。

### ③ Supabase前提のテーブル設計（ER図テキスト版）

```
users (Supabase Auth 管理) ─┐
                             │
                             ▼
                       workspaces ──┐
                             │      │
                             ▼      │
                       api_keys     │
                             │      │
                             ▼      │
                       usage_events │
                             │      │
                             ▼      │
                       daily_rollups◀┘
```

主要テーブル例：

```sql
-- 組織（個人でもworkspace=1個作る設計が後々楽）
create table workspaces (
  id uuid primary key default gen_random_uuid(),
  owner_id uuid references auth.users(id) not null,
  name text not null,
  created_at timestamptz default now()
);

-- 取り込んだAPIキー（暗号化推奨）
create table api_keys (
  id uuid primary key default gen_random_uuid(),
  workspace_id uuid references workspaces(id) on delete cascade,
  provider text check (provider in ('anthropic','openai','openrouter')),
  label text,
  encrypted_key text not null,
  created_at timestamptz default now()
);

-- 明細（ここが主戦場・行数が一番多い）
create table usage_events (
  id bigserial primary key,
  workspace_id uuid references workspaces(id) on delete cascade,
  api_key_id uuid references api_keys(id),
  occurred_at timestamptz not null,
  model text,
  input_tokens int,
  output_tokens int,
  cost_usd numeric(10,4),
  project_tag text
);

-- 集計（ダッシュボード高速化のための日次ロールアップ）
create table daily_rollups (
  workspace_id uuid references workspaces(id) on delete cascade,
  day date,
  model text,
  total_cost_usd numeric(12,4),
  total_tokens bigint,
  primary key (workspace_id, day, model)
);
```

### ④ RLS（Row Level Security）の考え方 ⭐今日の要点

Supabaseで**最初にやるべき**はRLS。これを後回しにすると「全ユーザーの課金データが丸見え」という事故が起きます。

```sql
-- workspacesテーブル：自分がownerのものしか見えない
alter table workspaces enable row level security;

create policy "workspace_owner_select" on workspaces
  for select using (owner_id = auth.uid());

create policy "workspace_owner_modify" on workspaces
  for all using (owner_id = auth.uid());

-- usage_events：自分が所有するworkspace配下のみ
alter table usage_events enable row level security;

create policy "usage_events_by_workspace" on usage_events
  for select using (
    workspace_id in (
      select id from workspaces where owner_id = auth.uid()
    )
  );
```

💡 **RLSの設計原則**:
1. **デフォルトdeny**: RLSを有効にした瞬間、全行が見えなくなる。そこから必要な穴だけ開ける
2. **auth.uid()を軸に組み立てる**: ログインユーザーのIDを起点に「たどれる範囲」を定義
3. **書き込みと読み込みは別ポリシー**: SELECT / INSERT / UPDATE / DELETE を明確に分ける

### ⑤ アーキテクチャ（Next.js + Supabase + Vercel）

```
[ユーザー] → [Next.js (Vercel)]
                  │
                  ├─ App Router / Server Components（ダッシュボード）
                  ├─ Server Actions（APIキー登録など）
                  │
                  ▼
            [Supabase]
              ├─ Auth（Google/GitHub）
              ├─ Postgres + RLS
              └─ Edge Functions（定期的にAnthropic APIからusage取り込み）

定期実行: Supabase Cron / Vercel Cron Jobs で1時間おきに pull
```

収益化までの最短ルート：
1. **Week 1-2**: MVP公開（無料・Supabaseの無料枠で十分）
2. **Week 3**: HNやX（旧Twitter）、Zenn、note でローンチ記事 → 100人集客
3. **Week 4**: Stripe連携で$9/月 プラン追加、無料は「APIキー1本・30日分」に制限
4. **Month 2-3**: 50人×$9 = $450/月 のスモールビジネス完成 ✅

---

## 実践ポイント（さとるさんへ）

さとるさんの現状（HTML/CSSは強い・React/Next.js/Supabaseは「一人だと難しい」段階）を踏まえた**今日のアクション**です：

1. **🎯 今日の15分タスク**: 上の `workspaces` テーブルと `usage_events` テーブルを、自分のSupabaseプロジェクトに**実際にCREATE TABLEしてみる**。RLSを有効化するときの挙動（SQL Editorで「行がゼロ件に見える」）を体感するのが最大の学び。
2. **📚 今週の学習テーマ**: **RLS**一点集中。Supabase公式ドキュメントの「Row Level Security」章を写経レベルで読む。非エンジニア→シニアへの階段で、セキュリティ観点は**差がつきやすい**領域。
3. **💡 副業活用**: LP制作の業務委託で「お問い合わせフォーム→Supabaseに保存→管理画面で確認」というミニ機能を提案に追加できれば、**単価を上げやすい**。RLSがあれば「クライアント自身のデータだけ見せる」安全な管理画面まで一気通貫で作れます。
4. **🚀 個人開発の型**: 今日のcodeburn分析で使った「Who / Problem / Why now → MVPスコープ → ER図 → RLS → アーキテクチャ」の5ステップを、自分が考えているプロダクトにも当てはめて書き出してみてください。これが**PM思考**の基礎体力です。

---

## 今日の思考トリガー

**「あなたが毎月$20以上払っているツールの中で、"コストの内訳が見えない"ものはどれですか？ その不満を、codeburnのように"可視化ダッシュボード"としてSupabase+Next.jsで作るなら、最小のテーブル3つは何になりますか？」**

---

_ソース: https://github.com/trending/typescript?since=weekly / https://github.com/AgentSeal/codeburn / https://github.com/joeynyc/hermes-hudui / https://github.com/Mouseww/anything-analyzer (2026年4月15日時点)_
