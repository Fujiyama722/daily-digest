# 📊 プロダクトマネジメント事例解説: 2026年05月06日号

おはようございます！シニア・プロダクトマネージャーの「学習コーチ」です。

水曜日は1つのプロダクトを深く分解して、要件定義 → DB設計 → アーキテクチャまで降りていきます。今日の主役は、GitHub Weeklyトレンドにも入っている **DocuSeal**（オープンソース版DocuSign）。「なぜ売れるのか」と「個人開発者でもSupabaseで似たものを作れるか」という2軸で見ていきましょう。

> ⚠️ 補足: 本日 `news.ycombinator.com` と `producthunt.com` は WebFetch がブロックされ取得できませんでした。そのため事例ソースは GitHub Trending（実取得）に絞って構成しています。

---

## 本日のハイライト

GitHub Trendingの実データから、PM視点で「市場が何を求めているか」を読み解けるシグナルを3つ。

- 🖋️ **電子契約のオープンソース化が伸び続けている**
  `docusealco/docuseal` が本日デイリートレンド4位、合計 13,915 stars（+929/日）。SaaSの月額課金に疲れた中小事業者が「自前ホスティング + 必要な機能だけ」を選ぶ流れ。
  ソース: https://github.com/trending（2026-05-06取得）／ https://github.com/docusealco/docuseal

- 🤖 **「ターミナル × エージェント」がメインストリーム化**
  `warpdotdev/warp` がWeekly +27,872 stars。CLI＝開発者の作業面そのものをAIに渡す体験が、コードエディタ拡張より「速い」と評価され始めています。
  ソース: https://github.com/trending?since=weekly（2026-05-06取得）／ https://github.com/warpdotdev/warp

- 🧠 **「Skills」というドキュメント資産が新しい競争軸**
  `mattpocock/skills` がWeekly +31,091 stars で1位級。AIエージェント時代は「コードベース」だけでなく「AIに渡す手順書」自体が差別化要素になる、というPM的シグナル。
  ソース: https://github.com/trending?since=weekly（2026-05-06取得）／ https://github.com/mattpocock/skills

## 注目リポジトリ / 事例

### 1. docusealco/docuseal — 本日のメインケース 🔍

- ⭐ 13,915 stars（デイリー +929）／ 言語: Ruby（Rails）+ Vue + TailwindCSS
- 概要: WYSIWYGなPDFフォームビルダー、12種類のフィールド、複数署名者、SMTP/S3連携、API/Webhook対応。OSS版は無料、クラウド版は有料（白ラベル・SSO・条件付きフィールド・SMS認証など）。
- ソース: https://github.com/docusealco/docuseal

**なぜこのプロダクトが伸びるのか（PM視点）**

1. **「DocuSign の代替」という既存ジョブに乗っている** — 新カテゴリを教育する必要がない（=CACが安い）
2. **OSS無料版とPro版の境界が綺麗** — 中小は無料で十分、エンタープライズはSSO/監査ログが必要 → アップセル線が明確
3. **API/Webhookと埋め込みSDKを最初から提供** — 自社プロダクトに「署名機能」を組み込みたいSaaS開発者が顧客になる（B2B2C）

### 2. warpdotdev/warp

- ⭐ 54,875 stars（Weekly +27,872）／ Rust製ターミナル
- ソース: https://github.com/warpdotdev/warp
- フリーミアム + 有料エンタープライズ。「開発者の毎日の入口」を押さえるプラットフォーム戦略。

### 3. ruvnet/ruflo

- ⭐ 43,388 stars（デイリー +2,441）／ TypeScript
- ソース: https://github.com/ruvnet/ruflo
- Claude向けエージェントオーケストレーション。AI時代の「ワークフローエンジン」というポジション争いが過熱中。

---

## 実践ポイント（さとるさんへ）

DocuSealをそっくり作るのは大変ですが、**「PDFは扱わず、Web上のフォームに署名 → PDF生成 → 双方にメール送付」という最小スコープなら、Supabase + Next.js で十分作れます。** これを題材に、PMが書く「設計の素」を一緒に作ってみましょう。

### 🎯 MVP要件定義（“1枚PM”）

| 項目 | 内容 |
|------|------|
| **誰が** | 個人事業主 / 業務委託のフリーランス |
| **何に困っているか** | 業務委託契約のたびにDocuSignを使うほどではないが、メール添付＋押印PDFは古い |
| **MVPでできること** | (1) 送信者がフォームを作る (2) 相手にURLを送る (3) 相手が署名・記入 (4) 双方にPDFが届く |
| **MVPで切るもの** | テンプレート共有・マルチ署名者・SSO・SMS認証・条件付きフィールド |
| **収益化** | 月3通まで無料、それ以上は月980円。Stripe決済 |

「**MVPで切るもの**」の欄が一番重要です。ここに具体的な機能名が並んでいない要件定義は、ほぼ確実に肥大化します。

### 🗄️ Supabaseテーブル設計（最小5テーブル）

```sql
-- 1. ユーザー（Supabase Authが auth.users を作るので、追加情報のみ）
create table profiles (
  id uuid primary key references auth.users(id) on delete cascade,
  display_name text,
  plan text not null default 'free',  -- 'free' | 'pro'
  created_at timestamptz not null default now()
);

-- 2. 文書テンプレート（送信者が作る雛形）
create table documents (
  id uuid primary key default gen_random_uuid(),
  owner_id uuid not null references auth.users(id) on delete cascade,
  title text not null,
  fields jsonb not null,  -- フィールド定義（ラベル・型・座標）をJSONで持つ
  created_at timestamptz not null default now()
);

-- 3. 署名依頼（送信者が「この文書を相手に送る」単位）
create table envelopes (
  id uuid primary key default gen_random_uuid(),
  document_id uuid not null references documents(id) on delete cascade,
  sender_id uuid not null references auth.users(id) on delete cascade,
  recipient_email text not null,
  status text not null default 'sent',  -- 'sent' | 'signed' | 'expired'
  access_token text not null unique,    -- URLに埋め込む推測困難なトークン
  expires_at timestamptz not null,
  created_at timestamptz not null default now()
);

-- 4. 署名済みデータ（相手が入力した値）
create table submissions (
  id uuid primary key default gen_random_uuid(),
  envelope_id uuid not null references envelopes(id) on delete cascade,
  values jsonb not null,
  signed_at timestamptz not null default now(),
  signer_ip inet
);

-- 5. 監査ログ（Pro版で見せる差別化機能）
create table audit_logs (
  id bigserial primary key,
  envelope_id uuid not null references envelopes(id) on delete cascade,
  event text not null,           -- 'opened' | 'signed' | 'downloaded'
  occurred_at timestamptz not null default now(),
  meta jsonb
);
```

### 🔐 RLS（Row Level Security）の考え方

Supabaseで個人開発をするなら、**「RLSをONにし忘れる = 全件公開」** という罠を絶対に避けてください。テーブルごとに「誰が何をできるか」を明文化するのがコツです。

```sql
-- documentsとenvelopesは「自分が作ったものだけ」見える
alter table documents enable row level security;
create policy "owner can read own documents"
  on documents for select
  using (owner_id = auth.uid());

create policy "owner can insert own documents"
  on documents for insert
  with check (owner_id = auth.uid());

alter table envelopes enable row level security;
create policy "sender can read own envelopes"
  on envelopes for select
  using (sender_id = auth.uid());
```

ここでPM的に重要なのは、**「相手（recipient）はログインしないユーザー」** という点です。彼らはRLSをすり抜けて signing ページにアクセスする必要があります。設計の選択肢は2つ:

1. **アクセストークン方式（推奨）** — `envelopes.access_token` をURLに埋め込み、Next.jsのRoute Handlerで `service_role key` を使ってサーバ側だけがDB読み出し → 結果をブラウザに返す。RLSは「ログインユーザーの読み書き」のみ守る役割に集中させる。
2. **匿名Auth + 一時的Policy** — Supabaseの匿名認証セッションを発行し、特定envelopeにのみ紐づくRLSを書く。柔軟だがポリシーが複雑化。

MVPでは1の「サーバ側でトークン照合 → 結果だけ返す」が学習コストも低くおすすめです。

### 🏗️ アーキテクチャ全体像（最小構成）

```
[ Next.js (Vercel) ]
   ├ /app/(sender)/...    ← 送信者向け。Supabase Auth + RLS
   ├ /app/sign/[token]/   ← 受信者向け。Auth不要、Route Handlerで token照合
   └ /api/webhooks/stripe ← 課金イベント受信

[ Supabase ]
   ├ Auth         （送信者の認証）
   ├ Postgres     （上記5テーブル + RLS）
   ├ Storage      （生成PDFを保存）
   └ Edge Function（PDF生成 + メール送信、Resend API呼び出し）

[ Stripe ]        （Pro課金、月980円）
```

**実装に課金が必要な部分**: Stripe（手数料3.6%程度／決済時のみ）、Resendなどメール送信API（無料枠あり、超えると月数百円〜）、Vercel/Supabaseは無料枠で当面足ります。

### さとるさんの今日の一手

- まず **「MVPで切るもの」のテーブル** だけを書いて、私（Claude Code）に「これMVPとして妥当？削るとしたらどこ？」と問いかける ← これだけで設計力が育ちます
- 今お持ちの React/Next.js/TypeScript/Supabase スタックを活かすなら、`/app/sign/[token]/page.tsx` の **未ログイン側だけ作ってみる** のが一番学習効果が高いです（RLSをバイパスしないAPI設計の練習になる）
- LP制作のスキルをそのまま活かして、ランディングページを `/app/page.tsx` に先に作ると、「製品より先にLPを書く」というプロが当たり前にやる順序が身につきます

---

## 今日の思考トリガー

**「あなたが今いちばん『毎月の支払いが惜しい』と感じているSaaSは何ですか？ その不満を、Supabaseと自分のスキルで“最小限だけ”解決するならどこを切り捨てますか？」**

この問いに30秒で答えられないと、PMとして「機能を引き算する力」が鈍っているサインです。逆にスラスラ言えれば、それはもう個人開発の最初のテーマになります。

---

_ソース: https://github.com/trending (2026年05月06日時点) / https://github.com/trending?since=weekly (2026年05月06日時点) / https://github.com/docusealco/docuseal (2026年05月06日時点) / https://github.com/warpdotdev/warp (2026年05月06日時点)_
