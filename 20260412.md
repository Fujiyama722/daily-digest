# 🛠️ 週末ハンズオン実践: 2026年04月12日号

おはようございます！フロントエンドメンターの「学習コーチ」です。

※ 本ダイジェストは日曜日向けのテンプレートが未定義のため、土曜日「週末ハンズオン実践」形式を採用しています。

---

## 本日のハイライト

今週のGitHubトレンドを俯瞰すると、はっきり見えてくるのは「**コーディングエージェントを“チーム化”する流れ**」です。単発のAI補助ではなく、Claude Code / Codex CLI を複数束ねてワークフローとして運用するリポジトリが軒並み急上昇しました。

- **multica-ai/multica** (TypeScript / 週+3,512★): コーディングエージェントを“本当のチームメイト”として扱うためのマネージドプラットフォーム。タスクアサイン・進捗追跡・スキル蓄積といった「PM的な発想」でエージェントを運用する思想が特徴。
  ソース: https://github.com/multica-ai/multica
- **Yeachan-Heo/oh-my-claudecode** (TypeScript / 週+4,044★): Claude Code 向けの Teams-first マルチエージェントオーケストレーション。個人開発者が複数の役割（設計/実装/レビュー）を並列で回すためのレシピ集として注目。
  ソース: https://github.com/Yeachan-Heo/oh-my-claudecode
- **forrestchang/andrej-karpathy-skills** (Markdown / 週+3,741★): Andrej Karpathy 氏の観察に基づき、Claude Code の挙動を調整するための Skills 設定集。LLM が苦手な箇所を“ガードレール”で埋める発想が学びになります。
  ソース: https://github.com/forrestchang/andrej-karpathy-skills
- **siddharthvaddem/openscreen** (TypeScript / 週+10,077★): 無料・オープンソース・商用可な Screen Studio 代替。個人開発のデモ動画・LP用の操作GIF作成に即戦力です。
  ソース: https://github.com/siddharthvaddem/openscreen
- **Anil-matcha/Open-Higgsfield-AI** (JavaScript / 週+1,381★): 20以上のモデルを内蔵したセルフホスト型AI画像生成+映像制作。個人開発のヒーロー画像/OGP生成に使える可能性あり。
  ソース: https://github.com/Anil-matcha/Open-Higgsfield-AI

> ※ Hacker News / HN Algolia 側は本日CDNから 403 が返却され取得不能でした。そのため出典は GitHub Trending に絞っています。

## 注目リポジトリ / 事例

### 1. multica-ai/multica — 「エージェントをチームメンバー扱いする」TypeScript基盤
- 概要: Claude Code / Cursor のようなコーディングAIを“単発ツール”ではなく“ジュニアメンバー”として管理するプラットフォーム。タスク委譲・進捗追跡・スキルの蓄積が軸。
- ソース: https://github.com/multica-ai/multica

### 2. siddharthvaddem/openscreen — 週+10,077★の爆伸びデモ録画ツール
- 概要: ブラウザ操作のデモ動画をカッコよく仕上げられる OSS。個人開発プロダクトの LP・プレスキット・Twitter 告知動画を内製する手段として実用度が極めて高い。
- ソース: https://github.com/siddharthvaddem/openscreen

### 3. forrestchang/andrej-karpathy-skills — Claude Code を“賢く縛る” Skills 集
- 概要: `.claude/skills/` 配下に置く Markdown 形式の指示書サンプル。AIが迷いがちな TypeScript の型運用やテスト設計を“補助輪”としてガイドする構成は、そのまま自分用Skills を書く練習台になります。
- ソース: https://github.com/forrestchang/andrej-karpathy-skills

---

## 実践ポイント（さとるさんへ）

今日の週末ハンズオンは **「React 19 の `useOptimistic` + Next.js 15 Server Actions + Supabase」** で、**楽観的UI付きの“今日のタスク”フォーム**を作ってみましょう。さとるさんの学習スタック(React / Next.js / TypeScript / Tailwind / Supabase)にどストライクで、しかも40〜60分で完結するサイズです。

### なぜこの組み合わせ？
- `useOptimistic`: React 19 で正式化された「送信前にUIを先に更新しておく」ためのフック。体感速度が劇的に上がる=個人開発でも差別化に直結。
- Server Actions: Next.js 15 (App Router) で“フォーム→サーバー処理”を fetch や API Route なしで書ける仕組み。学習者がつまずきがちな「クライアント⇄サーバーの境界」を一気に省略できる。
- Supabase: RLS さえ最初に整えれば、後は純粋に「フロントから supabase-js を叩くだけ」でDBに書き込める。

### 手順（コピペで動くサンプル）

**1) Supabase側: テーブル定義 (SQL Editorで1回だけ実行)**

```sql
create table if not exists tasks (
  id uuid primary key default gen_random_uuid(),
  title text not null,
  done boolean not null default false,
  created_at timestamptz not null default now()
);

alter table tasks enable row level security;

-- デモ用。本番は auth.uid() ベースに必ず変更してください。
create policy "public read" on tasks for select using (true);
create policy "public insert" on tasks for insert with check (true);
```

**2) `app/actions/tasks.ts` — Server Action**

```ts
"use server";

import { createClient } from "@supabase/supabase-js";
import { revalidatePath } from "next/cache";

// 型の“正解”を1箇所に集める。これがTypeScriptの恩恵の核。
export type Task = {
  id: string;
  title: string;
  done: boolean;
  created_at: string;
};

const supabase = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL!,
  process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
);

export async function addTask(formData: FormData): Promise<Task> {
  const title = String(formData.get("title") ?? "").trim();
  if (!title) throw new Error("title is required");

  const { data, error } = await supabase
    .from("tasks")
    .insert({ title })
    .select()
    .single();

  if (error) throw error;

  revalidatePath("/"); // SSRされたリストをサーバー側でも更新
  return data as Task;
}
```

**3) `app/page.tsx` — `useOptimistic` でサクサクUI**

```tsx
"use client";

import { useOptimistic, useRef, useTransition } from "react";
import { addTask, type Task } from "./actions/tasks";

type Props = { initialTasks: Task[] };

export default function TodayTasks({ initialTasks }: Props) {
  const formRef = useRef<HTMLFormElement>(null);
  const [isPending, startTransition] = useTransition();

  // optimisticTasks: 画面に映す“見かけ上の”配列
  // addOptimistic: 「この新タスクを先に足しちゃって！」と宣言する関数
  const [optimisticTasks, addOptimistic] = useOptimistic<Task[], string>(
    initialTasks,
    (prev, title) => [
      ...prev,
      {
        id: `tmp-${crypto.randomUUID()}`,
        title,
        done: false,
        created_at: new Date().toISOString(),
      },
    ],
  );

  async function action(formData: FormData) {
    const title = String(formData.get("title") ?? "");
    // 1. 先にUIへ“仮タスク”を追加（= 楽観的更新）
    addOptimistic(title);
    formRef.current?.reset();

    // 2. 裏でサーバーに本書き込み
    startTransition(async () => {
      await addTask(formData);
    });
  }

  return (
    <section className="mx-auto max-w-md p-6">
      <h1 className="mb-4 text-xl font-bold">今日のタスク</h1>

      <form ref={formRef} action={action} className="mb-6 flex gap-2">
        <input
          name="title"
          required
          placeholder="やること…"
          className="flex-1 rounded-md border border-gray-300 px-3 py-2"
        />
        <button
          type="submit"
          disabled={isPending}
          className="rounded-md bg-black px-4 py-2 text-white disabled:opacity-50"
        >
          追加
        </button>
      </form>

      <ul className="space-y-2">
        {optimisticTasks.map((t) => (
          <li
            key={t.id}
            className={`rounded-md border border-gray-200 px-3 py-2 ${
              t.id.startsWith("tmp-") ? "opacity-60 italic" : ""
            }`}
          >
            {t.title}
          </li>
        ))}
      </ul>
    </section>
  );
}
```

### 初心者向けに“型とロジック”をほどく

- `useOptimistic<Task[], string>` の **2つの型引数** は「状態の型 / 追加入力の型」です。TypeScript のジェネリクスは「関数の入出力の型を、使う側が“当てはめる”仕組み」だと捉えると腑に落ちます。
- `addOptimistic(title)` を呼んだ瞬間、React は “next render の間だけ有効な仮の状態” を作ります。サーバー処理が成功すれば `revalidatePath` により本物の Task がやってきて仮行が置き換わる、失敗すれば勝手にロールバックされる——この**巻き戻し可能性**が単純な `useState` と決定的に違う点です。
- `id: \`tmp-${...}\`` にしているのは「送信中だけグレーアウトしたい」ため。判定条件を `startsWith("tmp-")` に集約することで、UI層からサーバー状態を覗かずに済んでいます。境界をシンプルに保つのはシニアのコード設計で最も褒められる観点の一つです。

### 週末の進め方（おすすめ3段階）
1. **30分**: 上記をそのまま動かす。`npm run dev` で “追加→即反映→DBに入る” の体験をまず身体で覚える。
2. **30分**: `done` チェックボックスを `useOptimistic` で同じように実装してみる(更新系に拡張)。
3. **任意**: RLS を `auth.uid() = user_id` ベースに差し替え、Supabase Auth を繋いで「自分のタスクだけ見える」状態まで持っていく。ここまでやると“MVPの1/3”ぐらいまで来ます。

---

## 今日の思考トリガー

**「このタスクUIを“自分の個人プロダクトのMVPの一部”として組み込むとしたら、どんなドメイン(学習記録？家計簿？読書ログ？)に載せ替えるのが一番収益化のストーリーを描きやすいでしょうか？ そしてそのとき、`tasks` テーブルにはどんな列を1つだけ追加しますか？」**

_ソース: https://github.com/trending?since=weekly / https://github.com/trending/typescript?since=weekly / https://github.com/trending/javascript?since=weekly (2026年04月12日時点)_
