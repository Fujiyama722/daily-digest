# 🎨 トレンドUI実装チュートリアル: 2026年04月28日号

おはようございます！シニアUI/UXデザイナー兼フロントエンドエンジニアの「学習コーチ」です。

今週のGitHubトレンドを眺めると、上位はAIエージェント・コンテキスト管理・LLM周辺ツールばかり。「UI部品単体」のトレンドリポジトリが主役の週ではありませんでした。ただ、これは裏を返すと「AIツール時代に合うUI」が市場の中心になっているということ。今日はそこから示唆を取って、**Command Palette（コマンドパレット / ⌘+K UI）** を題材に、React + TypeScript + TailwindCSSで実装する基本形を解説します。

---

## 本日のハイライト

- 🔼 **GitHub Trending TypeScript（週間） 1位: zilliztech/claude-context** — 「Code search MCP for Claude Code」。今週 +3,537 ★ で総 9,843 ★。AIエージェントが"コードベース全体を文脈にする"流れが強まっており、開発者向けUIも「素早く検索→アクション実行」を前提に設計されつつあります。ソース: https://github.com/trending/typescript?since=weekly
- 🔼 **GitHub Trending JavaScript（週間）に HeyPuter/puter（総 40,714 ★, +253 ★）が継続ランクイン** — 「The Internet Computer」を標榜するブラウザ内OS。アプリランチャー・スポットライト風検索といった"OSライクUI"がWebに移植される好例です。ソース: https://github.com/trending/javascript?since=weekly / https://github.com/HeyPuter/puter
- 📰 **Hacker News API 取得不可** — 本日は `https://hacker-news.firebaseio.com/v0/topstories.json` および `https://hn.algolia.com/?q=frontend+UI+CSS+React&tags=story` が 403 で取得できませんでした。※本日は取得できませんでした。
- 🎯 **市場観察:** Linear, Vercel, Notion, Raycast, GitHub などモダンSaaSの共通言語として "⌘+K で全部やる" UIが定着。LP制作の現場でも、サイト内検索＋ナビ統合UIとして応用する事例が増えています。

---

## 注目リポジトリ / 事例

1. **zilliztech/claude-context** — Claude Code向けのコード検索MCPサーバー。今週 +3,537 ★（総 9,843 ★, TypeScript）。AI開発ツールが「対話 + 即時検索」を前提に進化していることの象徴。ソース: https://github.com/zilliztech/claude-context
2. **HeyPuter/puter** — ブラウザ内デスクトップOS。総 40,714 ★（JavaScript）。スポットライト型ランチャー・モーダル中心のUIアーキテクチャはCommand Paletteの上位互換として参考になる。ソース: https://github.com/HeyPuter/puter
3. **Stremio/stremio-web** — React/TypeScriptで作られたメディアセンターWeb版。総 10,660 ★。コンテンツ中心アプリでの検索+ナビ統合UIの実装例として読みやすい。ソース: https://github.com/Stremio/stremio-web

---

## 今日のハンズオン: ⌘+K Command Palette を作る

### なぜ今これか（UX/CVR視点）

- **学習コスト削減:** ユーザーが「ナビを探す→クリック」する代わりに、`⌘+K`（Win: `Ctrl+K`）でキーワードから直接ジャンプ。サイト内タスク完了までのクリック数が2〜4 → 1 に圧縮されます。
- **CVR効果:** ECやSaaSのLPでは「資料請求」「料金ページ」など主要CTAをCommand Paletteの上位候補に固定すると、回遊中ユーザーの離脱寸前のクリックを拾えます（"摩擦の最後の1mm"を消す装置）。
- **モバイル退化問題:** ⌘+Kはデスクトップ優位なので、モバイルではフローティングボタンで同じ検索UIを起動できる二段構えが定石。

### React + Next.js + TypeScript + Tailwind 実装（最小形）

```tsx
// app/components/CommandPalette.tsx
"use client";
import { useEffect, useMemo, useState } from "react";
import { useRouter } from "next/navigation";

type Command = {
  id: string;
  label: string;
  hint?: string;
  run: () => void;
};

export function CommandPalette() {
  const router = useRouter();
  const [open, setOpen] = useState(false);
  const [query, setQuery] = useState("");

  // 1) ⌘+K / Ctrl+K で開閉
  useEffect(() => {
    const onKey = (e: KeyboardEvent) => {
      const isToggle = (e.metaKey || e.ctrlKey) && e.key.toLowerCase() === "k";
      if (isToggle) {
        e.preventDefault();
        setOpen((v) => !v);
      }
      if (e.key === "Escape") setOpen(false);
    };
    window.addEventListener("keydown", onKey);
    return () => window.removeEventListener("keydown", onKey);
  }, []);

  // 2) コマンド定義（CTA優先で並べる）
  const commands: Command[] = useMemo(
    () => [
      { id: "pricing", label: "料金ページへ", hint: "/pricing", run: () => router.push("/pricing") },
      { id: "contact", label: "お問い合わせ", hint: "/contact", run: () => router.push("/contact") },
      { id: "blog", label: "ブログ一覧", hint: "/blog", run: () => router.push("/blog") },
      { id: "github", label: "GitHubを開く", hint: "external", run: () => window.open("https://github.com", "_blank") },
    ],
    [router]
  );

  // 3) インクリメンタル検索（部分一致・大小無視）
  const filtered = commands.filter((c) =>
    c.label.toLowerCase().includes(query.toLowerCase())
  );

  if (!open) return null;

  return (
    <div
      role="dialog"
      aria-modal="true"
      className="fixed inset-0 z-50 flex items-start justify-center bg-black/40 p-4 pt-[15vh]"
      onClick={() => setOpen(false)}
    >
      <div
        className="w-full max-w-xl overflow-hidden rounded-2xl bg-white shadow-2xl ring-1 ring-black/5"
        onClick={(e) => e.stopPropagation()}
      >
        <input
          autoFocus
          value={query}
          onChange={(e) => setQuery(e.target.value)}
          placeholder="コマンドを検索…（例: 料金）"
          className="w-full border-b border-gray-200 px-4 py-3 text-base outline-none"
        />
        <ul className="max-h-80 overflow-y-auto py-2">
          {filtered.length === 0 && (
            <li className="px-4 py-3 text-sm text-gray-500">該当なし</li>
          )}
          {filtered.map((c) => (
            <li key={c.id}>
              <button
                onClick={() => {
                  c.run();
                  setOpen(false);
                }}
                className="flex w-full items-center justify-between px-4 py-2 text-left hover:bg-gray-100"
              >
                <span className="text-sm">{c.label}</span>
                {c.hint && (
                  <span className="rounded bg-gray-100 px-2 py-0.5 text-xs text-gray-500">
                    {c.hint}
                  </span>
                )}
              </button>
            </li>
          ))}
        </ul>
      </div>
    </div>
  );
}
```

使い方: `app/layout.tsx` の `<body>` 直下に `<CommandPalette />` を1回配置するだけでサイト全体で `⌘+K` が効きます。

### 初心者向け：型とロジックのポイント解説

- **`type Command = { ... }`**: TypeScriptの「型エイリアス」。`run: () => void` は「引数なし・戻り値なしの関数」を意味します。型を先に決めておくと、`commands` 配列に間違ったオブジェクトを入れた瞬間にエディタが赤線で叱ってくれます（= バグの早期検知）。
- **`useState<boolean>` を `useState(false)` と書ける理由**: 初期値 `false` から TypeScript が「この state は boolean だな」と推論してくれるため。明示しなくても安全。
- **`useMemo`**: `commands` 配列を毎レンダリング作り直さないための最適化。`router` が変わらない限り再計算されません。今回は軽量なので必須ではないですが、配列が大きくなった時の備え。
- **`useEffect` のクリーンアップ**: `return () => window.removeEventListener(...)` を書かないとアンマウント後もリスナーが残り、メモリリークや重複発火の温床に。`useEffect` は「副作用を仕込んだら片付けまで」とセットで覚えてください。
- **`filtered = commands.filter(...)`**: JavaScriptの配列メソッド。元配列を変更せず、条件にマッチする要素だけを集めた**新しい配列**を返します。Reactでは「state を直接書き換えない」が鉄則なので、`filter` `map` `reduce` の3兄弟が主役になります。
- **`role="dialog"` / `aria-modal="true"`**: スクリーンリーダー向けに「これはモーダルですよ」と伝えるARIA属性。CVRを意識するなら**アクセシビリティもCV**だと思ってください（=より多くのユーザーが完走できる）。

---

## 実践ポイント（さとるさんへ）

- **今日の30分タスク:** お手元のNext.jsプロジェクト（または新規 `npx create-next-app`）で上のコードを `app/components/CommandPalette.tsx` に貼り付け、`layout.tsx` で読み込むところまで。⌘+Kで動けば成功。
- **+30分の発展課題:** `commands` を `lib/commands.ts` に外出しして「ページごとに追加コマンドを足せる」構造にする。これは"関数を引数として受け取る"設計で、TypeScript の型エイリアスと配列の組み合わせを身体で覚える練習になります。
- **LP制作への応用:** 業務委託のLPでも、ヘッダー右上に小さく「⌘K」ボタンを置き、料金/事例/問い合わせへ飛ばすだけで体感速度が大きく変わります。差別化要素として提案資料に入れてみてください。
- **Supabase接続の素振り:** 将来的に `commands` を Supabase の `pages` テーブルから読み込むと、CMSライクに育てられます。今日は配列で十分。「最初は配列、伸びてきたら DB」が個人開発の勝ち筋。

---

## 今日の思考トリガー

**「自分のサイト/個人開発プロダクトで、ユーザーが最初の30秒以内に到達してほしいページTOP3はどこか？ そのTOP3を ⌘+K の最上位3コマンドに固定するとしたら、何になるか？」**

これは"ナビを増やすデザイン"から"主要動線を圧縮するデザイン"へ思考を切り替える練習です。CVRは結局「目的地に何回で着けるか」のゲームなので、Command Paletteは単なる装飾ではなく**情報設計の鏡**になります。

---

_ソース: https://github.com/trending/javascript?since=weekly , https://github.com/trending/typescript?since=weekly , https://github.com/HeyPuter/puter , https://github.com/Stremio/stremio-web , https://github.com/zilliztech/claude-context （いずれも2026年04月28日時点）_
_※ Hacker News 関連エンドポイント（hacker-news.firebaseio.com / hn.algolia.com）は本日 403 のため取得できませんでした。_
