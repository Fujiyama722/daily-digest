# 📊 プロダクトマネジメント事例解説: 2026年06月10日号

おはようございます！シニア・プロダクトマネージャーの「学習コーチ」です。

水曜日は1つのプロダクトを深掘りして、要件定義・DB設計・アーキテクチャを「自分の個人開発に転用するならどう設計するか」という視点で読み解きます。今日は **ワークフロー自動化SaaS** をテーマに、GitHub上で圧倒的に支持されている n8n を題材にします。

---

## 本日のハイライト

### 1. n8n が GitHub Star 191,791 を突破 — Fair-code OSS × クラウドのハイブリッド成功例
ワークフロー自動化プラットフォーム **n8n-io/n8n** が約19万スター、450以上のインテグレーションを抱えるまでに成長しています。「Fair-code（ソースは公開、商用利用に制限）」というOSSモデルでコミュニティ拡大と収益化を両立させた、近年のPM教科書事例です。
- ソース: https://github.com/n8n-io/n8n

### 2. AI Agent Workflow 領域の地殻変動 — Dify が 144,580★ で実用フェーズへ
**langgenius/dify** は「Production-ready platform for agentic workflow development」として、社内RAG/AIエージェント基盤を非エンジニアでもGUI構築できる方向にシフト。AI×ノーコードは今年もっとも需要の伸びるカテゴリです。
- ソース: https://github.com/langgenius/dify

### 3. AI UI レイヤーの新定番 — open-webui が 140,841★
**open-webui/open-webui** は Ollama や OpenAI互換APIをラップする「ユーザーフレンドリーなAI UI」。バックエンドモデルが多様化する時代に、UI層を抽象化するプロダクトは今後も需要が伸びる領域です。
- ソース: https://github.com/open-webui/open-webui

> 補足: 本日 Product Hunt と Hacker News のフロントページは取得できませんでした（HTTP 403）。代わりに GitHub Search API 実データで検証した3件を取り上げています。

---

## 注目リポジトリ / 事例

| # | 名前 | スター | 概要 | URL |
|---|------|--------|------|-----|
| 1 | n8n-io/n8n | 191,791 | Fair-codeワークフロー自動化プラットフォーム | https://github.com/n8n-io/n8n |
| 2 | langgenius/dify | 144,580 | エージェント型AIワークフロー開発プラットフォーム | https://github.com/langgenius/dify |
| 3 | open-webui/open-webui | 140,841 | Ollama/OpenAI 対応のセルフホスト型AI UI | https://github.com/open-webui/open-webui |

---

## 実践ポイント（さとるさんへ）

非エンジニアからの個人開発で、いきなり n8n クラスを目指す必要はありません。**「業務委託で扱っている繰り返し作業を1つ自動化するMVP」** を Supabase + Next.js でつくる、というのが現実的な第一歩です。

### 🎯 ケーススタディ: 「LP制作タスクの進捗ダッシュボード」を n8n 風に作るなら

業務委託でLPコーディングをするとき、**「クライアント → タスク → 工程ステータス」** を可視化するシンプルなSaaSを設計してみます。

#### 要件定義（最小単位）
- ユーザーは自分が担当するクライアント案件だけ閲覧/編集できる
- 1案件は複数の「工程」（要件確認 / コーディング / レビュー / 納品）を持つ
- 工程には「完了予定日」「実績日」「担当者メモ」が紐づく
- 将来的に「工程が完了したら Discord に通知」のような自動化を足せる

#### Supabase テーブル設計
```sql
-- クライアント
create table clients (
  id uuid primary key default gen_random_uuid(),
  owner_id uuid not null references auth.users(id) on delete cascade,
  name text not null,
  created_at timestamptz default now()
);

-- 案件（プロジェクト）
create table projects (
  id uuid primary key default gen_random_uuid(),
  client_id uuid not null references clients(id) on delete cascade,
  owner_id uuid not null references auth.users(id) on delete cascade,
  title text not null,
  status text not null default 'draft', -- draft | active | done
  created_at timestamptz default now()
);

-- 工程（タスク）
create table tasks (
  id uuid primary key default gen_random_uuid(),
  project_id uuid not null references projects(id) on delete cascade,
  owner_id uuid not null references auth.users(id) on delete cascade,
  name text not null, -- 例: 'コーディング'
  due_date date,
  done_at date,
  memo text,
  position int not null default 0
);
```

#### RLS（Row Level Security）の考え方
Supabase の RLS は「他人のデータを絶対に見せない」を DB 層で保証する仕組みです。**API側で頑張るのではなく、テーブルに `owner_id` を必ず持たせ、RLS で `auth.uid() = owner_id` を強制する** のが鉄則。

```sql
alter table projects enable row level security;

create policy "owner can read"   on projects for select using (auth.uid() = owner_id);
create policy "owner can insert" on projects for insert with check (auth.uid() = owner_id);
create policy "owner can update" on projects for update using (auth.uid() = owner_id);
create policy "owner can delete" on projects for delete using (auth.uid() = owner_id);
```

> 💡 RLS は「クライアントから直接Supabaseを叩く」前提のとき特に効きます。Next.jsの App Router からブラウザ経由でクエリしても、他ユーザーのデータが漏れません。

#### MVPアーキテクチャ
- フロント: Next.js (App Router) + TailwindCSS
- 認証: Supabase Auth（メール or Google）
- DB: Supabase Postgres + RLS
- 通知（後付け）: Supabase Edge Functions → Discord Webhook

**ここがPM視点のキモ**: 最初から「自動化通知」「チーム共有」「課金プラン」を作りこむと前に進めません。n8n や Dify も初期は「シンプルなワークフロー実行」だけで出して、後からエンタープライズ機能を積みました。**MVP は "1人で使って便利" だけを徹底的に磨く** のが収益化への最短ルートです。

### 収益化への道筋（PM視点）
1. **フリーミアム**: 個人利用は無料、案件3つまで。
2. **Pro プラン**: 月980円で案件無制限 + Discord通知。
3. **Team プラン**: 月2,980円で複数ユーザー共有。

n8n が「セルフホスト無料 + クラウド有料」で成立しているように、**OSS or 個人無料を窓口にしてクラウド/Pro機能で稼ぐ** モデルは個人開発でも再現可能です。

---

## 今日の思考トリガー

**「あなたが業務委託で月に10回以上繰り返している作業はなんですか？ それを自動化する最小プロダクトを、ユーザー登録すら無しでまず "自分1人用ツール" として出すなら、どのテーブル設計から始めますか？」**

_ソース: https://github.com/n8n-io/n8n, https://github.com/langgenius/dify, https://github.com/open-webui/open-webui (2026年06月10日時点 / GitHub Search API 実取得)_
