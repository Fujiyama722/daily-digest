# 🛠️ 週末ハンズオン実践: 2026年05月02日号

おはようございます！フロントエンドメンターの「学習コーチ」です。

ゴールデンウィーク真っ只中の土曜日、いかがお過ごしでしょうか。週末は「読んで終わり」ではなく **手を動かして1コミット残す** のがいちばんの近道です。今日は今週GitHubで急上昇している「AIエージェント向けツールキット」のトレンドから、実務でも個人開発でもすぐ効く UI パターン── **⌘K コマンドパレット** を Next.js + TypeScript + Tailwind で自作します。LP コーディングの現場でも「サイト内検索 / ナビゲーション」として応用できる汎用テクニックです。

---

## 本日のハイライト

今週の GitHub Trending（Weekly）は、引き続き **「AI コーディングエージェントを賢く使うための周辺ツール」** が席巻しました。「LLM そのもの」ではなく **コンテキスト管理 / スキル定義 / コード検索** が主役になっているのが、2026年初夏の特徴です。

- **Claude Skills 文化が一気に成熟**: `.claude/` ディレクトリの中身を OSS として公開する流れが定着。週単位で数万 ⭐ を集める「Skills 系リポジトリ」が複数ランクインしています。
  - ソース: https://github.com/trending?since=weekly （2026年5月2日時点）
- **TypeScript ランキングは "Context 最適化" 一色**: `context-mode` や `claude-context` が同時にランクインし、エージェントに渡すトークンを 90%以上削るパターンが標準化しつつあります。
  - ソース: https://github.com/trending/typescript?since=weekly （2026年5月2日時点）
- **クライアントサイドで動く Code Intelligence**: `GitNexus` は「サーバーレスでコード知識グラフを作る」発想で、Web ブラウザだけで完結するのがポイント。Next.js + WebAssembly 系の知見と相性が良い領域です。
  - ソース: https://github.com/abhigyanpatwari/GitNexus （2026年5月2日時点）

> ※ 本日は Hacker News Algolia API への直接アクセスが 403 で取得できなかったため、HN 系のソースは GitHub Trending から確認できた範囲のみ掲載しています。

---

## 注目リポジトリ / 事例

1. **mattpocock/skills** — Skills for Real Engineers（Shell, 週間 +30,945 ⭐）
   TypeScript で有名な Matt Pocock が、自分の `.claude/` に入れている Skill 群をそのまま公開したもの。1スキル＝1ディレクトリ＋`SKILL.md` というシンプルな構成で「AI に何をどこまで任せるか」を学ぶ教科書になります。
   👉 https://github.com/mattpocock/skills

2. **abhigyanpatwari/GitNexus** — クライアントサイド知識グラフ（TypeScript, 週間 +5,209 ⭐）
   ブラウザだけで GitHub リポジトリを解析し、関数・依存関係を可視化する OSS。Next.js + Web Worker + IndexedDB の参考事例として読み応えがあります。
   👉 https://github.com/abhigyanpatwari/GitNexus

3. **mksglu/context-mode** — AIエージェント向けコンテキスト最適化（TypeScript, 週間 +2,332 ⭐）
   Claude Code / Cursor などに渡すツール出力を 98% 圧縮する "サンドボックス層"。プロンプト設計の前段で効くインフラ系 OSS で、構造化ログのテクニックが学べます。
   👉 https://github.com/mksglu/context-mode

---

## 実践ポイント（さとるさんへ）

業務で LP を組むときも、個人開発で Next.js のダッシュボードを作るときも、「ユーザーに最短で目的地へ行ってもらう導線」は CVR と離脱率を直接決めます。今日はその王道 UI、**⌘K (Mac) / Ctrl+K (Win) コマンドパレット** を **外部ライブラリ無し / Tailwind だけ / TypeScript の型を効かせて** 30分で作りきりましょう。

### Step 1. 型を先に決める（TS の主役は「データの形」）

```ts
// app/components/command-palette/types.ts
// 「検索可能な1件」の形を最初に固定する
export type CommandItem = {
  id: string;            // React の key にも使う一意な ID
  label: string;         // UI に表示する文言
  hint?: string;         // サブテキスト（任意）
  href: string;          // 遷移先（同一サイト内 or 外部URL）
  keywords?: string[];   // 検索でヒットさせたい別名
};
```

> 💡 **型を先に書くメリット**: 後で `items` 配列を渡すときに「あ、`id` 抜けてるよ」と IDE が即時に教えてくれます。`?` を付けたフィールドは `undefined` を許容、付けなければ必須。これだけで TS の 7割は使いこなせます。

### Step 2. 開閉状態と検索ロジックを切り出す（カスタムフック）

```tsx
// app/components/command-palette/use-command-palette.ts
"use client";
import { useEffect, useMemo, useState } from "react";
import type { CommandItem } from "./types";

export function useCommandPalette(items: CommandItem[]) {
  const [open, setOpen] = useState(false);
  const [query, setQuery] = useState("");

  // ⌘K / Ctrl+K でトグル、Esc で閉じる
  useEffect(() => {
    const onKey = (e: KeyboardEvent) => {
      if ((e.metaKey || e.ctrlKey) && e.key.toLowerCase() === "k") {
        e.preventDefault();
        setOpen((v) => !v);
      }
      if (e.key === "Escape") setOpen(false);
    };
    window.addEventListener("keydown", onKey);
    return () => window.removeEventListener("keydown", onKey);
  }, []);

  // クエリでフィルタ。useMemo でキー入力ごとの再計算を最小化
  const filtered = useMemo(() => {
    const q = query.trim().toLowerCase();
    if (!q) return items;
    return items.filter((it) => {
      const hay = [it.label, it.hint ?? "", ...(it.keywords ?? [])]
        .join(" ")
        .toLowerCase();
      return hay.includes(q);
    });
  }, [items, query]);

  return { open, setOpen, query, setQuery, filtered };
}
```

> 💡 **`useMemo` の意味**: `items` か `query` が変わったときだけ計算し直す、というキャッシュです。100件・1,000件と増えてもタイピングがカクつきません。逆に依存配列に何も入れないと「永遠に最初の結果」が返るので注意。

### Step 3. UI を Tailwind で組む（dialog 要素＋backdrop-blur）

```tsx
// app/components/command-palette/CommandPalette.tsx
"use client";
import Link from "next/link";
import type { CommandItem } from "./types";
import { useCommandPalette } from "./use-command-palette";

export function CommandPalette({ items }: { items: CommandItem[] }) {
  const { open, setOpen, query, setQuery, filtered } = useCommandPalette(items);
  if (!open) return null;

  return (
    <div
      role="dialog"
      aria-modal="true"
      className="fixed inset-0 z-50 flex items-start justify-center bg-black/40 backdrop-blur-sm pt-24"
      onClick={() => setOpen(false)}
    >
      <div
        className="w-full max-w-xl rounded-2xl bg-white shadow-2xl ring-1 ring-black/5 overflow-hidden"
        onClick={(e) => e.stopPropagation()}
      >
        <input
          autoFocus
          value={query}
          onChange={(e) => setQuery(e.target.value)}
          placeholder="検索…（Esc で閉じる）"
          className="w-full px-5 py-4 text-base outline-none border-b border-slate-200"
        />
        <ul className="max-h-80 overflow-y-auto py-2">
          {filtered.length === 0 && (
            <li className="px-5 py-3 text-sm text-slate-500">
              該当なし。キーワードを変えてみてください。
            </li>
          )}
          {filtered.map((it) => (
            <li key={it.id}>
              <Link
                href={it.href}
                onClick={() => setOpen(false)}
                className="flex items-center justify-between px-5 py-3 hover:bg-slate-100"
              >
                <span className="font-medium text-slate-900">{it.label}</span>
                {it.hint && (
                  <span className="text-xs text-slate-500">{it.hint}</span>
                )}
              </Link>
            </li>
          ))}
        </ul>
      </div>
    </div>
  );
}
```

### Step 4. 呼び出す（最小利用例）

```tsx
// app/layout.tsx などお好みの場所
import { CommandPalette } from "@/app/components/command-palette/CommandPalette";

const items = [
  { id: "home",    label: "トップへ",       href: "/",         keywords: ["home", "top"] },
  { id: "blog",    label: "ブログ一覧",     href: "/blog",     hint: "/blog" },
  { id: "contact", label: "お問い合わせ",   href: "/contact",  keywords: ["mail", "form"] },
];

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="ja">
      <body>
        {children}
        <CommandPalette items={items} />
      </body>
    </html>
  );
}
```

これで **⌘K / Ctrl+K** が押されたらモーダルが開き、入力に応じてフィルタ、Enter or クリックで遷移、Esc or 背景クリックで閉じる、という挙動が揃います。

### つまずきポイント先回り

- `"use client"` を忘れると `useState` / `useEffect` が動かない → ファイル冒頭に必ず書く
- `Link` を使わず `<a>` にすると Next.js のルーター遷移にならず遅い → 内部リンクは `next/link` 一択
- アクセシビリティを上げるなら次のステップで `↑↓ Enter` でのキーボード選択を実装してみる

---

## 今日の思考トリガー

**「自分が普段触っている LP やダッシュボードに ⌘K を載せるとしたら、最初の3項目には何を登録する？　その3つは『一番アクセスされている導線』と一致しているだろうか？」**

導線設計はデザインではなく **データの問題** です。直近の Google Analytics や Search Console を見て、上位3クエリ／上位3ページを `CommandItem[]` の最初に置く── その瞬間、UI は "勘" から "事実" に進化します。

_ソース: https://github.com/trending?since=weekly , https://github.com/trending/typescript?since=weekly , https://github.com/mattpocock/skills , https://github.com/abhigyanpatwari/GitNexus , https://github.com/mksglu/context-mode (2026年5月2日時点)_
