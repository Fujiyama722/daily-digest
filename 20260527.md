# 📊 プロダクトマネジメント事例解説: 2026年5月27日号

おはようございます！シニア・プロダクトマネージャーの「学習コーチ」です。

今日は、いま GitHub で大きな支持を集めている **オープンソースCRM「Twenty」** を題材に、「実際のWebサービスはどう設計されているのか」をPM視点で分解します。要件定義 → DB設計（Supabase前提）→ RLS（行レベルセキュリティ）→ 収益化まで、さとるさんが個人開発でMVPを作るときにそのまま流用できる思考の型をお渡しします。

---

## 本日のハイライト

### 1. 注目プロダクト: `twentyhq/twenty` —「AI時代のSalesforce代替」

今 GitHub Trending（本日・全言語）に入っている **Twenty** は、自らを *"The open alternative to Salesforce, designed for AI."*（Salesforceのオープンな代替、AI向けに設計）と説明するオープンソースのCRMです。TypeScript製で、★46,783 / Fork 6,645 という規模に育っています（数値はリポジトリページ掲載の実データ）。

- ソース: [github.com/twentyhq/twenty](https://github.com/twentyhq/twenty)（2026年5月27日時点）

PM視点で面白いのは、Twentyが「巨大で高価な既存SaaS（Salesforce）の代替」という**明確なポジショニング**を取っている点です。新規プロダクトは「ゼロから新しい市場を作る」より「すでに需要が証明された市場で、不満（高い・重い・閉じている）を解消する」ほうが成功確率が上がります。これは個人開発でも同じです。

### 2. もう1つの「代替プロダクト」事例: `Open-Dev-Society/OpenStock`

同じく本日トレンド入りしている **OpenStock**（TypeScript・★12,020 / Fork 1,625）は、*"an open-source alternative to expensive market platforms"*（高価な株式情報プラットフォームの代替）を掲げ、「リアルタイム価格・アラート通知・企業インサイト」を無料で提供します。

- ソース: [github.com/Open-Dev-Society/OpenStock](https://github.com/Open-Dev-Society/OpenStock)（2026年5月27日時点）

ここでも勝ちパターンは同じ：**「高い既存サービス」を「開いて・無料で」置き換える**。Twentyの「CRM」、OpenStockの「株式情報」と、ドメインは違っても**プロダクト戦略の骨格が共通**しているのが学びどころです。

### 3. 「自分でホストする」プロダクトの定番: `jellyfin/jellyfin`

長期トレンドの常連 **Jellyfin**（C#・★52,346 / Fork 4,870）は *"The Free Software Media System"*。自宅サーバーにメディアを置いて配信する、いわゆる「セルフホスト型」プロダクトです。

- ソース: [github.com/jellyfin/jellyfin](https://github.com/jellyfin/jellyfin)（2026年5月27日時点）

> ※ 本日のテーマ指定ソースである **Product Hunt（producthunt.com）** と **Hacker News（news.ycombinator.com）** は、実行環境のネットワーク制限（許可リスト外）により取得できませんでした。そのため、事例は取得可能だった GitHub の実データを用いています。Product Hunt発の新着プロダクトは次回以降に持ち越します。

---

## 注目リポジトリ / 事例

本日のPM事例として、「実プロダクトの設計図」を読める以下3つを挙げます（すべて GitHub 掲載の実データ）。

1. **`twentyhq/twenty`**（★46,783 / Fork 6,645）
   オープンソースCRM。後述するDB設計・マルチテナント・権限管理を「本物のプロダクト」で学べる、本日の主役。
   - URL: https://github.com/twentyhq/twenty

2. **`Open-Dev-Society/OpenStock`**（★12,020 / Fork 1,625）
   株式情報の代替プロダクト。リアルタイムデータ・アラート通知という「個人開発でも作れるMVP」の好例。
   - URL: https://github.com/Open-Dev-Society/OpenStock

3. **`jellyfin/jellyfin`**（★52,346 / Fork 4,870）
   セルフホスト型メディアサーバー。「課金SaaSにしない」プロダクト形態の代表例として対比に。
   - URL: https://github.com/jellyfin/jellyfin

_いずれも無料（OSS）です。READMEとデモ画面を眺めるだけでも「このプロダクトは誰の何を解決しているか」を読み解く練習になります。_

---

## 実践ポイント（さとるさんへ）

主役の **Twenty（CRM）** を題材に、PMが新規プロダクトを立ち上げるときの思考プロセスを、さとるさんのスタック（Next.js + Supabase）に落とし込みます。「ミニCRMを個人開発で作る」前提で読んでください。

### Step 1: 要件定義 —「誰の、どの課題を、どう解くか」を1文で

設計の前に、まず日本語1文で固めます。これがブレると後工程が全部ブレます。

> 「**小規模チーム**が、**顧客と商談の状況をExcelでバラバラに管理してしまう課題**を、**1画面で見える共有CRM**で解決する」

ここから「最低限必要な機能（MVP）」を逆算します。CRMなら大体こうなります：

- 顧客（会社）を登録できる
- 担当者（人）を会社に紐づけて登録できる
- 商談（案件）を作り、ステータス（見込み→提案→受注/失注）で管理できる
- チームのメンバー全員が同じデータを見られる（＝マルチテナント）

「あれもこれも」を足す前に、**この4つだけ**でMVPと割り切るのがPMの仕事です。

### Step 2: DB設計（Supabase / PostgreSQL）

上の要件を、そのままテーブルに翻訳します。Supabaseのテーブル定義（SQL）はこうなります。

```sql
-- チーム（テナント）= 「会社ごとの箱」。マルチテナントの要。
create table workspaces (
  id uuid primary key default gen_random_uuid(),
  name text not null,
  created_at timestamptz not null default now()
);

-- どのユーザーがどのworkspaceに所属するか（中間テーブル）
create table workspace_members (
  workspace_id uuid not null references workspaces(id) on delete cascade,
  user_id uuid not null references auth.users(id) on delete cascade,
  role text not null default 'member', -- 'admin' / 'member'
  primary key (workspace_id, user_id)
);

-- 顧客（会社）
create table companies (
  id uuid primary key default gen_random_uuid(),
  workspace_id uuid not null references workspaces(id) on delete cascade,
  name text not null,
  created_at timestamptz not null default now()
);

-- 商談（案件）
create table opportunities (
  id uuid primary key default gen_random_uuid(),
  workspace_id uuid not null references workspaces(id) on delete cascade,
  company_id uuid references companies(id) on delete set null,
  title text not null,
  stage text not null default 'lead',     -- lead / proposal / won / lost
  amount numeric(12,0) default 0,          -- 金額（円）
  created_at timestamptz not null default now()
);
```

**設計のポイント（初心者向け解説）:**
- `workspace_id` を**ほぼ全テーブルに持たせる**のがマルチテナント設計の肝。「このデータはどのチームのものか」を1列で表せます。
- `references ... on delete cascade` … 親（workspace）が消えたら子（company等）も自動で消える、という**お掃除ルール**。データの孤児（みなしご）を防ぎます。
- `uuid` を主キーに使うのは、連番（1,2,3…）と違って**他人のデータIDを推測されにくい**ため。SaaSのセキュリティ定石です。

### Step 3: RLS（行レベルセキュリティ）—「他チームのデータが絶対に見えない」を保証する

ここが**Supabaseで個人開発する最大の勘所**です。マルチテナントSaaSで一番怖いのは「A社のデータがB社に見えてしまう」事故。SupabaseではRLSで**DBレベルで遮断**できます。

```sql
-- まずRLSを有効化（これを忘れると全員が全データを読めてしまう！）
alter table companies enable row level security;

-- 「自分が所属しているworkspaceのデータだけ」操作できるポリシー
create policy "members can access their workspace companies"
on companies
for all                       -- select/insert/update/delete すべてに適用
using (
  workspace_id in (
    select workspace_id
    from workspace_members
    where user_id = auth.uid()  -- ログイン中ユーザーのID
  )
);
```

**ここが超重要:**
- `auth.uid()` … Supabase Authでログイン中のユーザーIDを返す関数。「**今アクセスしているのは誰か**」をDB自身が知っている、という発想です。
- `enable row level security` を**書き忘れると、ポリシーがあっても無効**（＝誰でも全部見える）。Supabaseの初心者が最もやらかす事故なので、テーブルを作ったら必ずセットで有効化する癖を。
- アプリ側（Next.js）のコードでうっかり絞り込みを忘れても、**DBが最後の砦**として他チームのデータを返しません。「アプリで守る」だけでなく「DBでも守る」の二重化が、収益化を狙うプロダクトの信頼性を支えます。

### Step 4: 収益化（マネタイズ）の型を決める

Twentyのようなプロダクトは「**オープンコア**」戦略を取ります：本体は無料OSS、**マネージド・クラウド版（自分でサーバー管理しなくていい）に課金**する形です。個人開発のMVPなら、もっとシンプルに次の3択から選びます。

- **フリーミアム**: 基本無料、商談数や保存件数の上限を超えたら月額課金（例：商談50件まで無料）
- **シート課金**: チームの人数 × 月額（CRMはこれが王道）
- **従量課金**: API呼び出し・通知数など使った分だけ（OpenStockのアラート通知などはこれと相性◎）

PM視点では「**機能で課金境界を引く**」のがコツ。Step1のMVP（顧客・商談管理）は無料で配り、「**チームで共有**」「**データ書き出し（CSV）**」など"仕事で本気で使う人だけが欲しい機能"を有料にする、といった線引きを最初に決めておきます。

---

## 今日の思考トリガー

**「あなたが今"Excelやメモアプリでなんとか管理しているもの"は何ですか？ それを Step1 の型（『誰が・どの課題を・どう解く』1文）で言語化し、Step2 のように3〜4テーブルのミニDBに分解してみてください。そのうち、どの機能なら『お金を払ってでも使いたい』と思われるでしょうか？」**

_ソース: [github.com/twentyhq/twenty](https://github.com/twentyhq/twenty) / [github.com/Open-Dev-Society/OpenStock](https://github.com/Open-Dev-Society/OpenStock) / [github.com/jellyfin/jellyfin](https://github.com/jellyfin/jellyfin) / [GitHub Trending（本日）](https://github.com/trending)（2026年5月27日時点。Product Hunt・Hacker News は環境のネットワーク制限により取得できず）_
