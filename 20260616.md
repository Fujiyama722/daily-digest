# 🎨 トレンドUI実装チュートリアル: 2026年06月16日号

おはようございます！シニアUI/UXデザイナー兼フロントエンドエンジニアの「学習コーチ」です。

火曜日は週の中でいちばん「手を動かす」日。今朝は2026年のSaaS／AI製品LPで定番化した **Bento Grid（ベントグリッド）+ カーソル追従グロウ** を、React・Next.js・TypeScript・TailwindCSSで一緒に組んでいきましょう。さとるさんがLP案件で武器になる「ちょっとリッチな1セクション」が完成します。

---

## 本日のハイライト

- **GitHub週間トレンド（JavaScript）で animate.css がじわり再浮上**: +42スター/週、累計 **82,610スター**。素のCSSアニメーション資産がAI製品LPの増加とともに見直されています。`animate-fade-in-up` 一発で済む手軽さは、Reactのトランジションを学ぶ前段としても優秀。
  - ソース: https://github.com/trending/css?since=weekly
- **prettier/prettier が安定の伸び**: +91スター/週、累計 **52,032スター**。チームでLP納品する時に「設定ファイル1枚で改行・引用符・末尾カンマ」を統一できる、地味だけど効くツール。
  - ソース: https://github.com/trending/javascript?since=weekly
- **zen-browser/desktop が +264/週**: 累計 **42,865スター**。Firefoxベースの「集中できるブラウザ」。LPコーダー視点では、独自タブUI/サイドバーUXがそのまま「次のトレンドUI」のお手本になっています。
  - ソース: https://github.com/zen-browser/desktop
- ※Hacker News (hn.algolia.com / news.ycombinator.com) は本日アクセスできなかったため、HN系の話題は割愛しています。

---

## 注目リポジトリ / 事例

1. **animate-css/animate.css** — 82,610スター。HTMLにクラス名を1つ付けるだけで使えるCSSアニメーション集。Tailwindユーザーでも `@layer utilities` に取り込んで併用可能。
   ソース: https://github.com/animate-css/animate.css
2. **prettier/prettier** — 52,032スター。LPコーディング案件のチーム作業で「フォーマット戦争」を終わらせる定番。VSCode/Antigravityでも保存時整形が標準化できる。
   ソース: https://github.com/prettier/prettier
3. **zen-browser/desktop** — 42,865スター。製品としての話題性に加え、ランディング https://zen-browser.app/ の「コンパクトサイドバー」「タブを浮かせる」表現が現行トレンドのリファレンスになる。
   ソース: https://github.com/zen-browser/desktop

---

## 今日のトレンドUI: Bento Grid + カーソル追従グロウ

### なぜ今これが「効く」のか（UX/CVR視点）

- **視線誘導が一目でできる**: 大小不揃いのカードを敷き詰めるBento Gridは、AppleのAI製品LP（Apple Intelligence）、Linear、Vercelなど"勝っているLP"がこぞって採用。情報量と余白を両立できるので、機能訴求LPのCVRを底上げしやすい。
- **「ホバーで光が追ってくる」演出**: マウス位置に応じてカード境界に光彩が走る演出は、**ユーザーが触っている感**を生み回遊時間（dwell time）を伸ばします。GA4で計測する `engagement_time_msec` のKPIに直結。
- **モバイルでもグリッド構造はそのまま機能**: グロウ演出は自動でオフ、グリッドは1〜2列に畳むだけ。LPの命である **「PC映え」と「スマホ可読性」** を同時に取れる構造です。

### 実装コード（Next.js 15 / React / TypeScript / TailwindCSS v4）

`components/BentoGrid.tsx` として保存して、`app/page.tsx` から `<BentoGrid />` するだけで動きます。

```tsx
"use client";

import { useRef, type MouseEvent, type ReactNode } from "react";

// ① カード1枚分のpropsを「型」で定義する。
//    ReactNodeはJSXでもstringでもOK、という「中身なんでも来い」の型。
type BentoCardProps = {
  title: string;
  description: string;
  className?: string; // グリッド上の占有サイズをTailwindで渡す
  children?: ReactNode;
};

function BentoCard({ title, description, className = "", children }: BentoCardProps) {
  // ② useRefは「DOMへの参照を保持する箱」。再レンダリングしても中身が消えない。
  //    HTMLDivElement | null で「初期はnull、あとでdivが入る」と教えてあげる。
  const cardRef = useRef<HTMLDivElement | null>(null);

  // ③ マウス座標を「カード内ローカル座標」に変換してCSS変数に書き込む。
  //    Reactのstateに入れると毎回再レンダリングが走って重いので、
  //    DOMの style に直接書く＝GPUに任せるのが今っぽい書き方。
  const handleMouseMove = (e: MouseEvent<HTMLDivElement>) => {
    const el = cardRef.current;
    if (!el) return;
    const rect = el.getBoundingClientRect();
    el.style.setProperty("--x", `${e.clientX - rect.left}px`);
    el.style.setProperty("--y", `${e.clientY - rect.top}px`);
  };

  return (
    <div
      ref={cardRef}
      onMouseMove={handleMouseMove}
      className={`group relative overflow-hidden rounded-2xl border border-white/10 bg-neutral-900/60 p-6 backdrop-blur transition-colors hover:border-white/20 ${className}`}
    >
      {/* ④ カーソル追従グロウ本体。CSS変数 --x / --y を radial-gradient の中心に使う */}
      <div
        aria-hidden
        className="pointer-events-none absolute inset-0 opacity-0 transition-opacity duration-300 group-hover:opacity-100"
        style={{
          background:
            "radial-gradient(420px circle at var(--x) var(--y), rgba(99,102,241,0.18), transparent 60%)",
        }}
      />
      <div className="relative z-10 flex h-full flex-col gap-3">
        <h3 className="text-lg font-semibold text-white">{title}</h3>
        <p className="text-sm leading-relaxed text-neutral-300">{description}</p>
        {children}
      </div>
    </div>
  );
}

export default function BentoGrid() {
  return (
    <section className="mx-auto max-w-6xl px-6 py-20">
      <h2 className="mb-10 text-3xl font-bold text-white sm:text-4xl">
        プロダクトの核となる3つの体験
      </h2>

      {/* ⑤ 12カラムのグリッドを土台に、各カードで col-span / row-span を変える。
             これだけで「Bento」になります。 */}
      <div className="grid grid-cols-1 gap-4 sm:grid-cols-6 sm:auto-rows-[180px]">
        <BentoCard
          title="瞬時に書ける"
          description="保存と同時に整形・型推論。手戻りゼロでLPを量産できます。"
          className="sm:col-span-4 sm:row-span-2"
        />
        <BentoCard
          title="型で守られる"
          description="TypeScriptが落とし穴を先回りで指摘。"
          className="sm:col-span-2"
        />
        <BentoCard
          title="どこでも動く"
          description="Vercelに push するだけで世界配信。"
          className="sm:col-span-2"
        />
        <BentoCard
          title="計測できる"
          description="GA4とSupabaseで意思決定を加速。"
          className="sm:col-span-3"
        />
        <BentoCard
          title="磨き続けられる"
          description="小さく試して、勝ったものだけ残す。"
          className="sm:col-span-3"
        />
      </div>
    </section>
  );
}
```

### 行間で何が起きているか（さとるさん向け解説）

- **`type BentoCardProps = {...}`**: TypeScriptの「型エイリアス」。コンポーネントに渡すpropsの形を先に決めておくと、書き手は補完が効き、読み手は仕様を一発で把握できる。FLOCSSでクラス命名を統一するのと同じ「設計」の発想です。
- **`useRef<HTMLDivElement | null>(null)`**: `useState` との違いは「中身が変わってもReactが気付かない＝再レンダリングを起こさない」こと。マウス座標みたいに高頻度更新されるものに最適です。
- **`el.style.setProperty("--x", ...)`**: ReactのstateではなくCSSカスタムプロパティに書き込むのがミソ。Tailwindの `radial-gradient(... at var(--x) var(--y))` と組ませることで、JSは「数値を渡すだけ」、見た目はCSSが担当、という綺麗な分業になります。
- **`group` と `group-hover:opacity-100`**: Tailwindのおなじみの仕掛け。親に `group` を付けると、子で `group-hover:` が使える。BEM/FLOCSS的に言えば「親要素の状態でModifierを切り替える」のをユーティリティ1つで書けるイメージ。

---

## 実践ポイント（さとるさんへ）

1. **まず1ファイルだけコピペして動かす**: Antigravity（IDE）で新規Next.jsプロジェクトを作り、上の `BentoGrid.tsx` を貼って `app/page.tsx` から呼ぶ。動いたら勝ち。動かなかったら **エラーメッセージをそのままClaude Codeに渡す** のが最速。
2. **カードの中身を「自分の案件LP」に差し替えてみる**: 今かかえているLP案件の訴求3〜5個を、`title` / `description` に流し込むだけで「リッチな機能訴求セクション」が1本完成します。**1案件で1個、自分のスニペット資産に育てていく** のがシニア化への近道。
3. **animate.css は"つなぎ"として併用OK**: スクロールインで `animate__fadeInUp` を当てるだけでも見栄えが化けます。慣れてきたら `framer-motion` に置き換える、というステップで段階的に学ぶと挫折しません。
4. **計測まで含めて「UI設計」**: VercelのAnalyticsでこのセクションのスクロール率を測れば、「Bentoを採用した／しなかった」LPでCVRがどう変わるかを自分のポートフォリオで語れるようになります。これは非エンジニア出身者がシニアと戦える数少ない武器です。

---

## 今日の思考トリガー

**「自分が今かかえているLP案件に、Bento Gridとカーソル追従グロウを入れるとしたら——どの訴求を"大きいカード"に、どれを"小さいカード"に置く？ その配置はクライアントが伝えたい順序と一致している？」**

UIトレンドは「採用するかどうか」より、**「なぜここに置くか」を言語化できるかどうか** がシニアとジュニアの分かれ目です。今日のチュートリアルを、ぜひ自分の案件で1個、本物のアウトプットに変えてみてください。

_ソース: https://github.com/trending/javascript?since=weekly , https://github.com/trending/css?since=weekly , https://github.com/animate-css/animate.css , https://github.com/prettier/prettier , https://github.com/zen-browser/desktop (2026年06月16日時点)_
