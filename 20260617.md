# 📊 プロダクトマネジメント事例解説: 2026年06月17日号

おはようございます！シニア・プロダクトマネージャーの「学習コーチ」です。

今日はProduct Huntで注目を集めるAIエージェント型SaaSを題材に、**「会議という人間の行為そのものをプロダクト化する」** というテーマで要件定義・DB設計まで深掘りします。

---

## 本日のハイライト

### 1. Mina Meeting Assistant — “AIが会議中に発言・実行する”AI Teammate

会議に参加し、リアルタイムで応答・タスク実行までこなすAIエージェント。Zoom/Google Meet/Microsoft Teamsに対応し、議事録・アクションアイテム・CRM更新・チケット起票・フォローアップメール送信などを **通話中に** 完結させる。

- ソース: <https://www.producthunt.com/products/mina-meeting-assistant>
- 公式: <https://getmina.ai/>
- カテゴリ: AI Chief of Staff / AI Meeting Notetakers
- 料金: Product Hunt経由で無料クレジット付与（詳細はサイト確認）。Reactive（ウェイクワード起動）/Proactive（常時介入）の2モード。Proactiveの方が高価。

> PMコメント: AI Notetaker市場（Otter, Fireflies, tl;dv 等）は「議事録の正確さ」が長らく主戦場だったが、Minaは **「議事録の後工程まで会議内で完了させる」** という機能上の越境を行っている。プロダクト境界の引き直しに注目。

### 2. Fundraisly — 投資家マッチングの“AI営業エージェント”

30万人超の投資家データと数百万件の投資案件を分析し、対象スタートアップに合う投資家を発見 → 自社ネットワーク経由のwarm intro経路をマッピング → 残りをcold outreachで補完。最終的に **20〜40件の質の高い投資家ミーティング** を獲得することを成果指標としている。

- ソース: <https://www.producthunt.com/products/fundraisly>
- カテゴリ: AI Agents / Sales / VC Tools
- 評価: 5.0 / 5.0（PH上、3レビュー時点）

> PMコメント: 成果指標を「リード件数」ではなく **「meeting booked」** に絞っている設計が秀逸。SaaS指標設計の良い参考例。

### 3. Kelviq — SaaS/AI向けMerchant of Recordプラットフォーム

決済・グローバルVAT/GST・サブスク・従量課金・ライセンスキー配布・コンプライアンスを1つに統合。Merchant of Record（MoR）モデルで **100カ国以上の税務登録を肩代わり**、135通貨対応。

- ソース: <https://www.producthunt.com/products/kelviq>
- 公式: <https://www.kelviq.com/>
- 料金: $5K取引額を超えてから **3.5% + $0.40/件**（無料枠なし）

> PMコメント: 個人開発で海外課金を始めるなら、Stripeを直接使うかMoR（Kelviq / Paddle / Lemon Squeezy）を使うかは早期に決める論点。**「自分で税務対応する時間 vs MoR手数料」** のトレードオフはMVPで必ず通る分岐点。

---

## 注目事例ディープダイブ: 「Mina Meeting Assistant」の要件定義とDB設計

さとるさんが個人開発で **「会議録のAI要約サービス（軽量版）」** を作る前提で、Mina的プロダクトを薄く切り出して設計してみます。

### 要件定義: MVPを「録音→要約→共有」に絞る

リアルタイム介入はMinaの圧倒的優位なのでMVPで張らない。**「録音アップロード → 文字起こし → AI要約 → 共有リンク発行」** の非同期フローに絞れば、個人開発でも到達可能です。

| 優先度 | 機能 | 補足 |
|---|---|---|
| Must | 音声アップロード（m4a/mp3） | Supabase Storageに保存 |
| Must | 文字起こし | Whisper API（OpenAI）など外部API |
| Must | AI要約・アクション抽出 | Claude API |
| Must | 共有リンク（閲覧専用） | RLSで`shared = true`のみ匿名閲覧可 |
| Should | 検索 | Postgres全文検索 or pgvector |
| Could | カレンダー連携 | Google Calendar API |
| Won't (MVP) | リアルタイム介入 | Mina本体の強み。捨てる勇気が大事 |

### Supabase テーブル設計（最小構成）

```sql
-- 会議メタデータ
create table meetings (
  id uuid primary key default gen_random_uuid(),
  user_id uuid references auth.users(id) not null,
  title text not null,
  audio_path text not null,             -- Storage上のパス
  duration_sec integer,
  status text not null default 'pending', -- pending/transcribing/summarized/failed
  shared boolean not null default false,
  share_token text unique,              -- 共有URL用
  created_at timestamptz default now()
);

-- 文字起こし（発話単位）
create table transcripts (
  id uuid primary key default gen_random_uuid(),
  meeting_id uuid references meetings(id) on delete cascade not null,
  speaker text,
  start_ms integer,
  end_ms integer,
  text text not null
);

-- AI要約・アクションアイテム
create table summaries (
  meeting_id uuid primary key references meetings(id) on delete cascade,
  summary_md text not null,
  action_items jsonb not null default '[]'::jsonb,
  generated_at timestamptz default now()
);
```

### RLS（Row Level Security）の考え方

**個人開発でいちばん事故るのがRLSの設計**です。「自分のデータしか触れない」を前提に、共有リンクだけ例外で開ける形にします。

```sql
alter table meetings enable row level security;
alter table transcripts enable row level security;
alter table summaries enable row level security;

-- 本人のみ読み書き可
create policy "owner can read own meetings"
on meetings for select
using (auth.uid() = user_id);

create policy "owner can insert own meetings"
on meetings for insert
with check (auth.uid() = user_id);

-- 共有リンク（share_token指定時のみ匿名読み取り可）
create policy "shared meetings readable by anyone with token"
on meetings for select
using (shared = true and share_token is not null);
```

ポイントは **「share_tokenを知っている人だけ読める」** という設計を、アプリ側ではなくDB側のRLSに寄せること。アプリ側ロジックでフィルタすると、API直叩きで漏れます。

### アーキテクチャ図（テキスト版）

```
[Next.js (Vercel)]
  ├─ ① 音声upload → Supabase Storage
  ├─ ② meetings INSERT (status=pending)
  └─ ③ /api/transcribe を非同期キック
        └─ Supabase Edge Function (Deno)
              ├─ Whisper API 呼び出し → transcripts INSERT
              ├─ Claude API で要約 → summaries INSERT
              └─ meetings.status = 'summarized'
```

非同期処理は **Vercelのサーバレス関数（10秒制限）に乗せない** のがコツ。Supabase Edge Function、もしくはVercelのBackground Functions / Inngest等のキューを噛ませます。

---

## 実践ポイント（さとるさんへ）

非エンジニア → シニアフロントエンドエンジニアへの登り方として、今日の事例から持ち帰ってほしい3点。

1. **「ペルソナ × 業務フロー」で機能を削る練習**
   Minaの機能は実は山ほどあるけど、MVPなら「録音→要約→共有」の3アクションでOK。**機能を増やすより削る判断** がPMの本質。さとるさんのLP制作受託でも「最初に何を載せないかリスト」を作ると上流の議論が速くなります。

2. **Supabase × RLSをCloud Codeで素振りしておく**
   Claude Code CLIに「上の3テーブル＋RLSポリシーをSupabaseに作るマイグレーションを書いて」と頼めば、`supabase/migrations/` 配下にSQLを生成してくれます。RLSは **「自分で書く前にAIに書かせて、自分でレビュー」** が最速。

3. **MoRをMVP時に決めておく**
   個人開発の収益化を視野に入れるなら、決済を **Stripe直 / Kelviq / Paddle / Lemon Squeezy** のどれにするかは「LP公開前に決める」テーマ。MoRは手数料が高い分、税務がゼロになる。受託で身につけた「制約条件から逆算する習慣」がそのまま使えます。

---

## 今日の思考トリガー

**「あなたが作りたいプロダクトの“MVPに含めない機能リスト”を5つ言えますか？ もし言えないなら、まだ要件定義は終わっていません。」**

PMの仕事の半分は「足し算」ではなく「引き算」です。Minaほどの機能群を見て「自分には作れない」と思うのではなく、**「同じ問題のうち、どの1スライスなら自分でも届くか」** を切り出す視点が、シニアへの第一歩。

---

_ソース:_

- _Product Hunt - Mina Meeting Assistant: <https://www.producthunt.com/products/mina-meeting-assistant>_
- _Product Hunt - Fundraisly: <https://www.producthunt.com/products/fundraisly>_
- _Product Hunt - Kelviq: <https://www.producthunt.com/products/kelviq>_
- _Hacker News フロントページ: <https://news.ycombinator.com/>_
- _Supabase RLS公式: <https://supabase.com/docs/guides/database/postgres/row-level-security>_

_(2026年06月17日時点 / JST朝のダイジェスト)_
