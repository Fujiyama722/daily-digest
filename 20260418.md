# 🛠️ 週末ハンズオン実践: 2026年04月18日号

おはようございます！フロントエンドメンターの「学習コーチ」です。

今週のGitHub Trendingは、AIエージェント関連のTypeScriptリポジトリが大きく伸びています。今日は、その中からさとるさんのスタックにぴったりな **TradingView/lightweight-charts** を使って、Next.js + TypeScript + TailwindCSSで「リアルタイム風データチャート」を構築するハンズオンをお届けします！

---

## 本日のハイライト

### 1. AIエージェント開発ツールが急上昇中

今週のGitHub Trendingでは、AIコーディングエージェント関連のリポジトリが軒並み爆発的な伸びを見せています。

- **NousResearch/hermes-agent**（+51,025⭐）: 「共に成長するAIエージェント」として話題に
  - ソース: https://github.com/trending?since=weekly
- **multica-ai/multica**（+10,588⭐）: AIコーディングエージェントをチームメイトのように管理できるオープンソースプラットフォーム
  - ソース: https://github.com/trending/typescript?since=weekly
- **coleam00/Archon**（+4,309⭐）: AIコーディングを決定的・再現可能にするハーネスビルダー
  - ソース: https://github.com/trending/typescript?since=weekly

### 2. Claude Code関連ツールも大人気

- **thedotmack/claude-mem**（+12,366⭐）: Claude Codeのセッション記録を自動圧縮し、次回セッションに関連コンテキストを注入するプラグイン。さとるさんのClaude Code活用にも直結！
  - ソース: https://github.com/trending/typescript?since=weekly
- **forrestchang/andrej-karpathy-skills**（+37,377⭐）: Andrej Karpathyの知見をもとにしたClaude Code用CLAUDE.mdファイル
  - ソース: https://github.com/trending?since=weekly

### 3. 今日のハンズオン題材: lightweight-charts

- **tradingview/lightweight-charts**（+929⭐ 今週）: HTML5 Canvasで高パフォーマンスな金融チャートを描画するライブラリ。軽量でReactとの統合も簡単
  - ソース: https://github.com/trending/typescript?since=weekly

---

## 注目リポジトリ

| リポジトリ | 概要 | 今週の⭐ |
|---|---|---|
| [multica-ai/multica](https://github.com/multica-ai/multica) | AIコーディングエージェントをタスク管理できるオープンソースプラットフォーム（TypeScript） | +10,588 |
| [tradingview/lightweight-charts](https://github.com/tradingview/lightweight-charts) | 高パフォーマンスな金融チャートライブラリ。HTML5 Canvas製で軽量（TypeScript） | +929 |
| [codeforreal1/compressO](https://github.com/codeforreal1/compressO) | 動画・画像を小さいサイズに変換。100%無料&オープンソース（TypeScript） | +546 |

---

## 🔧 ハンズオン: Next.js × lightweight-charts でインタラクティブチャートを作ろう

### なぜこのライブラリ？

Web開発の現場では「データの可視化」は頻出テーマです。ダッシュボード、アナリティクス、ポートフォリオサイト——あらゆる場面でチャートは求められます。`lightweight-charts` はTradingView社が開発した軽量チャートライブラリで、**Canvas APIベースで高パフォーマンス**、かつ**TypeScriptの型定義が充実**しているため、学習にも最適です。

### Step 1: プロジェクトのセットアップ

```bash
npx create-next-app@latest chart-demo --typescript --tailwind --app --use-npm
cd chart-demo
npm install lightweight-charts
```

### Step 2: チャートコンポーネントを作る

`src/components/PriceChart.tsx` を作成します。

```tsx
"use client";

import { useEffect, useRef } from "react";
import { createChart, type IChartApi, type ISeriesApi, LineSeries } from "lightweight-charts";

// --- 型定義の解説 ---
// IChartApi: チャート本体を操作するための型（リサイズ、削除など）
// ISeriesApi: データ系列（折れ線・ローソク足など）を操作するための型
// useRef<T>: DOMやオブジェクトへの「参照」を保持するReactフック
//   → 再レンダリングしても値が保持される（useStateと違い再描画を引き起こさない）

type PriceChartProps = {
  title?: string; // ?をつけると「省略可能なプロパティ」になる
};

export default function PriceChart({ title = "価格チャート" }: PriceChartProps) {
  // useRef<HTMLDivElement | null>(null) の意味:
  // 「HTMLDivElement型 または null型」の参照を、初期値nullで作成
  // → チャートを描画するDOM要素を参照するために使う
  const chartContainerRef = useRef<HTMLDivElement | null>(null);
  const chartRef = useRef<IChartApi | null>(null);

  useEffect(() => {
    // chartContainerRef.current が null の場合は何もしない（ガード節）
    if (!chartContainerRef.current) return;

    // チャートを生成し、DOM要素にマウントする
    const chart = createChart(chartContainerRef.current, {
      width: chartContainerRef.current.clientWidth,
      height: 400,
      layout: {
        background: { color: "#1a1a2e" },
        textColor: "#e0e0e0",
      },
      grid: {
        vertLines: { color: "#2a2a4a" },
        horzLines: { color: "#2a2a4a" },
      },
    });

    chartRef.current = chart;

    // 折れ線チャートのデータ系列を追加
    const lineSeries = chart.addSeries(LineSeries, {
      color: "#6366f1", // Tailwindの indigo-500 に相当
      lineWidth: 2,
    });

    // サンプルデータ（実際のアプリではAPIから取得する）
    // { time: "YYYY-MM-DD", value: number } の配列
    const sampleData = generateSampleData();
    lineSeries.setData(sampleData);

    // チャートを描画領域にフィットさせる
    chart.timeScale().fitContent();

    // ウィンドウリサイズ時にチャートサイズを追従させる
    const handleResize = () => {
      if (chartContainerRef.current) {
        chart.applyOptions({
          width: chartContainerRef.current.clientWidth,
        });
      }
    };
    window.addEventListener("resize", handleResize);

    // --- クリーンアップ関数 ---
    // useEffectの返り値として関数を返すと、コンポーネントのアンマウント時に実行される
    // メモリリークを防ぐために、イベントリスナーの解除とチャートの破棄を行う
    return () => {
      window.removeEventListener("resize", handleResize);
      chart.remove();
    };
  }, []); // 空の依存配列 → マウント時に1回だけ実行

  return (
    <div className="w-full max-w-4xl mx-auto p-4">
      <h2 className="text-xl font-bold text-white mb-4">{title}</h2>
      {/* ref属性にuseRefの値を渡すと、この<div>のDOM要素がcurrentに格納される */}
      <div
        ref={chartContainerRef}
        className="w-full rounded-lg overflow-hidden border border-gray-700"
      />
    </div>
  );
}

// サンプルデータ生成関数
function generateSampleData() {
  const data = [];
  const baseDate = new Date("2026-01-01");
  let value = 100;

  for (let i = 0; i < 90; i++) {
    const date = new Date(baseDate);
    date.setDate(date.getDate() + i);

    // ランダムウォーク: 前日の値に -2〜+2 のランダムな変動を加える
    value += (Math.random() - 0.48) * 4;

    data.push({
      time: date.toISOString().split("T")[0], // "YYYY-MM-DD" 形式に変換
      value: Math.round(value * 100) / 100,   // 小数点2桁に丸める
    });
  }

  return data;
}
```

### Step 3: ページに組み込む

`src/app/page.tsx` を編集します。

```tsx
import PriceChart from "@/components/PriceChart";

export default function Home() {
  return (
    <main className="min-h-screen bg-gray-950 flex flex-col items-center justify-center gap-8 p-8">
      <h1 className="text-3xl font-bold text-white">
        📊 Chart Demo
      </h1>
      <PriceChart title="サンプル価格推移（90日間）" />
      <p className="text-gray-400 text-sm">
        lightweight-charts × Next.js × TailwindCSS で構築
      </p>
    </main>
  );
}
```

### Step 4: 開発サーバーで確認

```bash
npm run dev
# http://localhost:3000 にアクセス
```

### 💡 TypeScript / React のポイント解説

| 概念 | 今回のコードでの使い方 | 一言解説 |
|---|---|---|
| `useRef` | DOM要素やチャートインスタンスの参照保持 | 再レンダリングしても値が消えない「箱」。`useState` と違い、値を変えても再描画が起きない |
| `useEffect` + クリーンアップ | チャート生成 → アンマウント時に破棄 | 外部ライブラリやDOMを直接操作する「副作用」を管理する仕組み |
| ジェネリクス `useRef<T>` | `useRef<HTMLDivElement \| null>` | `T` に型を渡して「この箱には何が入るか」を宣言。TypeScriptが型チェックしてくれる |
| ユニオン型 `A \| B` | `HTMLDivElement \| null` | 「AまたはB」の型。初期値がnullの場合によく使うパターン |
| オプショナルプロパティ `?` | `title?: string` | `?` をつけると「渡さなくてもOK」なプロパティになる |
| デフォルト引数 | `{ title = "価格チャート" }` | 値が渡されなかった場合のデフォルト値を設定 |

### 🚀 発展チャレンジ（余力があれば）

1. **ローソク足チャートに変更**: `LineSeries` → `CandlestickSeries` に変えて、`open/high/low/close` データを渡す
2. **Supabaseと連携**: Supabaseテーブルにデータを保存し、リアルタイムサブスクリプションでチャートを自動更新
3. **ダークモード切替**: TailwindCSSの `dark:` プレフィックスとチャートの `layout` オプションを連動させる

---

## 実践ポイント（さとるさんへ）

1. **`useRef` と `useEffect` は外部ライブラリ統合の基本パターン**です。今回のチャートライブラリに限らず、地図（Mapbox）、エディタ（Monaco）、アニメーション（GSAP）など、React外のライブラリを使う時はほぼ同じパターンになります。この「型」を体に染み込ませましょう。

2. **今週トレンドの `claude-mem` をClaude Codeに導入してみましょう**。セッション間でコンテキストが保持されるので、個人開発の継続性が格段に上がります。GitHubで使い方を確認してみてください。

3. **`compressO` のコードを読んでみましょう**。TypeScriptで書かれた画像圧縮ツールで、Next.jsアプリに画像最適化機能を組み込む参考になります。LPコーディングの業務でも画像最適化は重要ですよね。

---

## 今日の思考トリガー

**「今日のチャートコンポーネントを、自分の個人開発プロダクトに組み込むとしたら、どんなデータを可視化しますか？ ユーザーの行動データ？売上推移？それとも全く別のもの？ "データを見せる" ことでユーザーにどんな価値を提供できるか、考えてみてください。」**

---

_ソース:_
- _https://github.com/trending?since=weekly (2026年04月18日時点)_
- _https://github.com/trending/typescript?since=weekly (2026年04月18日時点)_
- _https://github.com/trending/javascript?since=weekly (2026年04月18日時点)_
