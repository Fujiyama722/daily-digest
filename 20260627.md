# 🛠️ 週末ハンズオン実践: 2026年6月27日号

おはようございます！フロントエンドメンターの「学習コーチ」です。

土曜日のテーマは「手を動かして学ぶ」。今日は GitHub Trending を眺めて、さとるさんが午前中だけで完成させられる週末プロジェクトを1本ご用意しました。題して **「Mini APIテスター」を Next.js + TypeScript + Tailwind で作る** ハンズオンです。

---

## 本日のハイライト

今週の GitHub Trending を眺めると、相変わらず「AIエージェント」関連が上位を占めていました。一方で、フロントエンド開発者にとって毎日使うであろう **API クライアント `Kong/insomnia`** が週間トレンドに復帰しているのが目を引きます。

- **`Kong/insomnia`**（TypeScript / Total ★39,786）— GraphQL/REST/gRPC/WebSocketに対応するOSSのAPIクライアント。今週も+1,188★。
  ソース: https://github.com/Kong/insomnia
- **`mattpocock/skills`**（Shell / Total ★147,389）— TypeScript教育で有名な Matt Pocock 氏の「Claude Code Skills」公開リポジトリ。2026年2月公開ながら爆伸び。
  ソース: https://github.com/mattpocock/skills
- **`penpot/penpot`**（Clojure / Total ★53,936）— Figma代替のOSSデザインツール。デザイン・コードのコラボに強み。
  ソース: https://github.com/penpot/penpot

ソース: GitHub Trending (weekly) https://github.com/trending?since=weekly（2026年6月27日 JST 朝時点）

---

## 注目リポジトリ / 事例

### 1. Kong/insomnia — 「APIクライアントとは何か」を体感する教材
ローカルでAPIリクエストを送信し、レスポンスを整形表示するOSS。今日のハンズオンで作る「Mini APIテスター」のミニ版だと思ってください。完成形を見ておくと、自分が何を作っているのかイメージしやすくなります。
ソース: https://github.com/Kong/insomnia

### 2. mattpocock/skills — Claude Code との付き合い方を学ぶ
TypeScriptの第一人者が「自分の .claude ディレクトリ」を公開しているリポジトリ。Skills（再利用可能なAI指示書）のサンプルが詰まっており、Claude Code（CLI）の使いこなしを一段引き上げてくれます。
ソース: https://github.com/mattpocock/skills

### 3. penpot/penpot — 「デザイン → 実装」の橋渡しを学ぶ
個人開発で「UIをどう作るか」で詰まったとき、Figmaの代替として無料で使えます。FigmaがあるならFigmaでOKですが、商用利用条件などを気にせず使えるOSSという選択肢を知っておくと安心です。
ソース: https://github.com/penpot/penpot

---

## 🧑‍🏫 週末ハンズオン: Mini APIテスターを作る

**所要時間の目安: 60〜90分**
**ゴール: URLを入力したら GET リクエストを送り、レスポンスのJSONを整形表示するWebアプリ**

### なぜこれを作るのか
- 個人開発で**避けて通れない「fetchで外部APIを叩いて画面に出す」処理**を一気通貫で書ける
- TypeScriptの**「未知のデータに型を付ける」**という最重要スキルを練習できる
- TailwindCSSで**「状態（読み込み中・エラー・成功）でUIを切り替える」**パターンを学べる

### セットアップ（5分）
```bash
npx create-next-app@latest mini-api-tester \
  --typescript --tailwind --app --eslint --src-dir --no-import-alias
cd mini-api-tester
npm run dev
```

### 完成コード `src/app/page.tsx`
```tsx
"use client";

import { useState } from "react";

// 🟦 ① レスポンスの「状態」を型で表現する（Discriminated Union）
type FetchState =
  | { status: "idle" }
  | { status: "loading" }
  | { status: "success"; data: unknown; statusCode: number }
  | { status: "error"; message: string };

export default function Home() {
  // 🟦 ② useState の型引数で「URLは常に文字列」だと明示する
  const [url, setUrl] = useState<string>("https://api.github.com/repos/vercel/next.js");
  const [state, setState] = useState<FetchState>({ status: "idle" });

  // 🟦 ③ async関数は Promise<void> を返す。例外は try/catch で握る
  const handleSend = async (): Promise<void> => {
    setState({ status: "loading" });
    try {
      const res = await fetch(url);
      // 🟦 ④ レスポンスの中身は「分からない」ので unknown 型で受ける
      const data: unknown = await res.json();
      setState({ status: "success", data, statusCode: res.status });
    } catch (e) {
      // 🟦 ⑤ catch の引数も unknown。instanceof で絞り込む
      const message = e instanceof Error ? e.message : "不明なエラー";
      setState({ status: "error", message });
    }
  };

  return (
    <main className="mx-auto max-w-3xl p-6 space-y-4">
      <h1 className="text-2xl font-bold">Mini APIテスター</h1>

      <div className="flex gap-2">
        <input
          type="url"
          value={url}
          onChange={(e) => setUrl(e.target.value)}
          className="flex-1 rounded border border-slate-300 px-3 py-2"
          placeholder="https://..."
        />
        <button
          onClick={handleSend}
          disabled={state.status === "loading"}
          className="rounded bg-slate-900 px-4 py-2 text-white disabled:opacity-50"
        >
          {state.status === "loading" ? "送信中..." : "送信"}
        </button>
      </div>

      {/* 🟦 ⑥ 状態ごとに描画を切り替える */}
      {state.status === "error" && (
        <p className="rounded bg-red-50 p-3 text-red-700">⚠️ {state.message}</p>
      )}
      {state.status === "success" && (
        <section className="space-y-2">
          <p className="text-sm text-slate-600">HTTP {state.statusCode}</p>
          <pre className="overflow-auto rounded bg-slate-900 p-4 text-xs text-slate-100">
            {JSON.stringify(state.data, null, 2)}
          </pre>
        </section>
      )}
    </main>
  );
}
```

### 解説 — ここが今日の学びの核です

**① Discriminated Union（判別可能なユニオン型）**
`status` という共通のフィールドを目印に、状態ごとに「持っているデータが違う」ことを型で表しています。`loading` のときに `data` を参照しようとすると TypeScript がコンパイル時にエラーを出してくれます。**「ありえない状態をそもそも作らせない」のがTSの真骨頂**です。

**② useStateの型引数**
`useState<string>("...")` のように `<>` で型を明示しておくと、後から `setUrl(123)` のような誤代入をブロックできます。初期値から推論される場合は省略可ですが、空配列や `null` を初期値にするときは必須です。

**④ `unknown` という型**
外部APIのレスポンスは「何が返ってくるか分からない」ので `any` ではなく `unknown` で受けるのが定石です。`any` は黒魔術、`unknown` は安全な「未知」。使う前に必ず型ガード（`typeof` / `instanceof` / Zodなどのスキーマ検証）でチェックする習慣を付けましょう。

**⑤ catch の `unknown`**
`try/catch` の `e` も TypeScript 4.4 以降は `unknown` がデフォルト。`e instanceof Error` で絞り込んでから `.message` を読みます。これは現場のレビューで指摘されがちなポイントです。

### 発展課題（余力があれば）
1. **HTTPメソッドの切り替え** — `GET` / `POST` を選べるセレクトボックスを追加。`POST` のときはリクエストボディの textarea を表示する。
2. **ヘッダー入力** — `Authorization: Bearer ...` を渡せるようにする。
3. **履歴機能** — 送信したURLを `localStorage` に保存して再利用できるようにする。

ソース: Next.js Docs (App Router) https://nextjs.org/docs/app（2026年6月27日時点）

---

## 実践ポイント（さとるさんへ）

- **「動くものを最後まで作る」を最優先に** — 上のコードはコピペで動きます。まず動かして、UIに自分の好きな色を足してみる。**修正→反映の往復回数が、フロントエンドの上達速度そのもの**です。
- **Claude Code への質問テンプレート** — 詰まったらこう聞いてみてください: 「`FetchState` の Discriminated Union が、なぜ if 文の中で `state.data` を許可してくれるのか、TypeScript の Narrowing の仕組みを初心者向けに図解してください」
- **Insomnia や Postman を触っておく** — 自作した「Mini APIテスター」と本物の Insomnia を見比べると、**プロのツールがなぜそのUIになっているかが立体的に理解できます**。これは個人開発の「UX眼」を鍛える最高のトレーニングです。

---

## 今日の思考トリガー

**「今日作った『Mini APIテスター』を、自分の収益化アイデアに組み込むとしたら、どんな『誰の・どの作業を10分短縮するツール』に化けさせますか？ そのターゲットユーザーの肩書きまで具体的に書き出してみてください。」**

良い週末を！ 🌱

_ソース: https://github.com/trending?since=weekly , https://github.com/Kong/insomnia , https://github.com/mattpocock/skills , https://github.com/penpot/penpot , https://nextjs.org/docs/app （いずれも2026年6月27日 JST 朝時点）_
