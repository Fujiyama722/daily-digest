# 🛠️ 週末ハンズオン実践: 2026年06月20日号

おはようございます！フロントエンドメンターの「学習コーチ」です。

土曜の朝はコーヒー片手に、平日に積み残した「ちょっと触ってみたかった技術」を1つだけ動かしてみる時間です。今週は GitHub Trending の TypeScript 部門に久しぶりに **Cypress** が浮上してきました。新しいフレームワークではありませんが、さとるさんが次のステップに進むうえで「触れておくと景色が変わる」道具です。今日はこれを Next.js プロジェクトに30分で組み込むハンズオンに仕立てました。

---

## 本日のハイライト

- **GitHub Trending（TypeScript / 週間）に `cypress-io/cypress` が再ランクイン**
  E2E（end-to-end）テストの定番ツール。最新は v15.17.0（2026年6月9日リリース）で、リリース総数は396に達しており継続的にメンテされています。
  ソース: https://github.com/cypress-io/cypress

- **`puppeteer/puppeteer` も週間トレンド常連**
  Chrome / Firefox を JavaScript からヘッドレス操作するライブラリ。95.1k ★・680リリース、TypeScript 比率 93.8%。スクレイピングや自動スクショに使えます。
  ソース: https://github.com/puppeteer/puppeteer

- **`iptv-org/iptv` が週間トレンド1位級（TypeScript）**
  世界中の公開IPTVチャンネルをまとめたプレイリスト。技術的には m3u 生成スクリプトが TypeScript で書かれており、CI/CD で自動更新される仕組みは「個人プロジェクトの定期更新ワークフロー」を学ぶ題材になります。
  ソース: https://github.com/iptv-org/iptv

> Hacker News Algolia API は本日アクセスが403で取得できませんでした。今週はGitHub Trendingからの題材に絞ります。

---

## 注目リポジトリ / 事例

### 1. cypress-io/cypress（今日の主役）
- 50.4k ★ / TypeScript / MIT
- 「ブラウザで動くものなら何でも、速く・簡単に・確実にテストする」が公式キャッチコピー
- E2E / Component Test の両方をサポート。React・Next.js との相性が良い
- 直接URL: https://github.com/cypress-io/cypress

### 2. puppeteer/puppeteer
- 95.1k ★ / TypeScript
- ヘッドレスブラウザ操作。LP制作の現場で「全ページのスクショを一気に取りたい」「フォーム送信を毎日自動でやりたい」といった用途で即戦力
- 直接URL: https://github.com/puppeteer/puppeteer

### 3. iptv-org/iptv
- 126k ★ / TypeScript・JavaScript
- 興味があれば `scripts/` 配下の TypeScript を読むだけでも勉強になります（クラス設計と型の付け方の良い見本）
- 直接URL: https://github.com/iptv-org/iptv

---

## 実践ポイント（さとるさんへ）

### 今週末の30分ハンズオン: 既存 Next.js プロジェクトに Cypress を入れる

LP コーディングの業務で「リンク切れ」「フォーム送信後の遷移崩れ」を手動で確認するのは時間がかかります。Cypress を1度入れておけば、PR を出すたびに自動でクリックして回ってくれる「もう一人の自分」を雇えます。

#### ステップ1: インストール（ターミナル）

```bash
npm install -D cypress
npx cypress open
```

初回起動でブラウザGUIが立ち上がり、`cypress/` ディレクトリが自動生成されます（課金不要・ローカル完結）。

#### ステップ2: 最初のE2Eテストを書く

`cypress/e2e/home.cy.ts` を新規作成:

```ts
// describe / it は Mocha 系のテスト構文。Cypress に同梱されているので import 不要。
describe('トップページの基本動作', () => {
  // beforeEach: 各テストの「前に」走る共通処理
  beforeEach(() => {
    cy.visit('/'); // baseUrl からの相対パス
  });

  it('見出しが表示される', () => {
    // cy.get(): CSSセレクタで要素を取得する Cypress のコア API
    // .should() に渡す文字列は「アサーション（期待値の宣言）」
    cy.get('h1').should('be.visible').and('contain.text', 'Welcome');
  });

  it('お問い合わせリンクを押すと /contact に遷移する', () => {
    cy.contains('a', 'お問い合わせ').click();
    cy.url().should('include', '/contact');
  });
});
```

#### ステップ3: `cypress.config.ts` で型を効かせる

```ts
import { defineConfig } from 'cypress';

export default defineConfig({
  e2e: {
    baseUrl: 'http://localhost:3000',
    // setupNodeEvents は Cypress 起動時に Node.js 側で1回だけ呼ばれる
    // 引数 on/config はそれぞれ「イベントフック登録関数」「読込済み設定」
    setupNodeEvents(on, config) {
      return config;
    },
  },
});
```

### ここで覚えてほしい JS/TS の3つの仕組み

1. **`defineConfig` という「型推論ヘルパー」パターン**
   `cypress.config.ts` を素のオブジェクト `export default { ... }` で書いても動きますが、`defineConfig({...})` で包むと TypeScript が中身の型を厳しくチェックしてくれます。これは Vite・Vitest・Astro・Tailwind v4 でも全く同じ流儀。「設定オブジェクトを関数で包む = 補完が効く」と覚えれば一気にエコシステムが読めるようになります。

2. **`cy.get(...).should(...)` の「チェーン」**
   各メソッドが「次の操作の入口」を返すので、`.` でつなげて命令を並べられます。これは JavaScript の **メソッドチェーン** という基本テクニックで、`array.filter(...).map(...).join(...)` と全く同じ構造。Cypress は内部で Promise ではなく独自のキューを使っていますが、書き味は配列メソッドと同じです。

3. **describe / it のネスト構造**
   `describe` は「テストのグループ名」、`it` は「個別のテストケース」。Markdown の見出し階層と同じイメージで、テスト結果のレポートがそのまま階層表示されます。最初は1つの `describe` の中に `it` を2〜3個書ければ十分です。

### LP案件への即効活用

- 納品前チェックリストを Cypress スクリプトに置き換える → クライアントへの「セルフチェック証跡」として PDF レポート添付できる
- ステージング環境に対して `cy.visit('https://staging.example.com')` で外形監視代わりに使える
- フォーム必須項目のバリデーションテストを書いておけば、CMS入稿ミスで本番が壊れる事故を防げる

---

## 今日の思考トリガー

**「もし"テストを書くのが当たり前"の自分になったら、業務委託の単価交渉で何と言える？ 『LP1本いくら』ではなく『品質保証込みでいくら』と言える人材は、市場でどれくらい希少だろうか？」**

技術スキルは、それ単体ではなく「クライアントの不安を減らす言葉」に翻訳できたとき初めて値段になります。今週末 Cypress を1本通したら、ぜひ自分の提案書のテンプレートを1行だけ書き換えてみてください。

---

_ソース: https://github.com/trending/typescript?since=weekly , https://github.com/cypress-io/cypress , https://github.com/puppeteer/puppeteer , https://github.com/iptv-org/iptv (2026年06月20日時点)_
