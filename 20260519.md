# 🎨 トレンドUI実装チュートリアル: 2026年05月19日号

おはようございます！シニアUI/UXデザイナー兼フロントエンドエンジニアの「学習コーチ」です。

火曜日は **「今っぽいUIを1つ、自分の手で実装する」** 日。今朝はGitHub Trending（JavaScript・TypeScript週間）を眺めて、`react-doctor` の登場が象徴的だなと感じました。AIにコードを書かせる時代だからこそ、**「UIの良し悪し」を自分の目で判断できる人** が強い。今日はその第一歩として、2026年も主役を張り続けている **Bento Grid（弁当箱グリッド）** をReact + TypeScript + TailwindCSSで組み上げます。

---

## 本日のハイライト

### 1. `millionco/react-doctor` が週間トレンド3位（TypeScript部門）

- 概要: Reactコードを静的解析して「健康スコア（0〜100）」と改善ポイントを出す診断ツール。Next.js / Vite / React Native対応、MITライセンス、CLI / GitHub Action / ESLintプラグインなど形態が豊富。
- 数字: 累計 10,153 スター / 今週 +2,430
- 注目点: AIエージェントが書いた雑なReactコードを「人間がレビューする前に一段フィルタする」用途で爆伸び。`useEffect` の依存配列ミスやアクセシビリティ違反を機械的に拾える。
- ソース: https://github.com/millionco/react-doctor

### 2. `bytedance/UI-TARS-desktop` が週間TS5位

- 概要: マルチモーダルAIエージェントのスタック。スクリーンショットを見てGUIを操作できる「画面理解」型エージェント。
- 数字: 累計 34,612 スター / 今週 +2,563
- 注目点: 「UIの構造化」がAI側でも進んでいる。**つまり人間が書くUIも、AIが読み取りやすい意味づけ（semantics）** がますます重要。FLOCSS的な命名・ARIA属性が活きてきます。
- ソース: https://github.com/bytedance/UI-TARS-desktop

### 3. CSS部門で `bradtraversy/50projects50days` が今週も健在

- 概要: HTML/CSS/JSだけで作る50個のミニUIプロジェクト集。アコーディオン、トグル、3Dカードなど定番UIを薄いコードで実装。
- 数字: 累計 40,553 スター / 今週 +28
- 注目点: 「Tailwindに行く前に素のCSSで仕組みを掴む」目的に最適。さとるさんのFLOCSS経験と相性が良い。
- ソース: https://github.com/bradtraversy/50projects50days

> ※ Hacker News Algolia（https://hn.algolia.com）は本日 HTTP 403 で取得できませんでした。フロントエンド系記事のピックアップは次回火曜にリベンジします。

---

## 注目リポジトリ / 事例

| # | リポジトリ | 用途 | URL |
|---|------------|------|-----|
| 1 | `millionco/react-doctor` | AI時代のReactコードレビュー自動化 | https://github.com/millionco/react-doctor |
| 2 | `bytedance/UI-TARS-desktop` | GUIを理解するAIエージェント基盤 | https://github.com/bytedance/UI-TARS-desktop |
| 3 | `bradtraversy/50projects50days` | 素のHTML/CSS/JSミニUI集（学習用） | https://github.com/bradtraversy/50projects50days |

いずれも無料で利用可能（OSS）。`react-doctor` のCLIはローカル実行なのでAPI課金なし、`UI-TARS-desktop` は内部でLLMを呼ぶため別途モデルAPI契約（OpenAI互換等）が必要です。

---

## 今日のハンズオン: Bento Grid Hero を組む

Bento Grid は Apple の製品紹介ページや Linear、Vercel のランディングで定番化したレイアウトパターン。**異なるサイズのカードを「弁当箱のように」敷き詰める** 構造で、「製品の特徴を一画面で見せたいLP」に最強です。さとるさんのLP案件で即使えます。

### ステップ1: 型を先に決める（TypeScript）

UIを書く前に「カード1枚は何を持つか」を型で定義します。型はあとから書き換えるより先に決めるほうが圧倒的に楽。

```tsx
// app/components/BentoCard.tsx
import { ReactNode } from "react";

// Bentoカード1枚分のデータ構造
export type BentoCardProps = {
  title: string;
  description: string;
  // "col-span-2 row-span-2" のようなTailwindクラスを文字列で受け取る
  className?: string;
  // カードの中に好きなJSX（アイコン、グラフ、画像）を入れられる
  children?: ReactNode;
};

export function BentoCard({
  title,
  description,
  className = "",
  children,
}: BentoCardProps) {
  return (
    <article
      className={`group rounded-2xl border border-white/10 bg-white/5 p-6
                  backdrop-blur transition hover:border-white/30
                  hover:bg-white/10 ${className}`}
    >
      <h3 className="text-lg font-semibold text-white">{title}</h3>
      <p className="mt-2 text-sm text-white/70">{description}</p>
      <div className="mt-4">{children}</div>
    </article>
  );
}
```

**型の読み解きポイント（初心者向け）**

- `type BentoCardProps = { ... }` … カードに渡せる「props（プロパティ）」の設計図。Reactではpropsは関数の引数のような存在で、型を付けることで「title を渡し忘れる」「数値を文字列の場所に入れる」といったミスをエディタが即指摘してくれます。
- `className?: string` … `?` は「省略可能」の印。`undefined` が来うることを意味します。だから `className = ""` のようにデフォルト値を入れて文字列結合（`` `... ${className}` ``）が安全に動くようにしています。
- `children?: ReactNode` … `ReactNode` は「Reactが描画できる何か（JSX・文字列・数値・配列など）」をまとめて表す型。これがあるおかげで `<BentoCard>...好きなJSX...</BentoCard>` と書けるようになる。

### ステップ2: グリッドで「弁当箱」を組む

Tailwind の `grid-cols-4` と `col-span-* / row-span-*` で異形セルを並べます。`md:` プレフィックスで「中サイズ画面以上で適用」になり、スマホでは自動で1列に潰れます。

```tsx
// app/page.tsx
import { BentoCard } from "./components/BentoCard";
import { StatCounter } from "./components/StatCounter";

export default function HomePage() {
  return (
    <main className="min-h-screen bg-neutral-950 px-6 py-20">
      <section
        className="mx-auto grid max-w-6xl gap-4
                   grid-cols-1 md:grid-cols-4 md:auto-rows-[180px]"
      >
        {/* 大きな主役カード：横2×縦2 */}
        <BentoCard
          title="AIと一緒に書くLPコーディング"
          description="HTMLセマンティクスと型を味方に、迷わず早く。"
          className="md:col-span-2 md:row-span-2"
        >
          <p className="text-3xl font-bold text-white">
            あなたの強みを、<br />スケールさせる場所。
          </p>
        </BentoCard>

        {/* 数字を見せる縦長カード */}
        <BentoCard
          title="今月のスコア"
          description="React Doctor 健康度"
          className="md:row-span-2"
        >
          <StatCounter target={92} suffix="点" />
        </BentoCard>

        {/* 横長の補助カード */}
        <BentoCard
          title="Tailwind"
          description="ユーティリティで時短"
          className="md:col-span-1"
        />
        <BentoCard
          title="Next.js"
          description="App Router 前提"
          className="md:col-span-1"
        />
        <BentoCard
          title="Supabase"
          description="DB と認証を即用意"
          className="md:col-span-2"
        />
      </section>
    </main>
  );
}
```

**レイアウトの読み解きポイント**

- `grid-cols-1 md:grid-cols-4` … モバイルでは縦1列、中サイズ以上で4列。**モバイルから先に書く** のがTailwindの作法（mobile-first）。
- `auto-rows-[180px]` … 行の高さを固定すると「弁当箱感」が出る。可変にしたければ `auto-rows-auto` でもOK。
- `md:col-span-2 md:row-span-2` … 主役カードを2×2マスで陣取り。視線の重心を作るための基本テクニック。

### ステップ3: 数値カウントアップ（React + 型）

Bentoには「数字でドンと見せる」セルが映えます。`0 → target` まで滑らかに増える小コンポーネントを書きます。

```tsx
// app/components/StatCounter.tsx
"use client";
import { useEffect, useState } from "react";

type StatCounterProps = {
  target: number;       // 最終的に表示したい数値
  durationMs?: number;  // アニメーション時間（ミリ秒）
  suffix?: string;      // "点" や "%" などの単位
};

export function StatCounter({
  target,
  durationMs = 1200,
  suffix = "",
}: StatCounterProps) {
  const [value, setValue] = useState(0);

  useEffect(() => {
    const startedAt = performance.now();
    let rafId = 0;

    const tick = (now: number) => {
      const progress = Math.min((now - startedAt) / durationMs, 1);
      // easeOutQuad: 最初速く、終盤ゆっくり止まる
      const eased = 1 - (1 - progress) ** 2;
      setValue(Math.round(target * eased));
      if (progress < 1) rafId = requestAnimationFrame(tick);
    };

    rafId = requestAnimationFrame(tick);
    return () => cancelAnimationFrame(rafId); // クリーンアップ大事
  }, [target, durationMs]);

  return (
    <p className="text-5xl font-bold tabular-nums text-white">
      {value}
      <span className="ml-1 text-base font-normal text-white/60">
        {suffix}
      </span>
    </p>
  );
}
```

**ロジックと型の読み解きポイント**

- `"use client"` … App Routerでは状態を持つコンポーネントの先頭に必須。これがないと `useState`/`useEffect` が使えません。
- `useState<number>` の `number` は実は省略可能で、`useState(0)` と書くだけでTypeScriptが「初期値が0だから型は `number` だな」と推論してくれます。これが **型推論** の威力。
- `useEffect(() => { ... return () => ... }, [deps])` の `return` はクリーンアップ関数。コンポーネントが画面から消えたとき `cancelAnimationFrame` が走らないと、裏でアニメーションが暴れます。**「副作用は必ず後片付けする」** が鉄則。
- `requestAnimationFrame` はブラウザの描画タイミングに合わせて呼ばれるAPI。`setInterval` よりCPUに優しく、画面の滑らかさが段違いです。
- `tabular-nums` というTailwindクラスは「数字の幅を等幅にする」CSS。カウント中に桁が増えてもガタつきません。地味ですがUI職人のひと手間。

---

## 実践ポイント（さとるさんへ）

1. **Bento Gridはまず「型 → 1枚カード → グリッド」の順** に。いきなり全部書こうとせず、`BentoCard` を1枚だけ画面に出してから複製する。これだけで詰まる確率が激減します。
2. **`react-doctor` を試験導入** してみてください。`npx react-doctor@latest` 系のCLIで既存のLPやNext.jsプロジェクトをスキャンするだけで、AIに書かせたコードのウィークポイントが可視化されます（公式リポジトリのREADMEで現行コマンドを確認）。ソース: https://github.com/millionco/react-doctor
3. **`bradtraversy/50projects50days` を「素のJS復習」教材として** ストックしておくと、Tailwind/Reactに頼らずDOM操作で動かす感覚を取り戻せます。型を意識する前段階として有効です。ソース: https://github.com/bradtraversy/50projects50days
4. **「props型を先に書く」習慣** をつけると、ReactでもFLOCSS時代の「設計→実装」の流れがそのまま活きます。さとるさんの強みをTSの世界に持ち込みましょう。

---

## 今日の思考トリガー

**「自分のLP案件で、もし “主役カード1つ + 補助カード4つ” のBento構成に作り直すとしたら、主役に置くべきは『製品の強み』『お客さまの声』『料金』のどれだろう？ なぜそれが視線の重心にふさわしい？」**

CVRを上げるBentoは「弁当箱を埋めること」が目的ではなく、**「主役セルにユーザーが最初に決断する材料を置くこと」** がコツです。手元の案件で1つ、配置を紙に書いてみてください。

---

_ソース: https://github.com/trending/javascript?since=weekly , https://github.com/trending/typescript?since=weekly , https://github.com/trending/css?since=weekly , https://github.com/millionco/react-doctor （いずれも2026年05月19日 JST 朝時点）_
