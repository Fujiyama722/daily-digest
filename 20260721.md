# 🎨 トレンドUI実装チュートリアル: 2026年07月21日号

おはようございます！シニアUI/UXデザイナー兼フロントエンドエンジニアの「学習コーチ」です。

火曜日はトレンドのUIパターンを1つ取り上げ、実装まで落とし込む日です。今週のGitHubトレンドを眺めていると、「AIに設計を任せる」流れと「デザインの原則をコードに落とす」流れが同時に加速していて、フロントエンドの現場感が一段変わってきたのを感じます。今日は、その中から**View Transitions APIを使ったなめらかなダークモード切り替え**を、Sato さんが個人開発ですぐ使える形で解説します。

---

## 本日のハイライト

### 1. `ibelick/ui-skills` が「デザインエンジニア」市場を切り拓く

- ⭐ 5,632（今週 +1,669）／言語: TypeScript 65.7%
- ソース: https://github.com/ibelick/ui-skills
- `npx ui-skills start` で、アクセシビリティ・タイポグラフィ・レイアウトなど、デザインエンジニア向けの実践ノウハウをCLIから呼び出せるツール群です。「Baseline UI」など、状態管理より先に整えるべき視覚品質を教えてくれます。**Sato さんのようにLPコーディングから React 開発へ移行中の人には、命名規則の先にある“見た目の設計論”を体系的に補える良い教材**です。

### 2. `pbakaus/impeccable` — AIハーネスに「美的感覚」を持たせる標準

- ⭐ 48,306（今週 +2,290）／言語: JavaScript 94.5%
- ソース: https://github.com/pbakaus/impeccable
- Cursor / Claude Code / Copilot などのAIコード生成ツールに、23コマンドと46の“検出ルール”を注入するSkill集です。AIに UI を書かせるとき、余白・コントラスト・アライメントの破綻を機械的に検出させ、デザイン品質を下げないための土台になります。

### 3. `tt-a1i/archify` — 図解を「英語一文」から自動生成

- ⭐ 6,189（今週 +2,103）／言語: JavaScript 78.9%
- ソース: https://github.com/tt-a1i/archify
- 自然文からアーキ図・シーケンス図を吐き出し、**ダーク/ライト切替・PNG/JPEG/WebP/SVG エクスポート**まで一気通貫。個人開発の README や記事執筆で「頭の中の構造」を可視化するときに強力です。

---

## 注目リポジトリ / 事例

| # | リポジトリ | 概要 | ⭐ (今週+) |
|---|---|---|---|
| 1 | [ibelick/ui-skills](https://github.com/ibelick/ui-skills) | デザインエンジニア向けの実践スキル集 (CLI) | 5,632 (+1,669) |
| 2 | [pbakaus/impeccable](https://github.com/pbakaus/impeccable) | AIコード生成に美的品質ルールを注入 | 48,306 (+2,290) |
| 3 | [tt-a1i/archify](https://github.com/tt-a1i/archify) | 自然文→技術図解ジェネレーター | 6,189 (+2,103) |

---

## 実践ポイント（さとるさんへ）

### 今日のトレンドUI: 「View Transitions APIで作る、なめらかなダークモード切り替え」

`archify` のダーク/ライト切替が話題になっているように、**テーマ切替は“単なる状態フラグ”ではなくトランジション体験**が差別化ポイントになっています。ここでは、Next.js (App Router) + TailwindCSS + TypeScript で、クリック位置からダークモードが円形に広がるアニメーションを作ってみましょう。

#### ステップ0: 前提

Next.js 15 系 + TailwindCSS v4 想定。`app/globals.css` に Tailwind を読み込んでいる前提です。

#### ステップ1: `app/globals.css` に View Transition の擬似要素を仕込む

```css
/* Chrome/Edge 系ブラウザで動作。Safari/Firefoxは通常のフェードにフォールバック */
@supports (view-transition-name: root) {
  ::view-transition-old(root),
  ::view-transition-new(root) {
    animation: none;
    mix-blend-mode: normal;
  }

  ::view-transition-old(root) {
    z-index: 1;
  }
  ::view-transition-new(root) {
    z-index: 9999;
  }

  .dark::view-transition-old(root) {
    z-index: 9999;
  }
  .dark::view-transition-new(root) {
    z-index: 1;
  }
}
```

#### ステップ2: テーマ切替コンポーネントを型付きで作る

```tsx
// app/components/ThemeToggle.tsx
"use client";

import { useCallback, useEffect, useState, type MouseEvent } from "react";

type Theme = "light" | "dark";

// document.startViewTransition の型が未整備な環境向けの補助型
type ViewTransitionDocument = Document & {
  startViewTransition?: (callback: () => void) => {
    ready: Promise<void>;
  };
};

export function ThemeToggle() {
  const [theme, setTheme] = useState<Theme>("light");

  // 初期化: localStorage or OS 設定を尊重
  useEffect(() => {
    const stored = window.localStorage.getItem("theme") as Theme | null;
    const prefersDark = window.matchMedia("(prefers-color-scheme: dark)").matches;
    const initial: Theme = stored ?? (prefersDark ? "dark" : "light");
    setTheme(initial);
    document.documentElement.classList.toggle("dark", initial === "dark");
  }, []);

  const toggle = useCallback(
    async (event: MouseEvent<HTMLButtonElement>) => {
      const next: Theme = theme === "light" ? "dark" : "light";
      const doc = document as ViewTransitionDocument;

      const apply = () => {
        document.documentElement.classList.toggle("dark", next === "dark");
        setTheme(next);
        window.localStorage.setItem("theme", next);
      };

      // 非対応ブラウザは即座に切り替え
      if (!doc.startViewTransition) {
        apply();
        return;
      }

      const x = event.clientX;
      const y = event.clientY;
      const endRadius = Math.hypot(
        Math.max(x, window.innerWidth - x),
        Math.max(y, window.innerHeight - y),
      );

      const transition = doc.startViewTransition(apply);
      await transition.ready;

      document.documentElement.animate(
        {
          clipPath: [
            `circle(0px at ${x}px ${y}px)`,
            `circle(${endRadius}px at ${x}px ${y}px)`,
          ],
        },
        {
          duration: 500,
          easing: "ease-in-out",
          pseudoElement:
            next === "dark"
              ? "::view-transition-new(root)"
              : "::view-transition-old(root)",
        },
      );
    },
    [theme],
  );

  return (
    <button
      type="button"
      onClick={toggle}
      aria-label={theme === "light" ? "ダークモードに切替" : "ライトモードに切替"}
      className="rounded-full border border-neutral-300 px-4 py-2 text-sm
                 hover:bg-neutral-100 dark:border-neutral-700 dark:hover:bg-neutral-800
                 transition-colors"
    >
      {theme === "light" ? "🌙 Dark" : "☀️ Light"}
    </button>
  );
}
```

#### ステップ3: レイアウトで使う

```tsx
// app/layout.tsx（抜粋）
import { ThemeToggle } from "./components/ThemeToggle";

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="ja">
      <body className="bg-white text-neutral-900 dark:bg-neutral-950 dark:text-neutral-50">
        <header className="flex justify-end p-4">
          <ThemeToggle />
        </header>
        {children}
      </body>
    </html>
  );
}
```

### 型とロジック、ここが初心者ポイント

- **`type Theme = "light" | "dark";`**: 「文字列リテラルの Union 型」。`Theme` に代入できる値をこの2つに限定でき、`if (theme === "drak")` のようなタイポをコンパイル時に潰せます。これが TypeScript の第一の恩恵です。
- **`event: MouseEvent<HTMLButtonElement>`**: React のイベント型はジェネリクスで対象要素を指定します。`event.currentTarget` の型が自動で `HTMLButtonElement` に絞られるので、`.value` などの独自プロパティにも安全にアクセスできます。
- **`document as ViewTransitionDocument`**: 型アサーション。`startViewTransition` は新しめのAPIで、環境によっては `lib.dom.d.ts` に無い場合があります。オプショナルにした独自型を用意することで、**非対応ブラウザ = `undefined` を型レベルで表現**しています。
- **`useCallback([theme])`**: 依存配列に `theme` を入れないと、クロージャが古い `theme` を捕まえてしまいバグります。「関数の中で参照した State は必ず依存配列に入れる」は React の基本ルール。
- **フォールバック分岐**: `if (!doc.startViewTransition)` の早期リターンで、Safari / Firefox でも壊れずに動くよう保険をかけています。**トレンドAPIを使うときほど、非対応時の挙動を先に決める**のがプロの流儀です。

### 発展課題（60分でできる）

1. `prefers-reduced-motion` を検知して、アニメーションを無効化する分岐を足す（アクセシビリティ配慮）
2. `Cookie` に保存し直して、Next.js の Server Component でも初期テーマがチラつかないようにする
3. 切替時に効果音を `use client` の中で鳴らす（`new Audio()`）

---

## 今日の思考トリガー

**「あなたが個人開発中のプロダクトで、ユーザーが1日に何度も触るインタラクションはどれですか？ そこに 0.3 秒の“気持ちよさ”を足せる余地はありませんか？」**

トレンドUIは、単に真似ることに価値はありません。**「どのインタラクションを磨けば、自分のプロダクトの体験価値が跳ねるか」を選ぶ判断力**こそ、シニアフロントエンドとの分岐点です。今日のダークモードを、あなたの Next.js プロジェクトの1画面にまず組み込んでみてください。

---

_ソース:_
- _[GitHub Trending TypeScript (weekly)](https://github.com/trending/typescript?since=weekly) (2026年07月21日時点)_
- _[GitHub Trending JavaScript (weekly)](https://github.com/trending/javascript?since=weekly) (2026年07月21日時点)_
- _[ibelick/ui-skills](https://github.com/ibelick/ui-skills), [pbakaus/impeccable](https://github.com/pbakaus/impeccable), [tt-a1i/archify](https://github.com/tt-a1i/archify)_
- _※本日は Hacker News (hn.algolia.com / firebaseio) がネットワークポリシーで取得できませんでした。GitHub Trending の実データのみで構成しています。_
