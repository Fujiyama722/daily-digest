# 🎨 トレンドUI実装チュートリアル: 2026年05月05日号

おはようございます！シニアUI/UXデザイナー兼フロントエンドエンジニアの「学習コーチ」です。

GW明けの火曜日、いかがお過ごしでしょうか。今日は「アニメーションUI」を題材に、最新ライブラリを React + TypeScript + TailwindCSS でどう組み込むか、型の仕組みも含めて手を動かしながら学んでいきましょう。

---

## 本日のハイライト

今週の GitHub Trending（JavaScript・週間）で目を引いたのが、王道のアニメーションエンジン **anime.js が v4.4.1 を 2026年4月30日にリリース** したことです。週間 +336★ と着実に伸び、累計 67,786★ を維持しています。

V4 系列は API が刷新され、ES Modules 前提・Tree-shaking 対応・TypeScript 型定義同梱と、現代のフロントエンド開発（Next.js / Vite ベース）にフィットする作りになっています。「過剰なリッチアニメーションは UX を損ねる」というのは UI/UX デザインの定石ですが、**マイクロインタラクション**（ボタンのホバー、リスト要素のステージャー登場、フォーム送信時のフィードバック）は CVR を押し上げる強力な武器です。ここを軽量ライブラリで丁寧に作り込めるエンジニアは、LP コーディングや個人プロダクト開発の両方で重宝されます。

なお、本日 Hacker News（hn.algolia.com）は 403 で取得できませんでした。代替として GitHub Trending JavaScript（週間）の上位 10 件を参照しています。

ソース: <https://github.com/trending/javascript?since=weekly>（2026年5月5日時点）

---

## 注目リポジトリ / 事例

### 1. juliangarnier/anime — JavaScript animation engine
- URL: <https://github.com/juliangarnier/anime>
- ★ 67,786 / 週間 +336
- v4.4.1（2026/4/30 リリース）。`animate()` `stagger()` `createTimeline()` などの関数を ES Modules で import し、CSS プロパティ・SVG・DOM 属性・素の JS オブジェクトまで動かせる。npm: `npm i animejs`

### 2. pbakaus/impeccable — The design language for AI
- URL: <https://github.com/pbakaus/impeccable>
- ★ 24,893 / 週間 +2,619
- 「AI ハーネスのデザイン能力を引き上げるデザイン言語」を標榜するプロジェクト。AI に UI を生成させる時代に、トークン化・ガイドライン化されたデザイン言語の重要性が高まっていることを示す事例。

### 3. fspecii/ace-step-ui — ACE-Step 1.5 用プロフェッショナル UI
- URL: <https://github.com/fspecii/ace-step-ui>
- ★ 2,768 / 週間 +1,359
- AI 音楽生成モデルのフロントエンド。複雑なパラメーター操作 UI を OSS で公開しており、「AI バックエンド + リッチな専用 UI」の構成を学ぶ参考になります（実利用には GPU 実行環境が前提）。

---

## 実践ポイント（さとるさんへ）

anime.js v4 を **Next.js (App Router) + TypeScript + TailwindCSS** に組み込んで、リスト要素を順にフェードイン＆スライドさせる「Stagger 登場アニメーション」を作ってみましょう。LP やランディング、ダッシュボードのカード一覧で即使える定番パターンです。

### Step 1. インストール

```bash
npm i animejs
npm i -D @types/animejs   # TS で型補完を効かせる場合（v4 では本体に型同梱なので不要なケースもあり）
```

### Step 2. クライアントコンポーネントを作る（`app/components/StaggerCards.tsx`）

```tsx
"use client";

import { useEffect, useRef } from "react";
import { animate, stagger } from "animejs";

type Card = {
  title: string;
  body: string;
};

type Props = {
  cards: Card[];
};

export default function StaggerCards({ cards }: Props) {
  // ① DOM への参照を保持する箱。<HTMLDivElement | null> で「最初は null、後で div が入る」と型に伝える
  const containerRef = useRef<HTMLDivElement | null>(null);

  useEffect(() => {
    // ② マウント後にだけ実行される。SSR 時には window が無いため、ここで初めて animate() を呼ぶのが鉄則
    if (!containerRef.current) return;

    animate(containerRef.current.querySelectorAll<HTMLElement>(".card"), {
      opacity: [0, 1],
      translateY: [24, 0],
      duration: 600,
      delay: stagger(80, { from: "first" }),
      ease: "outQuart",
    });
  }, [cards]);

  return (
    <div
      ref={containerRef}
      className="grid grid-cols-1 gap-4 md:grid-cols-3"
    >
      {cards.map((c) => (
        <article
          key={c.title}
          className="card rounded-2xl border border-zinc-200 bg-white p-6 shadow-sm opacity-0"
        >
          <h3 className="text-lg font-semibold text-zinc-900">{c.title}</h3>
          <p className="mt-2 text-sm text-zinc-600">{c.body}</p>
        </article>
      ))}
    </div>
  );
}
```

### Step 3. 型とロジックの解説（さとるさん向け）

- **`"use client"`**: Next.js App Router では、ブラウザの DOM/効果（`useEffect`・`useRef`）を使うコンポーネントの先頭にこの宣言が必要です。anime.js は DOM を触るので必須。
- **`useRef<HTMLDivElement | null>(null)`**: `useRef` は「描画されても値が再生成されない箱」。型引数の `HTMLDivElement | null` がポイントで、「最初は `null`、後で `<div>` が入る」という"時間差"を TS に伝えています。これにより `containerRef.current` を使う前に `if (!containerRef.current) return;` という **ナローイング**（型を絞り込む）が必要になり、安全に DOM 操作できます。
- **`querySelectorAll<HTMLElement>(...)`**: ジェネリクスで「返ってくる NodeList の要素は HTMLElement」と明示。これで anime.js に渡す配列の型が安定します。
- **`opacity: [0, 1]` / `translateY: [24, 0]`**: 配列で書くと「from → to」を意味する v4 の記法。Tailwind 側で `opacity-0` を初期値に当てておくと、JS 読み込み前のチラつきを防げます。
- **`stagger(80, { from: "first" })`**: 80ms ずつ遅延を増やしながら順番に再生。`"center"` にすると中央から外側へ広がる演出になります。
- **依存配列 `[cards]`**: `cards` が変わったらアニメをやり直す、という宣言。空配列 `[]` だと初回のみ動きます。

### Step 4. 使い方（`app/page.tsx`）

```tsx
import StaggerCards from "./components/StaggerCards";

const cards = [
  { title: "高速", body: "v4 で Tree-shaking 対応。必要な関数だけバンドル" },
  { title: "型安心", body: "TypeScript 型定義同梱で補完が効く" },
  { title: "軽量", body: "Framer Motion より小さい使い分け候補" },
];

export default function Page() {
  return (
    <main className="mx-auto max-w-5xl px-6 py-16">
      <h1 className="mb-8 text-3xl font-bold">Tuesday Stagger Demo</h1>
      <StaggerCards cards={cards} />
    </main>
  );
}
```

### UX/CVR 視点のひとこと

- **登場アニメは 600ms 以下、stagger は 80〜120ms** を目安に。長いとユーザーが「待たされている」感覚を持ちます。
- `prefers-reduced-motion` を尊重するなら、`useEffect` 冒頭で `window.matchMedia("(prefers-reduced-motion: reduce)").matches` を確認し、`true` の場合は即座に最終状態へ。アクセシビリティ上の必須対応です。
- LP では「ファーストビュー直後の 1 セクションだけ」に絞ると、信頼感を損なわずに動的さを出せます。

---

## 今日の思考トリガー

**「あなたが今受託している LP のうち、もし 1 ヶ所だけ stagger 登場アニメーションを入れるとしたら、どのセクションを選びますか？ そしてその選択は、訪問者にどんな感情を一瞬で抱かせたいからですか？」**

UI 技術の使いどころは「動かす技術力」ではなく「動かす場所を選ぶ目線」で差がつきます。週後半までに 1 つだけ、anime.js を投入する箇所を決めて、デモを Vercel にデプロイしてみましょう。

---

_ソース: <https://github.com/trending/javascript?since=weekly> / <https://github.com/juliangarnier/anime>（2026年5月5日時点）_
_補足: Hacker News（hn.algolia.com）は本日 403 のため取得できませんでした。_
