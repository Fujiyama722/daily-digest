# 📊 プロダクトマネジメント事例解説: 2026年05月20日号

おはようございます！シニア・プロダクトマネージャーの「学習コーチ」です。

今朝の Hacker News では「AI is killing B2B SaaS」という記事が再燃しており、Morgan Stanley の SaaS バスケットが Nasdaq から 40 ポイント遅れ、HubSpot や Klaviyo は約 30% 下落しているというデータが共有されていました。一方で Product Hunt のトップには Framer（startup-speed × enterprise-need を打ち出すノーコード Web ビルダー）が居座り続けています。「AI で何でも作れる時代に、誰が SaaS にお金を払うのか？」を、Figma と Framer の事例を題材に PM 視点で深掘りします。

---

## 本日のハイライト

- **「AI is killing B2B SaaS」論争が再燃**（[HN スレッド](https://news.ycombinator.com/item?id=46888441) / [反論スレッド](https://news.ycombinator.com/item?id=47144923)）
  「単機能 SaaS」は vibe coding で代替されつつあるが、独自データ・ネットワーク効果・ワークフロー埋め込みを持つ SaaS は生き残るという二極化が論点。PM として「自分のプロダクトの堀は何か？」を言語化できるかが問われる流れです。

- **Framer が PH トップ常連に**（[PollyReach / Framer 等](https://www.producthunt.com/)）
  Framer は 2026 年に ARR $100M 目標を掲げ、月間 50 万 MAU・有料顧客 4.5〜5.5 万社、ARPU 約 $1,000/年というユニットエコノミクスで成長中。YC Summer 2024 バッチの 40% がメインサイトに採用、Miro・Perplexity・Scale AI もエンタープライズ顧客に名を連ねる構図です。([Sacra](https://sacra.com/c/framer/))

- **Figma は「8 プロダクト戦略」へ拡張**（[Figma SWOT 2026](https://swotpal.com/blog/figma-swot-analysis-2026)）
  2025〜2026 で 4 → 8 プロダクトに拡大。$10K/年超の有料顧客の 75% が毎週 AI クレジットを消費する**ハイブリッド課金モデル**を 2026 年 3 月にローンチ。デザイナーだけでなく PM・開発者・マーケターをコアユーザに据える「cross-side network effect」を強化しています。

---

## 注目事例: Figma の「コラボレーションが堀」モデル

非エンジニア出身でこれから個人開発で収益化を目指すさとるさんに、いちばん勉強になる事例として Figma を取り上げます。

### なぜ Figma は AI 時代でも崩れにくいのか？

- **マルチプレイヤー編集が技術的堀**: WebGL + WebAssembly でブラウザネイティブに低レイテンシ同時編集を実現。個別機能はコピーできても「数千人規模でなめらかに動く同時編集体験」は再現コストが極端に高い。([Inside Figma's Moat](https://medium.com/@kamalmeet/inside-figmas-moat-d3e351dd5f73))
- **クロスサイドネットワーク効果**: デザイナーが Figma を使う → PM/エンジニア/マーケが招待される → 全員が「Figma に居る前提」になる → 離脱コストが組織単位で跳ね上がる。([How Figma Grows](https://www.howtheygrow.co/p/how-figma-grows))
- **「単機能 AI」では奪えない**: vibe coding で UI モック自体は作れても、「同じファイルに 30 人が居て意思決定する」体験は LLM では代替不可。

### 要件定義をシンプルに分解してみる

Figma の MVP を個人開発者目線で再構成すると、要件は意外と少ない:

1. **ユーザーが部屋（ファイル）を作れる**
2. **同じ部屋に複数人がリアルタイムで居られる**（カーソル位置・選択範囲が同期）
3. **編集内容が即座に他参加者に反映される**（衝突解決）
4. **コメント機能で非同期議論ができる**

---

## Supabase 前提のテーブル設計（コラボ系 MVP のミニ版）

「リアルタイム同時編集 + コメント」を Supabase で作る最小スキーマを書き出してみます。さとるさんが個人開発でコラボ系プロダクトを企画するときの土台になります。

```sql
-- ファイル（Figma でいう「デザインファイル」）
create table public.files (
  id uuid primary key default gen_random_uuid(),
  owner_id uuid not null references auth.users(id) on delete cascade,
  title text not null,
  created_at timestamptz not null default now()
);

-- 参加メンバー（cross-side network effect の源泉）
create table public.file_members (
  file_id uuid not null references public.files(id) on delete cascade,
  user_id uuid not null references auth.users(id) on delete cascade,
  role text not null check (role in ('owner','editor','viewer')),
  primary key (file_id, user_id)
);

-- 操作ログ（同時編集の差分を realtime で配信）
create table public.operations (
  id bigserial primary key,
  file_id uuid not null references public.files(id) on delete cascade,
  user_id uuid not null references auth.users(id),
  op jsonb not null,            -- {"type":"move","node":"...","x":12,"y":34}
  created_at timestamptz not null default now()
);

-- コメント（非同期コミュニケーション）
create table public.comments (
  id uuid primary key default gen_random_uuid(),
  file_id uuid not null references public.files(id) on delete cascade,
  author_id uuid not null references auth.users(id),
  body text not null,
  created_at timestamptz not null default now()
);
```

### RLS（Row Level Security）の考え方

Supabase の肝は「フロントから直接 DB を叩いても安全」にすることです。`file_members` に居る人だけが読めるよう RLS を貼ります:

```sql
alter table public.files enable row level security;

create policy "members can read files"
on public.files for select
using (
  exists (
    select 1 from public.file_members m
    where m.file_id = files.id
      and m.user_id = auth.uid()
  )
);

create policy "owner can update files"
on public.files for update
using (owner_id = auth.uid());
```

ポイントは **「auth.uid() = 現在ログイン中のユーザー」を起点に、結合テーブル（file_members）経由でアクセス権を判定する**こと。Figma 的な「招待された人だけ見える」を 10 行以下の SQL で実装できます。

### リアルタイム配信

`operations` テーブルに対して Supabase Realtime を有効化すると、フロント側はこれだけで他人の操作が流れてきます:

```ts
const channel = supabase
  .channel(`file:${fileId}`)
  .on(
    'postgres_changes',
    { event: 'INSERT', schema: 'public', table: 'operations', filter: `file_id=eq.${fileId}` },
    (payload) => applyOp(payload.new.op)
  )
  .subscribe();
```

「単機能 SaaS」を「ネットワーク効果のあるプロダクト」に進化させる第一歩は、**ユーザー同士が同じデータを触れる構造**を最初に入れることです。

---

## 実践ポイント（さとるさんへ）

1. **自分のプロダクト案に「2 人目のユーザー」を組み込む**
   LP 制作ツール、フォーム、ダッシュボード…何を作るにしても「1 人で完結する SaaS」より「招待された人が増えるほど価値が上がる」設計を最初から仕込むと、AI 時代の堀になります。Figma の `file_members` テーブルが教えてくれる教訓です。

2. **Supabase の RLS を「設計の入口」にする**
   テーブルを作るたびに「このテーブルは誰が読める？誰が書ける？」を SQL で書き下す癖をつけると、要件定義と権限設計が同時に終わります。これは PM の仕事を半分こなしていることに等しいです。

3. **Framer のユニットエコノミクスを覚えておく**
   ARPU $1,000/年・MAU 50 万・有料化率 ~10% という数字感は、個人開発の北極星を決めるときの相場観として超有用。「月 $10 のツールで 100 人集める」より「年 $1,000 のツールで 10 社に売る」のほうが、同じ売上でもサポート負荷が桁違いに小さい、という戦略判断ができるようになります。

---

## 今日の思考トリガー

**「あなたが今ぼんやり考えているプロダクトに『2 人目以降のユーザー』を入れるとしたら、誰を招待する設計にすれば離脱コストが一番高くなるか？」**

単機能 SaaS が AI に食われる時代、PM 視点で最初に詰めるべきは「機能の豪華さ」ではなく「ユーザー同士の関係性」です。今日の通勤時間に、紙でいいので 3 パターン書き出してみてください。

---

_ソース: [AI is killing B2B SaaS - HN](https://news.ycombinator.com/item?id=46888441), [AI isn't killing SaaS – it's killing single-purpose SaaS - HN](https://news.ycombinator.com/item?id=47144923), [Product Hunt](https://www.producthunt.com/), [Framer revenue & growth - Sacra](https://sacra.com/c/framer/), [Inside Figma's Moat](https://medium.com/@kamalmeet/inside-figmas-moat-d3e351dd5f73), [Figma SWOT Analysis 2026](https://swotpal.com/blog/figma-swot-analysis-2026), [How Figma Grows](https://www.howtheygrow.co/p/how-figma-grows) (2026年05月20日時点)_
