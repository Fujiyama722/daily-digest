# 🛠️ 週末ハンズオン実践: 2026年06月06日号

おはようございます！フロントエンドメンターの「学習コーチ」です。

今週は AI エージェント向け「コンテキスト圧縮」「メモリ管理」系のリポジトリが GitHub Trending を席巻しました。今日はこのトレンドを横目に見つつ、さとるさんの React/Next.js/TypeScript/Tailwind スタックで **「折りたたみ可能な要約カード」コンポーネント** を作る、土曜日らしい小さなハンズオンに落とし込みます。

---

## 本日のハイライト

### 1. AI に渡すテキストを「圧縮」して節約する流れが本格化

今週の GitHub Trending では `chopratejas/headroom`（Python、週間 +9,421 ⭐ / 累計 14,401 ⭐）が急上昇しました。ツール出力・ログ・RAG チャンクなどを LLM に渡す **前に圧縮** することで「60〜95% のトークン削減」を謳うライブラリです。Claude Code を日常的に使うさとるさんにとっても、「長い出力をそのままモデルに渡さず要約してから渡す」発想は普段のプロンプト設計に直結します。

- ソース: https://github.com/chopratejas/headroom

### 2. AI 時代の「記憶」を扱う TypeScript 製エンジン

`supermemoryai/supermemory`（TypeScript、週間 +2,740 ⭐ / 累計 25,679 ⭐）は、AI に長期記憶を持たせる API/アプリです。READMEでは LongMemEval / LoCoMo / ConvoMem の3つのベンチマークで「#1」を主張。Google Drive / Gmail / Notion 連携やマルチモーダル（PDF/画像/動画/コード）に対応。Next.js + Supabase で個人開発する際の「ユーザー固有のメモリ層」をどう設計するか、参考にできる構成です。

- ソース: https://github.com/supermemoryai/supermemory

### 3. 「複利で効くエンジニアリング」を Claude Code で実装する公式プラグイン

`EveryInc/compound-engineering-plugin`（TypeScript、週間 +2,111 ⭐ / 累計 19,915 ⭐）は、Claude Code / Codex / Cursor 向けの公式プラグイン。「実行 20% / 計画とレビュー 80%」の比率で技術負債を逆に減らす方法論を、37 個のスキルと 51 個のエージェントとして提供しています。`/ce-brainstorm` `/ce-plan` `/ce-code-review` `/ce-doc-review` `/ce-compound` といった slash command が用意されています。

- ソース: https://github.com/EveryInc/compound-engineering-plugin

> ⚠️ Hacker News（`hn.algolia.com` / `news.ycombinator.com` / Firebase API）は本日この実行環境からアクセス不可（403 / allowlist 外）でした。GitHub Trending 由来の情報のみで構成しています。

---

## 注目リポジトリ / 事例

| # | リポジトリ | 概要 | 言語 | 週間 ⭐ | 累計 ⭐ |
|---|------------|------|------|--------|---------|
| 1 | [chopratejas/headroom](https://github.com/chopratejas/headroom) | LLM に渡す前にトークンを圧縮 | Python | +9,421 | 14,401 |
| 2 | [supermemoryai/supermemory](https://github.com/supermemoryai/supermemory) | AI のための長期メモリ API | TypeScript | +2,740 | 25,679 |
| 3 | [EveryInc/compound-engineering-plugin](https://github.com/EveryInc/compound-engineering-plugin) | Claude Code 等向け公式プラグイン | TypeScript | +2,111 | 19,915 |

※ 数値は GitHub Trending（weekly）取得時点のもの。`headroom` の累計 ⭐ は週間獲得の 1.5 倍程度しかなく、新興リポジトリです。実運用に組み込む前にメンテナンス状況の確認を推奨。

---

## 週末ハンズオン: 「折りたたみ可能な要約カード」を作る

トレンドの「**長文を圧縮して必要な分だけ見せる**」というアイデアを、フロントエンドの小さな UI に落とし込みます。Next.js（App Router）+ TypeScript + TailwindCSS で動く **クライアントコンポーネント** です。記事一覧・通知・AI の長い回答を「冒頭だけ見せて、必要なときだけ展開」する場面にそのまま流用できます。

### 完成イメージ

- デフォルト: タイトル + 本文の最初の 80 文字 + 「もっと見る」ボタン
- クリック後: 本文全体が表示され、ボタンは「閉じる」に変化
- 開閉時に高さがふわっと変わる（Tailwind の `transition` を利用）

### コード（コピペで動きます）

```tsx
// app/components/SummaryCard.tsx
"use client";

import { useState } from "react";

// 1) props の「形」を型で宣言する
type SummaryCardProps = {
  title: string;
  body: string;
  /** 折りたたみ時に表示する文字数。省略時は 80 */
  previewLength?: number;
};

// 2) コンポーネント本体。React.FC ではなく素の関数で書くのが今の主流
export function SummaryCard({
  title,
  body,
  previewLength = 80,
}: SummaryCardProps) {
  // 3) 「開いているかどうか」を boolean で状態管理
  //    useState<boolean> と書くと、後で別の型を入れたらコンパイルエラーになる
  const [isOpen, setIsOpen] = useState<boolean>(false);

  // 4) 表示する本文を「開いていれば全文 / 閉じていれば冒頭だけ」で切り替える
  const isLong = body.length > previewLength;
  const shown = isOpen || !isLong ? body : `${body.slice(0, previewLength)}…`;

  return (
    <article className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm">
      <h2 className="text-lg font-semibold text-slate-900">{title}</h2>

      <p
        className={[
          "mt-2 whitespace-pre-wrap text-sm leading-relaxed text-slate-700",
          "transition-[max-height] duration-300 ease-out overflow-hidden",
          isOpen ? "max-h-[40rem]" : "max-h-40",
        ].join(" ")}
      >
        {shown}
      </p>

      {isLong && (
        <button
          type="button"
          onClick={() => setIsOpen((prev) => !prev)}
          className="mt-3 inline-flex items-center gap-1 rounded-full bg-slate-900 px-3 py-1 text-xs font-medium text-white hover:bg-slate-700"
          aria-expanded={isOpen}
        >
          {isOpen ? "閉じる" : "もっと見る"}
        </button>
      )}
    </article>
  );
}
```

使い方:

```tsx
// app/page.tsx
import { SummaryCard } from "./components/SummaryCard";

export default function Page() {
  return (
    <main className="mx-auto max-w-xl space-y-4 p-6">
      <SummaryCard
        title="今週の AI トレンド"
        body={`今週は LLM に渡すコンテキストを圧縮するライブラリ headroom が急上昇しました。
ツール出力・ログ・RAG チャンクなどを LLM に渡す前に削減することで、応答品質を保ちつつトークン消費を抑える設計が広がっています。
個人開発でも、ユーザーに見せる UI と AI に渡すデータを分けて考える発想が重要です。`}
      />
    </main>
  );
}
```

### JS/TS のロジック解説（さとるさん向け）

1. **`type` 宣言の意味**  
   `type SummaryCardProps = { ... }` で、このコンポーネントが **受け取るデータの形** を「契約」として書いています。`previewLength?: number` の `?` は「省略してよい」という意味。呼び出し側でうっかり `<SummaryCard title={123} ... />` のように数値を渡すと、TypeScript が **コンパイル時に** 教えてくれます。これが「型がある言語」の最大のメリットです。

2. **`useState<boolean>(false)` の意味**  
   `useState` は「再レンダリングをまたいでも値を覚えておく」React の道具です。`<boolean>` は型注釈で、「この箱には true / false しか入らない」と宣言しています。返り値は **配列の分割代入** で `[現在の値, 更新関数]` を受け取ります。`setIsOpen((prev) => !prev)` は「今の値の反対にする」関数で、`prev` を使うのは **連続クリック時に古い値を参照しないため** の定石です。

3. **配列の `slice` と `length`**  
   `body.length > previewLength` で「省略が必要か」を判定し、必要なら `body.slice(0, previewLength)` で先頭だけ切り出します。`slice` は元の文字列を **壊さない** メソッドなので、再レンダリング時にも `body` の中身は変わりません。「**元データを壊さず、表示用の値だけ毎回計算する**」のが React の基本姿勢です。

4. **`className` を配列で組み立てる理由**  
   `["base", "transition", isOpen ? "max-h-[40rem]" : "max-h-40"].join(" ")` のように書くと、条件分岐が見やすくなります。慣れたら `clsx` ライブラリに置き換えても OK ですが、最初は素の `Array#join` で十分です。

5. **`aria-expanded` を忘れない**  
   開閉式 UI ではスクリーンリーダーのために `aria-expanded` を付けるのが標準です。CVR を最後に支えるのは、ちゃんと全員に届く UI かどうか。

---

## 実践ポイント（さとるさんへ）

- **今日 30 分でやること**: 上のコードを Next.js プロジェクト（`npx create-next-app@latest`）の `app/components/SummaryCard.tsx` に貼り、`app/page.tsx` で 2〜3 件並べて表示する。
- **追加課題 1（型を広げる）**: `previewLength?: number` の隣に `variant?: "default" | "compact"` を追加し、`compact` のときはタイトルだけ強調する分岐を増やしてみる。**ユニオン型** に慣れる練習です。
- **追加課題 2（純関数を切り出す）**: `isLong` と `shown` の計算を `function buildPreview(body: string, previewLength: number, isOpen: boolean): { isLong: boolean; shown: string }` という **関数の外** に切り出してみる。テストしやすくなる感覚が掴めます。
- **追加課題 3（Supabase 連携）**: `title` `body` を Supabase の `notes` テーブル（`id uuid`, `title text`, `body text`, `user_id uuid`）から取ってきて並べる。RLS は `user_id = auth.uid()` の方針で。`headroom` の発想にならって、長文は要約フィールド `summary text` を別に持つテーブル設計も検討すると、AI に渡す用 / 画面に出す用を分けられます。
- **避けるべき脱線**: `headroom` 本体（Python / トークナイザ周り）にいきなり挑むのは時期尚早。まずは **「長い情報を必要な分だけ見せる」UI 哲学** だけ吸収し、自分の React/TS の引き出しを増やすほうが ROI が高いです。

---

## 今日の思考トリガー

**「自分が作っている画面で『最初から全部見せている情報』は、本当に全部必要ですか？ 折りたためる箇所を1つ選んで、UX と CVR の両面から仮説を立ててみよう。」**

良い週末を！来週も一緒に積み上げていきましょう。

---

_ソース: https://github.com/trending?since=weekly （2026年06月06日 JST 時点）_  
_補足: Hacker News 系（hn.algolia.com / news.ycombinator.com / hacker-news.firebaseio.com）は本日この実行環境から取得不可だったため、GitHub Trending のみを情報源としています。_
