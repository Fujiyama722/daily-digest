# 🛠️ 週末ハンズオン実践: 2026年07月04日号

おはようございます！フロントエンドメンターの「学習コーチ」です。

今週は Next.js 16.2 のリリースと、React/TypeScript 界隈で「AI に UI を作らせる」流れがさらに加速した1週間でした。土曜の朝は、その流れの中心にある **shadcn-ui/ui** を実際に触って、Tailwind + TypeScript の型の気持ち良さを体感してみましょう。

---

## 本日のハイライト

- **Next.js 16.2.10 リリース** — Vercel の React フレームワークが v16.2.10 に更新（7/1）。総スター数は 140k に到達。個人開発の Next.js プロジェクトを最新にしておく良いタイミングです。 <https://github.com/vercel/next.js>
- **shadcn-ui/ui が 118k スターに** — 「あなたのお気に入りのフレームワークで動く、美しくアクセシブルなコンポーネント集」。npm install ではなく **コードそのものをコピーして取り込む** 新しい配布モデルが定着し、React × TypeScript × Tailwind の事実上の標準UIキットに。 <https://github.com/shadcn-ui/ui>
- **logto-io/logto が今週も伸長（+1,314 stars, 累計 13.6k）** — SaaS / AI アプリ向けの OIDC・OAuth 2.1 ベース認証基盤。TypeScript 97.2%。Supabase Auth を触ったことがある人にとって、より本格的な認証の勉強になるOSS。 <https://github.com/logto-io/logto>
- **simplex-chat/simplex-chat 今週+6,376 stars（累計 17.8k）** — 「ユーザー識別子ゼロ」を掲げるメッセージングOSS。プライバシー設計の実例として、モバイル・デスクトップアプリのアーキテクチャを覗くだけでも価値あり。 <https://github.com/simplex-chat/simplex-chat>

---

## 注目リポジトリ / 事例

1. **shadcn-ui/ui** — <https://github.com/shadcn-ui/ui>
   TypeScript 89.8% / スター 118k。**「ライブラリではなくコード配布プラットフォーム」** という設計思想が肝。`npx shadcn add button` すると、`node_modules` にブラックボックスとして入るのではなく、自分の `components/ui/button.tsx` にコードそのものが降りてくるので、あとから型もスタイルも自由に読める・書き換えられる。
2. **logto-io/logto** — <https://github.com/logto-io/logto>
   TypeScript 97.2% / スター 13.6k。マルチテナント・SSO・RBAC まで一気通貫の認証基盤。Supabase Auth では物足りなくなったときの「次の一歩」として読む価値が高い。
3. **topoteretes/cognee** — <https://github.com/topoteretes/cognee>
   Python 85.1% / スター 26.8k（今週 +4,531）。AIエージェントに「長期記憶」を持たせる知識グラフエンジン。Next.js から API 経由で叩いて、自分の個人開発サービスに「覚えているAI」を組み込む練習台に。

---

## 実践ポイント（さとるさんへ）

今日は 30〜60 分の週末ハンズオンとして、**shadcn-ui/ui を新規 Next.js プロジェクトに導入して Dialog（モーダル）を型付きで動かす** をやってみましょう。LPコーディングの経験がそのまま活きるところ（HTML/セマンティクス/Tailwind クラス）と、TypeScript 型の勉強が同時に進みます。

### ① プロジェクト作成 → shadcn 初期化

```bash
# 1) Next.js プロジェクトを作る（App Router + TS + Tailwind）
npx create-next-app@latest my-ui-lab \
  --typescript --tailwind --app --eslint --src-dir --import-alias "@/*"

cd my-ui-lab

# 2) shadcn を初期化（テーマや components/ui フォルダを作ってくれる）
npx shadcn@latest init

# 3) 使いたいコンポーネントだけ「コードとして」プロジェクトに追加
npx shadcn@latest add button dialog
```

ここで注目してほしいのは、`add` した瞬間に `src/components/ui/button.tsx` と `dialog.tsx` が **自分のリポジトリの中に生まれる** こと。ブラックボックスがないので、名前・色・アニメーションを後から直せます。

### ② TypeScript の型を「読める」ようにする Button のコード例

`src/components/ui/button.tsx`（`shadcn add` で自動生成される中身のイメージ）:

```tsx
import * as React from "react";
import { cva, type VariantProps } from "class-variance-authority";
import { cn } from "@/lib/utils";

// ① variants を「デザインの選択肢」として定義
const buttonVariants = cva(
  "inline-flex items-center justify-center rounded-md text-sm font-medium transition-colors focus-visible:outline-none focus-visible:ring-2 disabled:opacity-50",
  {
    variants: {
      variant: {
        default: "bg-slate-900 text-white hover:bg-slate-800",
        outline: "border border-slate-300 bg-white hover:bg-slate-100",
        ghost: "hover:bg-slate-100",
      },
      size: {
        sm: "h-8 px-3",
        md: "h-10 px-4",
        lg: "h-12 px-6 text-base",
      },
    },
    defaultVariants: { variant: "default", size: "md" },
  }
);

// ② Props の型を「HTMLボタン属性 + variants」から合成
export interface ButtonProps
  extends React.ButtonHTMLAttributes<HTMLButtonElement>,
    VariantProps<typeof buttonVariants> {}

// ③ ref を forwardRef で通しつつ className をマージ
export const Button = React.forwardRef<HTMLButtonElement, ButtonProps>(
  ({ className, variant, size, ...props }, ref) => (
    <button
      ref={ref}
      className={cn(buttonVariants({ variant, size }), className)}
      {...props}
    />
  )
);
Button.displayName = "Button";
```

**初心者向け解説（型の気持ち）**

- `React.ButtonHTMLAttributes<HTMLButtonElement>` は「素のHTML `<button>` が本来受け取れる属性（`onClick` / `disabled` / `type` など）を全部型として貰う」という宣言です。おかげで `<Button onClick={...} disabled />` が「素のボタン」と同じ感覚で書けます。
- `VariantProps<typeof buttonVariants>` は、上で定義した `variants` の中身（`variant: "default" | "outline" | "ghost"` など）を **自動で** 型に変換してくれるユーティリティ。`variant="danger"` などと書くと **IDE の段階で赤線** が出るのはこれのおかげ。
- `extends A, B {}` で **2つの型を合体** させています。TypeScript の「交差型（intersection）」の実用例で、"HTMLボタンの機能" と "自作の見た目バリアント" を1つの `ButtonProps` にまとめています。
- `React.forwardRef` は「親コンポーネントから `ref` を渡せるようにする」ためのラッパー。`<Dialog>` などが内部でフォーカス制御に使うので、UI 部品はほぼ全部 `forwardRef` されています。

### ③ Dialog を実際に使ってみる

`src/app/page.tsx`:

```tsx
"use client";

import { useState } from "react";
import { Button } from "@/components/ui/button";
import {
  Dialog,
  DialogContent,
  DialogHeader,
  DialogTitle,
  DialogTrigger,
} from "@/components/ui/dialog";

export default function Home() {
  const [name, setName] = useState<string>("");

  return (
    <main className="mx-auto max-w-md p-8">
      <Dialog>
        <DialogTrigger asChild>
          <Button variant="outline">お問い合わせを開く</Button>
        </DialogTrigger>

        <DialogContent className="sm:max-w-md">
          <DialogHeader>
            <DialogTitle>お問い合わせ</DialogTitle>
          </DialogHeader>

          <input
            type="text"
            value={name}
            onChange={(e) => setName(e.target.value)}
            placeholder="お名前"
            className="w-full rounded-md border border-slate-300 px-3 py-2"
          />

          <Button
            onClick={() => alert(`${name} さん、送信しました`)}
            disabled={name.length === 0}
          >
            送信
          </Button>
        </DialogContent>
      </Dialog>
    </main>
  );
}
```

**読み解きポイント**

- `useState<string>("")` の `<string>` は「この state は必ず文字列」という型パラメータ。以降 `name` は string として推論され、`name.length` などが型安全に使えます。
- `onChange={(e) => setName(e.target.value)}` の `e` は `React.ChangeEvent<HTMLInputElement>` に自動推論されます。JSX の属性名（`onChange`）が型情報を持っているので、明示 import しなくても補完が効きます。これが「TypeScript × JSX」の大きな旨味。
- `disabled={name.length === 0}` のように、**state から派生した boolean を直接 JSX 属性に流す** のが React らしい書き方です。`if` 文でクラスを付け外しした LP コーディング時代の記述を、宣言的なスタイルに置き換える感覚を掴んでください。
- shadcn の `Dialog` は内部で Radix UI のプリミティブを使っており、Esc キー・フォーカストラップ・アクセシビリティ属性（`aria-*`）が **最初から正しく効いている**。セマンティクスを大切にしてきたさとるさんが一番安心して読める設計です。

### ④ 次の応用アイデア

- **shadcn の `Form` + Zod を追加** → 型駆動のフォームバリデーションに進む
- **Supabase の `insert()` と接続** → お問い合わせを DB に保存し、RLS で「未ログインは insert のみ許可」を書く
- **Tailwind の `dark:` バリアント** で `next-themes` を入れてダークモード対応

---

## 今日の思考トリガー

**「今の自分のLPコーディング案件で shadcn-ui/ui を『Reactに載せた再利用可能なコンポーネント集』として提案できるとしたら、どの案件のどの部品から置き換えると、次の案件の受注単価が上がるだろう？」**

「業務委託のLPコーディング」から「Reactコンポーネント設計もできるフロントエンドエンジニア」への一歩は、**既存の資産（HTML/CSS/セマンティクスの目利き）を持ち込みつつ、型と再利用の言語を1つ足す** ことです。週末の1時間の写経が、月曜の案件相談の会話を変える起点になります。

---

_ソース: <https://github.com/trending?since=weekly> / <https://github.com/shadcn-ui/ui> / <https://github.com/vercel/next.js> / <https://github.com/logto-io/logto> / <https://github.com/topoteretes/cognee> / <https://github.com/simplex-chat/simplex-chat> (2026年07月04日時点)_

_※ Hacker News Algolia の週次スレッド一覧は本日 HTTP 403 のため取得できませんでした。_
