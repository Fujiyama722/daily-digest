# 🎨 トレンドUI実装チュートリアル: 2026年06月09日号

おはようございます！シニアUI/UXデザイナー兼フロントエンドエンジニアの「学習コーチ」です。

今日は「**Generative UI（生成UI）**」というUIトレンドを、React + Next.js + TypeScript + TailwindCSS で実装する基礎を一緒に学んでいきましょう。AI時代の新しいUIパラダイムです。

---

## 本日のハイライト

### 1. 「Generative UI」がフロントエンド界の新トレンドに

今週のGitHub TypeScriptトレンド週間ランキングで、`CopilotKit/CopilotKit` が **34,077スター**を獲得し、Reactフロントエンドのトップ層に入りました。タグラインは "The Frontend Stack for Agents & Generative UI" で、React, Angular, Mobileに対応しています。

ポイントは「AIが応答内容に合わせてUIコンポーネントを動的に描画する」設計思想。チャットボックスにテキストだけを返すのではなく、状況に応じてカード・フォーム・グラフを生成する──これがGenerative UIです。

ソース: https://github.com/CopilotKit/CopilotKit （TypeScript 78.9% / MIT License）

### 2. 「メモリレイヤー」とUIの一体化

同じくTypeScriptトレンド上位の `supermemoryai/supermemory`（**26,185スター**）は "the memory and context layer for AI" として、ユーザーごとの記憶を約50msで呼び出せるAPIを提供。これと組み合わせると「ユーザーごとに違うUI」が現実的になります。

ソース: https://github.com/supermemoryai/supermemory

### 3. JavaScriptトレンドではSvelteも依然強い

JavaScript週間トレンドでは `sveltejs/svelte` が **87,150スター**（今週+488）を獲得。Reactとは別軸の「コンパイラ型フレームワーク」として、軽量UIの選択肢として根強い人気です。

ソース: https://github.com/sveltejs/svelte

※本日Hacker News API（hn.algolia.com / hacker-news.firebaseio.com）はいずれもHTTP 403で取得できませんでした。代替としてGitHub Trending週間データを主軸にしています。

---

## 注目リポジトリ / 事例

### 1. CopilotKit/CopilotKit
- **概要**: React/Next.js向けのAIフロントエンドフレームワーク。Chat UI・Generative UI・Shared State・Human-in-the-Loopが揃う
- **スター数**: 34,077（週間トレンドTypeScript上位）
- **ライセンス**: MIT（無料利用可）。ただしLLMのAPI利用料（OpenAI等）は別途必要
- **ソース**: https://github.com/CopilotKit/CopilotKit

### 2. supermemoryai/supermemory
- **概要**: AI向けの記憶・コンテキスト層。RAGとパーソナライズ記憶を1クエリで扱える
- **スター数**: 26,185
- **ライセンス**: リポジトリ参照（ホスト版APIは課金あり、セルフホストはOSS）
- **ソース**: https://github.com/supermemoryai/supermemory

### 3. sveltejs/svelte
- **概要**: コンパイラ型UIフレームワーク。仮想DOMを使わず軽量
- **スター数**: 87,150（週間+488）
- **ライセンス**: MIT
- **ソース**: https://github.com/sveltejs/svelte

---

## 実践ポイント（さとるさんへ）

さとるさんの個人開発スタック（**React / Next.js / TypeScript / TailwindCSS / Supabase**）と相性が良い CopilotKit を例に、**「Generative UIの最小サンプル」** を一緒に組み立てましょう。

### UX/CVR視点での意味

LPコーディングをやっているさとるさんなら、**「フォーム離脱率」** に心当たりがあるはず。Generative UIは「ユーザーの入力途中の文脈に合わせて、必要なフィールドだけ動的に出す」というアプローチを取れます。固定フォームと比べてCVR（コンバージョン率）改善が期待できる設計思想です。

### Step1: 型を先に定義する（TypeScriptの心臓部）

```tsx
// src/types/recommendation.ts

// 「商品レコメンド」というUIブロックの "形" を型で表現する
export type ProductCard = {
  productId: string;
  name: string;
  priceJpy: number;
  imageUrl: string;
  reason: string; // なぜAIがこれを推したか
};

// AIから受け取るレスポンスの取りうる形を Union 型で限定する
export type AiUiBlock =
  | { kind: "message"; text: string }
  | { kind: "product"; payload: ProductCard }
  | { kind: "form"; fields: Array<{ name: string; label: string }> };
```

**ここの解説（初心者向け）**:
- `type` は「データの形の設計図」。実行時には消えますが、エディタ上で間違いを即指摘してくれる安全装置です
- `|`（パイプ）は **Union型**。「どれかひとつ」を表現します。`kind` を見ればどの形かが特定できる仕組みを **Discriminated Union** と呼びます

### Step2: Generative UIのレンダラを書く

```tsx
// src/components/AiBlock.tsx
import type { AiUiBlock } from "@/types/recommendation";

type Props = { block: AiUiBlock };

export function AiBlock({ block }: Props) {
  // kind の値で分岐すると、TypeScriptが
  // それぞれのブロックで何が使えるかを自動推論してくれる
  switch (block.kind) {
    case "message":
      return (
        <p className="rounded-2xl bg-slate-100 px-4 py-3 text-slate-800">
          {block.text}
        </p>
      );

    case "product": {
      const { name, priceJpy, imageUrl, reason } = block.payload;
      return (
        <article className="flex gap-4 rounded-2xl border border-slate-200 p-4 shadow-sm">
          <img src={imageUrl} alt={name} className="h-20 w-20 rounded-xl object-cover" />
          <div className="flex flex-col gap-1">
            <h3 className="text-base font-semibold text-slate-900">{name}</h3>
            <p className="text-sm text-slate-500">¥{priceJpy.toLocaleString()}</p>
            <p className="text-xs text-emerald-700">理由: {reason}</p>
          </div>
        </article>
      );
    }

    case "form":
      return (
        <form className="flex flex-col gap-3">
          {block.fields.map((f) => (
            <label key={f.name} className="flex flex-col gap-1 text-sm">
              <span className="text-slate-700">{f.label}</span>
              <input
                name={f.name}
                className="rounded-lg border border-slate-300 px-3 py-2 focus:border-indigo-500 focus:outline-none"
              />
            </label>
          ))}
        </form>
      );
  }
}
```

**ここの解説**:
- `switch (block.kind)` で分岐すると、`case "product"` の中では `block.payload` が「必ず ProductCard 型」だと TypeScript が分かります。これが **型の絞り込み（narrowing）** です
- 「if文の代わりにswitch」を使うのは、`AiUiBlock` に新しい kind を増やしたとき、`case` を書き忘れると TS が警告してくれるから（網羅性チェック）
- TailwindCSSの `rounded-2xl` `shadow-sm` `text-slate-900` などは**ユーティリティクラス**。CSSファイルを書かずにデザインを当てます

### Step3: APIから受け取った配列をマッピング

```tsx
// src/app/chat/page.tsx
import { AiBlock } from "@/components/AiBlock";
import type { AiUiBlock } from "@/types/recommendation";

async function getBlocks(): Promise<AiUiBlock[]> {
  // 実際は CopilotKit や自作APIから取得
  const res = await fetch("/api/ai/blocks", { cache: "no-store" });
  return res.json();
}

export default async function ChatPage() {
  const blocks = await getBlocks();

  return (
    <main className="mx-auto flex max-w-xl flex-col gap-3 p-6">
      {blocks.map((b, i) => (
        <AiBlock key={i} block={b} />
      ))}
    </main>
  );
}
```

**ここの解説**:
- Next.js の App Router では、ページコンポーネント自身を `async` にして、サーバー側でデータ取得できます（**Server Components**）。LPの初期表示速度に効きます
- `Promise<AiUiBlock[]>` の `<...>` は **ジェネリクス**。「中身の型」を呼び出し側に教える書き方です

### 段階的に試すなら

1. **今日**: 上記 Step1〜3 を `apps/web` 配下にコピーして、ダミーデータで描画確認
2. **明日**: `npm i @copilotkit/react-core @copilotkit/react-ui` を入れて、CopilotKit のチャットUIに差し替え（OpenAI API キーが必要、無料枠の範囲で）
3. **週末**: Supabase の `products` テーブルから `ProductCard` を返す API Route を実装

---

## 今日の思考トリガー

**「あなたが今LPでコーディングしている『固定フォーム』のうち、ひとつでも『ユーザーの直前の選択肢によってフィールドが変わる』設計に変えたら、CVRはどれくらい動きそうですか？ そして、その動的フォームを CopilotKit の Generative UI で組み直すなら、最初に消すべき入力欄はどれですか？」**

_ソース: https://github.com/trending/typescript?since=weekly , https://github.com/trending/javascript?since=weekly , https://github.com/CopilotKit/CopilotKit , https://github.com/supermemoryai/supermemory , https://github.com/sveltejs/svelte (2026年06月09日時点)_
