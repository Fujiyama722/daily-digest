# 🛠️ 週末ハンズオン実践: 2026年04月11日号

おはようございます！フロントエンドメンターの「学習コーチ」です。

土曜日は、さとるさんが実際に手を動かして"身につける"日です。今日は、今週GitHubで急上昇した Repomix を軸に、**Claude Code × Next.js × Supabase** という個人開発スタックをもう一段加速させる週末ワークを一緒にやっていきましょう。最後には、今日の学びをそのまま個人開発プロジェクトに転用できる TypeScript のコード例も用意しました。

---

## 本日のハイライト

### 🔥 今週試したい「AIフレンドリー」なリポジトリ群

今週の GitHub Trending（TypeScript 部門）で目立ったのは、**AI コーディング体験そのものを拡張するツール**が上位を占めたことです。

- **yamadashy/repomix** ⭐ 23,352（+459/週） — リポジトリ全体を1つの "AI が読みやすいファイル" にパックしてくれる TypeScript 製ツール。`.gitignore` を尊重した賢いフィルタ、`--compress` で Tree-sitter ベースのトークン削減、Secretlint による秘密情報検知までこの1つで揃います。Claude Code 利用者にとって、**巨大な個人開発プロジェクトを Claude に丸ごと把握させる**ための最短ルートです。
  - ソース: [github.com/yamadashy/repomix](https://github.com/yamadashy/repomix)

- **multica-ai/multica** ⭐ 5,999（+3,201/週） — コーディングエージェントを"チームメイト"化するオープンソース・プラットフォーム。タスク割り当て・進捗追跡・スキル蓄積を管理できる、"エージェント PM" 的存在。
  - ソース: [github.com/trending/typescript?since=weekly](https://github.com/trending/typescript?since=weekly)

- **siddharthvaddem/openscreen** ⭐ 27,578（+12,278/週） — 透かしなし・商用利用可の無料デモ動画作成ツール（TypeScript 製）。昨日のレポートでも紹介しましたが、引き続き上位に。個人開発プロダクトの LP にそのまま載せられる品質のデモが作れます。
  - ソース: [github.com/trending/typescript?since=weekly](https://github.com/trending/typescript?since=weekly)

### 🧩 なぜ今"Repomix"が週末ワークにぴったりなのか

Claude Code は「1 ファイルずつ Read する」能力を持ちますが、**アーキテクチャ全体を俯瞰してリファクタを提案する**には、リポジトリ全体の文脈が必要になる場面があります。Repomix はそのギャップを埋めるツールで、

```bash
npx repomix@latest
```

の1行で `repomix-output.xml` という単一ファイルが生成されます。これを Claude の Projects 機能や ChatGPT にそのまま食わせれば、**「このプロジェクト全体を見てアーキテクチャの弱点を指摘して」** といった高次の相談ができるようになります。Claude Code と併用することで「局所的な実装は CLI、全体俯瞰は Web UI」という二刀流が完成します。

- ソース: [github.com/yamadashy/repomix](https://github.com/yamadashy/repomix)

---

## 注目リポジトリ / 事例

### 1. yamadashy/repomix ⭐ 23,352
AI に渡すための "コードベース・パッキングツール"。`.gitignore` 尊重、トークンカウント、`--compress` オプション、Secretlint 連携、VSCode 拡張まで完備。週末にまず `npx repomix@latest` を個人リポジトリで試すのが今日の一歩です。
- ソース: [github.com/yamadashy/repomix](https://github.com/yamadashy/repomix)

### 2. multica-ai/multica ⭐ 5,999
"managed agents platform" を名乗り、複数のコーディングエージェントをあたかも同僚のようにタスク管理できる OSS。個人開発者でも、Claude Code・Codex・Gemini CLI を役割分担して使う時代が見えてきました。
- ソース: [github.com/trending/typescript?since=weekly](https://github.com/trending/typescript?since=weekly)

### 3. YishenTu/claudian ⭐ 7,218
Obsidian に Claude Code を "AI コラボレーター" として埋め込むプラグイン。ノート＝長期記憶として Claude が読み書きできる構造は、個人開発の設計メモ管理と非常に相性が良いです。
- ソース: [github.com/trending/typescript?since=weekly](https://github.com/trending/typescript?since=weekly)

---

## 実践ポイント（さとるさんへ）

### STEP 1: Repomix を個人リポジトリで試す（5分）

まずは手を動かす。ターミナルを開いて、個人開発プロジェクトのルートで以下を実行するだけです。

```bash
# インストール不要、そのまま実行
cd ~/your-nextjs-project
npx repomix@latest

# 生成結果を確認
ls -lh repomix-output.xml
```

`.gitignore` は自動で尊重されるので、`node_modules` や `.env` が紛れ込む心配はありません。生成された `repomix-output.xml` を Claude の Web UI にアップロードして、**「このプロジェクトのディレクトリ構成の責務分離を評価して、改善点を3つ挙げて」** と聞いてみてください。

### STEP 2: Next.js + Supabase で "型安全なデータ取得ヘルパー" を作る（30分）

ここからが今日のコード・ハンズオンです。個人開発で Supabase を使うとき、いつも同じような `select` → `error チェック` → `型アサーション` を書いていませんか？ これを **ジェネリクス（generics）** を使った再利用可能なヘルパーにまとめてみましょう。

`lib/supabase/fetchOne.ts` を新規作成してください。

```ts
// lib/supabase/fetchOne.ts
import { createClient } from "@/lib/supabase/server";

// 💡 <T> は「型の引数」。呼び出し側がどんな型を返してほしいか指定できます。
//    関数を書く側は "まだ決まっていない型" として T を扱えるので、
//    1つの関数でどのテーブルにも対応できる「再利用可能な道具」になります。
export async function fetchOne<T>(
  table: string,
  column: string,
  value: string | number,
): Promise<T | null> {
  const supabase = await createClient();

  // .single() は「1件だけ取得」。0件や2件以上だとエラーになります。
  const { data, error } = await supabase
    .from(table)
    .select("*")
    .eq(column, value)
    .single();

  if (error) {
    // 本番では logger に送る。今は学習用に console でOK。
    console.error(`[fetchOne] ${table}.${column}=${value}`, error.message);
    return null;
  }

  // data は unknown 相当なので、呼び出し側が指定した T として扱う。
  // ここで as を使う代わりに zod などで検証する流派もあります（後述）。
  return data as T;
}
```

**ポイント解説（初心者向け）:**

- `<T>` は "関数版ジェネリクス" です。配列の `Array<string>` で見る `<string>` と同じ仕組み。関数を書く時点では型を決めず、**使う時に後から型を注入できる**のが強みです。
- `Promise<T | null>` は「非同期で、成功すれば T、失敗すれば null を返す」という約束。`await` した側で `if (user === null) return notFound();` と安全に分岐できます。
- `as T` は "型アサーション" と呼ばれ、「私は中身が T だと保証します」と TypeScript に伝える構文です。**便利ですが嘘をつける**ので、本当に安全にするには次のステップで紹介する zod 検証を挟むのが定石です。

### STEP 3: Server Component でそのまま使う

`app/users/[id]/page.tsx` でこのヘルパーを呼び出すと、1行でデータ取得が完結します。

```tsx
// app/users/[id]/page.tsx
import { notFound } from "next/navigation";
import { fetchOne } from "@/lib/supabase/fetchOne";

// 💡 取得したい行の"形"を type で宣言する。これが T の実体になります。
type User = {
  id: string;
  name: string;
  bio: string | null;
  created_at: string;
};

// Next.js 15 以降、params は Promise で渡ってきます（async で await する）
export default async function UserPage({
  params,
}: {
  params: Promise<{ id: string }>;
}) {
  const { id } = await params;

  // ← ここで <User> を指定すると、戻り値が User | null に確定する！
  const user = await fetchOne<User>("users", "id", id);

  if (!user) return notFound();

  return (
    <main className="mx-auto max-w-2xl px-6 py-12">
      <h1 className="text-3xl font-bold tracking-tight text-slate-900">
        {user.name}
      </h1>
      <p className="mt-4 whitespace-pre-wrap text-slate-600">
        {user.bio ?? "自己紹介はまだありません。"}
      </p>
      <time className="mt-6 block text-sm text-slate-400">
        登録日: {new Date(user.created_at).toLocaleDateString("ja-JP")}
      </time>
    </main>
  );
}
```

**Tailwind のポイント:**

- `mx-auto max-w-2xl` で中央寄せ＋読みやすい幅（約 672px）に制限。長文ページの基本形です。
- `tracking-tight` は `letter-spacing: -0.025em`。見出しを少しだけ引き締めると読みやすさが上がります。
- `whitespace-pre-wrap` は DB に改行込みで保存された文章を**そのまま改行表示**するためのクラス。LP コーダーの経験が活きるディテールです。

### STEP 4: Claude Code に "次の一手" を相談する

最後に、今日作った `fetchOne` をさらに堅牢にするため、Claude Code で以下のプロンプトを投げてみましょう。

> 「lib/supabase/fetchOne.ts を zod でランタイム検証するバージョンに書き換えて。
> 型引数 T の代わりに ZodSchema を受け取る形にして、失敗時は throw ではなく Result 型で返してほしい。」

これができると「`as` による型アサーションではなく、**実行時にも型が保証される**」堅牢なデータ取得層に進化します。次の週末テーマにピッタリの宿題です。

---

## 今日の思考トリガー

**「今日作った `fetchOne<T>` は、さとるさんの個人開発プロジェクトのどのテーブル（posts / comments / profiles …）で最初に使えそうですか？ そして、Repomix で丸ごとパックしたコードを Claude に読ませたとき、"このヘルパーを使えば消せるコードはどこ？"と聞くと、何箇所くらい出てくると思いますか？」**

_ソース: [GitHub Trending Weekly](https://github.com/trending?since=weekly), [GitHub Trending TypeScript](https://github.com/trending/typescript?since=weekly), [github.com/yamadashy/repomix](https://github.com/yamadashy/repomix) (2026年04月11日時点)_
