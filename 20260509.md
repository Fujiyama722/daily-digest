# 🛠️ 週末ハンズオン実践: 2026年05月09日号

おはようございます！フロントエンドメンターの「学習コーチ」です。

土曜日は手を動かす日。今週のGitHubトレンドを眺めると、TypeScript/JavaScriptランキングが「AIエージェント系」一色でした。とはいえ、さとるさんがいきなりエージェント基盤を作るのは現実的ではないので、今日は **「エージェントの出力を読みやすく見せるチャットUIをReact + TypeScript + TailwindCSSで作る」** という、現場で必ず使える小さな部品づくりをやります。型・条件付きスタイル・コンポーネント分割の3点を一気に体験できるメニューです。

---

## 本日のハイライト

- **GitHub Trending全体の1位は `ruvnet/ruflo`（TypeScript製のClaude向けエージェント・オーケストレーション基盤）**。週間で +11,930スター。今週のトレンドが「単発のAI機能」から「エージェントを束ねて動かす土台」に動いていることがよく分かります。ソース: https://github.com/trending?since=weekly
- **JavaScript Trending1位は `browserbase/skills`**（週間 +1,850スター）。Claude CodeにWebブラウザ操作スキルを追加する公式プラグイン集で、`npx skills add browserbase/skills` で導入できる仕組み。**Browserbaseのクラウド機能はAPIキー（有料プラン）が必要**ですが、ローカルで触る範囲なら無料で試せます。ソース: https://github.com/trending/javascript?since=weekly
- **TypeScript Trending内では `microsoft/vscode` が週+337スターで5位に再浮上**。エディタ自体がTypeScriptで書かれている事実は、「TSは大規模アプリでこそ効く」という証拠で、さとるさんが今TSを学んでいる方向は正しいです。ソース: https://github.com/trending/typescript?since=weekly

---

## 注目リポジトリ / 事例

### 1. ruvnet/ruflo — Claude向けエージェント・オーケストレーション基盤（TypeScript）
複数のAIエージェントを協調動作させるためのフレームワーク。今週GitHub全体トレンド1位。総スター 46,793 / 週間 +11,930。読むだけでも「TypeScriptで型を効かせて複雑な状態を扱う設計」のお手本になります。
URL: https://github.com/ruvnet/ruflo

### 2. browserbase/skills — Claude Code拡張スキル集（JavaScript）
Claude Codeに「ブラウザを操作する」「UIテストをAIに任せる」などの能力を後付けできるプラグイン集。総スター 2,715 / 週間 +1,850。**ローカルChromeでの動作は無料、クラウド経由のステルス機能はBrowserbaseの有料アカウントが必要**。
URL: https://github.com/browserbase/skills

### 3. louislam/uptime-kuma — セルフホスト型監視ツール（JavaScript / Node.js + Vue）
自前のサービスを「死活監視」するOSS。総スター 86,393 / 週間 +343。個人開発を本気で続けるなら、自分のVercelデプロイを監視するのに使えます。コードベースが手頃で「JSで作る本物のWebアプリ」の良い読み物です。
URL: https://github.com/louislam/uptime-kuma

---

## 実践ポイント（さとるさんへ）

今日のハンズオン: **「ChatBubble」コンポーネントをReact + TypeScript + TailwindCSSで作る**。

ruvnetやbrowserbaseのようなAIエージェント基盤の出力を、自分のNext.jsアプリに表示するときに必ず必要になる部品です。LP制作の経験があるさとるさんなら、HTML/CSSの感覚でほとんど読めます。注目してほしいのは「**TypeScriptがどこで何を守ってくれているか**」です。

```tsx
// app/components/ChatBubble.tsx
import { type ReactNode } from "react";

// 1. 「役割」を文字列リテラルの和集合で固定する。
//    これで role に "robot" などタイプミスを書くとビルド時に怒ってくれる。
type Role = "user" | "assistant";

// 2. Props（受け取る値）の形を type で宣言。
//    children を ReactNode にしておくと、文字列でもJSXでも受け取れる。
type ChatBubbleProps = {
  role: Role;
  timestamp: string; // 例: "07:30"
  pending?: boolean; // ?つきは「省略してOK」の意味
  children: ReactNode;
};

export function ChatBubble({
  role,
  timestamp,
  pending = false, // 省略されたら false が入る（デフォルト引数）
  children,
}: ChatBubbleProps) {
  // 3. 役割ごとに見た目を変える。三項演算子は「条件 ? Aの場合 : Bの場合」。
  const isUser = role === "user";

  // 4. Tailwindのクラスを「条件で組み立てる」基本パターン。
  //    テンプレートリテラル（バッククォート）の中に ${} で式を埋める。
  const bubbleClass = `
    max-w-[80%] rounded-2xl px-4 py-2 text-sm leading-relaxed
    ${isUser
      ? "ml-auto bg-blue-600 text-white"            // ユーザー: 右寄せ・青
      : "mr-auto bg-slate-100 text-slate-900"}       // AI: 左寄せ・グレー
    ${pending ? "opacity-60 animate-pulse" : ""}     // 生成中はうっすら点滅
  `;

  return (
    <div className="flex flex-col gap-1">
      <div className={bubbleClass}>{children}</div>
      <span
        className={`text-xs text-slate-500 ${isUser ? "text-right" : "text-left"}`}
      >
        {timestamp}
      </span>
    </div>
  );
}
```

使い方:

```tsx
// app/page.tsx の中などで
<main className="mx-auto flex max-w-xl flex-col gap-3 p-4">
  <ChatBubble role="user" timestamp="07:30">
    今週のGitHubトレンドを3行で要約して
  </ChatBubble>
  <ChatBubble role="assistant" timestamp="07:30" pending>
    生成中…
  </ChatBubble>
</main>
```

**ここで体験してほしい3つの「型のごほうび」**

1. `role="robot"` と書くと、エディタが赤線を引く。実行する前に間違いに気づける。
2. `timestamp` を渡し忘れると、これも赤線。**「必須項目を忘れない仕組み」が無料で手に入る**。
3. `pending` は省略可能。`?:` の有無で「必須／任意」がコードに残るので、半年後の自分が読んでも迷わない。

**手順（30〜60分）**

1. 既存のNext.jsプロジェクト（無ければ `npx create-next-app@latest --ts --tailwind`）に `app/components/ChatBubble.tsx` を作って上のコードを貼る。
2. `app/page.tsx` で2〜3個並べて、ブラウザで見た目を確認する。
3. **わざと型エラーを起こしてみる**。`role="bot"` に書き換えて赤線が出るのを確認 → これがTSが守ってくれている瞬間です。
4. 余裕があれば `role` に `"system"` を追加してみる。`type Role` の1行を直すだけで、使い手側に「systemも書けるよ」と伝わるのを体感してください。

ポイントは **「派手な機能を作らない」** こと。小さなコンポーネントを1つ完璧に理解する方が、シニアへの距離は確実に縮みます。

---

## 今日の思考トリガー

**「自分が今LPで書いているCSSのうち、`isUser` のような『状態で見た目を切り替える』部分はどこだろう？ それをReactコンポーネントにしたとき、Propsとして外に出すべき値は何で、内側に隠すべき値は何か？」**

_ソース: https://github.com/trending?since=weekly , https://github.com/trending/javascript?since=weekly , https://github.com/trending/typescript?since=weekly , https://github.com/browserbase/skills (2026年05月09日時点)_
