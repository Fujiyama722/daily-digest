# 🎨 トレンドUI実装チュートリアル: 2026年03月31日号

おはようございます！シニアUI/UXデザイナー兼フロントエンドエンジニアの「学習コーチ」です。

今日は今週GitHubでトレンド入りしているKanbanボードアプリ **PLANKA** を参考に、**ドラッグ&ドロップ対応のKanbanボードUI**をReact/Next.js/TypeScript/TailwindCSSで実装する方法を解説します。

---

## 本日のハイライト

### 1. PLANKA — オープンソースKanbanボードが急上昇中（★11,759）
セルフホスト型のプロジェクト管理ツール。Reactベースのフロントエンドで、リアルタイム同期・ドラッグ&ドロップ・リッチテキスト編集を搭載。TrelloやNotionのKanbanビューに匹敵する機能をOSSで提供しています。
🔗 https://github.com/plankanban/planka

### 2. easy-vibe — AIでアプリを作る「バイブコーディング」教材（★4,782）
「話せればアプリが作れる」をコンセプトにした学習プラットフォーム。Supabase・Stripe決済・Claude Codeを活用したフルスタック開発をステップ別に学べます。さとるさんの個人開発スタックとほぼ同じ構成で、非常に参考になります。
🔗 https://github.com/datawhalechina/easy-vibe

### 3. everything-claude-code — Claude Code最適化システム（★119,305）
136以上のスキル定義と30の専門サブエージェントを搭載したClaude Codeの拡張システム。TypeScript/Pythonなど言語別レビュアーやセキュリティスキャナーも含まれています。
🔗 https://github.com/affaan-m/everything-claude-code

---

## 注目リポジトリ

| リポジトリ | 概要 | Stars |
|---|---|---|
| [planka](https://github.com/plankanban/planka) | セルフホスト型Kanbanボード。React + Node.js + Docker | ★11,759 |
| [easy-vibe](https://github.com/datawhalechina/easy-vibe) | AIバイブコーディング学習教材。Supabase/Stripe対応 | ★4,782 |
| [claude-hud](https://github.com/jarrodwatts/claude-hud) | Claude Codeのコンテキスト使用量・ツール状況を可視化するプラグイン | ★15,353 |

---

## トレンドUI実装チュートリアル: Kanbanボード

### UX/CVR視点での解説

Kanbanボードは**タスクの状態を視覚的に管理**するUIパターンです。ユーザーがカードをドラッグして列（ステータス）間を移動できることで、以下のUXメリットがあります。

- **直感的操作**: マウス操作だけでステータス変更が完了 → 操作ステップ削減
- **全体俯瞰**: 全タスクの進捗が一画面で把握可能 → 意思決定速度UP
- **達成感の可視化**: 「Done」列にカードが溜まることでモチベーション維持

SaaSやプロジェクト管理ツールでは、このUIがあるだけで**継続利用率（リテンション）が向上**するため、個人開発のプロダクトにも積極的に取り入れたいパターンです。

---

### 実装コード例: ドラッグ&ドロップKanbanボード

#### ステップ1: 型定義（TypeScript）

```typescript
// types/kanban.ts

// カード1枚のデータ構造を定義する「型（type）」
// typeは「このデータはこういう形ですよ」という設計図のようなもの
type Card = {
  id: string;        // 一意な識別子（データベースのIDに相当）
  title: string;     // カードのタイトル
  description: string; // カードの説明文
};

// Kanbanの「列」を表す型
// columnsの中にCardsの配列が入るイメージ
type Column = {
  id: string;        // 列の識別子
  title: string;     // 列のタイトル（例: "Todo", "In Progress", "Done"）
  cards: Card[];     // この列に含まれるカードの配列
};

// ドラッグ操作の結果を表す型
// 「どのカードを」「どこから」「どこへ」移動したかを記録
type DragResult = {
  cardId: string;
  sourceColumnId: string;
  targetColumnId: string;
  targetIndex: number;  // 移動先の列内での位置（0始まり）
};
```

> **初心者向け解説**: TypeScriptの `type` は「データの形」を事前に宣言するものです。JavaScriptでは `{id: "1", title: "タスク"}` のようなオブジェクトを自由に作れますが、TypeScriptでは「idは必ずstringで、titleも必ずstringで…」と約束することで、タイプミスや入れ忘れをエディタが教えてくれます。

---

#### ステップ2: Kanbanボードコンポーネント

```tsx
// components/KanbanBoard.tsx
"use client"; // Next.js App Routerでクライアントコンポーネントとして動作させる宣言

import { useState, DragEvent } from "react";

// --- 型定義（実際はファイル分割推奨）---
type Card = {
  id: string;
  title: string;
  description: string;
};

type Column = {
  id: string;
  title: string;
  cards: Card[];
};

// --- 初期データ ---
const initialColumns: Column[] = [
  {
    id: "todo",
    title: "📋 Todo",
    cards: [
      { id: "1", title: "LP設計", description: "ワイヤーフレーム作成" },
      { id: "2", title: "API設計", description: "Supabaseテーブル定義" },
    ],
  },
  {
    id: "in-progress",
    title: "🔨 In Progress",
    cards: [
      { id: "3", title: "ヘッダー実装", description: "レスポンシブ対応" },
    ],
  },
  {
    id: "done",
    title: "✅ Done",
    cards: [],
  },
];

export default function KanbanBoard() {
  // useState: コンポーネントの「状態（データ）」を管理するReactの仕組み
  // columns = 現在のデータ、setColumns = データを更新する関数
  const [columns, setColumns] = useState<Column[]>(initialColumns);

  // 現在ドラッグ中のカード情報を保持する状態
  const [draggedCard, setDraggedCard] = useState<{
    card: Card;
    sourceColumnId: string;
  } | null>(null);
  // ↑ null は「まだ何もドラッグしていない」状態を表す

  // --- ドラッグ開始時の処理 ---
  // カードをつかんだ瞬間に呼ばれる関数
  const handleDragStart = (card: Card, columnId: string) => {
    setDraggedCard({ card, sourceColumnId: columnId });
  };

  // --- ドロップ許可 ---
  // デフォルトではドロップを受け付けないため、preventDefault()で許可する
  const handleDragOver = (e: DragEvent<HTMLDivElement>) => {
    e.preventDefault();
  };

  // --- ドロップ時の処理 ---
  // カードを離した瞬間に呼ばれる関数
  const handleDrop = (targetColumnId: string) => {
    if (!draggedCard) return; // ドラッグ中のカードがなければ何もしない

    // 同じ列にドロップした場合は何もしない
    if (draggedCard.sourceColumnId === targetColumnId) {
      setDraggedCard(null);
      return;
    }

    // columns配列を新しい配列に変換（イミュータブル更新）
    setColumns((prev) =>
      prev.map((col) => {
        // 元の列からカードを除去
        if (col.id === draggedCard.sourceColumnId) {
          return {
            ...col, // スプレッド構文: 既存のプロパティをコピー
            cards: col.cards.filter((c) => c.id !== draggedCard.card.id),
          };
        }
        // 移動先の列にカードを追加
        if (col.id === targetColumnId) {
          return {
            ...col,
            cards: [...col.cards, draggedCard.card],
          };
        }
        // それ以外の列はそのまま返す
        return col;
      })
    );

    setDraggedCard(null); // ドラッグ状態をリセット
  };

  return (
    <div className="flex gap-4 p-6 min-h-screen bg-gray-50">
      {columns.map((column) => (
        <div
          key={column.id}
          onDragOver={handleDragOver}
          onDrop={() => handleDrop(column.id)}
          className="flex-1 min-w-[280px] max-w-[360px] bg-gray-100
                     rounded-xl p-4 shadow-sm"
        >
          {/* 列ヘッダー */}
          <div className="flex items-center justify-between mb-4">
            <h2 className="font-bold text-gray-700">{column.title}</h2>
            <span className="text-sm text-gray-400 bg-gray-200
                           rounded-full px-2 py-0.5">
              {column.cards.length}
            </span>
          </div>

          {/* カード一覧 */}
          <div className="flex flex-col gap-3">
            {column.cards.map((card) => (
              <div
                key={card.id}
                draggable          // HTML標準のドラッグ可能属性
                onDragStart={() => handleDragStart(card, column.id)}
                className="bg-white rounded-lg p-4 shadow-sm border
                         border-gray-200 cursor-grab active:cursor-grabbing
                         hover:shadow-md transition-shadow duration-200"
              >
                <h3 className="font-semibold text-gray-800 text-sm">
                  {card.title}
                </h3>
                <p className="text-gray-500 text-xs mt-1">
                  {card.description}
                </p>
              </div>
            ))}
          </div>

          {/* 空の列にドロップしやすくするための領域 */}
          {column.cards.length === 0 && (
            <div className="border-2 border-dashed border-gray-300
                          rounded-lg p-8 text-center text-gray-400 text-sm">
              ここにカードをドロップ
            </div>
          )}
        </div>
      ))}
    </div>
  );
}
```

> **初心者向け解説 — ロジックの流れ**:
> 1. `useState` で列データとドラッグ中のカード情報を管理
> 2. カードに `draggable` 属性をつけてドラッグ可能にする
> 3. `onDragStart` → つかんだカードの情報を state に保存
> 4. `onDragOver` → `preventDefault()` でドロップを許可
> 5. `onDrop` → state を更新して、元の列からカードを消し、新しい列に追加
>
> **スプレッド構文 `...col`** は「元のオブジェクトの全プロパティをコピーして、一部だけ上書きする」書き方です。Reactでは元のデータを直接変更せず、新しいオブジェクトを作って `setState` に渡す（**イミュータブル更新**）のがルールです。

---

#### ステップ3: ページに配置（Next.js App Router）

```tsx
// app/kanban/page.tsx
import KanbanBoard from "@/components/KanbanBoard";

export default function KanbanPage() {
  return (
    <main>
      <KanbanBoard />
    </main>
  );
}
```

---

### 発展: Supabase連携のヒント

実際のプロダクトでは、カードの移動をデータベースに保存する必要があります。

```typescript
// handleDrop内でSupabaseに保存する例
import { createClient } from "@supabase/supabase-js";

const supabase = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL!,
  process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
);

// カードのcolumn_idとposition（順番）を更新
await supabase
  .from("cards")
  .update({ column_id: targetColumnId, position: targetIndex })
  .eq("id", draggedCard.card.id);
```

PLANKAのようなリアルタイム同期も、SupabaseのRealtime機能を使えば実現可能です。

---

## 実践ポイント（さとるさんへ）

1. **まずはコピペで動かす**: 上記のKanbanBoardコンポーネントをそのままNext.jsプロジェクトに配置して動作確認してみましょう。HTML/CSSの知識がベースにあるので、TailwindCSSのクラス名は直感的に理解できるはずです。

2. **型定義から設計を始める習慣**: `Card`型や`Column`型を先に書くことで「このデータには何が必要か？」を整理できます。これはDB設計にも直結するスキルです。

3. **PLANKAのコードを読んでみる**: OSSのコードリーディングは最高の学習教材です。`/client`ディレクトリのReactコンポーネント構成を眺めるだけでも、大規模アプリの設計感覚が身につきます。

4. **easy-vibeの教材もチェック**: さとるさんと同じSupabase + AI開発のスタックで学べる教材です。Stage 2のフルスタック開発セクションが特におすすめ。

---

## 今日の思考トリガー

**「あなたの個人開発プロダクトに『ドラッグ&ドロップ』を加えるとしたら、どの画面のどの操作に使いますか？ ユーザーが"つかんで動かしたくなる"データは何でしょう？」**

---

_ソース:_
- _https://github.com/trending/javascript?since=weekly (2026年03月31日時点)_
- _https://github.com/plankanban/planka (2026年03月31日時点)_
- _https://github.com/datawhalechina/easy-vibe (2026年03月31日時点)_
- _https://github.com/affaan-m/everything-claude-code (2026年03月31日時点)_
