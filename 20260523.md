# 🛠️ 週末ハンズオン実践: 2026年5月23日号

おはようございます！フロントエンドメンターの「学習コーチ」です。

土曜日は今週学んだことを手を動かして確かめる日。今日は今週特に伸びている AI コーディング系ツールを題材に、Next.js + TypeScript + TailwindCSS で「ローカルAPIを叩いて結果を表示する」という、個人開発で頻出のパターンを写経できる粒度で組み立てます。

---

## 本日のハイライト

今週の GitHub Weekly Trending（TypeScript カテゴリ）から、さとるさんのスタックに馴染みやすい題材を選定しました。

- **#1 colbymchenry/codegraph（TypeScript / 週間 +10,749★）**：Claude Code / Codex / Cursor 用に「コードベースを事前にグラフ化」して AI のトークン消費を約 35% 削減、ツール呼び出しを約 70% 削減するインデクサ。Tree-sitter + SQLite + MCP サーバーというシンプルな構成で、ローカル完結で動きます。ソース: https://github.com/colbymchenry/codegraph
- **#2 rohitg00/agentmemory（TypeScript / 週間 +7,000★）**：AI コーディングエージェント用の永続メモリ。`npx` 一発でローカルサーバー（:3111）と可視化ダッシュボード（:3113）が立ち上がり、REST API（124 エンドポイント）と MCP（51 ツール）の両方が使える設計です。ソース: https://github.com/rohitg00/agentmemory
- **#3 stablyai/orca（TypeScript / 週間 +677★）**：複数の AI エージェント（Claude Code / Codex / Grok など）を git worktree 単位で並列実行するデスクトップ ADE。中身は TypeScript 97.6%。Antigravity を普段使いするさとるさんなら、設計思想を読むだけでも勉強になります。ソース: https://github.com/stablyai/orca

※ Hacker News の検索系エンドポイント（hn.algolia.com）は本日 HTTP 403 で取得できなかったため、GitHub Trending のみを一次情報として採用しています。

---

## 注目リポジトリ / 事例

### 1. rohitg00/agentmemory — 今日のメイン題材
- 概要: AI エージェントの行動を自動キャプチャ → 要約 → BM25 + ベクトル + ナレッジグラフのハイブリッド検索で次セッションに注入。コンテキスト消費を約 92% 削減すると謳う。
- ライセンス / 課金: OSS。ローカル実行のため利用自体は無料（埋め込みモデルにクラウド API を使う設定にした場合のみ、その API 利用料が発生）。
- URL: https://github.com/rohitg00/agentmemory

### 2. colbymchenry/codegraph — 比較対象として知っておきたい
- 概要: コードベース側の「地図」を作るアプローチ。agentmemory が「過去の会話・観察」を覚えるのに対し、こちらは「今のコードの構造」を覚える、と整理すると役割の違いがクリアになります。
- ライセンス / 課金: MIT、ローカル完結で無料。
- URL: https://github.com/colbymchenry/codegraph

### 3. humanlayer/12-factor-agents — 設計思想の教科書
- 概要: 本番運用に耐える LLM アプリの 12 原則をまとめたドキュメント集。今週 +1,729★。手を動かす前に 1 セクションだけ読むと、上の 2 つを使う意味が腑に落ちます。
- 課金: なし（ドキュメントリポジトリ）。
- URL: https://github.com/humanlayer/12-factor-agents

---

## 実践ポイント（さとるさんへ）

今日の週末ハンズオンは **「agentmemory のローカル API を叩く、Next.js + TypeScript + TailwindCSS の最小ダッシュボード」** です。所要 60〜90 分。React の `useEffect` / `useState`、TypeScript の `type`、TailwindCSS のユーティリティクラスを 1 つの小さな成果物で総ざらいできます。

### Step 1: agentmemory をローカル起動（約 5 分）

ターミナルで以下を実行（Node.js 18+ 推奨）。

```bash
npx -y @agentmemory/agentmemory
```

起動すると `http://localhost:3111`（REST API）と `http://localhost:3113`（ダッシュボード）が立ち上がります。今日はこの 3111 番ポートに、自分で書いた Next.js から fetch します。

> 💡 ポート 3111 / 3113 が他のアプリと衝突した場合は、`PORT=4111 npx -y @agentmemory/agentmemory` のように環境変数で逃がせます。

### Step 2: Next.js プロジェクトを作る（約 5 分）

```bash
npx create-next-app@latest memory-viewer \
  --typescript --tailwind --app --eslint --src-dir --import-alias "@/*"
cd memory-viewer
npm run dev
```

`--app` で App Router、`--src-dir` で `src/` 配下構成、`--typescript --tailwind` で型と Tailwind を最初から有効化。さとるさんがいつも「習熟度低い」と言っているスタックを、CLI のフラグだけで丸ごとセットアップできます。

### Step 3: 型を先に書く（約 10 分）— TypeScript の肝

App Router のルートハンドラから fetch する前に、**「返ってくる JSON の形」を型として宣言**します。これが TypeScript で開発が楽になる一番のポイントです。

`src/types/memory.ts` を新規作成。

```ts
// agentmemory の検索 API のレスポンスを、最低限の形だけ宣言する。
// 実 API は項目がもっと多いが、UI で使う分だけ拾えば良い。
export type Memory = {
  id: string;
  content: string;
  score: number;       // 検索スコア（0〜1）
  createdAt: string;   // ISO 文字列で来る前提
};

export type SearchResponse = {
  query: string;
  results: Memory[];
};
```

ポイントは 3 つ:
1. **`type` は「データの形」を後から読み返せるラベル**。`Memory` という名前を付けておくと、他のファイルで `Memory[]` と書くだけで「Memory が並んだ配列」と一発で伝わる。
2. **存在しないフィールドを足さない**。型は「未来の自分への嘘」になりやすいので、UI に出すものだけ並べる。
3. **`createdAt: string` にしている**のは、JSON は Date 型を持てないため。表示する直前に `new Date(createdAt)` で変換する、という運用に統一しておくと混乱しません。

### Step 4: API ルートで CORS を回避する（約 15 分）— Next.js の役割

ブラウザから直接 `localhost:3111` を叩くと CORS で弾かれることが多いので、**Next.js の API ルートを「中継サーバー」として使います**。これが Next.js を使う一番のうま味です。

`src/app/api/memories/route.ts`:

```ts
import { NextResponse } from "next/server";
import type { SearchResponse } from "@/types/memory";

const AGENTMEMORY_URL = "http://localhost:3111";

export async function GET(req: Request) {
  const { searchParams } = new URL(req.url);
  const q = searchParams.get("q") ?? "";

  // Next.js のサーバー側で fetch するので、ブラウザの CORS は関係なくなる。
  const res = await fetch(`${AGENTMEMORY_URL}/search?q=${encodeURIComponent(q)}`, {
    // 開発中はキャッシュさせない。Next.js 15 系のデフォルト挙動を明示。
    cache: "no-store",
  });

  if (!res.ok) {
    return NextResponse.json({ error: "agentmemory unreachable" }, { status: 502 });
  }

  const data = (await res.json()) as SearchResponse;
  return NextResponse.json(data);
}
```

ロジックの流れを 1 行ずつ追うと、

1. URL のクエリ文字列（`?q=xxx`）から検索ワードを取り出す。
2. それを agentmemory 側の `/search` にそのまま渡す（`encodeURIComponent` で日本語や記号も安全に）。
3. 失敗したら 502 を返し、ブラウザ側で UI に出し分けられるようにする。
4. 成功したら **`as SearchResponse` で「ここから先は SearchResponse の形」だと TypeScript に宣言**して返す。

`as` は「コンパイラに型を断言するキャスト」で、強力ですが乱用厳禁。**今回のように外部 API の戻り値を初めて型付ける入口でのみ使う**、と覚えると安全です。

### Step 5: UI を Tailwind で組む（約 20 分）

`src/app/page.tsx` を以下に置き換え。

```tsx
"use client";

import { useEffect, useState } from "react";
import type { Memory } from "@/types/memory";

export default function Home() {
  const [q, setQ] = useState("");
  const [items, setItems] = useState<Memory[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  // q が変わるたびに 300ms 待ってから検索（簡易デバウンス）
  useEffect(() => {
    const id = setTimeout(async () => {
      setLoading(true);
      setError(null);
      try {
        const res = await fetch(`/api/memories?q=${encodeURIComponent(q)}`);
        if (!res.ok) throw new Error("fetch failed");
        const data = await res.json();
        setItems(data.results ?? []);
      } catch (e) {
        setError("agentmemory に接続できませんでした。`npx @agentmemory/agentmemory` が起動中か確認してください。");
      } finally {
        setLoading(false);
      }
    }, 300);
    return () => clearTimeout(id); // クリーンアップ：前の予約をキャンセル
  }, [q]);

  return (
    <main className="mx-auto max-w-2xl px-4 py-10">
      <h1 className="text-2xl font-bold tracking-tight">Memory Viewer</h1>
      <p className="mt-1 text-sm text-gray-500">
        agentmemory のローカル API（:3111）を叩いて結果を一覧します。
      </p>

      <input
        type="search"
        value={q}
        onChange={(e) => setQ(e.target.value)}
        placeholder="例: 'Next.js でハマったこと' を検索"
        className="mt-6 w-full rounded-lg border border-gray-300 px-4 py-2 text-sm
                   focus:border-gray-900 focus:outline-none"
      />

      {loading && <p className="mt-4 text-sm text-gray-500">検索中…</p>}
      {error && (
        <p className="mt-4 rounded-md bg-red-50 px-3 py-2 text-sm text-red-700">{error}</p>
      )}

      <ul className="mt-6 space-y-3">
        {items.map((m) => (
          <li
            key={m.id}
            className="rounded-lg border border-gray-200 bg-white p-4 shadow-sm
                       transition hover:border-gray-400"
          >
            <p className="text-sm leading-relaxed text-gray-800">{m.content}</p>
            <div className="mt-2 flex items-center justify-between text-xs text-gray-500">
              <time dateTime={m.createdAt}>
                {new Date(m.createdAt).toLocaleString("ja-JP")}
              </time>
              <span className="rounded bg-gray-100 px-2 py-0.5 font-mono">
                score {m.score.toFixed(2)}
              </span>
            </div>
          </li>
        ))}
      </ul>
    </main>
  );
}
```

**ロジックの読みどころ:**

- `useState<Memory[]>([])` のジェネリクス `<Memory[]>` が今日いちばん覚えてほしい構文。**「これは Memory が並んだ配列のステートだよ」と React に教える書き方**で、これを付けておくと `items.map((m) => ...)` の `m` に自動で `Memory` 型が付き、`m.content` のタイポが即座にエラーになります。
- `useEffect` の中で `setTimeout` を予約し、`return () => clearTimeout(id)` でキャンセルしているのが **デバウンス（連打防止）パターン**。さとるさんが今後検索 UI を作るたびに必要になる定石です。「クリーンアップ関数 = 次の effect が走る前に呼ばれる片付け係」と覚えてください。
- TailwindCSS は **「class を組み合わせる詰将棋」**。`mx-auto max-w-2xl px-4 py-10` のように「中央寄せ → 最大幅 → 余白」と意味のあるかたまりで読むクセを付けると、CSS 設計の知識（さとるさんが FLOCSS で培ったやつ）がそのまま活きます。

### Step 6: 動かしてみる

1. ターミナル A: `npx -y @agentmemory/agentmemory`
2. ターミナル B: `npm run dev`（プロジェクト内で）
3. ブラウザで `http://localhost:3000` を開き、検索ボックスに何か入れてみる
4. agentmemory がまだ何も学習していない状態だと結果 0 件になるので、`http://localhost:3113` のダッシュボードから手動でメモリを追加するか、Claude Code 側で MCP 連携してから再度試す

### 今週の "ひとつ深掘り" 課題

UI ができたら、`src/app/api/memories/route.ts` を `POST` も受け付けるよう拡張して **「ブラウザ側からメモリを書き込めるフォーム」** を 1 つ足してみてください。`Request.json()` でボディを受け取り、`/save` 系エンドポイントに転送する流れを書ければ、**「クライアント ↔ Next.js API Route ↔ 外部サービス」という個人開発の最頻出構造を 1 周体験**したことになります。Supabase に置き換える日も近いです。

---

## 今日の思考トリガー

**「自分のコードベースと自分の作業履歴、AI に渡すなら『どちらを』『どれだけ』覚えさせるべきだろうか？ codegraph（コード構造）と agentmemory（過去の文脈）の役割分担を、自分の個人開発プロジェクトで言語化してみよう。」**

この問いを言語化できると、AI を「便利ツール」から「設計に組み込めるインフラ」として捉え直せます。シニアフロントエンドエンジニアへの距離を一段縮める問いです。

_ソース: https://github.com/trending/typescript?since=weekly , https://github.com/colbymchenry/codegraph , https://github.com/rohitg00/agentmemory , https://github.com/humanlayer/12-factor-agents , https://github.com/stablyai/orca （2026年5月23日 JST 時点）_
