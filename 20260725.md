# 🛠️ 週末ハンズオン実践: 2026年07月25日号

おはようございます！フロントエンドメンターの「学習コーチ」です。

土曜の朝はゆっくりコーヒーを淹れて、平日の忙しさから離れて手を動かす時間ですね。今週は「AIエージェントの Skills（スキル）」という概念が GitHub トレンドの首位近辺に急浮上しました。今日はこの流れに乗って、Matt Pocock さん（TypeScript 界隈で有名な教育者）の `skills` リポジトリを覗きつつ、**「Skills 的な考え方を自分の React 開発に持ち込む」**ハンズオンをやってみましょう。

---

## 本日のハイライト

### 1. Matt Pocock 氏の "Skills for Real Engineers" が急伸

- **リポジトリ**: [mattpocock/skills](https://github.com/mattpocock/skills)
- **総スター数**: 186,555（週間 +10,536）
- 概要: Matt Pocock 氏が自身の `.agents` ディレクトリから公開した、AI コーディングエージェント向けの「スキル集」。プランニング、実装、レビューなどのワークフローをテキスト（Shell + Markdown）で表現しています。
- なぜ注目？: Matt Pocock 氏はもともと TypeScript の教育コンテンツ（total typescript）で著名な人物。その人が「AI エージェントに何をどう任せるか」をオープンソース化した意義は大きく、コミュニティで大量のフォーク・翻訳版が生まれています。
- ソース: https://github.com/mattpocock/skills

### 2. MoonshotAI の Kimi Code CLI が地味に伸長

- **リポジトリ**: [MoonshotAI/kimi-code](https://github.com/MoonshotAI/kimi-code)
- **総スター数**: 4,927（週間 +1,610）
- 概要: TypeScript 製の CLI エージェント。Claude Code、Codex に続く「次世代 CLI エージェント」を標榜する系譜のツール。
- 注意: 実運用には Kimi モデルの API 利用料が発生します（無料枠あり／有料）。まずは無理に乗り換えず、Claude Code のワークフローを詰めるのが先です。
- ソース: https://github.com/MoonshotAI/kimi-code

### 3. 中国発「AI Agent 本」の全文公開

- **リポジトリ**: [bojieli/ai-agent-book](https://github.com/bojieli/ai-agent-book)
- **総スター数**: 19,072（週間 +17,443）
- 概要: 『深入理解 AI Agent：设计原理与工程実践』という書籍の全文・PDF・章立て準拠のコードを公開したリポジトリ。中国語ですが、AI エージェントの設計原理を体系的に学べる無料教材です。
- さとるさん向けメモ: 今すぐ読み進める必要はないですが、「AI エージェントの原理」を学びたくなった時のブックマーク候補として。
- ソース: https://github.com/bojieli/ai-agent-book

※ 本日は Hacker News Algolia API がリクエスト拒否のため、HN 側の関連ストーリーは取得できませんでした。

---

## 注目リポジトリ / 事例

1. **[mattpocock/skills](https://github.com/mattpocock/skills)** — AI コーディングエージェント向けスキル集。Markdown で「作業手順」を書いて Claude Code に食わせる、という発想の教科書的リファレンス。
2. **[MoonshotAI/kimi-code](https://github.com/MoonshotAI/kimi-code)** — TypeScript 実装の CLI エージェント。Claude Code の代替候補として観察対象に。
3. **[bojieli/ai-agent-book](https://github.com/bojieli/ai-agent-book)** — 無料公開されている AI エージェント設計本。理論を深めたい時のリソース。

---

## 実践ポイント（さとるさんへ）

### 週末ハンズオン: 「自分専用の TypeScript 型ユーティリティ」を1つ作る

Matt Pocock 氏が有名なのは、**TypeScript の型を「小さな道具」として組み合わせて使う**という考え方を広めたことです。これは Skills の発想（「小さな手順書を組み合わせる」）と地続きです。

今日は難易度低めの「ユーティリティ型」を1つ、自分の手でタイプしてみましょう。所要時間 20〜30 分の目安です。

#### 課題: `Nullable<T>` を作る

`null` と `undefined` を許容する型 `Nullable<T>` を定義してください。Next.js + Supabase を触っていると、DB から返ってくる値が「あるかもしれない、null かもしれない」という場面が頻出します。都度 `string | null | undefined` と書くのは面倒なので、型ユーティリティで一発解決します。

```ts
// src/lib/types.ts

// ステップ1: 型エイリアスの基本
export type Nullable<T> = T | null | undefined;

// ステップ2: 使ってみる
type UserName = Nullable<string>;
// これは string | null | undefined と等価

// ステップ3: 型ガードもセットで用意する
export function isPresent<T>(value: Nullable<T>): value is T {
  return value !== null && value !== undefined;
}
```

#### 解説（初心者向け）

- **`type Nullable<T> = ...`**: `T` はジェネリクスと呼ばれる「プレースホルダー」。`Nullable<string>` と書けば `T` が `string` に置き換わり、`string | null | undefined` になります。関数の「引数」の型版だとイメージしてください。
- **`value is T`**: これは「型述語（type predicate）」といって、この関数が `true` を返したら「呼び出し元では `value` を `T` として扱ってよい」と TypeScript コンパイラに教える仕組みです。

#### React コンポーネントで使ってみる

```tsx
// src/components/UserGreeting.tsx
import { isPresent, type Nullable } from "@/lib/types";

type Props = {
  name: Nullable<string>;
};

export function UserGreeting({ name }: Props) {
  if (!isPresent(name)) {
    return <p className="text-gray-500">ゲストさん、こんにちは</p>;
  }
  // ここでは name は string として確定している（TS が型を絞り込んでくれる）
  return <p className="text-gray-900">{name} さん、こんにちは</p>;
}
```

`isPresent` を通した後、`name` が自動的に `string` になっていることに注目してください。**「型が絞り込まれる（narrowing）」という感覚**を体で覚えるのが、TypeScript 上達の最短ルートです。

#### 発展: 配列版を作ってみる

もし余力があれば、「`null`/`undefined` が混ざった配列から有効な要素だけを取り出す」ユーティリティも書けます。

```ts
export function compact<T>(items: Nullable<T>[]): T[] {
  return items.filter(isPresent);
}

// 使用例
const raw: Nullable<number>[] = [1, null, 2, undefined, 3];
const clean = compact(raw); // number[] = [1, 2, 3]
```

Supabase のクエリ結果を整形するときにも便利です。

---

## 今日の思考トリガー

**「もし自分の個人開発で『Skill（手順書）』を1つ書くとしたら、どんな作業を切り出す？——たとえば『LP を1ページ作る手順』『Supabase テーブルを追加する手順』など、繰り返している作業ほど価値がある。」**

Matt Pocock 氏が Skills を公開して大量のフォークが生まれているのは、「暗黙知だった作業手順を Markdown に落とすと、AI にも他人にも自分にも伝わる資産になる」ということが証明されたからです。まずは「.claude/skills/」フォルダに1本、自分用の手順書を書くところから始めてみてください。

---

_ソース: https://github.com/trending?since=weekly (2026年07月25日時点)_
