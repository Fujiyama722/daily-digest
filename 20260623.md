# 🎨 トレンドUI実装チュートリアル: 2026年06月23日号

おはようございます！シニアUI/UXデザイナー兼フロントエンドエンジニアの「学習コーチ」です。

火曜日は週に1つ、トレンドのUIパターンを取り上げて React + Next.js + TypeScript + TailwindCSS で実装するレッスンの日。今週は GitHub Trending(weekly) 上位の **bradtraversy/50projects50days**（HTML/CSS/JSのミニUI集、40,588★）から、定番だけど効果絶大な「**Expanding Cards（拡張カード）**」UIパターンを取り上げます。LP制作のヒーロー領域や採用ページ、ポートフォリオで使えるやつです。

---

## 本日のハイライト

- **bradtraversy/50projects50days（40,588★）** — Brad Traversy氏による「素のHTML/CSS/JSだけで作る50個のミニUIプロジェクト集」。今週もCSSカテゴリのトレンドに浮上。FLOCSSでCSS設計に慣れているさとるさんにとって、CSSの基礎技 → JSの基礎技 → モダンなReact実装、と段階的に学べる教材として最適。
  ソース: https://github.com/bradtraversy/50projects50days

- **ibelick/ui-skills（3,318★・週+506）** — 「Skills for Design Engineers」を掲げるCLIツール。`npx ui-skills start` で motion / layout などカテゴリ別にUI実装のベストプラクティスを引き出せる、いわば**デザインエンジニアの引き出し集**。Astro製、MIT。
  ソース: https://github.com/ibelick/ui-skills

- **sveltejs/kit（20,614★・週+37）/ sindresorhus/eslint-plugin-unicorn（5,116★・週+71）** — 今週のJS Trendingから。SvelteKitは「React以外の選択肢」を知るうえで触っておきたい。eslint-plugin-unicornは300+ルールでコードの品質を底上げ、Next.jsプロジェクトに追加するだけで型安全や近代的な記法が身につきます。
  ソース: https://github.com/trending/javascript?since=weekly

## 注目リポジトリ / 事例

1. **bradtraversy/50projects50days** — UIパターンの参照辞書 → https://github.com/bradtraversy/50projects50days
2. **ibelick/ui-skills** — UIスキルをCLIで引き出す新感覚ツール → https://github.com/ibelick/ui-skills
3. **sveltejs/kit** — React以外を覗き込む窓 → https://github.com/sveltejs/kit

---

## 本日の実装チュートリアル: Expanding Cards を React + Tailwind で

### UX/CVR視点での解説

Expanding Cards は **「複数の選択肢を一覧で見せつつ、1つだけ詳細を強調表示する」** UIです。横並びの画像カードのうち、ホバー/クリックされたものだけが flex で大きく広がり、他は細く縮む。

- **CVR効果**: LPの「サービス3つ紹介」や「料金プラン3種」を縦に積むより、横並びExpanding Cardsにすると**ファーストビュー内に収まる**ため離脱が減ります。
- **UX注意点**: モバイルでは横並びだとカードが細くなりすぎるので、`md:` 以上だけ Expanding 化し、SPでは縦積みに切り替えるのが正解。

### コード（React + Next.js App Router + TypeScript + TailwindCSS）

`app/components/ExpandingCards.tsx` として配置する想定:

```tsx
"use client";

import { useState } from "react";
import clsx from "clsx";

// 1. カード1枚分のデータ型を定義
//    readonly を付けるのは「Propsとして受け取った後、書き換えない」ことを型で保証するため
type Card = {
  readonly id: string;
  readonly title: string;
  readonly imageUrl: string;
};

// 2. コンポーネント Props の型。
//    Card[] ではなく ReadonlyArray<Card> にすると、内部で push() などの破壊的操作ができなくなる
type Props = {
  readonly cards: ReadonlyArray<Card>;
};

export function ExpandingCards({ cards }: Props) {
  // 3. 「今どのカードが開いているか」を id で管理。string | null で「未選択」も表現する
  const [activeId, setActiveId] = useState<string | null>(cards[0]?.id ?? null);

  return (
    <div className="flex h-[80vh] w-full gap-4 p-4">
      {cards.map((card) => {
        const isActive = card.id === activeId;
        return (
          <button
            key={card.id}
            type="button"
            onMouseEnter={() => setActiveId(card.id)}
            onFocus={() => setActiveId(card.id)}
            className={clsx(
              "relative overflow-hidden rounded-2xl bg-cover bg-center",
              "transition-[flex-grow] duration-700 ease-out",
              // ここが肝: flex-grow を 1 or 10 で切り替えるだけで拡張アニメーションになる
              isActive ? "flex-[10]" : "flex-[1]"
            )}
            style={{ backgroundImage: `url(${card.imageUrl})` }}
            aria-pressed={isActive}
          >
            <span
              className={clsx(
                "absolute bottom-6 left-6 text-2xl font-bold text-white drop-shadow-lg",
                "transition-opacity duration-500",
                isActive ? "opacity-100" : "opacity-0"
              )}
            >
              {card.title}
            </span>
          </button>
        );
      })}
    </div>
  );
}
```

呼び出し側（`app/page.tsx`）:

```tsx
import { ExpandingCards } from "./components/ExpandingCards";

const CARDS = [
  { id: "1", title: "デザインから始める", imageUrl: "/img/design.jpg" },
  { id: "2", title: "実装で形にする",   imageUrl: "/img/code.jpg"   },
  { id: "3", title: "公開して届ける",   imageUrl: "/img/ship.jpg"   },
] as const; // ← as const を付けると要素1つ1つが完全に readonly な型になる

export default function Page() {
  return <ExpandingCards cards={CARDS} />;
}
```

### 初心者向け解説: 何が起きているか

- **`flex-[1]` vs `flex-[10]`**: Tailwindで `flex-grow` を直接指定。親の flex コンテナの中で、選ばれたカードだけ「自分は10人分の幅を取りたい」と主張するので、結果として広がります。CSSアニメは `transition-[flex-grow]` で滑らかに。
- **`useState<string | null>`**: TypeScriptに「この箱には文字列か null しか入りません」と教えています。これがあると、もし誤って `setActiveId(123)` と書いた瞬間、保存前にエディタが赤線で警告してくれます。
- **`as const`**: 配列リテラルに付けると、型推論が `string` → `"design.jpg"` のような**リテラル型**まで絞り込まれ、データの安全性が一段上がります。
- **アクセシビリティ**: `<div>` ではなく `<button>` を使い、`aria-pressed` でスクリーンリーダーに現在開いているカードを伝えています。LP制作で意外と見落とされがちなポイント。

---

## 実践ポイント（さとるさんへ）

1. **まずは素のHTML/CSS版を写経**: `bradtraversy/50projects50days` の `expanding-cards` ディレクトリで素のJS版を10分で動かしてみる。FLOCSSで命名し直すと、CSS設計の頭の体操にもなります。
2. **次に上のReact版を写経**: Next.jsの新規プロジェクト (`npx create-next-app@latest`) に貼り付けるだけで動きます。`useState` と `transition-[flex-grow]` の挙動を `console.log` を入れて観察。
3. **業務に持ち込む**: 今請けているLP案件の「サービス紹介3つ」セクションをこのパターンに置き換えるとしたら？ ヒーロー直下の離脱率は下がりそうか？ クライアントへの提案ネタとして寝かせておきましょう。
4. **`eslint-plugin-unicorn` を入れてみる**: 個人開発のNext.jsプロジェクトに `npm i -D eslint-plugin-unicorn` → `.eslintrc.json` に追加。最初は警告だらけで嫌になりますが、1つ直すごとに「なぜモダンJSではこう書くか」が身体に染み込みます。

---

## 今日の思考トリガー

**「あなたが今請けているLPコーディング案件のどこか1セクションを、もし React + Tailwind + 状態管理 で書き直すとしたら、どこを選びますか？ そしてそれは『現在の素HTML/CSS』と比べて、ユーザー体験・開発体験のどちらをどれだけ良くしますか？」**

「JSライブラリを使う理由」を**自分の言葉で説明できる**ようになると、ReactやNext.jsの学習速度が一気に上がります。

---

_ソース:_
- _https://github.com/trending/javascript?since=weekly (2026年06月23日時点)_
- _https://github.com/trending/css?since=weekly (2026年06月23日時点)_
- _https://github.com/trending/typescript?since=weekly (2026年06月23日時点)_
- _https://github.com/bradtraversy/50projects50days (2026年06月23日時点)_
- _https://github.com/ibelick/ui-skills (2026年06月23日時点)_
- _https://github.com/sveltejs/kit (2026年06月23日時点)_
- _https://github.com/sindresorhus/eslint-plugin-unicorn (2026年06月23日時点)_

_※本日 Hacker News (hn.algolia.com / news.ycombinator.com) はネットワーク許可リスト外のため取得できませんでした。次回以降に再挑戦します。_
