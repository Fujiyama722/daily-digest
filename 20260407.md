# 🎨 トレンドUI実装チュートリアル: 2026年04月07日号

おはようございます！シニアUI/UXデザイナー兼フロントエンドエンジニアの「学習コーチ」です。

今日取り上げるのは、2026年のWebデザインを代表するトレンド **Bento Grid（ベントグリッド）** です。Apple や Linear が採用したことで一気に広まり、LP・ダッシュボード・ポートフォリオなど幅広い場面で使われるようになりました。さとるさんの業務（LPコーディング・新規ページ作成）でも即戦力になるパターンです。

---

## 本日のハイライト

- **Bento Grid が2026年の主役UI**: カードを非均一なサイズで配置する「お弁当箱」型レイアウト。情報の優先度を視覚的に表現しやすく、ファーストビューで複数の価値訴求ができるため LP の CVR 向上に貢献します。(ソース: https://superfiles.in/bento-grid-ui-design-trend.php)
- **Tailwind CSS 公式でも Bento Grid ブロックが提供**: Tailwind Plus（旧 Tailwind UI）に Bento Grid のマーケティング用ブロックが正式採用。`grid-cols-*` と `row-span-*` / `col-span-*` だけで実装できる手軽さが特徴です。(ソース: https://tailwindcss.com/plus/ui-blocks/marketing/sections/bento-grids)
- **Aceternity UI が Framer Motion との組み合わせ解説を公開**: Next.js + TailwindCSS + Framer Motion でアニメーション付きの Bento Grid を作るチュートリアル。個人開発のポートフォリオに使える。(ソース: https://ui.aceternity.com/blog/how-to-create-a-bento-grid-with-tailwindcss-nextjs-and-framer-motion)
- **GitHub Trending の JavaScript 週間では Claude Code 周辺ツール(oh-my-claudecode, open-claude-cowork)が上位**: UIトレンドとは別軸だが、AIエージェント実装系が引き続き熱い。(ソース: https://github.com/trending/javascript?since=weekly)

## 注目リポジトリ / 事例

1. **anbrela/react-bento** — React + TailwindCSS + react-grid-layout でドラッグ＆リサイズ可能な Bento Grid を実装したライブラリ。ダッシュボード用途に最適。ソース: https://github.com/anbrela/react-bento
2. **magicui.design/bento-grid** — コピペで使える Bento Grid コンポーネント集。Next.js のサンプルコードそのまま動く。ソース: https://magicui.design/docs/components/bento-grid
3. **Tailwind Plus Bento Grid Blocks** — Tailwind 公式の LP 向け Bento Grid テンプレ。セマンティクスを意識した HTML 構造が参考になる。ソース: https://tailwindcss.com/plus/ui-blocks/marketing/sections/bento-grids

---

## 実践ポイント（さとるさんへ）

### なぜ Bento Grid は CVR に効くのか（UX視点）

- **情報の優先度を面積で表現できる**: 一番伝えたい価値を大きなセルに、補足情報を小さなセルに配置することで、ユーザーの視線誘導がコントロールしやすい。
- **F 字・Z 字読みからの脱却**: 従来の縦積み LP では離脱する層にも、視覚的な面白さで留まってもらえる。
- **モバイルでは縦1列に落とせる**: 後述する Tailwind の `md:` プレフィックスで 1 カラム→複数カラムへきれいに切り替えできる。

### React + Next.js + TypeScript + TailwindCSS での最小実装

さとるさんの FLOCSS・セマンティクスの知識がそのまま活きる構成にしました。`<section>` と `<article>` を使い、`aria-labelledby` でアクセシビリティも担保します。

```tsx
// app/components/BentoGrid.tsx
import type { ReactNode } from "react";

// 👇 型定義: 各セルが持つデータの形を TypeScript で宣言する
//    これがあると「title がない」「span の値が typo している」をコンパイル時に検知できる
type BentoItem = {
  id: string;
  title: string;
  description: string;
  icon: ReactNode;
  // 'md' 以上の画面で何マス分を占めるか。リテラル型でタイポ防止
  colSpan?: 1 | 2 | 3;
  rowSpan?: 1 | 2;
};

// 👇 Props の型。items は BentoItem の配列
type BentoGridProps = {
  items: BentoItem[];
};

// Tailwind のクラスはビルド時に静的解析されるため、
// 動的文字列連結（例: `md:col-span-${n}`）だと purge されてしまう。
// そこでマップで安全に対応表を作る（初心者が詰まりやすいポイント）
const colSpanMap: Record<NonNullable<BentoItem["colSpan"]>, string> = {
  1: "md:col-span-1",
  2: "md:col-span-2",
  3: "md:col-span-3",
};

const rowSpanMap: Record<NonNullable<BentoItem["rowSpan"]>, string> = {
  1: "md:row-span-1",
  2: "md:row-span-2",
};

export function BentoGrid({ items }: BentoGridProps) {
  return (
    <section
      aria-labelledby="features-heading"
      className="mx-auto max-w-6xl px-4 py-16"
    >
      <h2
        id="features-heading"
        className="mb-8 text-3xl font-bold tracking-tight"
      >
        サービスの特徴
      </h2>

      {/* モバイル: 1 カラム / md 以上: 3 カラム・2 行の Bento */}
      <div className="grid grid-cols-1 gap-4 md:grid-cols-3 md:auto-rows-[12rem]">
        {items.map((item) => (
          <article
            key={item.id}
            className={[
              "group rounded-2xl border border-neutral-200 bg-white p-6",
              "shadow-sm transition hover:-translate-y-1 hover:shadow-md",
              colSpanMap[item.colSpan ?? 1],
              rowSpanMap[item.rowSpan ?? 1],
            ].join(" ")}
          >
            <div className="mb-3 text-2xl" aria-hidden>
              {item.icon}
            </div>
            <h3 className="mb-2 text-lg font-semibold">{item.title}</h3>
            <p className="text-sm leading-relaxed text-neutral-600">
              {item.description}
            </p>
          </article>
        ))}
      </div>
    </section>
  );
}
```

使い方:

```tsx
// app/page.tsx
import { BentoGrid } from "./components/BentoGrid";

const items = [
  {
    id: "speed",
    title: "高速表示",
    description: "Next.js の App Router で初期表示を最適化。",
    icon: "⚡",
    colSpan: 2, // ← 一番推したい価値を大きく
    rowSpan: 2,
  },
  {
    id: "secure",
    title: "セキュア",
    description: "Supabase Row Level Security で堅牢に。",
    icon: "🔒",
  },
  {
    id: "ai",
    title: "AI 連携",
    description: "Claude API で賢く自動化。",
    icon: "🤖",
    colSpan: 2,
  },
  {
    id: "mobile",
    title: "モバイル最適",
    description: "1 カラムに自動で落ちる。",
    icon: "📱",
  },
] as const; // ← as const で各フィールドをリテラル型に固定し、colSpan の型エラーを防ぐ

export default function Home() {
  return <BentoGrid items={[...items]} />;
}
```

### JS/TS 初心者向け解説

- **`type` と `Record<K, V>`**: `Record<"sm" | "md", string>` のように書くと「キーは sm か md、値は string」というオブジェクトの型を一行で作れます。`colSpanMap` がまさにこれで、想定外のキー（例: `colSpanMap[4]`）はコンパイルエラーになります。
- **`NonNullable<T>`**: `colSpan?: 1 | 2 | 3` は「undefined でも良い」という意味ですが、マップのキーに undefined は使えません。そこで `NonNullable<BentoItem["colSpan"]>` と書くと undefined を除外した `1 | 2 | 3` だけが取り出せます。
- **`as const`**: 普通に配列を書くと `colSpan: number` と広めに推論されますが、`as const` を付けると `colSpan: 2` のようにリテラル型に固定され、`BentoItem` の `1 | 2 | 3` と噛み合います。
- **Tailwind の動的クラス禁止**: `` `md:col-span-${n}` `` と書くと Tailwind のビルドが認識できず、本番でスタイルが消えます。マップで静的に持つのが鉄則です。

### FLOCSS 的に見ると？

`BentoGrid` は Project レイヤー（`.p-bento-grid`）相当。`article` は Component 層に切り出して `BentoCard` にすると、他ページでも再利用できてスケールしやすいです。Tailwind でも命名の思想は活きます。

---

## 今日の思考トリガー

**「あなたが今作りたい個人開発プロダクトのトップページを Bento Grid で組むなら、一番大きなセルにはどんな価値を置きますか？ ユーザーが 3 秒で離脱しない理由を、そのセル 1 枚で説明できますか？」**

_ソース: [Superfiles - Bento Grid UI Design Guide 2026](https://superfiles.in/bento-grid-ui-design-trend.php), [Tailwind CSS Bento Grids 公式](https://tailwindcss.com/plus/ui-blocks/marketing/sections/bento-grids), [Aceternity UI - Bento Grid Tutorial](https://ui.aceternity.com/blog/how-to-create-a-bento-grid-with-tailwindcss-nextjs-and-framer-motion), [GitHub Trending JavaScript Weekly](https://github.com/trending/javascript?since=weekly) (2026年04月07日時点)_
