# 📊 プロダクトマネジメント事例解説: 2026年07月15日号

おはようございます！シニアプロダクトマネージャーの「学習コーチ」です。

今日は水曜日、実在するプロダクトをPMの視点で解剖する日です。本日はProduct Hunt / Hacker Newsのフロントページが取得できませんでした（アクセス制限）。その代わり、GitHubトレンド上位に浮上した **Penpot** を主役に据え、要件定義・DB設計・RLS・収益化までを実データで掘り下げます。個人開発者が「デザインコラボSaaS」を作る場合に何を最初に決めるべきか、というテーマで読み進めてみてください。

---

## 本日のハイライト

Product Hunt (`https://www.producthunt.com/`) と Hacker News (`https://news.ycombinator.com/`) は本日、エージェント経由での取得ができませんでした（HTTP 403）。今日は代替としてGitHub Trending上に出ているプロダクト群から、事業モデルとして分析価値の高いものを選んでいます。

**GitHub Trending本日 上位に含まれる「プロダクト実例」**

- **penpot/penpot** — オープンソースのデザインコラボプラットフォーム。総スター56.1k、本日+264スター。最新版 v2.16.2（2026-07-01リリース）。
  ソース: `https://github.com/penpot/penpot`
- **OpenCut-app/OpenCut** — オープンソースのCapCut代替動画エディタ。総スター69k、本日+4,349スター。TypeScriptで書き直し中。
  ソース: `https://github.com/OpenCut-app/OpenCut`
- **iOfficeAI/OfficeCLI** — AIエージェント向けOffice操作ツール。週間+7,596スター。
  ソース: `https://github.com/iOfficeAI/OfficeCLI`

いずれも「単機能ではなくワークフロー全体を握りにいく」プロダクトで、PMの言葉でいう *system of record* を目指しています。個人開発でMVPを組むとき、この視座は真似できます。

---

## 注目リポジトリ / 事例：Penpotを解剖する

### 1. プロダクトの立ち位置

- 名前: **penpot/penpot**（Kaleidos社が主導、OSS + SaaS + セルフホスト）
- キャッチコピー（README原文）: *"The open-source design platform for Product teams that need scalable collaboration"*
- 直接URL: `https://github.com/penpot/penpot`
- 数値: 総スター 56.1k / 主要言語 Clojure(73.0%) + ClojureScript + Rust(6.1%) / 最新 v2.16.2（2026-07-01）

**PM視点で見た差別化**：Figmaがクローズド寄りに進む中で、Penpotは *self-hostable*・*SVG/CSS/HTMLをそのまま出力*・*デザイントークンをネイティブ管理* を武器にしています。「Product teams」を明示的にターゲットに書いているのが上手い。Figmaと真正面で戦わず、「コンプライアンス制約のある企業 + 開発者との受け渡しを重視するチーム」というセグメントに寄せた要件定義になっています。

### 2. 要件定義（PMがPRDに書くべき最小セット）

Penpotの機能を並べ替えると、MVPで最初に握る要件は以下の5点に集約できます。

| # | 要件カテゴリ | 個人開発版でのMVP判断 |
|---|---|---|
| 1 | マルチテナント（Team / Workspace） | 必須。1人開発でも「Team」概念を最初から入れる |
| 2 | リアルタイム共同編集 | 後回し可。まずは「同時1名編集 + 履歴」で切る |
| 3 | デザイントークン（Design as Code） | 差別化になるが実装重い。v2で入れる |
| 4 | Inspect（SVG/CSS/HTML書き出し） | MVPで入れると開発者に刺さる |
| 5 | セルフホスト（Docker/K8s） | 個人開発ではDocker Composeまでで十分 |

**「何を捨てるか」を書くのがPRDの本質**です。Penpotが「リアルタイム共同編集」を大看板にできるのは、Clojureの並行性モデルと自前インフラを持てるからで、Supabase + Vercelスタックだけの個人開発では *同時編集は要件から外す* のが正解です。代わりに「Figma/Penpot風のUIで、1人でも高速に描ける → 書き出しで開発者に渡す」に絞ります。

### 3. Supabase前提のDB設計（コピペで使える最小スキーマ）

Penpotのドメインモデルは大まかに `team → project → file → page → object` の階層です。これをSupabase (Postgres) で組むなら以下が出発点です。

```sql
-- 認証は auth.users を使う前提
create table teams (
  id uuid primary key default gen_random_uuid(),
  name text not null,
  owner_id uuid not null references auth.users(id) on delete cascade,
  created_at timestamptz not null default now()
);

create table team_members (
  team_id uuid not null references teams(id) on delete cascade,
  user_id uuid not null references auth.users(id) on delete cascade,
  role text not null check (role in ('owner','editor','viewer')),
  primary key (team_id, user_id)
);

create table projects (
  id uuid primary key default gen_random_uuid(),
  team_id uuid not null references teams(id) on delete cascade,
  name text not null,
  created_at timestamptz not null default now()
);

create table design_files (
  id uuid primary key default gen_random_uuid(),
  project_id uuid not null references projects(id) on delete cascade,
  name text not null,
  -- ページやオブジェクトのツリーはJSONBに寄せる（MVPは全体1ドキュメント）
  document jsonb not null default '{"pages":[]}'::jsonb,
  updated_at timestamptz not null default now()
);
```

**設計上の判断ポイント（PMがエンジニアと詰めるところ）**

- **`document jsonb` に寄せるか、`pages`/`objects` を別テーブルに分けるか。** MVPはJSONB一本で速く出す。将来オブジェクト単位の共同編集が必要になったら分割。
- **`role` を文字列にするか enum にするか。** Postgresの `check` 制約で十分。RLSポリシー側で参照するので変更コストは上がる。
- **削除はハードか論理か。** 個人開発ではハード削除でよい。有償プラン投入時に `deleted_at` を足す。

### 4. RLS（Row Level Security）の考え方 — ここが個人開発の落とし穴

SupabaseはRLSを *デフォルトで有効化していない* テーブルからは誰でも読めます。事故の温床です。以下のパターンを最初に入れてください。

```sql
alter table teams enable row level security;
alter table team_members enable row level security;
alter table projects enable row level security;
alter table design_files enable row level security;

-- 自分がメンバーであるチームだけ見える
create policy "teams_select_own" on teams
  for select using (
    exists (
      select 1 from team_members m
      where m.team_id = teams.id and m.user_id = auth.uid()
    )
  );

-- editor 以上だけ書き換えできる
create policy "design_files_write" on design_files
  for update using (
    exists (
      select 1 from projects p
      join team_members m on m.team_id = p.team_id
      where p.id = design_files.project_id
        and m.user_id = auth.uid()
        and m.role in ('owner','editor')
    )
  );
```

**PMがコードレビューで見るべき観点**は「その画面で読める行の集合はSQLで表現できているか？」の一点です。フロント側で `.eq('user_id', user.id)` するだけの実装は、Supabaseの匿名キーがブラウザに見えている以上、必ず破られます。**RLSが唯一の防衛線**であることを非エンジニアメンバー含めて共有するのがPMの仕事です。

### 5. アーキテクチャ図（テキスト版）

```
[Browser: Next.js/React on Vercel]
        │  Supabase JS client (anon key + JWT)
        ▼
[Supabase Postgres]  ── RLS ──▶ teams / projects / design_files
        │
        ├─ Auth (Email + OAuth)
        ├─ Storage (書き出したSVG/PNG)
        └─ Edge Functions (課金Webhook, 招待メール)
        │
[Stripe] ─ webhook ─▶ Edge Function ─▶ subscriptions テーブル更新
```

Penpot本家は自前でPostgres/Redis/S3互換ストレージを持ちますが、個人開発では **Supabaseに寄せる = 運用工数を無料で買う** ということです。ここは迷わず寄せていい判断です。

---

## 実践ポイント（さとるさんへ）

1. **今日の30分**: Supabaseで上記の `teams` / `team_members` / `projects` / `design_files` の4テーブルをコピペで作り、RLSを有効化してから、意図的にRLSを一時OFFにして `select` してみてください。**「RLSを切ると全件見える」という体感**が、今後の個人開発の安全ネットになります。
2. **今週末の課題**: Next.js + Supabaseで「Team作成 → メンバー招待 → プロジェクト作成」までの最小UIを作る。**LP的な見た目のスキルはさとるさんの強み**なので、Tailwindで先にUIを組んでから配線するのが最速です。
3. **PMとしての思考の型**: Penpotがなぜ「デザイントークン」と「セルフホスト」を強調しているかを1行で言語化してみてください。差別化仮説の書き方の練習になります。個人開発でも、README冒頭1行は *売れる/売れない* を決めます。
4. **収益化の初手**: OSSプロダクトの多くは *セルフホスト無料 + Cloud有料 + Enterpriseサポート* の三層構造です。個人開発で真似するなら、**Cloud版だけを最初に有料化**して、セルフホストは提供しない、が現実解です（サポート工数がゼロで済む）。

---

## 今日の思考トリガー

**「あなたが今作りたいプロダクトを、Penpotのように *『Xが必要なチームのための、Yの代替』* という1文で言い切れますか？ 主語（誰の課題か）と代替対象（何と比較されるか）が両方入っていない場合、PRDを書く前にそこを詰めてください。」**

_ソース:_
- Penpotリポジトリ: `https://github.com/penpot/penpot` (2026年07月15日時点、総スター56.1k)
- OpenCut: `https://github.com/OpenCut-app/OpenCut` (2026年07月15日時点、総スター69k)
- GitHub Trending 本日: `https://github.com/trending?since=daily`
- GitHub Trending 週間: `https://github.com/trending?since=weekly`
- 補足: Product Hunt (`https://www.producthunt.com/`) と Hacker News (`https://news.ycombinator.com/`) は本日エージェント経由での取得ができませんでした（HTTP 403）
