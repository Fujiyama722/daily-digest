# 🛠️ 週末ハンズオン実践: 2026年06月13日号

おはようございます！フロントエンドメンターの「学習コーチ」です。

土曜日は手を動かす日。今週のGitHubトレンドを眺めていると、「AIエージェントの能力をいかに圧縮・整理して使うか」というテーマが目立ちます。さとるさんが普段Claude Codeを使う背景にも直結する話題ですね。今日は週末ハンズオンとして、Reactの**`useOptimistic`フック**を使った「投稿後すぐUIに反映される、爽快な投稿フォーム」をNext.js + TypeScript + TailwindCSSで実装してみましょう。Supabaseに保存するシーンを想定したコードなので、個人開発にそのまま転用できます。

---

## 本日のハイライト

### 1. AIエージェントの「文脈圧縮」が今週の主役
今週のGitHubトレンド週間ランキングで上位に来ているのは、いずれも **「AIエージェントをどう賢く・軽く動かすか」** に取り組むプロジェクトです。`mvanhorn/last30days-skill`（リサーチ用エージェントスキル）と `chopratejas/headroom`（トークン使用量を60〜95%圧縮するライブラリ）はどちらも今週だけで1万スター以上の急成長。AIをただ呼び出すだけでなく、**「入力を整理して、出力をUIに反映する」** という設計力が、フロントエンドエンジニアにとっても重要なスキルになってきています。

- ソース: https://github.com/trending?since=weekly (2026年6月13日時点)

### 2. AppleがLinuxコンテナをMacで軽量に動かすツール v1.0.0 リリース
`apple/container` が **2026年6月9日にv1.0.0** をリリース。Apple Silicon最適化でLinuxコンテナを軽量VMとして動かせます。Mac中心の個人開発者にとって、SupabaseのローカルDB検証やNext.jsのDockerビルド確認が一段とやりやすくなる、嬉しいニュースです（macOS 26が必須）。

- ソース: https://github.com/apple/container (2026年6月13日時点)

### 3. 「実装で差がつく」のは小さなUX改善
ハイライトを総合すると、ツールが進化しても **使い手のUX設計とコードの丁寧さ**が差別化要素であることに変わりはありません。今日のハンズオンで扱う**楽観的UI更新（Optimistic UI）**は、Reactアプリの「気持ちよさ」を引き上げる地味だけど強力なテクニックです。

---

## 注目リポジトリ / 事例

### 1. mvanhorn/last30days-skill
Reddit / X / YouTube / Hacker News / Polymarket / Web を横断検索し、エンゲージメント量で重み付けして要約するエージェントスキル。Claude Code Marketplaceから導入可能で、Pythonで書かれています。
- 🔗 https://github.com/mvanhorn/last30days-skill

### 2. chopratejas/headroom
LLMに送る前のログやツール出力を **60〜95% 圧縮** するライブラリ。Pythonライブラリ・ローカルプロキシ・MCPサーバとして使え、Claude Code/Cursor/Aiderと統合できます。トークンコスト削減の文脈で要注目。
- 🔗 https://github.com/chopratejas/headroom

### 3. apple/container
Apple SiliconでLinuxコンテナを **軽量VM** として動かす公式ツール。Swift 98%実装、6月9日にv1.0.0リリース。
- 🔗 https://github.com/apple/container

---

## 実践ポイント（さとるさんへ）

### 週末ハンズオン: `useOptimistic` で「サクサク投稿フォーム」を作る

**狙い:** Supabaseに投稿を保存するアプリで、「ボタンを押した瞬間にリストへ追加 → サーバ確定後に整合」という体感の良いUIを作る。React 19の標準フック `useOptimistic` の使い方が身につきます。

#### 完成イメージ
- 入力ボックスに文字を打って「投稿」を押す
- 即座にリスト上部に薄いグレーで表示（送信中）
- サーバ保存が成功したら通常表示に切り替わる
- 失敗したら自動でリストから消える

#### コード例（`app/posts/PostForm.tsx`）

```tsx
'use client';

import { useOptimistic, useRef, startTransition } from 'react';

// 投稿1件の型を明示しておくと、後で props を渡すときに安全
type Post = {
  id: string;
  body: string;
  pending?: boolean; // 楽観表示用のフラグ
};

type Props = {
  posts: Post[];
  // サーバ側のSupabase保存関数（後述）
  createPost: (body: string) => Promise<Post>;
};

export function PostForm({ posts, createPost }: Props) {
  const formRef = useRef<HTMLFormElement>(null);

  // 第1引数: ベースとなる「サーバの真実」
  // 第2引数: 仮の値を受け取って、新しい配列を返す「更新関数」
  const [optimisticPosts, addOptimisticPost] = useOptimistic(
    posts,
    (current, newBody: string): Post[] => [
      { id: `tmp-${Date.now()}`, body: newBody, pending: true },
      ...current,
    ],
  );

  async function handleSubmit(formData: FormData) {
    const body = String(formData.get('body') ?? '').trim();
    if (!body) return;

    // useOptimistic の更新はトランジション内で呼ぶ必要がある
    startTransition(() => {
      addOptimisticPost(body);
    });
    formRef.current?.reset();

    try {
      await createPost(body);
    } catch (err) {
      // 失敗時はサーバ状態に巻き戻る（optimistic分が消える）
      console.error(err);
    }
  }

  return (
    <div className="mx-auto max-w-md space-y-4 p-4">
      <form ref={formRef} action={handleSubmit} className="flex gap-2">
        <input
          name="body"
          placeholder="今日の学びをひとこと"
          className="flex-1 rounded border border-slate-300 px-3 py-2 text-sm"
        />
        <button
          type="submit"
          className="rounded bg-slate-900 px-4 py-2 text-sm font-medium text-white hover:bg-slate-700"
        >
          投稿
        </button>
      </form>

      <ul className="space-y-2">
        {optimisticPosts.map((post) => (
          <li
            key={post.id}
            className={`rounded border px-3 py-2 text-sm ${
              post.pending
                ? 'border-dashed border-slate-300 bg-slate-50 text-slate-400'
                : 'border-slate-200 bg-white text-slate-800'
            }`}
          >
            {post.body}
          </li>
        ))}
      </ul>
    </div>
  );
}
```

#### コード解説（JS/TS初心者向け）

- **`type Post = { id: string; body: string; pending?: boolean }`**
  - TypeScriptの`type`は「このオブジェクトはこういう形をしている」と契約する仕組み。`pending?` の `?` は「あってもなくても良い」プロパティを意味します。これがあると、エディタが補完を効かせてくれて、タイポによるバグを未然に防げます。
- **`useOptimistic(posts, updater)`**
  - 「**サーバが返した本物のデータ**」と「**いま操作中の仮データ**」を分けて扱える React 19 標準のフック。`updater` は **現在の配列と新しい値** を受け取って **新しい配列を返す純粋関数** にするのがコツ。
- **`startTransition`**
  - 「これは緊急じゃない更新ですよ」とReactに伝えるAPI。`useOptimistic` の更新はこの中で行うのがルール。これがないと React が警告を出します。
- **`formRef.current?.reset()`**
  - `?.` は **オプショナルチェイニング**。`formRef.current` が `null` の場合は何もしない、という安全装置。フォームが既にアンマウントされた直後のエラーを防げます。

#### Supabase連携の例（`app/posts/actions.ts`、Server Actions）

```ts
'use server';

import { createClient } from '@/lib/supabase/server';

export async function createPost(body: string) {
  const supabase = await createClient();
  const { data, error } = await supabase
    .from('posts')
    .insert({ body })
    .select('id, body')
    .single();

  if (error) throw new Error(error.message);
  return { id: data.id, body: data.body };
}
```

`use server` を付けたファイルにある関数は、Next.js App Router上で **「ブラウザから呼べるサーバ関数」** として扱われます。`PostForm` から直接 `createPost(body)` を呼べるのがポイント。RLSは `auth.uid() = user_id` の前提で書くと安全です。

#### 今日のチェックリスト
- [ ] `useOptimistic` の3引数（state / updater / actionの呼び出し）を口頭で説明できるか
- [ ] `pending` 表示と確定後の表示で **見た目が変わる** ことをブラウザで確認
- [ ] エラー時にリストが自動で巻き戻ることを `throw new Error()` で試す
- [ ] SupabaseのRLSポリシーを書いてからCloudにデプロイ

---

## 今日の思考トリガー

**「あなたが作るアプリの中で、ユーザーが『1秒待つだけ』でストレスを感じる操作はどこですか？ その1箇所に `useOptimistic` を入れるだけで、体験はどれだけ変わるでしょうか？」**

_ソース: https://github.com/trending?since=weekly , https://github.com/apple/container , https://github.com/mvanhorn/last30days-skill , https://github.com/chopratejas/headroom (2026年6月13日時点)_
