# 🎨 トレンドUI実装チュートリアル: 2026年06月30日号

おはようございます！シニアUI/UXデザイナー兼フロントエンドエンジニアの「学習コーチ」です。

今週のGitHub TypeScriptトレンド週間ランキング1位は、Google Labs の `design.md` フォーマット仕様。「AIコーディングエージェントに、デザインシステムを正しく伝える」という、まさに私たち（デザイナー × Claude Code ユーザー）のための仕様が爆速で支持されています。

そこで今日は **「design.md 流のデザイントークン定義 × Tailwind CSS × React/TypeScript の Bento Grid 実装」** をハンズオン形式で進めます。

---

## 本日のハイライト

- **google-labs-code/design.md（TS週間 1位 / 累計 23,170★ / 今週 +6,728★）**
  YAML フロントマター（機械可読のトークン）+ Markdown 本文（人間可読の設計意図）で「なぜその値か」までセットでAIに渡せるフォーマット仕様。Apache 2.0。
  → ソース: https://github.com/google-labs-code/design.md

- **mui/material-ui（JS週間 累計 98,495★ / 今週 +78★）**
  Material Design 準拠の React コンポーネント大ライブラリ。最新は **v9.1.2（2026/06/23 リリース）**。MIT。
  → ソース: https://github.com/mui/material-ui

- **zarazhangrui/frontend-slides（JS週間 累計 23,940★ / 今週 +1,307★）**
  コーディングエージェントのフロントエンドスキルを使い、Web 上で美しいスライドを生成するプロジェクト。
  → ソース: https://github.com/zarazhangrui/frontend-slides

---

## 注目リポジトリ / 事例

### 1. google-labs-code/design.md — 「AIに渡せるデザインシステム」
従来 Figma → コードへの橋渡しは手作業でしたが、`design.md` を1ファイルリポジトリに置くだけで、Claude Code が色・タイポ・余白・コンポーネント状態を一貫して再現できます。Satoru さんが業務委託 LP 案件で「毎回ブランドガイドを口頭で説明する」消耗から解放される可能性があります。
→ https://github.com/google-labs-code/design.md

### 2. mui/material-ui — React 学習の「型の教科書」
コンポーネント Props がフル TypeScript で型付けされており、IDE 補完を眺めるだけで「Props は何で構成されるか」が学べます。学習用に `<Button variant="contained" color="primary">` の `variant`/`color` をホバーすると、文字列リテラル型（Union 型）の実例が読めます。
→ https://github.com/mui/material-ui

### 3. zarazhangrui/frontend-slides — 「AIにフロントを書かせる」最前線
コーディングエージェントを使った UI 自動生成の好例。中身を読むと、エージェントへの「役割指示 + 制約 + 出力例」のプロンプト設計が参考になります。
→ https://github.com/zarazhangrui/frontend-slides

---

## 実践ポイント（さとるさんへ）

今日は **Bento Grid（ベントーグリッド）** を実装します。2024 年以降、Apple・Vercel・Linear などのトップ tier プロダクトで採用が広がっている「弁当箱型カードレイアウト」です。

### なぜ Bento Grid が CVR に効くのか（UX 視点）

- 情報の**優先度**を「大きいセル/小さいセル」で視覚的に表現できる
- スキャンしやすく、**ファーストビュー直帰率**が下がる
- カードごとにマイクロインタラクション（hover lift）を設けやすく、**CTA への誘導動線**が自然になる

### Step 1: design.md 風にデザイントークンを定義

`tailwind.config.ts` にトークンを集約。**「色のリテラルを直に書かない」** のがプロの作法です。

```ts
// tailwind.config.ts
import type { Config } from 'tailwindcss'

const config: Config = {
  content: ['./app/**/*.{ts,tsx}', './components/**/*.{ts,tsx}'],
  theme: {
    extend: {
      colors: {
        // design.md と同じ命名で揃えると、AI に渡したとき迷わない
        brand: {
          primary: '#1A1C1E',
          secondary: '#6C7278',
          accent: '#3B82F6',
        },
        surface: {
          base: '#FAFAFA',
          card: '#FFFFFF',
        },
      },
      borderRadius: {
        bento: '1.25rem', // 20px
      },
    },
  },
}
export default config
```

### Step 2: 型でカードの「役割」を縛る（TypeScript 入門）

JS との一番の違いは「**Props の形をコンパイル時に固定できる**」こと。Union 型でサイズを 3 種類に絞ると、`size="huge"` のようなタイポを IDE が即指摘してくれます。

```tsx
// components/BentoCard.tsx
import type { ReactNode } from 'react'

type BentoSize = 'sm' | 'md' | 'lg'

type BentoCardProps = {
  title: string
  description: string
  size?: BentoSize
  icon?: ReactNode
  href?: string
}

// Union 型を Tailwind のクラスに対応付ける「ルックアップテーブル」
// オブジェクトリテラル + `as const` で「これ以上値は増えない」と TS に教える
const SIZE_CLASSES: Record<BentoSize, string> = {
  sm: 'md:col-span-1 md:row-span-1',
  md: 'md:col-span-2 md:row-span-1',
  lg: 'md:col-span-2 md:row-span-2',
} as const

export function BentoCard({
  title,
  description,
  size = 'sm',
  icon,
  href,
}: BentoCardProps) {
  const Tag = href ? 'a' : 'div'

  return (
    <Tag
      href={href}
      className={[
        SIZE_CLASSES[size],
        'group relative flex flex-col justify-between',
        'rounded-bento bg-surface-card p-6',
        'ring-1 ring-black/5 shadow-sm',
        'transition-all duration-300 ease-out',
        'hover:-translate-y-1 hover:shadow-lg hover:ring-brand-accent/30',
      ].join(' ')}
    >
      <div className="text-brand-accent">{icon}</div>
      <div className="mt-6">
        <h3 className="text-lg font-semibold text-brand-primary">{title}</h3>
        <p className="mt-2 text-sm leading-relaxed text-brand-secondary">
          {description}
        </p>
      </div>
    </Tag>
  )
}
```

**型の読み方をおさらい:**

- `type BentoSize = 'sm' | 'md' | 'lg'` … 「**文字列リテラル Union 型**」。値が3つしか取れない、と TS に約束させる書き方。
- `Record<BentoSize, string>` … 「キーは `BentoSize` の3つ、値は `string` 」のオブジェクトを必ず作る、というジェネリクス。1つでも漏れるとコンパイルエラー。`switch` 文より宣言的で読みやすい。
- `?` 付き Props（`size?`, `icon?`, `href?`） … 「省略可能」。呼び出し側で書かなくてもよい。

### Step 3: ページで並べる

```tsx
// app/page.tsx
import { BentoCard } from '@/components/BentoCard'

export default function Page() {
  return (
    <main className="min-h-screen bg-surface-base p-8">
      <section className="mx-auto grid max-w-5xl auto-rows-[180px] grid-cols-1 gap-4 md:grid-cols-3">
        <BentoCard
          size="lg"
          title="Webサイト運用"
          description="日次の更新からアクセス解析まで、運用フローを一括代行。"
          href="/services/ops"
        />
        <BentoCard
          size="md"
          title="LPコーディング"
          description="FLOCSS準拠、Lighthouse 95+を保証。"
        />
        <BentoCard size="sm" title="保守契約" description="月額で安心。" />
        <BentoCard size="sm" title="新規ページ作成" description="最短3営業日。" />
        <BentoCard
          size="md"
          title="A/Bテスト設計"
          description="仮説→計測→改善のループを伴走。"
        />
      </section>
    </main>
  )
}
```

### 動作確認のコツ

`npm run dev` でブラウザを開いたら、まず **モバイル幅 → タブレット幅 → デスクトップ幅** の3段階でリサイズ。`md:` 以上で初めて Bento の魅力が出ます。SP では1カラム積みでも CTA がスクロールで読まれます。

---

## 今日の思考トリガー

**「自分の業務委託サービスを `design.md` 1枚で表現するなら、ブランドカラー・タイポ・コアコンポーネントの状態は、それぞれ何種類に絞り込めますか？ 値を増やしたくなった瞬間が、UI が壊れ始める瞬間です。」**

_ソース: https://github.com/google-labs-code/design.md , https://github.com/mui/material-ui , https://github.com/trending/typescript?since=weekly , https://github.com/trending/javascript?since=weekly (2026年06月30日 JST 時点)_
